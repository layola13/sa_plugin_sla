# 030 Manual Guard Branch

This directory matches the guarded-match topic for the catalog slot.

- `main.rs`: Rust reference for the `match` arm guard semantics used by this slot.
- `main.sla`: Sla companion for the `match` arm guard semantics used by this slot.
