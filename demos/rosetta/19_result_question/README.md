# 019 Result Question

This directory matches the `Result` question-mark topic for the catalog slot.

- `main.rs`: Rust reference for the `?` propagation semantics used by this slot.
- `main.sla`: Sla companion for the `?` propagation semantics used by this slot.
