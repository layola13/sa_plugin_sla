# 031 Trait Static Dispatch

This directory matches the static trait-dispatch topic for the catalog slot.

- `main.rs`: Rust reference for the generic trait call semantics used by this slot.
- `main.sla`: Sla companion for the generic trait call semantics used by this slot.
