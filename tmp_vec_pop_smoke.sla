fn vec_pop_smoke() -> i32 {
    let values = vec(1, 2);
    return values.pop().unwrap() + values.pop().unwrap_or(0) + values.pop().unwrap_or(4);
}

@test "vec pop option smoke"() {
    let got = vec_pop_smoke();
    if got != 7 {
        panic(7007);
    };
}
