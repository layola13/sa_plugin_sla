# 007 Trait Vtable

This directory matches the trait-dispatch topic for the catalog slot.

- `main.rs`: Rust reference for the dynamic-dispatch semantics used by this slot.
- `main.sla`: Sla companion for the dynamic-dispatch semantics used by this slot.
