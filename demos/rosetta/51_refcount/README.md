# 051 Refcount

This directory matches the reference-counting topic for the catalog slot.

- `main.rs`: Rust reference for the `Rc` clone/read semantics used by this slot.
- `main.sla`: Sla companion for the `Rc` clone/read semantics used by this slot.
