# 075 Async Bridge

This directory now records the current async-bridge surrogate honestly.

- `main.rs`: local Rust surrogate that keeps only the final bridged value observable.
- `main.sla`: matching Sla surrogate for the same final-value async observable.
