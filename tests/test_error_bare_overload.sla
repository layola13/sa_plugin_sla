struct Vec2 {
    x: i32,
    y: i32,
}

overload Vec2 {
    fn +(self: Vec2, other: Vec2) -> Vec2 {
        return Vec2 { x: self.x + other.x, y: self.y + other.y };
    }
}

fn main() -> i32 {
    return 0;
}
