# Sla Compiler Implementation Tasks

This document tracks the tasks and implementation progress of the Sla compiler plugin (`sa_plugin_sla`).

---

## Task List

### 0. Roadmap To 100% Y-Shared Direct SAB Completion
- [ ] **Operating Rules Until Completion**
  - [x] Keep the architecture Y-shaped: `SLA AST/typecheck -> shared frontend/shared lowering rules/std metadata -> {SA text emitter, SAB structured emitter}`.
  - [x] Direct SAB must stay direct; do not implement `.sla -> .sa text -> .sab` as the normal path.
  - [x] Use `sa plugin install --dev .` as the official install/host verification gate; do not manually copy plugin artifacts.
  - [x] Report a feature completion percentage after each completed feature, plus broader Y/shared-lowering percentage, direct SAB fallback-removal percentage, and current no-fallback sweep pass count.
  - [x] Commit only verified completed slices. Partial exploration stays uncommitted.
  - [ ] Before each implementation slice, update this file with the slice target, expected verification, boundaries, and progress estimate.
- [ ] **Progress Accounting Rules**
  - [ ] A feature slice is 100% only after local build, focused local no-fallback run, official `sa plugin install --dev .`, `SA_PLUGIN_DEV=1 sa sla help`, focused host no-fallback run, SA-text parity when the feature has a text-backend path, relevant regressions, and docs/progress updates.
  - [ ] If a slice fixes only the direct SAB tail but leaves duplicated lowering decisions outside `lowering_rules.zig`, shared frontend, or `sla_std/std_surface.sla_meta`, count it as a partial bug fix and keep the Y/shared task open.
  - [ ] Update three numbers after every completed feature: feature slice percent, approximate Y/shared-lowering percent, and approximate direct SAB fallback-removal percent, plus the latest full no-fallback sweep count.
  - [ ] Do not treat fallback success as progress toward direct SAB completion unless the fallback use is removed or documented as an explicitly unsupported surface with a concrete future task.
  - [ ] Use the same percent scale in every status update: 0% not started, 25% design/owner identified, 50% implementation drafted and builds, 75% focused local no-fallback filters pass, 90% full local fixture/regressions pass, 100% host install/help/host regressions/sweep/docs complete.
  - [ ] Keep the global numbers conservative: a draft that has not passed host `sa plugin install --dev .` and `SA_PLUGIN_DEV=1 sa sla help` does not move the global Y/shared-lowering or fallback-removal percentages.
- [ ] **Non-Negotiable Architecture Boundaries**
  - [x] The compiler shape is a Y: one shared frontend and shared lowering contract, then two emitter tails. The task is not to build a second `sab -> sa` or `.sla -> .sa -> .sab` compiler path.
  - [ ] New language or std semantics must land in one of: `src/lowering_rules.zig`, the shared frontend/typecheck result, `sla_std/std_surface.sla_meta`, or `sa_std` macro/import surface. `src/sab_codegen.zig` should only consume that contract and emit structured SAB.
  - [ ] If direct SAB needs a temporary local helper, create a follow-up task to move the semantic decision into the shared contract before counting the feature 100%.
  - [ ] Any future fallback use must be named by fixture, lowering kind, and owning phase. Silent fallback is incompatible with 100% completion.
- [ ] **Current Recovery Point For Next Context**
  - [x] Last verified committed baseline is `33c1a51` (`Lower Option closure methods through shared plan`), with full no-fallback `tests/test_unit_*.sla` sweep at 48/64 passing, Y/shared-lowering approximately 58%, and direct SAB fallback removal approximately 81%.
  - [ ] Active uncommitted slice is smart-pointer/borrow/RefCell convergence. Current dirty files are expected to include `tasks.md`, `progress.md`, `sla_std/std_surface.sla_meta`, `src/lowering_rules.zig`, `src/codegen.zig`, `src/sab_codegen.zig`, active/source std Vec files, and active/source std Rc/Arc files until the slice is either completed or deliberately split. If generated `.test.sa` artifacts appear deleted, inspect before commit rather than blindly restoring or staging.
  - [ ] Current draft work has moved past the earlier borrow-temp `UseAfterMove` / loop `PhiStateConflict` blocker locally. The full `tests/test_unit_borrow_temp_release_order.sla` fixture now passes local direct SAB no-fallback 25/25 and SA-text parity 25/25, and `tests/test_unit_smart_pointer_struct_field_cleanup.sla` passes local direct SAB no-fallback 3/3. These are strong local draft proofs, but the feature is not complete until RefCell struct payload, pure call-target syntax, host install/help, host regressions, and sweep gates pass.
  - [ ] Drafted but not verified complete: shared `StaticCallResultPlan` / void-result planning, no-destination SAB emission for planned `void` calls, macro planned-call use of the same body helper, address-source temporary release tracking, std-template operand coercion for expanded macro values, shared smart-pointer helpers, RefCell borrow metadata transfer, smart-pointer value-slot materialization, index-address metadata for pointer-backed collection elements, Rc/Arc clone metadata wrappers, and borrowed smart-pointer receiver materialization.
  - [x] Vec element-slot blocker was locally addressed by adding `VEC_GET_MUT_PTR_U64` to the active/source std Vec surfaces and routing `index_address Vec` metadata to that pointer-returning macro. The filter `vec field smart pointer index borrow releases owner temps` passed locally under direct SAB no-fallback after that change.
  - [ ] Rc/Arc clone wrappers are drafted in active/source std files as `RC_CLONE_OUT` and `ARC_CLONE_OUT`, with `associated Rc clone ... out,value` and `associated Arc clone ... out,value` metadata. The first `tests/test_unit_refcell_struct_payload.sla` test now passes under local SAB no-fallback, but the second still fails with signal 11.
  - [ ] Immediate next command after this documentation update: `SLA_SAB_TRACE_UNSUPPORTED=1 SLA_SAB_NO_FALLBACK=1 timeout 180s ./zig-out/bin/sla-local-cli sla test tests/test_unit_refcell_struct_payload.sla --test-backend sab --jobs 1 --trace-panic`.
  - [ ] Current known blocker from the latest run: `tests/test_unit_refcell_struct_payload.sla` test `rc refcell struct payload shared mutation` fails with signal 11 under local direct SAB no-fallback after `Rc::clone(&rc1)` metadata was added.
  - [ ] Current likely bad SAB shape: `Rc::clone(&rc1)` materializes `&rc1` as a borrow register, then `RC_CLONE_OUT` clones that borrow/view register as if it were the underlying Rc allocation pointer. The fix should come from a shared associated-call receiver/materialization rule or metadata flag that loads the smart-pointer value from a borrowed receiver before invoking clone, and should apply symmetrically to `Arc::clone(&arc1)`.
  - [ ] Remaining structural call blocker: some generated SAB still disassembles void calls as one textual call target containing arguments, for example `call "@sla__refcell_payload_write(tmp_35, tmp_36)"`. The final Phase 1/3 claim must make call targets pure (`@func`) with arguments materialized through the shared call plan; do not count this slice 100% while `@func(arg)` survives in emitted SAB for tracked regressions.
  - [ ] After the RefCell struct payload fixture is green, rerun the full borrow-temp fixture, smart-pointer cleanup fixture, build/test, host install/help, host focused no-fallback runs, `parallel.sla`, and the full no-fallback sweep before committing.
  - [ ] Do not commit this active slice until the official install gate and focused host regressions listed under the smart-pointer/RefCell tasks pass.
- [ ] **Active Slice Evidence Ledger: Smart Pointer / Borrow / RefCell**
  - [ ] This ledger is only for continuation context. A checked or passing filtered command here does not make the feature 100%; the feature is 100% only after the full local + host gates in this document pass.
  - [x] Drafted shared/static void-call planning has passed `zig build --summary all` during this slice, and the original LLVM named-void-call verifier symptom is no longer the current blocker.
  - [x] Filter passed individually: `borrowed field call argument releases before field owner temp` under local direct SAB no-fallback.
  - [x] Filter passed after address/release metadata fixes: `borrowed field temp releases across loop iterations` under local direct SAB no-fallback.
  - [x] Filter passed individually after RefCell release metadata transfer: `borrowed rc refcell field releases before owner temp` under local direct SAB no-fallback.
  - [x] Filter passed individually after RefCell release metadata transfer: `arc refcell field borrow releases deref owner temp` under local direct SAB no-fallback.
  - [x] Filter passed individually after smart-pointer value-slot materialization: `nested rc box field borrow releases all temporaries` under local direct SAB no-fallback.
  - [x] Filter passed individually after smart-pointer value-slot materialization: `nested box rc field borrow releases all temporaries` under local direct SAB no-fallback.
  - [x] Filter passed individually after std Vec element-slot metadata fix: `vec field smart pointer index borrow releases owner temps` under local direct SAB no-fallback.
  - [x] Full local direct SAB no-fallback passed for `tests/test_unit_borrow_temp_release_order.sla` with 25/25 tests.
  - [x] SA-text parity passed for `tests/test_unit_borrow_temp_release_order.sla` with 25/25 tests.
  - [x] Local direct SAB no-fallback passed for `tests/test_unit_smart_pointer_struct_field_cleanup.sla` with 3/3 tests.
  - [x] SA-text parity passed for `tests/test_unit_refcell_struct_payload.sla` with 2/2 tests.
  - [x] Local direct SAB no-fallback now passes `tests/test_unit_refcell_struct_payload.sla` test `refcell struct payload borrow` after adding Rc clone metadata/macros.
  - [ ] Current failing proof: local direct SAB no-fallback `tests/test_unit_refcell_struct_payload.sla` test `rc refcell struct payload shared mutation` fails with signal 11, likely from borrowed Rc receiver materialization in `Rc::clone(&rc1)`.
  - [ ] Required next proof before claiming Phase 1 progress: both RefCell struct payload tests pass under SAB no-fallback, then rerun the full borrow-temp fixture, smart-pointer cleanup fixture, SA-text parity, host install/help, host no-fallback gates, `parallel.sla`, and sweep count.
- [ ] **Phase 1: Borrow, Precedence, And Smart-Pointer Convergence**
  - [ ] Finish smart-pointer constructor/get metadata and shared classification for `Box`, `Rc`, `Arc`, and focused `RefCell` construction.
  - [ ] Extract shared address-materialization rules for `&field`, `&index`, `&*smart`, `&**chain`, and postfix-under-prefix precedence so SA-text and SAB consume one plan.
  - [ ] Move RefCell borrow/borrow_mut lifecycle into a shared borrow-lifecycle plan, including panic-on-conflict and release across scopes, branches, loops, call args, and early returns.
  - [ ] Sub-slice 1A: close `Box`/`Rc`/`Arc` constructor/get/deref receiver semantics through `lowering_rules.zig` helpers and `sla_std/std_surface.sla_meta`, then verify the two non-RefCell borrow-temp tests before touching broader RefCell lifecycle.
  - [ ] Sub-slice 1B: finish address temporary ownership so borrowed call arguments release the borrow before releasing field/index/smart-pointer owner temps, including loop iteration cleanup and early-return cleanup.
  - [ ] Sub-slice 1C: finish RefCell shared lifecycle planning only after 1A/1B are green; handle `borrow`, `borrow_mut`, release shared/mut, branch/loop merge state, and pointer-backed payloads without copying legacy emitter-local maps into SAB.
  - [ ] Target fixtures: `tests/test_unit_borrow_temp_release_order.sla`, `tests/test_unit_refcell_struct_payload.sla`, `tests/test_unit_borrow_direct.sla`, smart-pointer demos, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`.
  - [ ] Expected outcome: borrow-precedence track 100%; no-fallback sweep should improve beyond 48/64; update exact percentage after verification.
- [ ] **Phase 2: Macro Expansion Completion Through Shared Rules**
  - [ ] Audit remaining macro-shaped failures and ensure macro expansion decisions live in shared expansion/lowering rules, not in SAB-only branches.
  - [ ] Cover nested macro calls, macro-local hygiene, caller argument substitution, borrow/move prefixes, aggregate literals, tuple destructuring, index access/assignment, block shadowing, and macro-expanded static calls.
  - [ ] Treat macro expansion as the first priority immediately after the active borrow/smart-pointer slice is stable enough not to mask macro failures. If a macro-shaped failure is actually missing call/result/address materialization, route the implementation to Phase 3 or Phase 1 but keep the macro fixture as the acceptance test.
  - [ ] Target fixtures: `tests/test_unit_user_macro_direct.sla`, macro-heavy regression tests, `tests/test_unit_pkgjson_codegen.sla` if triage confirms macro lowering is involved, and `parallel.sla`.
  - [ ] Completion rule: no new macro syntax or macro-lowered call shape may pass only through fallback; every direct macro feature needs a direct/no-fallback regression.
  - [ ] Expected outcome: macro-expansion convergence 100%; update broader Y/shared percentage after sweep.
- [ ] **Phase 3: Full Shared Call And Argument Materialization Plan**
  - [ ] Extend `lowering_rules.CallArgMaterializationPlan` into a complete call plan with result destination, argument register ownership, release policy, array-to-slice borrow, dyn fat-pointer borrow, copy-struct value, auto-borrow, move/borrow prefixes, and void-result handling.
  - [ ] Make SA-text and SAB ordinary calls, macro-expanded calls, inherent/trait static dispatch, extern calls, and method calls consume the same plan.
  - [ ] Target fixtures: `tests/test_unit_var_comprehensive.sla`, `tests/test_unit_rc_dyn_trait.sla`, nested import/extern call fixtures, existing dyn-borrow tests, and `parallel.sla`.
  - [ ] Completion rule: if SAB cannot materialize a shared plan kind yet, the task remains open; do not add duplicate emitter-local semantics.
- [ ] **Phase 4: Std Surface Metadata Generalization**
  - [ ] Add or extend metadata rule kinds only when a failing fixture proves the need: constructors, methods, fallible methods, result-valued methods, branch macros, index/index_assign, clone/as_ptr/from_raw/get_mut, and protocol hooks.
  - [ ] Keep library layout and macro knowledge in `sla_std/std_surface.sla_meta` plus `sa_std` macros, not as hardcoded Zig branches for `Vec`, `Rc`, `Set`, `Option`, `Result`, or future std types.
  - [ ] Target fixtures: `tests/test_unit_sets.sla`, `tests/test_unit_std_import.sla`, `tests/test_unit_sla_import_nested_contract.sla`, smart-pointer/RefCell tests, Option/Result/Cell regressions, and collection demos.
  - [ ] Completion rule: every std-shaped feature has metadata, explicit arg slots/deps/panic behavior, direct SAB verification, and SA-text parity or a documented reason the SA-text path already owns it.
- [ ] **Phase 5: Aggregate, Enum, Derive, And Operator Semantics**
  - [ ] Move remaining aggregate layout/update decisions into shared rules: struct update, enum payload layout, match extraction, derived equality/order/hash/debug metadata, spaceship compare, and copy/drop cleanup policy.
  - [ ] Target fixtures: `tests/test_unit_derive_semantics.sla`, `tests/test_unit_enum_match.sla`, `tests/test_unit_struct_update.sla`, `tests/test_unit_spaceship_cmp.sla`, and derive/component regressions.
  - [ ] Completion rule: SA-text and SAB must agree on layout offsets, value sizes, payload extraction, operator dispatch, and cleanup ownership from shared rules.
- [ ] **Phase 6: Protocol Lowering And Iteration**
  - [ ] Lower basic and generic `for in` protocols through a shared iterator/protocol plan, including range/array/slice/Vec forms, item binding ownership, loop cleanup, and break/continue release state.
  - [ ] Target fixtures: `tests/test_unit_for_in_protocol.sla`, `tests/test_unit_generic_for_in_protocol.sla`, array/slice/Vec loop regressions, and rosetta iterator demos.
  - [ ] Completion rule: protocol dispatch must be data/planned lowering, not type-name branches in one emitter.
- [ ] **Phase 7: Import, Package, And Std Resolution Parity**
  - [ ] Keep source expansion, import expansion, package metadata, nested contract loading, extern callee resolution, std root resolution, and test filtering in the shared frontend trunk.
  - [ ] Target fixtures: `tests/test_unit_sla_import_nested_contract.sla`, `tests/test_unit_std_import.sla`, `tests/test_unit_pkgjson_codegen.sla`, workspace/demo package tests, and host-dispatched CLI runs.
  - [ ] Completion rule: both `compileSlaToSaStringWithOptions` and `compileSlaFileToSabWithOptions` receive identical frontend results before their emitter tails.
- [ ] **Phase 8: Async/Await And Remaining Runtime Shapes**
  - [ ] Triage async/await after the shared call/std/control tracks are stable; reuse std `Future`/task surface metadata and shared state-machine rules rather than SAB-only async lowering.
  - [ ] Target fixture: `tests/test_unit_async_await.sla` plus focused async demos.
  - [ ] Completion rule: async lowering has direct SAB coverage, SA-text parity, and no fallback dependency for the supported subset.
- [ ] **Phase 9: Fallback Removal Gate**
  - [ ] Run the full no-fallback sweep after every completed feature and update this document and `progress.md` with pass/fail counts.
  - [ ] Sweep command template for a local count: `passed=0; total=0; for f in tests/test_unit_*.sla; do total=$((total+1)); if SLA_SAB_NO_FALLBACK=1 timeout 120s ./zig-out/bin/sla-local-cli sla test "$f" --test-backend sab --jobs 1 --trace-panic >/tmp/sla-sweep.log 2>&1; then passed=$((passed+1)); else printf 'FAIL %s\n' "$f"; tail -40 /tmp/sla-sweep.log; fi; done; printf 'PASS %s/%s\n' "$passed" "$total"`.
  - [ ] Host sweep uses the same loop with `SA_PLUGIN_DEV=1 sa sla test` after `sa plugin install --dev .`; do not mix local CLI and host CLI counts in one reported percentage.
  - [ ] Drive `tests/test_unit_*.sla` from 48/64 to 64/64 under `SLA_SAB_NO_FALLBACK=1` or explicitly document any unsupported language surface as a remaining task with a concrete fixture.
  - [ ] Before declaring 100%: run `zig build --summary all`, `zig build test --summary all`, `sa plugin install --dev .`, `SA_PLUGIN_DEV=1 sa sla help`, focused host no-fallback regressions, `/home/vscode/projects/sla_ecs/lib/parallel.sla`, full no-fallback sweep, and `git diff --check`.
  - [ ] Completion definition: Y/shared-lowering 100%, direct SAB fallback-removal 100% for the tracked unit corpus, all reported macro expansion and borrow/precedence goals complete, progress/tasks docs updated, and verified commits made for completed slices.
- [ ] **Failure Ownership Matrix From The 48/64 Baseline**
  - [ ] `tests/test_unit_borrow_temp_release_order.sla`: Phase 1 + Phase 3; shared smart-pointer/address/RefCell lifecycle plus void-result call planning.
  - [ ] `tests/test_unit_refcell_struct_payload.sla`: Phase 1; shared RefCell payload-pointer extraction and release lifecycle, then std-surface metadata parity.
  - [ ] `tests/test_unit_smart_pointer_struct_field_cleanup.sla`: Phase 1 + Phase 4; smart-pointer deref/address cleanup through shared projection and metadata rules.
  - [ ] `tests/test_unit_rc_dyn_trait.sla`: Phase 3 + Phase 4; direct SAB consumption of shared dyn-borrow/call materialization plus Rc metadata such as clone/get/as_ptr as fixtures require.
  - [ ] `tests/test_unit_var_comprehensive.sla`: Phase 3 + Phase 5; full shared call/materialization, assignment, aggregate copy, cleanup, and result-destination policy.
  - [ ] `tests/test_unit_sets.sla`: Phase 4; set collection operations expressed through std surface metadata and sa_std macros, not Set-specific Zig code.
  - [ ] `tests/test_unit_std_import.sla`: Phase 4 + Phase 7; std import resolution must enter the shared frontend trunk and expose needed metadata/import deps to both emitters.
  - [ ] `tests/test_unit_sla_import_nested_contract.sla`: Phase 7; nested contract/source expansion and extern callee resolution must be identical before emitter split.
  - [ ] `tests/test_unit_pkgjson_codegen.sla`: Phase 2 + Phase 7; first triage whether failure is macro expansion, package resolution, or extern/std metadata, then route to the matching shared layer.
  - [ ] `tests/test_unit_struct_update.sla`: Phase 5; shared aggregate update/copy/drop layout rules for both emitters.
  - [ ] `tests/test_unit_enum_match.sla`: Phase 5; shared enum tag/payload layout, match extraction, branch cleanup, and value merge policy.
  - [ ] `tests/test_unit_derive_semantics.sla`: Phase 5; derive lowering/data layout and generated operator/function calls must use shared rules.
  - [ ] `tests/test_unit_spaceship_cmp.sla`: Phase 5; comparison/operator dispatch should share metadata or operator plans, with SAB only emitting structured operations from the plan.
  - [ ] `tests/test_unit_for_in_protocol.sla`: Phase 6; non-generic protocol lowering and loop cleanup through a shared iterator plan.
  - [ ] `tests/test_unit_generic_for_in_protocol.sla`: Phase 6; generic iterator/protocol specialization through the same plan after monomorphization.
  - [ ] `tests/test_unit_async_await.sla`: Phase 8; async state-machine/Future/task surface only after call/std/control-flow plans are stable.
- [ ] **100% Execution Order After The Active Borrow Slice**
  - [ ] Finish Phase 1 borrow/smart-pointer/RefCell to 100%, because it removes the current focused blocker and validates scope release across branches/loops before broader call work.
  - [ ] Run the Phase 2 macro-expansion audit next and close pure macro-expansion gaps before broad library/language sweeps. Any macro failure that proves to be missing call/address/std materialization must still end with a macro no-fallback regression.
  - [ ] Finish Phase 3 call/result materialization to 100%, including `void` calls, array-to-slice, dyn fat pointers, copy structs, ownership/release policy, and macro/static/extern parity.
  - [ ] Sweep Phase 4 std metadata failures in fixture-sized slices: Rc/dyn, sets, std import helpers, and any smart-pointer metadata that Phase 1 deliberately leaves out.
  - [ ] Finish Phase 5 aggregate/enum/derive/operator semantics, because these share ABI/layout assumptions and should be solved as one rule family rather than isolated backend cases.
  - [ ] Finish Phase 6 for-in/protocol lowering, then Phase 7 import/pkg/std parity, then Phase 8 async/await; keep async last unless an earlier fixture proves a smaller shared Future/runtime rule is needed.
  - [ ] Final Phase 9 gate: full no-fallback sweep 64/64, `parallel.sla` host no-fallback green, no unexpected fallback logs, docs/progress synced, and all verified feature slices committed.
- [ ] **Phase Exit Criteria And Estimated Progress Path To 100%**
  - [ ] Phase 1 exit: `tests/test_unit_borrow_temp_release_order.sla`, `tests/test_unit_refcell_struct_payload.sla`, `tests/test_unit_smart_pointer_struct_field_cleanup.sla`, and existing borrow/direct smart-pointer regressions pass under direct SAB no-fallback locally and through host `SA_PLUGIN_DEV=1`. Expected progress movement when verified: borrow/precedence 100%; Y/shared-lowering roughly 64-68%; direct SAB fallback removal roughly 84-86%; no-fallback sweep expected to move above 48/64 by the exact number of fixed files.
  - [ ] Phase 2 exit: macro-shaped failures either pass no-fallback through shared expansion/lowering or are reclassified into std/import/call phases with a concrete fixture. Expected movement: macro-expansion convergence 100%; no hidden fallback path for user macro expansion or macro-expanded calls.
  - [ ] Phase 3 exit: all static/extern/method/macro-expanded calls consume one shared call plan, including result destination, void-result, array-to-slice, dyn fat pointer, copy-struct value, generated identifier classification, argument release, and receiver auto-borrow. Expected movement: Y/shared-lowering roughly 72-76%; direct SAB fallback removal roughly 88-90% if `var_comprehensive` and `rc_dyn_trait` call blockers pass.
  - [ ] Phase 4 exit: remaining std/library-shaped failures are expressed as metadata plus sa_std macro/import dependencies, including sets, Rc helpers, std import helpers, and any smart-pointer methods outside Phase 1. Expected movement: Y/shared-lowering roughly 80-84%; direct SAB fallback removal roughly 92-94%.
  - [ ] Phase 5 exit: aggregate update, enum/match payload extraction, derive semantics, spaceship/operator dispatch, value-size/layout, and cleanup ownership all use shared layout/operator plans. Expected movement: Y/shared-lowering roughly 88-92%; direct SAB fallback removal roughly 96% if enum/derive/struct-update/spaceship fixtures pass.
  - [ ] Phase 6 exit: basic and generic `for in` protocol lowering consumes one iterator/protocol plan after monomorphization, including item ownership and loop cleanup. Expected movement: direct SAB fallback removal roughly 98% if both for-in fixtures pass.
  - [ ] Phase 7 exit: package/std/import/nested-contract resolution is byte-identical before the Y split, and extern callee resolution reaches both emitters from the shared frontend trunk. Expected movement: remaining import/pkg failures closed; host-dispatched CLI parity verified.
  - [ ] Phase 8 exit: supported async/await subset lowers through shared Future/task/state-machine rules with direct SAB no-fallback and SA-text parity. Expected movement: tracked unit-file no-fallback target reaches 64/64 if no unsupported surface remains.
  - [ ] Phase 9 exit: final numbers are recorded in `progress.md` and this file; Y/shared-lowering 100%; direct SAB fallback removal 100% for tracked corpus; full local and host gates pass; all completed slices are committed.
- [ ] **Resume Commands For The Active Slice**
  - [ ] Build and format before rerunning tests: `zig fmt src/lowering_rules.zig src/codegen.zig src/sab_codegen.zig`; `zig build --summary all`.
  - [ ] Current first SAB check: `SLA_SAB_TRACE_UNSUPPORTED=1 SLA_SAB_NO_FALLBACK=1 timeout 180s ./zig-out/bin/sla-local-cli sla test tests/test_unit_refcell_struct_payload.sla --test-backend sab --jobs 1 --trace-panic`.
  - [ ] Disasm the current RefCell/Rc failure: `./zig-out/bin/sla-local-cli sla sab disasm $(ls -t .sla-cache/sab/test_unit_refcell_struct_payload-*.sab | head -1) | nl -ba | sed -n '130,190p'`; inspect the `Rc::clone(&rc1)` path, especially borrow/view creation, `RC_CLONE_OUT`, strong-count load/store, and the later `&*rc2` deref.
  - [ ] Inspect receiver materialization code before editing: `rg -n "planStaticCall|CallArgMaterialization|coerceTemplate|receiver|clone|smartPointerReceiverNeedsLoad|genStdSurface|genCall|genRefCellBorrowCall|genSmartPointer" src/lowering_rules.zig src/codegen.zig src/sab_codegen.zig sla_std/std_surface.sla_meta`.
  - [ ] Fix `Rc::clone(&rc1)` / `Arc::clone(&arc1)` through a shared associated-call receiver or std-surface argument rule: borrowed smart-pointer receivers must load/materialize the underlying smart-pointer value before clone macros run. Do not add an isolated `if Rc clone` SAB branch unless it is temporary and immediately tracked for migration.
  - [ ] Re-run local SAB after the clone receiver fix: `SLA_SAB_TRACE_UNSUPPORTED=1 SLA_SAB_NO_FALLBACK=1 timeout 180s ./zig-out/bin/sla-local-cli sla test tests/test_unit_refcell_struct_payload.sla --test-backend sab --jobs 1 --trace-panic`.
  - [ ] Re-run local focused regressions after RefCell passes: `SLA_SAB_TRACE_UNSUPPORTED=1 SLA_SAB_NO_FALLBACK=1 timeout 180s ./zig-out/bin/sla-local-cli sla test tests/test_unit_borrow_temp_release_order.sla --test-backend sab --jobs 1 --trace-panic`; `timeout 180s ./zig-out/bin/sla-local-cli sla test tests/test_unit_borrow_temp_release_order.sla --test-backend sa --jobs 1 --trace-panic`; `SLA_SAB_TRACE_UNSUPPORTED=1 SLA_SAB_NO_FALLBACK=1 timeout 180s ./zig-out/bin/sla-local-cli sla test tests/test_unit_smart_pointer_struct_field_cleanup.sla --test-backend sab --jobs 1 --trace-panic`.
  - [ ] Verify pure call-target syntax before the host gate: disassemble the latest focused SAB artifacts and search for illegal call targets with arguments, for example `./zig-out/bin/sla-local-cli sla sab disasm <latest.sab> | rg 'call .*@[^" ]+\\('`. Any `@func(arg)` target keeps Phase 1/3 open even if runtime tests pass.
  - [ ] Official gate only after local focus passes: `zig build test --summary all`; `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host no-fallback focused fixtures; host SA-text parity; host regressions for `tests/test_unit_option_methods.sla`, `tests/test_unit_option_direct.sla`, `tests/test_unit_result_direct.sla`, `tests/test_unit_refcell_struct_payload.sla`, `tests/test_unit_smart_pointer_struct_field_cleanup.sla`, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`.
- [ ] **Final 100% Acceptance Checklist**
  - [ ] Local compiler health: `zig build --summary all`, `zig build test --summary all`, and `git diff --check` all pass.
  - [ ] Official plugin health: `sa plugin install --dev .` completes from this repo, and `SA_PLUGIN_DEV=1 sa sla help` shows the SLA CLI surface from the installed dev plugin.
  - [ ] Direct SAB health: `SLA_SAB_NO_FALLBACK=1` passes the full tracked `tests/test_unit_*.sla` sweep locally and through host `SA_PLUGIN_DEV=1 sa sla test` with the count recorded in this file and `progress.md`.
  - [ ] Reported user blocker health: `/home/vscode/projects/sla_ecs/lib/parallel.sla` passes direct SAB no-fallback through the host CLI and no SAB disasm contains an illegal target such as `@func(arg)`.
  - [ ] Macro-expansion health: macro fixtures pass no-fallback without SAB-only macro semantics, and any macro-shaped failure is either fixed or reclassified to a concrete shared call/std/import task.
  - [ ] Borrow/precedence health: focused borrow, smart-pointer, RefCell, address-of-field/index/deref, branch, loop, call-argument, and early-return fixtures pass direct SAB no-fallback and SA-text parity where applicable.
  - [ ] Y-shared health: every remaining high-level semantic decision has a shared owner; no new feature is implemented only as a type/library-name branch in `src/sab_codegen.zig`.
  - [ ] Documentation health: `progress.md` records final feature percent, Y/shared-lowering percent, direct SAB fallback-removal percent, and full sweep counts; `tasks.md` has no stale current-blocker notes.
  - [ ] Commit health: all verified completed slices are committed, and the final commit message names the shared rule/metadata contracts plus the exact verification gates.
- [ ] **Commit And Documentation Rules For 100% Completion**
  - [ ] Never commit a slice that only moves the failing symptom; commit only after the slice's local and host gates pass and `progress.md` records feature percent, Y/shared-lowering percent, direct SAB fallback-removal percent, and full no-fallback sweep count.
  - [ ] Each completed slice must state what shared rule or metadata contract was extracted, which emitter-local branches were removed or delegated, which fixtures prove no-fallback direct SAB, and which SA-text parity checks ran.
  - [ ] If a fixture remains unsupported at final gate, it must have an explicit task with owner phase, failing command, exact unsupported lowering kind, and planned shared-rule/metadata contract; otherwise 100% is not reached.
- [ ] **Detailed Slice Queue From Current State To 100%**
  - [ ] Slice 1A, smart-pointer constructors and deref classification: finish the current shared `SmartPointerType` / `smartPointerDerefType` contract in `src/lowering_rules.zig`, keep `Box`/`Rc`/`Arc`/focused `RefCell` metadata in `sla_std/std_surface.sla_meta`, and make both emitters delegate type probes and receiver-load decisions to that contract. Exit gate: focused non-RefCell smart-pointer borrow filters, local build, SA-text parity where the text backend already supports the shape. Progress when verified: feature 100%, Phase 1A closed, no global percentage movement until host gate.
  - [ ] Slice 1B, address-projection temporary ownership: keep the locally passing `tests/test_unit_borrow_temp_release_order.sla` fixture green while migrating any remaining emitter-local address/release decisions toward a shared address/projection plan in `lowering_rules.zig`. The earlier `UseAfterMove` / `PhiStateConflict` loop failure is now a regression, not the active blocker. Exit gate: full borrow-temp fixture under SAB no-fallback and SA-text parity, plus no illegal `@func(arg)` call targets in its SAB disasm.
  - [ ] Slice 1C, RefCell lifecycle and payload access: move `borrow` / `borrow_mut` result value and guard release policy into a shared borrow-lifecycle plan with mode, owner cell, payload type, release macro, branch merge, loop merge, call-arg cleanup, and early-return cleanup. Exit gate: `tests/test_unit_borrow_temp_release_order.sla`, `tests/test_unit_refcell_struct_payload.sla`, focused RefCell demos, Cell/Option/Result regressions, host install/help, and host no-fallback runs.
  - [ ] Slice 1C.1, borrowed smart-pointer clone receiver: fix `Rc::clone(&rc1)` and `Arc::clone(&arc1)` by extending the shared associated-call/materialization contract or std-surface metadata so `&Rc<T>` / `&Arc<T>` receivers are loaded to the underlying smart-pointer value before clone macros run. Exit gate: `tests/test_unit_refcell_struct_payload.sla` local SAB no-fallback 2/2, a focused Arc clone/deref regression if one exists or is added, SA-text parity, and no direct SAB library-name branch that owns clone semantics alone.
  - [ ] Slice 1C.2, RefCell pointer-backed payload release: after clone receiver materialization is correct, verify `RefCell<Box<T>>`, `RefCell<Rc<T>>`, `RefCell<Arc<T>>`, and struct payload borrows do not copy owners, double-release guards, or lose guard release on branch/loop/call paths. Exit gate: borrow-temp fixture, RefCell struct payload fixture, smart-pointer cleanup fixture, and disasm review of borrow/release ordering.
  - [ ] Slice 1D, pure call-target and result-destination closeout: finish the shared call/result plan enough that SAB call operands always use a pure target plus materialized argument operands. This includes void calls that currently still disassemble as `call "@func(args)"`. Exit gate: focused void-call regression, `parallel.sla`, borrow/RefCell fixtures, and a disasm grep showing no `@func(` target strings in tracked SAB artifacts.
  - [ ] Slice 1E, Phase 1 closure commit: after 1A-1D pass, run the full local and host gates, update `progress.md` and this file with feature 100%, exact no-fallback count, Y/shared percentage, direct SAB fallback-removal percentage, then commit one verified borrow/smart-pointer/RefCell/call-target slice. Do not split commits for partial 1A/1B/1C/1D unless a sub-slice independently passes the official host gate and has no hidden follow-up needed for its own feature claim.
  - [ ] Slice 2A, macro sweep classification: immediately after Phase 1, run the macro-heavy no-fallback set and classify every failure as pure macro expansion, call/materialization, std metadata, import/pkg, or aggregate/control. Record each failure with command, owning phase, and shared contract target before editing. Macro expansion remains the next priority after the active borrow slice.
  - [ ] Slice 2B, pure macro convergence: close failures caused by macro hygiene, caller substitution, nested macro expansion, macro-local shadowing, macro-expanded static calls, borrow/move prefixes, tuple/aggregate/index expressions, or release statements in macros through the shared expansion/lowering path. Exit gate: `tests/test_unit_user_macro_direct.sla`, any newly failing macro fixture, SA-text parity, host install/help, and host no-fallback.
  - [ ] Slice 2C, macro acceptance handoff: if a macro fixture fails because the expanded body needs a missing call/std/import/address rule, create or move it to Phase 1/3/4/7 with the macro fixture kept as an acceptance test. Do not mark macro convergence 100% while any macro failure can only pass through fallback.
  - [ ] Slice 3A, result-destination and void-call contract: finish `StaticCallResultPlan` / full call result planning so void calls emit no destination in SAB and normal calls share one result destination policy. Exit gate: focused void-call regression, `tests/test_unit_borrow_temp_release_order.sla`, `parallel.sla`, and SA-text parity.
  - [ ] Slice 3B, full argument materialization: complete direct SAB consumption of the shared plan for array-to-slice borrow, dyn fat-pointer borrow, copy-struct value, receiver auto-borrow, move/borrow prefixes, generated identifier classification, argument release order, and temporary ownership. Exit gate: `tests/test_unit_var_comprehensive.sla`, `tests/test_unit_rc_dyn_trait.sla`, existing dyn-borrow tests, nested import/extern call fixtures, and macro-expanded call regressions.
  - [ ] Slice 3C, call-site unification audit: remove or delegate remaining high-level call branches from emitter-local code where the shared plan can own them. Any intentionally unsupported materialization must be named with fixture, plan kind, and follow-up before Phase 3 can exit.
  - [ ] Slice 4A, std metadata rule gaps: extend `sla_std/std_surface.sla_meta` only for proven failing surfaces, starting with Rc/dyn helpers, Set collection operations, std import helper calls, smart-pointer methods left out of Phase 1, and result/option-like branch macros. Each rule must declare import path, macro name, arg slots, deps, panic behavior, and result/void contract.
  - [ ] Slice 4B, sa_std macro surface sync: when metadata needs a new macro, add it to both the active installed std root and the source std tree used by this workspace, then document the macro in the active slice notes. Verify with `sa plugin install --dev .`; do not rely only on local CLI cache state.
  - [ ] Slice 4C, std metadata audit: scan `src/sab_codegen.zig` for type/library-name branches that should now be metadata or shared-plan driven. Keep low-level SAB emission helpers, but move semantic choices into `lowering_rules.zig`, shared frontend results, or `sla_std/std_surface.sla_meta` before counting Phase 4 complete.
  - [ ] Slice 5A, aggregate and assignment plan: move struct update, aggregate copy/drop, scalar-vs-pointer field layout, assignment cleanup, and var comprehensive ownership into shared aggregate/value plans. Exit gate: `tests/test_unit_struct_update.sla`, `tests/test_unit_var_comprehensive.sla`, array-field regressions, and SA-text parity.
  - [ ] Slice 5B, enum/match plan: share enum tag/payload layout, payload extraction, branch cleanup, and value merge policy. Exit gate: `tests/test_unit_enum_match.sla`, match regressions, and full local no-fallback sweep update.
  - [ ] Slice 5C, derive/operator plan: share derived equality/order/hash/debug metadata, spaceship comparison, struct arithmetic/operator dispatch, and layout-dependent helper generation. Exit gate: `tests/test_unit_derive_semantics.sla`, `tests/test_unit_spaceship_cmp.sla`, derive/component regressions, and SA-text parity.
  - [ ] Slice 6A, iterator/protocol plan: lower non-generic `for in` through a shared protocol plan for range, array, slice, and Vec forms, including item binding ownership and loop cleanup. Exit gate: `tests/test_unit_for_in_protocol.sla` plus array/slice/Vec loop regressions.
  - [ ] Slice 6B, generic iterator plan: apply the same protocol contract after monomorphization for generic iterator forms. Exit gate: `tests/test_unit_generic_for_in_protocol.sla`, generic loop demos, and host no-fallback.
  - [ ] Slice 7A, shared import/pkg frontend parity: ensure source expansion, import expansion, package metadata, nested contract loading, extern callee resolution, std root resolution, and test filtering happen before the Y split. Exit gate: `tests/test_unit_sla_import_nested_contract.sla`, `tests/test_unit_std_import.sla`, `tests/test_unit_pkgjson_codegen.sla`, workspace/demo package tests, and host-dispatched CLI runs.
  - [ ] Slice 7B, host CLI parity audit: after import/pkg changes, run `sa plugin install --dev .`, `SA_PLUGIN_DEV=1 sa sla help`, host no-fallback tests, and compare failures against local `./zig-out/bin/sla-local-cli` failures. Any local/host mismatch blocks Phase 7 completion.
  - [ ] Slice 8A, async triage: only after call/std/control/import phases are stable, classify `tests/test_unit_async_await.sla` into Future/task std metadata, shared state-machine lowering, call materialization, or unsupported runtime surface. Record the exact owner before implementing.
  - [ ] Slice 8B, supported async lowering: implement only the supported async subset through shared Future/task/state-machine rules, with direct SAB no-fallback and SA-text parity. Unsupported async runtime surfaces must be explicit tasks, not silent fallback.
  - [ ] Slice 9A, fallback inventory: run local full no-fallback sweep and host full no-fallback sweep, then list every remaining fallback/unsupported failure by fixture, lowering kind, owning phase, and exact command. The inventory must be empty or converted into explicit unfinished tasks before 100% can be claimed.
  - [ ] Slice 9B, final Y audit: inspect code for semantic decisions added only to `src/sab_codegen.zig` during the campaign; migrate them to shared rules/frontend/metadata or document a low-level emitter-only reason. Exit gate: no high-level type/library semantics exist only in one emitter.
  - [ ] Slice 9C, final acceptance and commit: run `zig fmt`, `zig build --summary all`, `zig build test --summary all`, `git diff --check`, `sa plugin install --dev .`, `SA_PLUGIN_DEV=1 sa sla help`, focused host regressions, `parallel.sla`, full local sweep, full host sweep, docs sync, then commit verified completion.
- [ ] **Progress Report Template To Use After Every Completed Feature**
  - [ ] Report in this exact shape: `Feature: <name> 100%; Y/shared-lowering: <old>% -> <new>%; direct SAB fallback-removal: <old>% -> <new>%; no-fallback sweep: <passed>/<total>; host gate: passed; commit: <hash or pending>`.
  - [ ] If a slice is not 100%, report it as draft evidence only: `Feature: <name> <percent>% drafted; global Y/shared and fallback-removal unchanged; blocker: <exact command/error>`.
  - [ ] Keep `progress.md` and `tasks.md` synchronized in the same edit before a final status update or commit.
- [ ] **Current Active Slice Decision Checklist**
  - [ ] Reproduce the current `tests/test_unit_refcell_struct_payload.sla` signal 11 before editing and preserve the disasm if the failure shape changes.
  - [ ] Determine whether `Rc::clone(&rc1)` receives an owned Rc value, a borrow/view register, or an address slot; clone macros must operate on the owned Rc allocation pointer, not the address of the local slot or a transient borrow guard.
  - [ ] Put the receiver-load decision in a shared rule or metadata contract so SA-text and SAB agree on borrowed smart-pointer receiver materialization.
  - [ ] Apply the same rule to `Arc::clone(&arc1)` and any future smart-pointer clone helpers; do not solve only the exact Rc fixture.
  - [ ] Keep the already-green loop/address regressions in the rerun set because clone/materialization changes can alter release metadata and call-argument cleanup.
  - [ ] Separately inspect pure call-target syntax after runtime tests pass; `@func(args)` as a call target is a syntax/contract bug even when the verifier temporarily accepts a shape.
- [ ] **Host CLI Commands That Must Stay In The Loop**
  - [ ] Install gate: `sa plugin install --dev .` from `/home/vscode/projects/sa_plugins/sa_plugin_sla`.
  - [ ] CLI surface gate: `SA_PLUGIN_DEV=1 sa sla help`; this is the required proof that the installed dev plugin is what the host CLI sees.
  - [ ] Host focused SAB gate template: `SLA_SAB_NO_FALLBACK=1 SA_PLUGIN_DEV=1 timeout 180s sa sla test <fixture.sla> --test-backend sab --jobs 1 --trace-panic`.
  - [ ] Host SA-text parity template: `SA_PLUGIN_DEV=1 timeout 180s sa sla test <fixture.sla> --test-backend sa --jobs 1 --trace-panic` when the feature has an SA-text path.
  - [ ] Host `parallel.sla` regression: run from `/home/vscode/projects/sla_ecs` with `SLA_SAB_NO_FALLBACK=1 SA_PLUGIN_DEV=1 timeout 180s sa sla test lib/parallel.sla --test-backend sab --jobs 1 --trace-panic`.

### 1. Lexer & Parser Updates
- [x] **Postfix `?` Operator Support**
  - [x] Add `?` token tag to `src/lexer.zig`.
  - [x] Update `lexer.zig` scanner to scan postfix `?` token.
  - [x] Update `src/parser.zig` to parse postfix `?` operator.
- [x] **Generic Struct Literal Support**
  - [x] Parse `Type<T> { field: value }` without adding any new keyword.
  - [x] Keep source cleanup implicit; no `drop` keyword or `drop()` function is introduced.

### 2. Type Checker & Symbol Resolver (Semantic Analyzer)
- [x] **Scope Builder & Symbol Table**
  - [x] Implement nested block scope stack.
  - [x] Track local variable declarations, parameter bindings, and constant values.
- [x] **Type Resolver**
  - [x] Parse and resolve primitive types (`int`, `float`, `bool`, `void`, `ptr`).
  - [x] Bind user-defined structs.
  - [x] Register signatures from FFI contract parser (`.sai`/`.sal`).
- [x] **Borrow Checker & Ownership Validator**
  - [x] Track variable ownership states (`Active` vs `Consumed`).
  - [x] Check move semantics (`^` transfer) and reject use-after-move.
  - [x] Validate borrow rules (`&` reference) and forbid mutable/immutable aliasing conflicts.

### 3. Monomorphization Engine
- [x] **Generic Collection & Instantiation**
  - [x] Traverse the AST to gather all generic struct instantiations and generic function calls.
  - [x] Map generic type parameters (like `T`) to concrete types (like `int`).
  - [x] Generate specialized non-generic structs and functions (e.g. `Option_int_Layout`, `unwrap_or_int`).

### 4. AST to SA Lowerer (Code Generation)
- [x] **Control Flow Flattener**
  - [x] Lower `if/else` conditional expressions into flat, conditional jumps with automated register cleanups before merge targets (preventing `PhiStateConflict`).
  - [x] Lower `switch` pattern matching expressions to flat conditional equality ladders.
- [x] **Hygienic Macro Expander**
  - [x] Perform compile-time Alpha-conversion (renaming local macro variables to globally unique names).
  - [x] Lower `macro` definitions and calls directly to SA's `[MACRO]` / `EXPAND` preprocessor statements.
- [x] **Scope Lifetime Auto-Cleanup Generator**
  - [x] Track active resources at block exits.
  - [x] Automatically inject SA `!reg` cleanup before scope endings so user `.sla` code does not need explicit releases.
- [x] **Loop Allocation Hoister**
  - [x] Detect `stack_alloc` statements inside `for` loops.
  - [x] Hoist allocations outside and before the loop label, reusing memory inside the loop body.
- [x] **Postfix `?` Unwrapper**
  - [x] Lower `expr?` into a conditional error check, injecting automated local variable cleanups and an early `return Err(^err)` on error branch.
- [x] **Struct Literal Lowering**
  - [x] Lower Sla struct literals to flat SA `alloc N` plus `store base+offset` instructions.
  - [x] Keep struct layouts as compile-time Sla metadata instead of emitting forbidden `#struct { ... }` brace syntax into `.sa`.

### 5. Integration & CLI Build Command
- [x] **Code Output Writer**
  - [x] Output compiled `.sa` files; Sla-owned struct layouts stay as compile-time metadata.
  - [x] Connect Lexer -> Parser -> Type Checker -> Monomorphizer -> Codegen -> Output in `src/plugin.zig`.
- [x] **Direct SAB Output Mainline**
  - [x] Add `src/sab_codegen.zig` as a direct SLA AST/type-checker to SAB backend, separate from `.sa` text codegen.
  - [x] Keep `sla -> .sa` and `sla -> .sab` as two independent mainlines; SAB generation does not call `compileSlaToSaString` or the SA text flattener.
  - [x] Confirm the SA compiler dependency from `https://github.com/layola13/sci/` is documented and installed before plugin development/verification.
  - [x] Confirm SA host `.sab` input support is used by `sa sla build-exe` and `sa sla sab workspace` via managed `.sla-cache/sab/...` paths.
  - [x] Add `sa sla sab build` / `sa slab build` support with default managed output under `.sla-cache/sab/`.
  - [x] Support optional visible SAB artifacts through `--out/-o` for `sab build`.
  - [x] Add `sa sla sab workspace` / `sa slab workspace` support with workspace package resolution, managed `.sla-cache/sab/` input for `sa build-exe`, and optional `--sab-out` / `--emit-sab` artifacts.
  - [x] Preserve `sa sla sab disasm` as a debug-only reader for SAB files.
  - [x] Make `sa sla test` default to `--test-backend auto`, writing managed SAB under `.sla-cache/sab/` and invoking `sa test` on SAB by default; the legacy `.test.sa` path is now only used by explicit `--test-backend sa`.
  - [x] Add `--test-backend sab` for explicit SAB artifact verification with no legacy `.sa` backend fallback, and `--test-backend sa` for explicit legacy `.sa` text tests.
  - [x] Add an in-memory SA-compatible SAB encoder fallback inside the SAB mainline so SA features not yet covered by direct AST-to-SAB codegen still produce `.sab` output without writing `.sa` text.
  - [x] Confirm SCI SAB v4 metadata support preserves structured operands, atomic operand text, native register names, package identity, upstream locations, and verified function register ids needed by SA backends without storing per-instruction raw `.sa` text.
  - [x] Add SCI regression coverage for v4 no-raw-text SAB decode, all `InstKind` / `OpKind` / operand tags, structured call parsing, and LLVM lowering of localized const vtable slots.
  - [x] Add direct SAB lowering for language-level function pointer values, including vtable const declarations, borrowed function values, parameter passing, generic function specializations, and `call_indirect`.
  - [ ] Optimize SLA-to-SAB generation time; current SA backend compile from `.sab` is faster than raw `.sa`, but `sa sla sab build` still spends extra time in SA-compatible flatten/verify/encode and cache writes.
  - [x] Cache the resolved `sa_std` root inside each direct SAB codegen instance so repeated std macro fragments do not repeat filesystem root probing.
  - [x] Cache decoded std import modules inside each direct SAB codegen instance so dependency preloading for multiple rules from the same `sa_std` import path does not repeat full SCI flatten/encode/decode work.
  - [x] Cache reusable identifier-only std macro templates inside each direct SAB codegen instance so repeated Option/Result-style macro fragments reuse decoded structured SAB bodies with placeholder substitution.
  - [x] Add focused tests for direct SAB output, managed cache behavior, SAB magic, decoded instructions, and no generated `.sa` side output.
  - [x] Add direct-only regression tests (`allow_fallback = false`) so new direct SAB features cannot silently pass through the SA-compatible fallback path.
  - [x] Add direct SAB lowering for plain struct literals, field access, struct returns, resolved call symbols, and multi-argument function calls.
  - [x] Add a dev/debug no-fallback gate (`SLA_SAB_NO_FALLBACK=1`) so direct SAB gaps fail loudly instead of hiding behind the compatibility encoder.
  - [x] Add direct SAB lowering for ordinary closure bindings and closure calls, including captured outer locals and one/two-parameter inline closure bodies.
  - [x] Add direct SAB lowering for Phase 1 scalar `var` slots, identifier assignment, stack-slot load/store, and basic `while` loops.
  - [x] Fix direct SAB branch-condition cleanup so temporary conditions are released on each branch while local/parameter conditions remain active.
  - [x] Add direct SAB lowering for tuple literals, tuple field access, and tuple destructuring, including direct-only regression coverage.
  - [x] Add direct SAB lowering for fixed array literals, repeat literals, and literal index reads/writes for focused scalar arrays, including direct-only regression coverage.
  - [x] Add direct SAB lowering for dynamic fixed-array index reads/writes and basic numeric range `for` loops, including direct-only regression coverage and no-fallback coverage for `tests/test_unit_arrays.sla`.
  - [x] Add direct SAB lowering for scalar value-producing `if` expressions, typed `if` bindings, nested branch assignments, and f32/f64 arithmetic/comparison op selection.
  - [x] Add parser and direct SAB lowering for boolean `&&` / `||` expressions, including dev-plugin no-fallback verification.
  - [x] Add direct SAB lowering for primitive numeric `as` casts, including structured conversion op operands and dev-plugin no-fallback verification.
  - [x] Add direct SAB lowering for scalar borrow/deref and non-void tail-expression returns, including direct-only regression coverage.
  - [x] Extend direct SAB borrow lowering to addressable field/deref/index sources, covering postfix-under-prefix precedence such as `&item.value` and `&*value` without fallback.
  - [x] Add direct SAB lowering for move-prefixed call arguments, including type-checker single-consumption handling and dev-plugin no-fallback verification.
  - [x] Add direct SAB inline expansion for focused user `macro` calls with hygienic macro-local renaming and caller-scope argument substitution.
  - [x] Extend direct SAB user macro expansion through nested control flow, ordinary/nested macro calls, casts, aggregate literals, tuple destructuring, index access/assignment, and block-scoped hygienic shadowing.
  - [x] Add direct SAB `stack_alloc()` lowering and raw stack-allocation lifetime tracking.
  - [x] Move the first std surface lowering path into generic import metadata / macro-fragment lowering so direct SAB can consume std macros without hardcoding ordinary library logic in Zig.
  - [x] Extend the std surface metadata format beyond the current associated/method/index macro bridge, still without adding compiler branches for `Vec`, `thread`, ECS, or other library names.
  - [x] Add generic fallible std surface macro metadata with explicit ok/output slots and panic-on-false lowering, covering `Vec.remove` without a `Vec.remove` compiler branch.
  - [x] Add generic std surface constructor metadata and result-valued method metadata, covering focused `Option` `Some`/`None` construction plus `is_some`/`is_none` without `Option` compiler branches.
  - [x] Preserve const-bearing std macro fragments in direct SAB lowering, including structured `panic_msg` operands and focused `Option.unwrap()` coverage without `Option` compiler branches.
  - [x] Add focused direct SAB metadata coverage for `Option.unwrap_or`, including no-fallback Some/None branch regression coverage without `Option` compiler branches.
  - [x] Add shared-plan direct SAB coverage for closure-taking `Option.map`, `Option.and_then`, and `Option.unwrap_or_else`, with `Option` payload access routed through std surface metadata (`OPTION_GET`) instead of a SAB-only layout branch.
  - [x] Add focused direct SAB metadata coverage for `Result` `Ok`/`Err` construction plus `is_ok`/`is_err`/`unwrap`/`unwrap_or`, including no-fallback regression coverage without `Result` compiler branches.
  - [x] Share the Y-shaped compile front-end trunk between the SA-text and SAB compile entry points (`compileSlaToSaStringWithOptions` and `compileSlaFileToSabWithOptions`).
    - Progress: extracted `runSlaFrontend` in `src/plugin.zig`, owning the byte-identical pipeline read → source-expand → parse → `@import`-expand → test-filter → monomorphize → load-contracts → type-check → primary-decl filter.
    - Progress: both tails now construct caller-owned `Monomorphizer`/`TypeChecker`, call the shared trunk, and keep them alive across their own codegen tail (SA-text: `rewriteProgramImportsForOutput` + `codegen.generate`; SAB: `pruneUnreachableFilteredTestDecls` + `sab_codegen.generate` with the existing SA-compatible fallback). Per-stage `SLA_PROFILE` timing now also covers the SA-text path.
    - Constraint: the front-end trunk must stay the single source of truth for both tails; new front-end stages go in `runSlaFrontend`, not in a single tail.
    - Verified in the current tree: `zig build --summary all`; `zig build test --summary all` (56/56); SA-text backend runs for `test_unit_generics.sla`, `test_unit_assign_move_cleanup.sla`, `test_unit_overload_add.sla`, and `test_unit_basic.sla`.
    - Progress estimate: 100% for shared front-end trunk extraction; broader Y/shared-lowering progress is approximately 45% after this trunk extraction plus the direct-SAB convergence fixes below.
  - [x] Start the Y-shaped shared lowering-rules path instead of growing independent SA-text and SAB semantic branches.
    - Progress: `src/lowering_rules.zig` now owns shared derive-name matching, struct derive lookup, ordinary static-call target resolution, and call-argument prefix rules used by both `codegen.zig` and `sab_codegen.zig`.
    - Progress: `lowering_rules.StaticCallPlan` now feeds resolved/static call target selection in both emitters, and `prefixedIdentifierCallArg` owns the shared `&name` / `^name` spelling rule for SA text call arguments.
    - Progress: release classification and parameter-aware auto-borrow predicates now live in shared lowering rules, with the SA text emitter delegating to them.
    - Progress: `CallArgMaterializationPlan` now represents array-to-slice borrow, auto-borrow, copy-struct value, ordinary value, and release decisions for resolved static-call arguments, and the SA text resolved-call path consumes it.
    - Progress: resolved static-call dyn borrow arguments now materialize through the shared plan by creating a fat pointer, passing `&fat_reg`, and releasing the fat pointer after the call.
    - Progress: legacy `genCallArgForParam` now delegates copy/value/release decisions to the shared materialization plan.
    - Progress: ordinary function/extern expression-call fallback now consumes the shared materialization helper for array-to-slice borrow, dyn fat-pointer borrow, receiver-style auto-borrow, copy/value, generated-identifier, and release decisions.
    - Progress: remaining SA text legacy method/statement call branches now consume the shared materialization helper, with statement-receiver borrow passthrough captured as a shared predicate.
    - Progress: direct SAB ordinary static calls now consume the shared materialization plan for value arguments, parameter-aware auto-borrow, generated identifier classification, and temporary release selection while leaving unsupported array/dyn/copy SAB materializations explicit.
    - Progress: direct SAB macro-expanded static calls now consume the shared materialization plan using effective macro arguments for planning and macro-context expressions for register materialization.
    - Progress: aggregate ABI layout rules now live in `lowering_rules.zig` and feed both SA-text and SAB emitters, including array-typed struct field pointer-slot layout.
    - Progress: direct SAB dyn-borrow call arguments now consume the shared materialization plan, with shared trait/dyn naming and method-slot rules used by both emitters.
    - Progress: inherent associated calls and dot inherent methods now publish resolved implementation symbols into `resolved_call_symbols`, so both emitters converge through `lowering_rules.planStaticCall` instead of SA-text-only fallback branches.
    - Progress: fixed-array destructuring now consumes shared array element/rest layout rules, and `Slice<T>` index reads flow through std surface metadata just like `Vec<T>` reads.
    - Progress: associated std-surface rules can now consume constructor value arguments, and `Cell<T>` `new`/`get`/`set` are expressed through metadata plus std macros rather than compiler branches.
    - Progress: closure-taking `Option.map`, `Option.and_then`, and `Option.unwrap_or_else` now classify through `lowering_rules.OptionClosureCallPlan`; SA-text uses the shared plan for dispatch, and direct SAB consumes the same plan with std surface `Option` macros including `OPTION_GET` metadata.
    - Progress estimate: the first shared-rules extraction, static-call plan, call-materialization predicate, resolved-call materialization-plan, resolved-call dyn borrow, legacy helper adoption, ordinary function/extern expression-call adoption, remaining SA text legacy method/statement adoption, SAB ordinary/static macro call adoption, aggregate ABI layout, SAB dyn-borrow consumption, static dispatch resolved-symbol convergence, Vec index-assignment metadata, fixed-array destructuring/std-slice-index, Cell std-surface, and Option closure-plan slices are 100%; the broader Y/shared-lowering track is approximately 58%.
    - Constraint: do not implement high-level language/library semantics only in `sab_codegen.zig`; future direct SAB work should extend shared rules/plans or std surface metadata so SA text and SAB emitters converge through the shared contract.
  - [x] Converge direct-SAB control-flow release scoping and struct-operator lowering with the SA-text emitter so both backends agree on a shared corpus.
    - Branch scoping: `genIfStatement`/`genIfValue` now snapshot `self.locals` length + `released_regs` before each branch and restore after, with the merge release state computed as the intersection over non-terminated incoming paths (`restoreReleased`/`setMergeReleased`). Fixes `UnknownRegister` for then-branch `let`s leaking into the else path (e.g. generics `unwrap_or`).
    - Loop scoping: `genWhile` applies the same body-local snapshot/restore so a body `let` (e.g. `let next = ...`) does not leak into a trailing `return`'s `releaseOpenLocals` on the zero-iteration path.
    - Struct operators: `genBinary` delegates struct-typed operands to a new `genStructArithmetic` mirroring SA-text `genStructArithmeticExpr` (same-struct `+`/`-`, scalar `*` either order), field-wise into a fresh alloc instead of a primitive `op` on struct pointers (which segfaulted). Out-of-matrix struct shapes return `UnsupportedSabDirectFeature` so the SA-compatible fallback takes over.
    - Shared call tail: extracted `emitPlannedStaticCall` from `genCall` so the planned static-call emission is reused.
    - Verified in the current tree: `zig build --summary all`; `zig build test --summary all` (56/56); no-fallback SAB for `test_unit_generics.sla`, `test_unit_assign_move_cleanup.sla`, `test_unit_overload_add.sla`, full `test_unit_fn_ptr_value.sla`, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; SA-text backend for the three fixed files plus `test_unit_basic.sla`.
    - Current no-fallback `tests/test_unit_*.sla` sweep is 48/64 passing after the later array-field ABI, dyn-borrow plan-consumption, static dispatch resolved-symbol convergence, Vec index-assignment metadata, fixed-array destructuring/std-slice-index, Cell std-surface, and Option closure-plan slices. Remaining direct-SAB gaps include async/for-in/enum/set/derive semantics shapes, borrow-temp release ordering, rc/dyn/refcell/smart-pointer paths, import-contract extern callee resolution, std import, struct update, and var comprehensive.
    - Progress estimate: 100% for these three direct-SAB divergence fixes; broader Y/shared-lowering progress is approximately 58%, while direct SAB fallback removal is approximately 81% by feature track after the later convergence slices.
  - [x] Converge array-typed struct field ABI through shared lowering rules instead of a SAB-only layout branch.
    - Progress: `lowering_rules.zig` now owns shared ABI size, aggregate alignment, struct/tuple field layout, and inline fixed-array element layout helpers.
    - Progress: both `codegen.zig` and `sab_codegen.zig` delegate aggregate layout to those helpers. This preserves SA-text behavior while making direct SAB represent `[T; N]` fields as pointer-sized slots, matching the arrays generated by literal lowering.
    - Verified in the current tree: `zig build --summary all`; focused shared ABI Zig unit filter; no-fallback SAB for `test_unit_array_struct_field_cleanup.sla`, `test_unit_struct_field_array_loop.sla`, `test_unit_arrays.sla`, and `test_unit_field_array_cleanup.sla`; SA-text backend for the two fixed fixtures. Full no-fallback `tests/test_unit_*.sla` sweep at this point was 38/64 passing.
    - Progress estimate: 100% for array-typed struct field ABI convergence; broader Y/shared-lowering progress at this point was approximately 48%, and direct SAB fallback removal was approximately 73% by feature track.
  - [x] Consume the shared dyn-borrow call-materialization plan in direct SAB.
    - Progress: pure trait/dyn naming helpers (`mangleTraitMethodName`, `concreteTypeName`, `dynTraitName`, `vtableName`, `dynMethodSlot`) moved into `lowering_rules.zig` and are now delegated by SA-text and SAB emitters.
    - Progress: direct SAB emits trait impl method functions and trait vtable consts, materializes `&Concrete -> &dyn Trait` arguments through `DYN_NEW`, and lowers focused no-extra-arg dyn method calls via shared slot offsets plus structured `call_indirect`.
    - Verified in the current tree: `zig build --summary all`; no-fallback SAB for `test_unit_dyn_borrow_arg.sla`; SA-text backend for the same fixture; no-fallback SAB regression for `test_unit_overload_add.sla`. Full no-fallback `tests/test_unit_*.sla` sweep at this point was 39/64 passing.
    - Progress estimate: 100% for direct SAB dyn-borrow argument materialization through the shared plan; broader Y/shared-lowering progress at this point was approximately 50%, and direct SAB fallback removal was approximately 74% by feature track.
  - [x] Converge inherent and trait static dispatch through the shared resolved-call symbol plan.
    - Progress: type checking for `Type::method(...)` and `value.method(...)` now records resolved implementation symbols, so static dispatch flows through `StaticCallPlan` for both SA-text and SAB.
    - Verified in the current tree: no-fallback SAB and SA-text backend for `test_unit_impl_static_methods.sla` and `test_unit_trait_static_dispatch.sla`; full no-fallback `tests/test_unit_*.sla` sweep at this point was 43/64 passing. This also moved `test_unit_derive_component.sla` and `test_unit_using_static_extension.sla` into the passing set.
    - Progress estimate: 100% for static dispatch resolved-symbol convergence; broader Y/shared-lowering progress at this point was approximately 52%, and direct SAB fallback removal was approximately 76% by feature track.
  - [x] Fix thread-closure SAB call lowering so captured-argument calls keep a pure call target symbol instead of generating illegal `@func(arg)` call text; reproduce with `/home/vscode/projects/sla_ecs/lib/parallel.sla`.
    - Note: the original fallback `parallel.sla` ForbiddenSyntax repro is unblocked in the updated SCI host by accepting fallback-generated single-operand structured `panic_msg` in SAB v4 decode/verify.
    - Boundary: this completed the focused thread-closure/parallel path at the time. The current active slice has a separate general call-plan bug where some void/static calls still disassemble as a target string containing arguments, so the broader pure-call-target audit remains open under Slice 1D / Phase 3.
    - Progress: direct no-fallback lowers the focused escaped thread closure case `thread::spawn(^|| f(value))` where `f` is a captured function-pointer callee, using generated vtable/spawn-wrapper/worker entries and structured `call_indirect` inside the worker.
    - Progress: the full `SLA_SAB_NO_FALLBACK=1 ... /home/vscode/projects/sla_ecs/lib/parallel.sla` path now passes after expanding std-dependency preloading and moving typed `Vec<T>` index reads to std surface metadata.
    - Progress estimate: 100% for the reported illegal call-target blocker.
    - Verified with `SLA_SAB_NO_FALLBACK=1 SLA_PROFILE=1 timeout 180s ./zig-out/bin/sla-local-cli sla test /home/vscode/projects/sla_ecs/lib/parallel.sla --test-backend sab --jobs 1 --trace-panic`.
  - [x] Replace the temporary direct `Vec<T>` index ABI lowering with typed std surface metadata/macro lowering once `sa_std` exposes typed element-load slice/vec macros.
    - Progress: `sla_std/std_surface.sla_meta` uses `VEC_GET_TYPED_{elem_ty}`, direct SAB carries `StdSurfaceArgKind.elem_ty`, `elementLoadType`, and `stdSurfaceMacroName`, and concrete typed Vec/Slice macros provide the element-width-specific loads.
    - Progress estimate: 100%; `genIndex` now uses the generic std surface rule for Vec indexing instead of a direct Vec ABI branch.
  - [x] Move direct SAB `Vec<T>` index assignment onto std surface metadata instead of a compiler-side Vec ABI branch.
    - Progress: `sla_std/std_surface.sla_meta` now has an `index_assign` rule for `Vec`, direct SAB parses/preloads/emits that rule generically, and `sa_std/vec.sa` exposes `VEC_SET_TYPED` with `receiver,index,value,elem_size,elem_ty` so scalar and pointer-backed struct writes share one std-surface contract.
    - Progress: std macro-fragment caching now falls back to real-argument flattening when placeholder type operands are invalid, allowing typed std macros to use concrete primitive type names without per-type compiler branches.
    - Verified in the current tree: no-fallback SAB and SA-text backend for `test_unit_vec_index_assign.sla`; no-fallback SAB regressions for `test_unit_vec_len_direct.sla`, `test_unit_arrays.sla`, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; the later fixed-array destructuring/std-slice-index slice moves the full no-fallback `tests/test_unit_*.sla` sweep to 46/64 passing.
    - Progress estimate: 100% for metadata-driven `Vec<T>` index assignment; broader Y/shared-lowering progress is approximately 55% after the later slice, and direct SAB fallback removal is approximately 79% by feature track.
  - [x] Add direct SAB fixed-array destructuring with blank identifier/rest slice support through shared array/slice ABI and std surface metadata.
    - Progress: `lowering_rules.zig` now exposes `SliceAbi` and fixed-array rest-length calculation, and direct SAB `genLetDestructure` uses those rules plus shared array element layout for `[_, name, ..rest]` instead of returning `UnsupportedSabDirectFeature`.
    - Progress: ordinary `let _ = value` discards the generated value without defining `_`, and rest slices keep the temporary array owner alive until scope cleanup.
    - Progress: `sla_std/std_surface.sla_meta` now maps `Slice<T>` indexing to typed `SLICE_GET_TYPED_{elem_ty}` macro wrappers, so `rest[0]` uses the same metadata/macro path as `Vec<T>` indexing rather than a compiler-side Slice branch.
    - Verified in the current tree: local and host no-fallback SAB for `test_unit_blank_identifier.sla`; SA-text backend for the same fixture; host no-fallback SAB regressions for `test_unit_arrays.sla`, `test_unit_vec_index_assign.sla`, `test_unit_user_macro_direct.sla`, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; `zig build --summary all`; `zig build test --summary all`; full no-fallback `tests/test_unit_*.sla` sweep is 46/64 passing.
    - Progress estimate: 100% for fixed-array destructuring/blank identifier direct SAB support; broader Y/shared-lowering progress is approximately 55%, and direct SAB fallback removal is approximately 79% by feature track.
  - [x] Move focused `Cell<T>` construction/get/set support onto std surface metadata instead of compiler-side Cell branches.
    - Progress: associated std-surface calls can now materialize a `value` argument when the metadata rule asks for it, covering constructor-like APIs such as `Cell::new(value)` generically.
    - Progress: `sla_std/std_surface.sla_meta` describes `Cell new/get/set`, and `sa_std/core/cell.sa` exposes `CELL_NEW_VALUE` as a wrapper over the existing Cell layout macros.
    - Verified in the current tree: official `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host no-fallback SAB for `test_unit_cell_bool.sla`; SA-text backend for the same fixture; host no-fallback SAB regressions for `test_unit_option_direct.sla`, `test_unit_vec_len_direct.sla`, `test_unit_blank_identifier.sla`, and `test_unit_vec_index_assign.sla`; `zig build --summary all`; `zig build test --summary all`; full no-fallback `tests/test_unit_*.sla` sweep is 47/64 passing.
    - Progress estimate: 100% for metadata-driven Cell bool support; broader Y/shared-lowering progress is approximately 56%, and direct SAB fallback removal is approximately 80% by feature track.
  - [x] Move closure-taking `Option` methods onto a shared plan consumed by both emitters.
    - Progress: `lowering_rules.zig` now owns `OptionClosureCallPlan` for `map`, `and_then`, and `unwrap_or_else`; `codegen.zig` delegates Option closure-method classification to that plan, and `sab_codegen.zig` consumes it to emit structured branches plus inline closure bodies.
    - Progress: `sla_std/std_surface.sla_meta` exposes `method Option get ... OPTION_GET out,receiver`, so direct SAB gets Some payloads through std surface macro fragments rather than a compiler-side Option layout branch.
    - Verified in the current tree: official `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host no-fallback SAB and SA-text backend for `test_unit_option_methods.sla`; host no-fallback SAB for `/home/vscode/projects/sla_ecs/lib/parallel.sla`; local no-fallback regressions for `test_unit_option_direct.sla` and `test_unit_result_direct.sla`; `zig build --summary all`; `zig build test --summary all`; full no-fallback `tests/test_unit_*.sla` sweep is 48/64 passing.
    - Progress estimate: 100% for shared-plan Option closure-method support; broader Y/shared-lowering progress is approximately 58%, and direct SAB fallback removal is approximately 81% by feature track.
  - [ ] Finish smart-pointer construction/deref/borrow support on shared lowering rules plus std surface metadata.
    - Target: cover `Box::new`, `Rc::new`, `Arc::new`, focused `RefCell::new`, and `*` / `&*` deref-address paths for Box/Rc/Arc through shared smart-pointer classification and metadata-driven std macro fragments, not SAB-only library branches.
    - Current slice: shared `SmartPointerType` helpers, std metadata for `Box`/`Rc`/`Arc`/`RefCell`, SAB deref/address/index consumption, RefCell borrow metadata transfer, and smart-pointer value-slot materialization are drafted. `RC_CLONE_OUT` / `ARC_CLONE_OUT` metadata and std wrappers are also drafted, but clone receiver materialization is not yet correct for borrowed receivers.
    - Latest local proof: `zig build --summary all` and `zig build test -Dtest-filter="lowering" --summary all` passed; `tests/test_unit_borrow_temp_release_order.sla` passes local SAB no-fallback 25/25 and SA-text 25/25; `tests/test_unit_smart_pointer_struct_field_cleanup.sla` passes local SAB no-fallback 3/3; `tests/test_unit_refcell_struct_payload.sla` passes SA-text 2/2 and first SAB no-fallback test.
    - Current blocker before this slice can be counted complete: local SAB no-fallback `tests/test_unit_refcell_struct_payload.sla` still fails test `rc refcell struct payload shared mutation` with signal 11. The suspected cause is `Rc::clone(&rc1)` cloning a borrow/view register instead of first materializing the underlying Rc pointer from the borrowed local slot.
    - Required fix shape: express borrowed smart-pointer receiver loading as a shared associated-call/materialization rule or metadata contract that both emitters can consume. The same rule must cover `Arc::clone(&arc1)`; do not hide the behavior inside a direct `Rc::clone` branch in `sab_codegen.zig`.
    - Remaining call-shape blocker: verify that planned void calls and macro/static calls no longer encode arguments inside the call target string. A disasm line like `call "@sla__refcell_payload_write(tmp_35, tmp_36)"` means Slice 1D/Phase 3 is still open.
    - Verification gate: `zig build --summary all`; `zig build test --summary all`; official `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; no-fallback SAB and SA-text verification for `tests/test_unit_borrow_temp_release_order.sla`, `tests/test_unit_refcell_struct_payload.sla`, and `tests/test_unit_smart_pointer_struct_field_cleanup.sla`; no-fallback regressions for `tests/test_unit_option_methods.sla`, `tests/test_unit_option_direct.sla`, `tests/test_unit_result_direct.sla`, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; full no-fallback sweep count update.
    - Progress estimate: roughly 80% drafted for this smart-pointer/borrow/RefCell slice but 0% completed/verified by the official 100% rule; broader Y/shared-lowering remains approximately 58% until host verification, direct SAB fallback removal remains approximately 81%, and current no-fallback unit-file pass rate remains 48/64 until the next sweep.
  - [ ] Extract a shared address-materialization plan for borrow/postfix precedence chains and consume it from both SA-text and SAB emitters.
    - Target: represent addressable sources such as `&item.field`, `&items[index]`, `&*smart`, `&**holder.item`, and nested field/index/deref chains as one shared plan that records base expression, projections, smart-pointer deref operations, required std-surface rules, and release order.
    - Reason: `tests/test_unit_borrow_temp_release_order.sla` is not only smart-pointer construction; it stresses owner temporary cleanup across fields, indexes, loops, branch merges, call arguments, and early returns. This should converge through a shared address plan rather than ad hoc `genAddressOf` behavior in one backend.
    - Implementation direction: add address/projection structs to `lowering_rules.zig`; keep type- and precedence-sensitive classification shared; let SA-text print the existing textual operations from the plan while SAB emits structured loads/stores/std macro fragments from the same plan.
    - Verification gate: no-fallback SAB plus SA-text for focused borrow/preference fixtures: `tests/test_unit_borrow_direct.sla`, `tests/test_unit_borrow_temp_release_order.sla`, array/index borrow tests, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`.
    - Progress estimate at task creation: 0% for shared address-materialization; expected to move broader Y/shared-lowering by about +4 to +6 points when complete.
  - [ ] Move RefCell borrow/borrow_mut handle lifecycle onto a shared borrow-lifecycle plan instead of copying the SA-text cleanup maps into SAB.
    - Target: cover `RefCell<T>::borrow`, `borrow_mut`, payload pointer extraction for pointer-backed payloads such as `RefCell<Box<T>>` and `RefCell<RefCellPayload>`, panic-on-conflict lowering, and deterministic release on normal scope exit, branch merge, loop iteration, void call argument cleanup, and early return.
    - Implementation direction: define shared plan data for borrow mode, handle owner cell register/expression, payload type, release macro, borrowed value register, and branch/loop merge policy; SA-text keeps its current macro spelling through the shared plan, while SAB emits structured std macro fragments and release branches from that same contract.
    - Current local evidence: borrow-temp and smart-pointer cleanup fixtures are green locally, and one RefCell struct payload SAB test is green. The remaining RefCell struct payload failure is currently assigned to smart-pointer clone receiver materialization, but any subsequent guard-release or payload-pointer failure stays in this lifecycle task.
    - Boundary: broader `Mutex`/`RwLock` guard lifetimes can reuse this shape later but are not part of the first RefCell completion unless a verified fixture requires them.
    - Verification gate: no-fallback SAB and SA-text for `tests/test_unit_borrow_temp_release_order.sla`, `tests/test_unit_refcell_struct_payload.sla`, focused RefCell demos, and existing Cell/Option/Result regressions; full no-fallback sweep count must be updated.
    - Progress estimate at task creation: 0% for shared RefCell lifecycle; expected to move direct SAB fallback removal by about +2 to +4 points if both borrow fixtures pass.
  - [ ] Finish macro-expansion convergence through the Y-shaped shared lowering path.
    - Target: all user macro expansions that survive parsing/type checking should lower through one shared expansion/substitution contract before emitter-specific output, including nested macro calls, macro-local hygiene, argument substitution, borrow/move prefixes, aggregate/index expressions, and macro-expanded static calls.
    - Current baseline: focused direct SAB user macro expansion and macro-expanded static-call materialization already pass; remaining work is to keep future macro features from creating separate SA-text/SAB semantic branches and to add no-fallback coverage whenever a macro expansion gap appears in the sweep.
    - Implementation direction: put new macro classification or argument/materialization decisions in `lowering_rules.zig` or the shared frontend expansion stage, then have both emitters consume the same expanded AST/plans. Do not add SAB-only macro semantics for library names.
    - Verification gate: no-fallback SAB and SA-text for `tests/test_unit_user_macro_direct.sla`, macro-heavy direct tests, `tests/test_unit_pkgjson_codegen.sla` if it remains macro-related after triage, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`.
    - Progress estimate at task creation: focused macro support is mostly complete, but macro convergence hardening is approximately 70%; mark a sub-slice 100% only after a failing macro-shaped fixture is fixed and installed through `sa plugin install --dev .`.
  - [ ] Generalize std-surface metadata for remaining library-shaped direct SAB gaps instead of adding library branches.
    - Target: extend metadata/rule consumers for smart-pointer clone/as_ptr/from_raw/get_mut, collection/set helpers, std import helpers, and result/option-like branch rules only when a failing no-fallback fixture proves the need.
    - Current known failing candidates: `tests/test_unit_sets.sla`, `tests/test_unit_std_import.sla`, `tests/test_unit_sla_import_nested_contract.sla`, `tests/test_unit_rc_dyn_trait.sla`, and smart-pointer/RefCell tests.
    - Implementation direction: prefer new data-driven rule kinds or shared plans over `if type == Vec/Rc/Set` branches in `sab_codegen.zig`; every new std rule should list macro import path, argument slots, deps, panic behavior, receiver materialization, and result/void contract in `sla_std/std_surface.sla_meta`.
    - Immediate metadata gap: Rc/Arc clone needs either metadata arg semantics or a shared call-materialization hook that distinguishes borrowed receiver slots from owned smart-pointer values before invoking `RC_CLONE_OUT` / `ARC_CLONE_OUT`.
    - Verification gate: each completed library-shaped slice needs local build/test, official plugin install, `SA_PLUGIN_DEV=1 sa sla help`, focused no-fallback host test, and regression check for `parallel.sla`.
    - Progress estimate at task creation: metadata infrastructure is approximately 65% mature; this track will close remaining direct SAB fallback by several small 100% slices.
  - [ ] Triage and batch the remaining non-library language gaps through shared rules before removing the fallback.
    - Current no-fallback failures to classify after the borrow/smart-pointer slice: async/await, derive semantics, enum match, for-in protocols, generic for-in, pkgjson codegen, rc/dyn trait, sets, nested import contract extern callee resolution, spaceship compare, std import, struct update, and var comprehensive.
    - Planned batches: enum/match + derive/spaceship shared data-layout rules; for-in protocol + generic iterator lowering; import/extern callee resolution in the shared frontend trunk; struct update and var comprehensive through shared aggregate/assignment plans; async/await last unless a smaller shared Future rule emerges.
    - Completion rule: do not call the overall Y-shared work complete until the full `SLA_SAB_NO_FALLBACK=1` sweep is green or every remaining fallback use has a documented shared-rule task with a concrete failing fixture.
    - Verification gate: update pass/fail counts after every feature; commit only verified completed slices, not partial exploration.
    - Progress estimate at task creation: overall Y/shared-lowering approximately 58%; direct SAB fallback removal approximately 81%; no-fallback unit-file pass rate 48/64.
  - [ ] Add generic exported closure/function-object entry lowering before enabling no-fallback thread-spawn style cases; do not copy the legacy text backend's `thread`-specific lowering into `sab_codegen.zig`.
    - Progress: first direct entry model is in place for zero-arg escaped thread closures, including capture collection, slot materialization, vtable const emission, worker emission, and FFI spawn wrapper emission. General exported closure/function-object lowering beyond this focused consumer remains open.
  - [ ] Remove the remaining in-memory SA-compatible fallback from the normal SAB path by replacing std/macro/closure gaps with generic direct SAB lowering or a generic SAB macro representation, not library-name special cases.
    - Progress estimate: approximately 81% overall direct SAB fallback removal; next priority is direct SAB consumption of the remaining shared call/materialization plan kinds and result-destination planning so both emitters reuse lowering decisions.
  - [ ] Expand the shared static-call plan into a full call/materialization plan, including parameter-aware auto-borrow, array-to-slice borrow, dyn fat-pointer borrow, temp release policy, and result destination contract shared by SA text and SAB emitters.
    - Progress: shared release policy, auto-borrow predicates, resolved static-call argument materialization, resolved static-call dyn fat-pointer borrow materialization, SA text legacy call array/dyn/materialization adoption, SAB ordinary static-call value/auto-borrow consumption, SAB macro static-call value/auto-borrow consumption, and a drafted `void` result-destination plan are in place; remaining pieces are validation of the result-destination contract plus direct SAB materialization for array/dyn/copy kinds.
    - Immediate sub-slice status: `void` result-destination planning is drafted so SAB planned calls can emit no destination for void-returning functions while SA-text keeps its existing legal void-call behavior, but disasm still shows some calls with arguments embedded in the target string. The sub-slice is not complete until targets are pure and args are structured/materialized separately.
    - Verification for the `void` / pure-call-target sub-slice: focused no-fallback `tests/test_unit_borrow_temp_release_order.sla`; `tests/test_unit_refcell_struct_payload.sla`; a small void-call regression if none exists; SA-text parity for the same fixture; `parallel.sla` no-fallback regression; and disasm grep proving tracked SAB artifacts contain no call target matching `@func(`.
- [x] **SLA CLI Project Helpers**
  - [x] Add `sa sla init [path]` to scaffold a minimal SLA binary project without overwriting existing files.
  - [x] Add `sa sla skills [--json]` to list plugin capabilities and generate Codex/Claude agent skill files in text mode.
  - [x] Include `sla init` and `sla skills` in plugin skill descriptors and `sa sla help` / per-command help output.
  - [x] Verify host-dispatched `SA_PLUGIN_DEV=1 sa sla skills --json` returns JSON, not text output.
  - [x] Add focused command tests for skills JSON, agent skill generation, and init overwrite protection.
- [x] **SA std imports**
  - [x] Parse top-level `@import "..."` declarations without adding new keywords.
  - [x] Preserve direct `sa_std/...` imports in generated SA.
  - [x] Load imported `.sai` and `.sal` contracts before type checking so std externs resolve in Sla.
  - [x] Resolve `sa_std/...` contracts from `SA_STD_DIR` or `$HOME/projects/sci/sa_std` during Sla type checking.

### 6. Edge Cases & Validation
- [x] **Internal Function Calls**
  - [x] Correctly compile internal Sla calls to native SA `@call` instructions instead of macro expansions.
- [x] **Recursive Loop Allocation Hoisting**
  - [x] Traverses nested loops and conditional branches inside loops recursively to hoist all stack allocations.
- [x] **Hygienic Macro Variable Renaming**
  - [x] Tracks all declared macro-local variables and maps references to their unique mangled names during codegen.
- [x] **Block vs Return Cleanup Resolution**
  - [x] Excludes returned variables from early return cleanup lists, and prevents block-level exit cleanups from overriding return statement cleanup actions.

### 7. Unit Tests & Build Automation
- [x] **Integrated build test Step**
  - [x] Added `zig build test` target in `build.zig` to compile and run all unit tests automatically.
- [x] **Monomorphizer Unit Test**
  - [x] Implemented in `src/monomorphizer.zig` to verify specialization of generic types.
- [x] **Codegen Unit Test**
  - [x] Implemented in `src/codegen.zig` to verify translation of binary expressions and return statements to SA.

### 8. Sla Unit Test Framework (`@test`)
- [x] **Lexer: `@` token + comparison operators**
  - [x] `at` token tag for `@` symbol (no new keyword).
  - [x] `==`, `!=`, `<=`, `>=` two-char token tags added to lexer.
- [x] **AST: `TestDecl` node**
  - [x] `TestDecl` struct in `src/ast.zig`: `name`, `is_ignored`, `should_panic`, `body`.
  - [x] `test_decl: TestDecl` variant added to `Node` union.
- [x] **Parser: `@test` declaration parsing**
  - [x] Parses `@test [ignored] [should_panic] "name"() { ... }` syntax.
  - [x] No new keywords consumed — `ignored` and `should_panic` are identifiers.
  - [x] New comparison ops (`==`, `!=`, `<=`, `>=`) parsed in `parseInfixExpr`.
- [x] **Type Checker: `checkTest`**
  - [x] Type checks test body as void-returning, no-parameter block.
  - [x] `panic()` recognized as built-in void intrinsic.
  - [x] `if` condition accepts integer comparisons (0/1) in addition to booleans.
- [x] **Monomorphizer: `test_decl` traversal**
  - [x] Specializes generic calls inside test bodies.
- [x] **Codegen: `genTestDecl`**
  - [x] Emits `@test [ignored] [should_panic] "name"():` SA header.
  - [x] Unique entry labels per test (`L_TEST_ENTRY_N`).
  - [x] `panic(code)` lowered to SA `panic(code)` syntax.
  - [x] Loop allocation hoisting works inside test bodies.
- [x] **CLI: `sa sla test <file.sla> [options]`**
  - [x] Defaults to `--test-backend auto`, compiling `.sla` to managed SAB under `.sla-cache/sab/` and spawning `sa test <file.sab>`.
  - [x] Supports `--test-backend sab` for explicit SAB output and `--test-backend sa` for the legacy `.sla` -> `.test.sa` temp file path.
  - [x] Strips plugin-only `--test-backend` before forwarding remaining args to `sa test`.
  - [x] Applies `--filter` pruning before monomorphization/type checking on both SAB and SA test paths to keep focused tests small.
  - [x] Skill entry added: `sla test <file> [--test-backend auto|sab|sa] [sa-test-options...]`.
- [x] **Example test files**
  - [x] `tests/test_unit_basic.sla` — add/factorial/counter/ignored tests.
  - [x] `tests/test_unit_panic.sla` — `should_panic` and `ignored+should_panic`.
  - [x] `tests/test_unit_generics.sla` — `Option<T>` generics inside `@test`.
