# Sla Compiler Implementation Tasks

This document tracks the tasks and implementation progress of the Sla compiler plugin (`sa_plugin_sla`).

---

## Task List

### 0. Roadmap To 100% Y-Shared Direct SAB Completion
- [ ] **Operating Rules Until Completion**
  - [x] Active implementation workspace is `/home/vscode/projects/sa_plugins/sa_plugin_sla`. Do not switch implementation or task planning into `/home/vscode/projects/sla_ecs`; use `sla_ecs` only as an explicit host regression target such as `lib/parallel.sla`.
  - [x] Keep the architecture Y-shaped: `SLA source -> AST/typecheck -> shared frontend/shared lowering rules/std metadata -> {SA text emitter -> .sa -> sa compile, SAB structured emitter -> .sab -> sa compile}`.
  - [x] The shared layer is before the SA/SAB fork; direct SAB must stay direct and must not implement `.sla -> .sa text -> .sab` as the normal path.
  - [x] New semantics belong in `src/lowering_rules.zig`, shared frontend/typecheck results, `sla_std/std_surface.sla_meta`, or the `sa_std` macro/import surface. `src/sab_codegen.zig` should consume those contracts and emit SAB.
  - [x] Use `sa plugin install --dev .` as the official install/host verification gate; do not manually copy plugin artifacts.
  - [x] Report feature completion percentage, broader Y/shared-lowering percentage, direct SAB fallback-removal percentage, and current no-fallback sweep count after every completed feature.
  - [x] Commit only verified completed slices. Partial exploration stays uncommitted.
  - [x] If more than 10 verified feature slices accumulate without a commit, stop and recommend a batch commit; if the verified set is clean, commit the completed slices before continuing.
  - [x] Before each implementation slice, update this file with the target, owner phase, expected verification, architectural boundary, and progress estimate.
- [ ] **Progress Accounting Rules**
  - [ ] A feature slice is 100% only after local build, focused local no-fallback run, official `sa plugin install --dev .`, `SA_PLUGIN_DEV=1 sa sla help`, focused host no-fallback run, SA-text parity when applicable, relevant regressions, docs/progress updates, and `git diff --check`.
  - [ ] If a slice fixes only the direct SAB tail but leaves duplicated lowering decisions outside `lowering_rules.zig`, shared frontend, or `sla_std/std_surface.sla_meta`, count it as a partial bug fix and keep the Y/shared task open.
  - [ ] Do not treat fallback success as direct-SAB progress unless the fallback use is removed or documented as an explicitly unsupported surface with a concrete future task.
  - [ ] Use this percent scale: 0% not started, 25% design/owner identified, 50% implementation drafted and builds, 75% focused local no-fallback filters pass, 90% full local fixture/regressions pass, 100% host install/help/host regressions/sweep/docs/diff-check complete.
  - [ ] Keep global numbers conservative: a draft that has not passed host `sa plugin install --dev .` and `SA_PLUGIN_DEV=1 sa sla help` does not move global Y/shared-lowering or fallback-removal percentages.
- [ ] **P1 Short-Term Stability, Labels, And Benchmark Formalization**
  - [ ] Push `sa_plugin_sla` direct-SAB no-fallback from the historical 58/69 baseline to 69/69 as a key mainline stability metric for moving Sla from usable-with-fallback to truly stable direct SAB. Current newer tracked corpus is above that historical denominator, but this P1 task remains the canonical short-term metric until the old 69-file set is explicitly mapped and closed.
  - [x] Provide compiler-side stability metadata support for downstream projects: a manifest/schema and verification output that can record labels such as `stable-demo`, `verified-sa-backend`, `verified-sab-backend`, `experimental-parallel`, and `shape-only-reflect`. This repo must not define or maintain `sla_ecs` labels directly; it only supplies the contract/tooling that downstream projects can consume. Implemented by `src/stability_metadata.zig`, `sa sla stability schema`, `sa sla stability verify <manifest.json> [--json]`, `sap.json` help, and `docs/stability_metadata_cn.md`; verified with focused Zig tests, full `zig build`, full `zig build test` (90/90), local/installed CLI smoke, `sa plugin install --dev .`, and `SA_PLUGIN_DEV=1 sa sla help`.
  - [x] Add standalone benchmark harness coverage and publish it as a formal benchmark track owned by this repo: `.sla -> .sab` cold/warm compile time, `.sla -> native` build time, and SAB fallback vs direct SAB. Domain throughput such as ECS spawn/query/schedule is exposed through configurable external benchmark commands/hooks, not implemented as `sla_ecs` business logic in this compiler repo. Implemented by `tools/bench_sla_pipeline.sh` plus `docs/benchmarking_cn.md`; smoke verified with `bash -n`, `--runs 1 --skip-native`, full built-in `--runs 1`, and `--external 'noop::true'` JSONL output.
- [ ] **P2 Medium-Term Generation, Formal, And Version Semantics**
  - [ ] Provide the compiler-side generated-source support needed by downstream composition-runner generators: stable schema inputs, import/codegen contracts, typecheck hooks, and verification metadata. Do not maintain `sla_ecs` runner generation in this repo; downstream projects own their generated runner specs and outputs.
  - [ ] Turn the `sci` Referee formal verification roadmap into an executable plan. The README-level Coq/Lean4 high-assurance direction needs a staged implementation route if high assurance is a core project claim.
  - [ ] Establish explicit version semantics across the three repos: semver policy, changelog discipline, and a compatibility matrix. Account for `sci` currently declaring `0.0.1` in `build.zig.zon` while build scripts can derive versions from git tags.
- [ ] **Current Recovery Point For Next Context**
	- [x] Current docs-issue correction pass: tightened and reverified three previously over-broad green claims with 10s focused local evidence. `src/codegen.zig` now treats `JoinHandle.join()` as consuming the actual receiver register and no longer emits a trailing `!handle`; `tests/test_unit_join_handle_vec_direct.sla` passes SA-text and strict SAB locally. `src/sab_codegen.zig` now keeps non-owning stack-derived string/Slice sources alive when borrowed stack-storage `let` bindings are created for `STR_PTR(text)` / `STR_LEN(text)`, avoiding strict SAB `StackEscape`; `tests/test_unit_str_ptr_len_identifier_direct.sla` passes strict SAB locally. Direct SAB identifier assignment now marks non-Copy RHS identifiers consumed after `assign` without emitting extra `move_ next_bag`; `tests/test_unit_sa_aggregate_reassign_move_cleanup.sla` passes strict SAB locally. Additional 10s strict SAB guards passed for loop cleanup, reassignment after release, fn-ptr value/thread bool, binary Copy param, string equality, and RefCell owner release. `zig build --summary all` and focused Zig test `binary expression releases materialized cast result` passed; no compile caches were deleted, and observed rebuilds of large Zig modules took about 46-67s.
	- [x] Completed docs-priority compiler issue: `docs/sa_scope_taskset_move_cleanup_useaftermove_issue_cn.md` cleanup subproblem is fixed for the current compiler repro and downstream host surface. Added `tests/test_unit_sa_aggregate_reassign_move_cleanup.sla`, covering tuple-destructured non-Copy aggregate reassignment `let (next_bag, value) = ...; bag = next_bag;`. Root cause was generated-SA assignment consuming the RHS while compiler-side cleanup state still treated the RHS binding as live, so loop-body cleanup emitted `!next_bag` / `!next_a` / `!next_world`. `src/codegen.zig` now transfers compiler-side value-state metadata and marks non-Copy, non-borrow-like identifier RHS values consumed after assignment without emitting an extra `!rhs`; the target binding is restored to live after receiving the new value. Verification: `zig fmt --check src/codegen.zig`; `zig build --summary all`; local rebuilt CLI SA focused fixture and cleanup guards; official `sa plugin install --dev .`; installed SA/SAB focused fixture; installed cleanup guards; downstream `/home/vscode/projects/sla_ecs/lib/entity_dynamic.sla` generated-SA 7/7; downstream `test_ecs_mut_parallel.sla --filter "task pool scoped task set" --test-backend sa` 16/16; full downstream generated-SA `test_ecs_mut_parallel.sla` 129/129; full downstream default/SAB `test_ecs_mut_parallel.sla` 129/129. The earlier `ecs_parallel_scope_run_result_recursive` 21 args vs 22 params `InvalidArgsCount` observation is historical/transient and no longer reproduces.
	- [x] Completed docs-priority compiler issue: `docs/execute_extra_parse_tsconfig_useaftermove_issue_cn.md` is fixed, and the original UnknownRegister blocker in `docs/sab_large_execute_unknown_register_issue_cn.md` is no longer current for downstream execute/cli host evidence. Root causes: SA-text imported macro value materialization did not record actual releases into `consumed_bindings`; identifier reassignment did not clear stale consumed state; loop/branch cleanup needed lexical release behavior after non-fallthrough state pollution; direct SAB reused by-value primitive Copy param registers across consuming SAB ops. Implementation: `src/codegen.zig` now records ordinary/resource/container releases from imported macro materialization, removes reassigned identifiers from consumed state, emits lexical cleanup release for break/continue/non-terminating cleanup, and releases loop-body top-level locals before natural while backedges. `src/sab_codegen.zig` now materializes by-value primitive Copy params into entry stack slots and consumes the original param register after initial store. Added regressions: `tests/test_unit_tsconfig_buffer_cleanup.sla`, `tests/test_unit_reassign_after_release.sla`, `tests/test_unit_loop_body_local_cleanup.sla`, and `tests/test_unit_sab_binary_copy_param_direct.sla`. Verification: `zig fmt --check src/codegen.zig src/sab_codegen.zig`; `zig build --summary all`; `zig build test --summary all` (94/94); focused SA/SAB fixture gates; official `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; downstream `/home/vscode/projects/mnt/sla_tsgo` `members/execute/src/cli.sla` check; downstream `test_cli_contract.sla` SA/default 8/8; downstream `test_execute_extra_contract.sla` SA/default 23/23; downstream `test_execute_contract.sla` default/SAB no verifier trap, with only historical runtime/business assertions remaining (33/49 passed). `docs/ls_autoinsert_aggregate_scanner_register_redefinition_issue_cn.md` is fixed and downstream-reverified; `sla_tsgo` restored the historical LS autoinsert aggregate assertion and it passes under SA backend.
	- [x] Completed docs-priority compiler issue: `docs/join_handle_vec_memory_leak_issue_cn.md` is fixed for the current compiler repro. Added `tests/test_unit_join_handle_vec_direct.sla`, covering `Vec<JoinHandle<i32>>` push, index read, `join().unwrap()`, and loop aggregation. Root cause was SA-text ordinary binary-expression cleanup using call-argument release rules, leaving the materialized `len(handles) as i64` cast result active after comparison. `src/lowering_rules.zig` now owns shared pointer-carrier cast classification / cast-result materialization planning, and `src/codegen.zig` releases binary operand result registers only when the cast actually materializes a temp. Added Zig regression `binary expression releases materialized cast result`. Verification: `zig build --summary all`; `zig build test --summary all` (93/93); focused codegen Zig test; local rebuilt CLI SA-text and strict direct-SAB no-fallback for the new fixture (1/1 each); official `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; installed CLI SA-text and strict direct-SAB no-fallback for the new fixture (1/1 each). Downstream arbitrary-N executor coverage remains `sla_ecs`-owned regression evidence after its business test is restored.
	- [x] Completed docs-priority compiler issue: `docs/selection_ranges_for_positions_memoryleak_issue_cn.md` is fixed for the current compiler repro and downstream `sla_tsgo` host surface. Added `tests/test_unit_str_ptr_len_identifier_direct.sla`, covering `ptr_len_roundtrip(STR_PTR(text), STR_LEN(text))` so imported `sa_std/string.sa` macros receive owned string/Slice bindings through identifier expressions. Root cause was imported macro value-arg cleanup using ordinary call-argument rules, leaving receiver load temps from `STR_PTR(text)` / `STR_LEN(text)` active after macro expansion in both SA-text and direct SAB. `src/codegen.zig` and `src/sab_codegen.zig` now release imported macro value args when argument expression lowering produced a temporary result. Verification: `zig build --summary all`; `zig build test --summary all` (93/93); local rebuilt CLI SA-text and strict direct-SAB no-fallback for the new fixture (1/1 each); official `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; installed CLI SA-text and strict direct-SAB no-fallback for the new fixture (1/1 each); downstream direct temporary probe SA-text and strict SAB (2/2 each); downstream `selectionrange.sla` check; downstream stable `tests/test_selectionrange_contract.sla` SA-text and default backend (4/4 each). Downstream temporary probe file was removed after verification.
	- [x] Completed docs-priority follow-up verification: `docs/document_symbol_protocol_summary_memoryleak_issue_cn.md`, `docs/linked_editing_direct_tsx_memoryleak_issue_cn.md`, and the current scanner-helper surface in `docs/module_specifier_subpath_match_memoryleak_issue_cn.md` are fixed/verified by the same imported macro value-arg receiver-temp cleanup. Downstream temporary probes passed and were removed: document symbol direct summary SA-text 2/2 and strict SAB 2/2; linked editing TSX direct SA-text 1/1 and strict SAB 1/1; module specifier scanner helpers SA-text 1/1 and strict SAB 1/1. Stable downstream contracts passed SA/default: document symbols 9/9, linked editing 5/5, modulespecifiers 19/19.
	- [x] Refreshed Phase 9 direct-SAB no-fallback baseline for the current worktree after the docs-priority regression fixtures were added. Current `tests/test_unit_*.sla` corpus is 118 fixture files. Local sweep with `SLA_SAB_NO_FALLBACK=1 ./zig-out/bin/sla-local-cli sla test ... --test-backend sab` passed 118/118; installed host sweep with `SA_PLUGIN_DEV=1 SLA_SAB_NO_FALLBACK=1 sa sla test ... --test-backend sab` also passed 118/118. Installed host `/home/vscode/projects/sla_ecs/lib/parallel.sla` strict direct-SAB was rechecked at 1/1, and SAB build/disasm to `/tmp/parallel_phase9.disasm.sa` had a clean illegal visible call-target guard. This confirms direct SAB fallback-removal remains 100% for the tracked unit corpus, but global completion remains open for broader RefCell lifecycle/projection planning, macro convergence, call/materialization, SCI boundary, async scope, docs sync, and verified commits.
	- [x] Completed docs-priority compiler issue: `docs/sab_anyof9_table_erased_memoryleak_issue_cn.md` `first_type_id` MemoryLeak is fixed for the compiler-owned SAB cleanup surface. Direct SAB now emits visible `.move_` consume instructions for entry stack-slotted by-value Copy scalar params and for param cleanup consume paths, instead of only marking those registers consumed in emitter state. Verification: `zig fmt --check src/sab_codegen.zig`; `zig build --summary all`; `zig build test --summary all` (94/94); official `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; local and installed strict SAB focused scalar/COPY-param fixtures; downstream strict SAB `system_param_table_erased.sla --filter "filtered pair mut system params"` pass; downstream relationship/observer AnyOf wrapper strict SAB filters pass; full local strict SAB unit sweep 118/118; full installed host strict SAB unit sweep 118/118; `git diff --check`. Downstream `world_table_erased.sla --filter "anyof nested"` no longer verifier-traps but fails with `panic 15305` under both SA backend and strict SAB, so that remaining assertion is downstream semantic/business evidence rather than a current SAB cleanup blocker.
	- [x] Completed Phase 1 RefCell lifecycle hardening sub-slice while continuing to consider the actual issue-ticket set `docs/*issue*.md` (`docs/*issue.md` has no strict matches). `src/lowering_rules.zig` now owns `planRefCellHandleCellRelease`, which decides when an active RefCell borrow handle must be released because its `cell_reg` matches the owner being released. `src/codegen.zig` consumes the shared rule for SA-text's owner cleanup scan and now snapshots/restores `consumed_bindings`, `borrow_source_temps`, and `refcell_borrow_handles` after emitting chained `if let` fail-path cleanup, so fail-path consumed state does not pollute success-path lowering. `src/sab_codegen.zig` now releases matching active borrow handles before releasing the owner register, and branch-local cleanup uses a RefCell/borrow-temp aware helper with a branch-local `seen` set so fail-path cleanup emits dynamic-borrow releases without mutating global `released_regs` or RefCell metadata maps. Added/extended `tests/test_unit_refcell_owner_release_direct.sla` for explicit owner release and chained `if let` failure cleanup with a RefCell owner plus active borrow handle. Verification: `zig fmt --check src/lowering_rules.zig src/codegen.zig src/sab_codegen.zig`; focused lowering-rule test; `zig build --summary all`; `zig build test --summary all` (94/94); local SA-text and strict direct-SAB focused fixture 2/2; local rosetta 104 SA/SAB 1/1; existing RefCell payload SA/SAB 7/7; borrow-temp strict SAB 25/25; official `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; installed SA-text and strict direct-SAB focused fixture 2/2; installed rosetta 104 SA/SAB 1/1; full local strict direct-SAB unit sweep 119/119; full installed host strict direct-SAB unit sweep 119/119. This is 100% for the focused owner-release / branch-cleanup / chained-if-let fail-state sub-slice; broader RefCell lifecycle, pointer-backed imported-macro alias semantics, shared call/materialization convergence, and global completion remain open.
	- [x] SLA frontend import architecture first slice: `src/plugin.zig` now uses an in-process `SlaModuleTable` keyed by resolved path, so repeated `.sla` imports share one parsed AST during a single import-expansion pass and duplicate modules are emitted once. This is a stop-the-bleeding foundation only: imported declarations are still flattened into the current program to preserve existing TypeChecker/monomorphizer behavior, so large unique import graphs still need shallow symbol indexing, namespace isolation, and lazy typecheck before they can reach sub-second compile targets.
	- [x] SLA test-codegen lazy-body slice: `src/plugin.zig` now enables `prune_for_test_codegen` for SA/SAB test-input generation. The frontend prunes unreachable top-level functions plus unreachable inherent impl/overload methods before typecheck, then applies exact typechecked reachable output pruning before direct-SAB codegen. Trait impls stay intact for vtable safety. This is test-path only and does not change normal build/check full validation.
	- [x] SLA test-codegen import-expansion pruning slice: `src/plugin.zig` now builds the imported `.sla` module graph before flattening on the test-codegen path, indexes imported/top-level function and method symbols, computes a syntactic reachable set from selected tests plus const/macro/trait roots, and skips unreachable imported top-level functions plus unreachable inherent impl/overload methods during flattening. Trait impls and non-function declarations remain intact for vtable/type safety. Added `sla test import expansion prunes unreachable imported functions`; focused pruning/import tests, `zig build --summary all`, full `zig build test --summary all` (98/98), and 10s strict local SAB import/parallel-runner guards pass. Downstream `sla_tsgo` checker-pool/project-background tests still timeout at 10s, so this is a partial latency slice, not issue closure.
	- [x] SLA frontend callable-index slice: `src/plugin.zig` now builds a `SlaCallableIndex` for test-codegen reachability. The index records callable names plus function/method bodies once for the root and imported `.sla` module graph, then consumes the syntactic worklist with O(1) body lookups instead of repeatedly scanning every root/imported decl for each reachable name. Verified with focused reachability tests, `zig build --summary all`, full `zig build test --summary all` (99/99), `SA_PLUGIN_DEV=1 sa sla help`, and 10s downstream profile refreshes that still timeout. The initial `sa plugin install --dev .` gate failure was later traced to forced ReleaseFast compilation and fixed by the dev install latency slice below. This is ModuleGraph infrastructure only: declarations are still flattened for the current TypeChecker/monomorphizer, so full namespace isolation, exported-signature-only imports, lazy imported-body typecheck, and lazy direct-SAB dependency loading remain open.
	- [x] SLA frontend callable-index associated-candidate slice: `SlaCallableIndex` now records short method name -> mangled associated-method symbol candidates while registering callable bodies, so syntactic reachability for method/associated calls no longer scans every callable symbol by suffix for each call. Added `sla test import expansion prunes unreachable imported methods`, proving imported inherent methods keep the called method while pruning an unused broken method before typecheck. Verification: focused imported-method/function/pre-typecheck pruning tests; `zig build --summary all`; full `zig build test --summary all` (100/100); official `SA_PLUGIN_DEV=1 sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; local and installed strict SAB guards for impl static methods, Rc dyn trait, and SLA import. Downstream `sla_tsgo` checker-pool/project-background 10s profiles still timeout, so this is another ModuleGraph/callable-index foundation slice, not issue closure.
	- [x] SLA frontend Module Table export-index foundation slice (2026-07-08): `src/plugin.zig` now has `SlaModuleExports`, indexing each resolved `.sla` module's exported type/function/const/macro/impl surface by name, and `SlaCallableIndex.module_sources`, recording the owning module path for every registered callable. `buildReachableImportedTestSymbols` now routes imported modules through `SlaCallableIndex.addDeclsFromModule(decls, module)`, so the test-codegen reachability computation attributes each reachable callable to its source module instead of re-scanning every flattened body. Added Zig regression `sla module exports index records per-module function sources` proving `exportsType/exportsFunction` plus `moduleSource(...)` for top-level and inherent-method callables attribute the correct owning module. Verification: `zig fmt --check src/plugin.zig`; `zig build --summary all`; focused `module exports index` (1/1), `prunes unreachable imported` (2/2), and `sab test codegen omits unreachable` (2/2) tests; full `zig build test --summary all` (101/101, up from 100/100); `SA_PLUGIN_DEV=1 sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; installed host strict `SLA_SAB_NO_FALLBACK=1` guards for import/nested/wildcard/wildcard-bare/sa_std-output-path(parallel)/trait-dispatch/rc-dyn-trait/parallel-runner-loop-fnptr-transfer. Known pre-existing/unrelated: `test_unit_sla_import_sa_std_output_path.sla` strict SAB `panic 1501` reproduces identically on the committed baseline, so it is not a regression of this slice. This is the first slice of the real ModuleGraph exported-signature index (per-module exported symbol tables are populated at parse time and reachability carries module-qualified ownership). It is not yet namespace isolation or lazy imported-body typecheck; imported function/method bodies are still flattened. Next Module Table work: drive module-qualified reachability via `moduleSource` + `SlaModule.exports`, then selectively flatten only contributing modules' bodies.
	- [x] SLA frontend Module Table exported-signature surface follow-up (2026-07-08): `SlaModuleExports` now records a metadata-only `FunctionSignature` table beside `function_decls`, carrying name, params, return type, extern/ABI/no-mangle flags, and owning module path. This gives future exported-signature-only TypeChecker traversal a signature lookup surface that does not require using a function body pointer as the only exported function representation. The export-index regression now asserts the imported `i32` function signature and module path. Verification so far, all serial with timeouts: `timeout 120s zig fmt --check src/plugin.zig`; focused module exports index (1/1). Module Table filter (7/7), import-pruning filter (2/2), and SAB unreachable-codegen filter (2/2) also pass. `timeout 180s zig build --summary all`; `timeout 240s zig build test --summary all` (108/108); `env SA_PLUGIN_DEV=1 timeout 180s sa plugin install --dev .`; `env SA_PLUGIN_DEV=1 timeout 60s sa sla help`; and installed strict SAB guards for `test_unit_sla_import.sla` (2/2) and `test_unit_trait_static_dispatch.sla` (4/4) also pass. `timeout 120s git diff --check` is clean.
	- [x] SLA frontend Module Table exported-type signature surface follow-up (2026-07-08): `SlaModuleExports` now records a metadata-only `TypeSignature` table beside `type_decls`, carrying exported type name, type kind (`struct_decl`/`enum_decl`/`trait_decl`/`type_alias_decl`), generic parameter names, and owning module path. This gives future exported-signature-only TypeChecker traversal a type metadata surface that does not require treating the full AST type declaration as the only exported type representation. The export-index regression now asserts the imported `ImportedTag` struct signature and module path. Verification so far, all serial with timeouts: `timeout 120s zig fmt --check src/plugin.zig`; focused module exports index (1/1). Module Table filter (7/7), import-pruning filter (2/2), and SAB unreachable-codegen filter (2/2) also pass. `timeout 180s zig build --summary all`; `timeout 240s zig build test --summary all` (108/108); `env SA_PLUGIN_DEV=1 timeout 180s sa plugin install --dev .`; `env SA_PLUGIN_DEV=1 timeout 60s sa sla help`; and installed strict SAB guards for `test_unit_sla_import.sla` (2/2) and `test_unit_trait_static_dispatch.sla` (4/4) also pass. `timeout 120s git diff --check` is clean.
	- [x] SLA frontend Module Table exported const/macro signature surface follow-up (2026-07-08): `SlaModuleExports` now records metadata-only `ConstSignature` and `MacroSignature` tables beside `const_decls` and `macro_decls`, carrying const name/optional declared type/module path and macro name/parameter names/module path. This completes the current function/type/const/macro exported metadata surface before the future exported-signature-only TypeChecker traversal. The export-index regression now asserts the imported const `i32` signature and imported macro parameter metadata. Verification so far, all serial with timeouts: `timeout 120s zig fmt --check src/plugin.zig`; focused module exports index (1/1). Module Table filter (7/7), import-pruning filter (2/2), and SAB unreachable-codegen filter (2/2) also pass. `timeout 180s zig build --summary all`; `timeout 240s zig build test --summary all` (108/108); `env SA_PLUGIN_DEV=1 timeout 180s sa plugin install --dev .`; `env SA_PLUGIN_DEV=1 timeout 60s sa sla help`; and installed strict SAB guards for `test_unit_sla_import.sla` (2/2) and `test_unit_trait_static_dispatch.sla` (4/4) also pass. `timeout 120s git diff --check` is clean.
	- [x] SLA frontend Module Table explicit dependency-graph follow-up (2026-07-08): `SlaModule` now records `resolved_imports` when the `SlaModuleTable` parses a module, giving each module an explicit dependency edge list alongside its AST and exports. `appendModuleDeclsSelective()` and `collectSlaModulesRecursive()` now traverse those stored edges instead of rescanning AST import declarations and resolving paths again. The export-index regression now asserts that a module records its direct `.sla` child import in `resolved_imports`, proving the Module Registry shape is present before future namespace/lazy TypeChecker work. Verification so far, all serial with timeouts: `timeout 120s zig fmt --check src/plugin.zig`; focused module exports index (1/1); Module Table filter (7/7); import-pruning filter (2/2); SAB unreachable-codegen filter (2/2). `timeout 180s zig build --summary all`; `timeout 240s zig build test --summary all` (108/108); `env SA_PLUGIN_DEV=1 timeout 180s sa plugin install --dev .`; `env SA_PLUGIN_DEV=1 timeout 60s sa sla help`; and installed strict SAB guards for `test_unit_sla_import.sla` (2/2) and `test_unit_trait_static_dispatch.sla` (4/4) also pass. `timeout 120s git diff --check` is clean.
	- [x] SLA frontend Module Table module-qualified export lookup follow-up (2026-07-08): `SlaModuleTable` now has module-qualified signature lookup helpers for exported functions, types, consts, and macros using `(module_path, name)`. The same-name sibling-module regression imports `dep.sla` and `sibling.sla`, both exporting `imported_a`, `ImportedTag`, `IMPORTED_VALUE`, and `imported_macro`, and proves the table returns the metadata for the requested module path instead of collapsing exports into a single global name. Verification was intentionally batched rather than updating docs after each test: `timeout 120s zig fmt --check src/plugin.zig`; focused module exports index (1/1); Module Table filter (7/7); import-pruning filter (2/2); SAB unreachable-codegen filter (2/2); `timeout 180s zig build --summary all`; `timeout 240s zig build test --summary all` (108/108); `env SA_PLUGIN_DEV=1 timeout 180s sa plugin install --dev .`; `env SA_PLUGIN_DEV=1 timeout 60s sa sla help`; installed strict SAB import guard (2/2); installed strict SAB trait-dispatch guard (4/4); `timeout 120s git diff --check`. This is still a data-contract foundation for future namespace resolution, not completed namespace isolation, exported-signature-only TypeChecker traversal, or lazy direct-SAB dependency loading.
	- [x] SLA frontend Module Table selective-flatten slice (2026-07-08): `expandSlaImports()` now builds a full imported `.sla` module graph, indexes each module's callable bodies and exported surface, computes a syntactic reachable set from root/test roots, and only flattens contributing imported modules' reachable function/method bodies plus required non-function surfaces. Non-contributing imported modules no longer contribute broken unused bodies to TypeChecker, and transitive imports still recurse through a non-contributing parent when a child module is reachable. Trait impls keep complete method signatures by turning unreachable trait methods into decl-only stubs, avoiding TypeChecker body checks for unused broken trait methods while preserving trait completeness. Referenced imported const initializers and macro bodies are now scanned as symbol roots, so helper functions used only by imported consts/macros stay reachable without flattening unrelated bodies. Exported type signatures are scanned too, so struct/enum/trait/type-alias references can pull in child-module type dependencies without pulling in child function bodies. Generic argument types are now scanned from `call_expr.generics` and `generic_func_ref.generics`, so imported types used only as generic function/call arguments make their module contribute without admitting unrelated imported bodies. Added Zig regressions `sla module table skips non contributing imported module bodies`, `sla module table selective flatten in non test compile path`, `sla module table reaches contributing transitive module through non contributing parent`, `sla module table prunes unreachable trait impl methods in contributing module`, `sla module table follows imported const initializer reachability`, `sla module table follows imported type signature dependencies`, and `sla module table follows generic argument type dependencies`. Latest verification is serial and timeout-guarded: `timeout 120s zig fmt --check src/plugin.zig`; focused generic-argument regression (1/1); `timeout 180s zig build test -Dtest-filter="sla module table" --summary all` (7/7); import-pruning filter (2/2); SAB unreachable-codegen filter (2/2). Broader gates rerun so far after the generic-argument follow-up: `timeout 180s zig build --summary all`; `timeout 240s zig build test --summary all` (108/108). `env SA_PLUGIN_DEV=1 timeout 180s sa plugin install --dev .`, `env SA_PLUGIN_DEV=1 timeout 60s sa sla help`, and installed strict SAB guards for `test_unit_sla_import.sla` (2/2) and `test_unit_trait_static_dispatch.sla` (4/4) have also been rerun. `timeout 120s git diff --check` is clean. This reduces blind imported-body flattening on normal and test paths, but full namespace isolation, exported-signature-only TypeChecker traversal, and lazy direct-SAB dependency loading remain open.
	- [x] Dev install latency fix: `build.zig` now treats installer-requested `-Doptimize=ReleaseFast` as Debug only when `SA_PLUGIN_DEV=1/true` is present. The global installer still invokes ReleaseFast, but dev installs now reuse the fast Debug/incremental artifact path instead of silently compiling ReleaseFast for more than 300 seconds. Formal non-dev ReleaseFast remains unchanged and still needs separate compiler-latency work. Verification: direct `SA_PLUGIN_DEV=1 zig build -Doptimize=ReleaseFast --summary all` used cached Debug artifacts; non-dev verbose ReleaseFast still emitted real ReleaseFast compile commands; `SA_PLUGIN_DEV=1 sa plugin install --dev .` passed in 0.31s; `zig build --summary all`; full `zig build test --summary all` (99/99); `SA_PLUGIN_DEV=1 sa sla help`; downstream checker-pool/project-background 10s profiles still timeout and remain open.
	- [x] SLA test-codegen vtable-aware trait impl output pruning slice: after typecheck, `src/plugin.zig` now drops trait impl declarations before direct-SAB codegen only when they have no reachable trait method and no reachable dyn borrow/Box dyn/Rc dyn vtable evidence. Dyn/vtable-needed impls remain whole for vtable safety. Added `sla sab test codegen omits unreachable trait impls after type checking`; full `zig build test --summary all` is 99/99 and `tests/test_unit_rc_dyn_trait.sla` strict SAB remains green under a 10s guard. Downstream timeout docs remain open.
	- [x] Completed docs-priority direct-SAB register-scan issue: `docs/sab_task_pool_custom_batch_width_unknown_dst_issue_cn.md` is fixed. `src/sab_codegen.zig::recordCallBodyRegs()` now records ordinary identifiers from call body text only when they are already current-scope registers, while still recording direct call targets. This prevents std macro fragment/global symbol names such as `dst` from being recorded as undeclared registers in later focused tests. Verification: focused Zig reg-scan test; `zig build --summary all`; `zig build test --summary all` (99/99); official dev install/help; installed downstream strict SAB 10s focused custom batch width and external executor identity pass.
	- [ ] Next SLA frontend architecture slice: replace AST concatenation with a real ModuleGraph/Module Table contract that registers exported signatures separately from function bodies, then extend lazy traversal into TypeChecker and direct-SAB codegen dependency loading so large single-test graphs stop timing out after reachable filters. Keep all dangerous downstream repros under `timeout 10s`; if a whole-file smoke emits no output, stop and return to minimal fixtures.
	- [ ] New docs issue audit item: `docs/sa_backend_mut_parallel_whole_file_for_stmt_typemismatch_issue_cn.md` tracks a generated-SA whole-file `tests/test_ecs_mut_parallel.sla` typecheck failure at `for_stmt` (`error.TypeMismatch`) after focused executor/bridge paths pass. Do not run the documented 120-240s `--jobs 1` whole-file commands as the default smoke; first isolate a small compiler-owned repro, then only use the whole-file path under an explicit short guard or after the focused fixture explains the type mismatch.
	- [ ] Next implementation target: resume broader Phase 1/3 full-completion work while continuing to consider the actual issue-ticket set `docs/*issue*.md` (`docs/*issue.md` has no strict matches in this repo). Remaining open work includes shared RefCell borrow-handle lifecycle, pointer-backed imported-macro alias semantics, and shared call/materialization convergence. Treat downstream repos only as host evidence; compiler fixes belong in this repo's shared frontend/lowering/std metadata and SA-text/direct-SAB emitters.
	- [x] Completed docs-priority compiler issue: `docs/sab_empty_filter_memoryleak_issue_cn.md` is fixed for the current compiler repro and downstream host surface. `src/plugin.zig` now detects explicit non-empty `--filter` values that select no expanded `@test` declarations before SA/SAB test-input generation, writes the canonical empty test result, and returns success without generating managed SAB artifacts. Added Zig regression `sla test empty filter skips sab compilation`. Verification: focused Zig test; `zig build --summary all`; `zig build test --summary all` (92/92); official `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; installed downstream strict SAB no-fallback empty filters for `definitely no such ecs test` and stale `ecs_world_try_query_single returns one`; installed downstream real matching SAB filter `ecs_world_try_get returns ok for present component` still passes 1/1. `sla_ecs` remains downstream regression evidence only; no implementation scope moved into that repo.
	- [x] Completed docs-priority compiler issue: `docs/sab_aggregate_mut_parallel_use_after_move_issue_cn.md` is fixed for the current compiler repro and downstream host surface. Direct SAB planned-call move-prefixed args now transfer values into fresh temps before emitting `^tmp`, including `^field` / `^index` non-identifier move expressions, and mark moved source bindings consumed so tail cleanup does not release them again. Added `tests/test_unit_borrowed_enum_field_arg_direct.sla`; verified local/installed focused fixture SA/SAB, full strict direct-SAB sweep 110/110 files and 264/264 cases, official dev install/help, installed downstream `test_ecs_mut_parallel.sla` 91/91, and installed downstream `parallel.sla` 1/1. `sla_ecs` remains downstream evidence only.
	- [x] Completed docs-priority compiler issue: `docs/sab_thread_fnptr_ready_batch_unknown_register_issue_cn.md` is fixed for the current compiler repro and downstream host surface. Added `tests/test_unit_fn_ptr_thread_pair_direct.sla`, covering two `thread::spawn` closures that capture function-pointer parameters plus cloned `Arc<*TinyWorld>` values. Direct SAB now keeps call-body referenced registers in function metadata and lowers Rc/Arc receiver `clone()` through direct SAB smart-pointer clone macros instead of emitting unresolved `@sla__clone(shared)` call text. Verification: `zig build test -Dtest-filter="escaped thread closure function pointer" --summary all`; `zig build test -Dtest-filter="paired escaped thread function pointer" --summary all`; `zig build --summary all`; local SA-text and strict direct-SAB no-fallback for the new fixture; local strict direct-SAB no-fallback for existing function-pointer value and void fixtures; official `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; installed downstream focused `/home/vscode/projects/sla_ecs/tests/test_ecs_mut_parallel.sla` SAB/default tests for `ready pair bridge advances plan`, `ready pair runner rejects mismatched batch order`, `ready triple bridge releases dependent`, `width dispatch selects pair`, and `width dispatch one releases dependent`. `sla_ecs` remains downstream evidence only; no implementation scope moved into that repo.
	- [x] Inserted priority audit from `docs/` issue tickets before returning to the broader RefCell slice. Checked the docs issue records and marked fixed items with current evidence: `docs/sab_call_target_issue_cn.md` is verified fixed by official dev install, `SA_PLUGIN_DEV=1 sa sla help`, strict host direct-SAB no-fallback for `/home/vscode/projects/sla_ecs/lib/parallel.sla` (1/1), SAB build/disasm to `/tmp/parallel_docs_issue.disasm.sa`, and a clean illegal-call-target guard (`rg 'call [^\n]*"?@[^\s,"]*\('` returned no matches; disasm shows `call r490,"@sla__ecs_parallel_sum_i32_chunk","tmp_52"` and `call r499,"@sla__ecs_parallel_sum_i32_chunk","tmp_59"`). `docs/sla_compiler_issues_and_lua_refactor_diagnostics_cn.md` issue 1 is fixed upstream in SCI because `macroDefBodyLines()` now rejects missing `owned_body_lines` instead of slicing the current file, verified with `zig test src/flattener.zig --test-filter "macroDefBodyLines rejects missing owned macro body"` in `/home/vscode/projects/sci`; issue 2 is fixed in this plugin by final-output-relative import rewriting, verified with `SA_PLUGIN_DEV=1 sa sla build tests/import_fixtures/output_relative/main.sla --out /tmp/output_relative_root.sa` and rewritten imports; issue 3 is fixed upstream in SCI with `owned_by_files`, verified with `zig test src/cli.zig --test-filter "source tree hash missing import returns PackageNotResolved without ownership errors"`; issue 4 remains a `sa_lua` application/runtime task, not a `sa_plugin_sla` compiler fix. `docs/struct_update_sab_review_cn.md` is already historical/fixed for the tracked scalar and pointer-backed cases, reverified with host SA-text and strict direct-SAB no-fallback for `tests/test_unit_struct_update.sla` (2/2 each) and `tests/test_unit_struct_update_pointer_backed.sla` (1/1 each). `docs/trace.md` was audited for stale entries: focused user-macro SA-text parity is now fixed for `tests/test_unit_user_macro_direct.sla` (host SA-text and strict direct-SAB 2/2 each), while broader macro convergence, full RefCell lifecycle, pointer-backed imported-macro alias semantics, complete shared call planning, broader async, closures/callables, and global 100% completion remain open.
		- [x] Completed inserted docs-priority implementation task: rosetta `104_if_let_chains` now passes direct SAB no-fallback without falling back through SA text. `src/lowering_rules.zig` exposes shared `planLetPattern()` / `LetPatternPlan` for the same Option/Result/user-enum pattern classification used by `while let`, and `src/sab_codegen.zig` consumes that shared plan for chained `if let` statements and value-producing `if let` expressions while keeping labels, result slots, register release, and RefCell branch-state snapshots emitter-local. Verification: `zig fmt src/lowering_rules.zig src/sab_codegen.zig`; `zig test src/lowering_rules.zig --test-filter "shared while let pattern classification"`; `zig build --summary all`; `zig build test --summary all` (84/84); local strict direct-SAB no-fallback and SA-text parity for `demos/rosetta/104_if_let_chains/main.sla` (1/1 each); local strict direct-SAB no-fallback regressions for `tests/test_unit_async_while_let_future_queue_direct.sla` (1/1) and `tests/test_unit_refcell_struct_payload.sla` (7/7); official `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host strict direct-SAB no-fallback and SA-text parity for rosetta 104 (1/1 each); host strict direct-SAB no-fallback for `/home/vscode/projects/sla_ecs/lib/parallel.sla` (1/1). Progress: 100% for this focused docs issue slice; broader pattern surface and global completion remain open; broader Y/shared-lowering remains about 98-99%; direct SAB fallback-removal remains 100% for the tracked unit corpus.
		- [x] Completed inserted docs-priority Phase 3 call/materialization follow-up: direct SAB now consumes the existing shared `CallArgMaterializationKind.array_to_slice_borrow` plan for focused ordinary planned static calls instead of returning `UnsupportedSabDirectFeature`. Added `tests/test_unit_array_to_slice_call.sla`, covering `&[T; N] -> &[T]` call arguments from both local arrays and temporary array literals. `src/sab_codegen.zig` materializes a stack `Slice` with `SLICE_NEW`, marks the stack slice non-owning, and releases any temporary backing array after the call through the existing shared release decision; `src/codegen.zig` was also tightened so SA-text releases temporary backing arrays for `&[literal]` call args. The SAB macro planned-call branch now has the same helper wired, but broader macro argument-address semantics for macro-body `&values` remains a separate Phase 2 macro-convergence issue and is not claimed complete here. Verification: `zig fmt src/codegen.zig src/sab_codegen.zig`; `zig build --summary all`; `zig build test --summary all` (85/85); local SA-text and strict direct-SAB no-fallback for `tests/test_unit_array_to_slice_call.sla` (1/1 each); local strict direct-SAB regressions for RefCell payload (7/7), borrow-temp (25/25), and `/home/vscode/projects/sla_ecs/lib/parallel.sla` (1/1); local full strict direct-SAB no-fallback sweep 103/103 files, 256/256 cases; official `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host SA-text and strict direct-SAB no-fallback for the focused fixture (1/1 each); host strict direct-SAB no-fallback for borrow-temp (25/25) and `/home/vscode/projects/sla_ecs/lib/parallel.sla` (1/1); host full strict direct-SAB no-fallback sweep 103/103 files, 256/256 cases; focused SAB disasm illegal-call-target grep returned no matches. Rosetta `116_va_list_variadic` remains open as a separate demo issue because SA-text currently panics with code 116 and direct SAB now reaches a different callee-declaration gap after this blocker.
		- [x] Completed inserted docs-priority issue task: `docs/result_entityitem_filter_cleanup_issue_cn.md` is fixed for the currently available host repro surface. Direct SAB now handles the tracked `Result<EntityItem<T>>`/borrowed-composite cleanup path, `Box::from_raw`/`Box::into_raw` raw-pointer std surface calls, void function-pointer indirect calls, and the follow-on primitive reassigned `let`/param loop-variable shape that host ECS exposed as `scan` UseAfterMove. Implementation notes: `src/sab_codegen.zig` now releases borrow params and move/by-value pointer-shaped params through `shouldReleaseParamLocal()`, lowers `unsafe { ... }` block expressions, bitcasts pointer-to-pointer casts into fresh temps, emits void fn-pointer `call_indirect` without a destination, consumes std-surface `consume_value` metadata for `Box::into_raw`, and slots reassigned primitive direct-SAB bindings so `scan = scan - 1` / `found = scan` do not redefine or consume live scalar registers. `sla_std/std_surface.sla_meta` now declares `Box::from_raw` and consuming `Box::into_raw`. Added regressions: `tests/test_unit_result_entityitem_self_cleanup.sla`, `tests/test_unit_box_from_raw_direct.sla`, `tests/test_unit_fn_ptr_void_direct.sla`, and `tests/test_unit_scalar_reassign_scan_direct.sla`. Verification: `zig build --summary all`; `zig build test --summary all` (85/85); local SA-text plus strict direct-SAB no-fallback for the scalar reassignment and Result focused fixtures; local strict direct-SAB no-fallback for Box/from_raw and void fn-pointer fixtures; local full strict direct-SAB no-fallback sweep 107/107 `tests/test_unit_*.sla` files, 260/260 cases; official `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; installed host strict direct-SAB no-fallback and SA-text for `/home/vscode/projects/sla_ecs/tests/test_ecs_result_facades.sla` (172/172 each); installed host strict direct-SAB no-fallback for `/home/vscode/projects/sla_ecs/lib/parallel.sla` (1/1); installed focused Result/Box/fn-ptr/scalar/borrow-temp/RefCell regressions; historical `--filter "ecs_world_try_query_single returns one"` still selects 0 tests and is not counted as evidence; SAB disasm illegal-call-target guard for `parallel.sla` is clean and shows `@sla__ecs_parallel_sum_i32_chunk` target/argument separation. Progress: 100% for this focused docs-priority issue slice; broader full-completion roadmap remains open.
		- [x] Completed inserted docs-priority issue task: `docs/sab_scalar_param_cleanup_issue_cn.md` is fixed for the current compiler repro and installed host surface. `src/sab_codegen.zig` now classifies parameter cleanup as skip/consume/release so direct SAB consumes copy-value scalar parameters at function exit without emitting invalid primitive `release` instructions, while still releasing borrow and pointer-shaped owned params. `src/lowering_rules.zig` now makes auto-borrowed receiver call-arg cleanup release owner temporaries when the receiver expression is temporary, and `src/codegen.zig` passes receiver-style auto-borrow options through associated-target user-method lowering; this fixes the documented expression-chain temporary leak as a shared call-arg materialization decision consumed by SA-text/SAB. Added `tests/test_unit_scalar_param_cleanup_direct.sla`, covering the table-erased-like wrapper, `_auto` wrapper, unused scalar param consumption, and `temporary().method()` owner cleanup. Verification: `zig test src/lowering_rules.zig --test-filter "shared lowering rules normalize derives and call argument prefixes"`; `zig build --summary all`; `zig build test --summary all` (85/85); local and installed SA-text plus strict direct-SAB no-fallback for the focused fixture; scalar reassignment, Result/EntityItem, Box/from_raw, void fn-pointer, borrow-temp (25/25), and RefCell payload (7/7) regressions; local full strict direct-SAB no-fallback sweep 108/108 files, 262/262 cases; official `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; installed host `lib/system_param_table_erased.sla` check plus focused SA-text and strict direct-SAB no-fallback test; installed host `parallel.sla` strict direct-SAB no-fallback (1/1). Apache-2.0 `LICENSE` was added to match `/home/vscode/projects/sci`. Progress: 100% for this focused docs issue slice; broader full-completion roadmap remains open.
		- [x] Completed opportunistic imported-macro whole-aggregate borrowed address follow-up: imported macro loading now preserves direct callees used by macro bodies, and shared imported-macro arg planning passes borrowed whole pointer-backed aggregate identifiers as values instead of materializing pointer-to-pointer stack slots. `tests/import_fixtures/imported_borrow_helpers.sa` and `tests/test_unit_imported_macro_address_direct.sla` now cover `&%value` whole-aggregate reads through an imported macro. Verification: installed SA-text and strict direct-SAB no-fallback for `tests/test_unit_imported_macro_address_direct.sla` (6/6 each), plus the local full strict direct-SAB no-fallback sweep 108/108 files, 262/262 cases. Progress: 100% for this focused imported-macro sub-slice; broader macro convergence and pointer-backed aggregate alias semantics remain open.
		- [ ] Active full-completion continuation target: broader Phase 1 shared RefCell borrow-handle lifecycle and alias planning across scopes, branches, loops, call args, and early returns, plus remaining pointer-backed aggregate borrow/alias cases inside imported macros that still rely on emitter-local lifetime decisions. Starting baseline: focused user-macro SA-text parity and the Phase 3 array-to-slice call-materialization sub-slice are complete; broader Y/shared-lowering is about 98-99%; direct SAB tracked-corpus fallback removal is 100%; current local/host no-fallback unit sweep baseline is 118/118 fixture files for the current worktree. This remains the next broader implementation target; do not treat the completed focused sub-slices as global 100% completion.
  - [x] Completed active Phase 1 hardening sub-slice: shared RefCell runtime import scanning now includes every `if let` chain value, not only the parser's first `ife.cond` value and branch bodies. This closes the import/runtime-surface decision gap for `if let ... && let RefCell::new(...) = ...`-shaped chains without adding emitter-specific logic. Verification: `zig fmt src/lowering_rules.zig`; `zig test src/lowering_rules.zig --test-filter "shared refcell runtime scanner detects constructor and receiver calls"`; `zig build --summary all`; `zig build test --summary all` (84/84); local strict direct-SAB no-fallback for `tests/test_unit_refcell_struct_payload.sla` (7/7) and `tests/test_unit_borrow_temp_release_order.sla` (25/25); official `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host strict direct-SAB no-fallback for RefCell payload (7/7), borrow-temp (25/25), and `/home/vscode/projects/sla_ecs/lib/parallel.sla` (1/1); `git diff --check`. Progress: 100% for this focused shared runtime-scan sub-slice; full RefCell borrow-handle lifecycle remains open.
  - [x] Completed active Phase 2/SA-text hardening sub-slice: SA-text std macro/import pre-scanners now traverse every chained `if let` condition value. `src/codegen.zig` routes the Iter, Box, Vec, Option, Cmp, Result, Mpsc, Rc, VecDeque, HashMap, HashSet, BTreeSet, BTreeMap, Atomic, Ptr, Cell, Async, and TraitObject scanners through shared `if_expr` helpers that inspect `IfLetCond.value`, and the same field is now covered by thread-spawn helper collection, loop-counter/hoisted-allocation pre-passes, and identifier-consumption checks. Added focused AST-level coverage for a chained value containing `Box::new(move payload)`. Verification: `zig fmt src/codegen.zig`; `zig test src/codegen.zig --test-filter "if let chain scanner visits chained values"`; `zig test src/lowering_rules.zig --test-filter "shared refcell runtime scanner detects constructor and receiver calls"`; `zig build --summary all`; `zig build test --summary all` (85/85); local strict direct-SAB no-fallback for `tests/test_unit_refcell_struct_payload.sla` (7/7) and `tests/test_unit_borrow_temp_release_order.sla` (25/25); official `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host strict direct-SAB no-fallback for RefCell payload (7/7), borrow-temp (25/25), and `/home/vscode/projects/sla_ecs/lib/parallel.sla` (1/1); `git diff --check`. Progress: 100% for this focused SA-text pre-scan sub-slice; broader shared macro expansion/call planning and full RefCell lifecycle remain open.
  - [ ] Current global completion gap: tracked direct SAB corpus is green, but the whole roadmap is still open until the remaining shared architecture tasks are closed or explicitly scoped: fuller RefCell lifecycle/projection planning, broader macro expansion/substitution convergence, complete shared call/argument materialization, std/import/package parity, broader async state-machine support, generic SCI fragment naming/boundary work, final local/host gates, synchronized docs, and verified commits.
  - [x] Completed active Phase 2 hardening slice: focused user-defined macro SA-text parity. SA-text now inline-expands parsed user macros at call sites instead of emitting legacy `[MACRO]` / `EXPAND` text blocks; `src/type_checker.zig` type-checks full macro bodies with infer-typed parameters, and `src/codegen.zig` tracks macro argument/register/type state, hygienic macro-local names, block scopes, loop counters, nested macro calls, field/index/deref borrowed address args, aggregate/index/static-call expressions, and focused Box smart-pointer `&*boxed` / `&**nested_box` address forms. Verification: `zig fmt src/codegen.zig src/type_checker.zig`; `zig build --summary all`; `zig build test --summary all` (84/84); local SA-text and strict direct-SAB no-fallback for `tests/test_unit_user_macro_direct.sla` (2/2 each), imported-macro address (5/5 each), RefCell payload (7/7 each), async block_on (1/1 each), and borrow-temp strict direct SAB (25/25); local full strict direct-SAB no-fallback sweep over 102 fixture files / 255 test cases; `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host SA-text and strict direct-SAB no-fallback for the user-macro/imported-macro/RefCell/async fixtures; host strict direct-SAB no-fallback for borrow-temp and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; host full strict direct-SAB no-fallback sweep over 102 fixture files / 255 test cases; `git diff --check`. Progress: 100% for this focused user-macro SA-text parity sub-slice; broader macro expansion convergence remains open for a fuller shared expansion/substitution contract; broader Y/shared-lowering remains about 98-99%; direct SAB fallback-removal remains 100%; current no-fallback baseline remains 102/102 fixture files.
  - [x] Completed active Phase 1 hardening slice: shared imported macro address-slot argument planning. Imported macro loading now records direct `%param+offset` uses in `address_slot_arg_mask`, propagates both borrowed and address-slot masks through nested `EXPAND`, and keeps legacy `&%param` borrowed behavior separate. `ImportedMacroCallPlan` carries the shared mask and classifies direct address-slot args before the SA/SAB fork: field/index/deref expressions pass true addresses, value temps pass values, and pointer-like or inferred identifier values pass directly to avoid pointer-to-pointer macro expansion. SA-text and direct SAB consume the same actions while preserving emitter-local register/operand allocation, release tracking, and macro-fragment emission. Verification: `zig fmt src/type_checker.zig src/plugin.zig src/lowering_rules.zig src/codegen.zig src/sab_codegen.zig`; focused imported-macro lowering tests; `zig build --summary all`; `zig build test --summary all` (84/84); local strict direct-SAB no-fallback and SA-text parity for `tests/test_unit_async_block_on_direct.sla` (1/1 each) and `tests/test_unit_imported_macro_address_direct.sla` (5/5 each); official `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host strict direct-SAB no-fallback and SA-text parity for the async/imported-macro fixtures; host strict direct-SAB no-fallback for RefCell payload (7/7), borrow-temp (25/25), and `/home/vscode/projects/sla_ecs/lib/parallel.sla` (1/1); host full no-fallback sweep over 102 `tests/test_unit_*.sla` fixture files passed, totaling 255/255 test cases; `git diff --check`. Progress: 100% for this imported macro address-slot arg sub-slice; broader Y/shared-lowering about 98-99%; direct SAB fallback-removal remains 100%; current no-fallback baseline remains 102/102 fixture files. Out of scope/open: full RefCell borrow-handle lifecycle, broader pointer-backed aggregate alias semantics inside imported macros, generic SCI fragment naming, wider async arbitrary multi-await/control-flow state machines, and broader shared macro expansion convergence.
  - [x] Completed active Phase 1 hardening slice: shared borrow-address temp release planning. `BorrowAddressTempReleasePlan` plus `planBorrowAddressTempRelease()` in `src/lowering_rules.zig` cover the pure decision that a borrowed-address value carrying release metadata must release the borrow value and recursively release source temps. `src/codegen.zig` consumes the plan for SA-text `borrow_source_temps`, and `src/sab_codegen.zig` consumes it for direct SAB `borrow_address_temps`. Concrete string/register maps, release instructions, recursive release calls, slice freeing, and direct SAB macro-fragment behavior remain emitter-local. Verification: `zig fmt src/lowering_rules.zig src/codegen.zig src/sab_codegen.zig`; `zig test src/lowering_rules.zig --test-filter "shared refcell borrow call plan"` (1/1); `zig build --summary all`; `zig build test --summary all` (84/84); local direct SAB no-fallback and SA-text parity for `tests/test_unit_refcell_struct_payload.sla` (7/7 each); local direct SAB no-fallback for `tests/test_unit_borrow_temp_release_order.sla` (25/25) and `tests/test_unit_imported_macro_address_direct.sla` (4/4); `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host strict direct-SAB no-fallback for RefCell payload, borrow-temp, imported macro address, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; host SA-text parity for RefCell payload; host full no-fallback sweep over 102 `tests/test_unit_*.sla` fixture files passed, totaling 254/254 test cases; `git diff --check`. Progress: 100% for this shared borrow-address temp release planning sub-slice; broader Y/shared-lowering remains about 98%; direct SAB fallback-removal remains 100%; current no-fallback baseline remains 102/102 fixture files. Out of scope/open: full RefCell borrow-handle lifecycle across all scopes/branches/loops/call args and pointer-backed aggregate alias preservation inside imported macros.
  - [x] Completed active Phase 1 hardening slice: shared borrow-address temp binding-transfer planning. SA-text let-binding `borrow_source_temps` metadata transfer now consumes `lowering_rules.planBorrowAddressTempTransfer()` when moving metadata from a generated value temp into the binding name, matching the direct SAB `transferReleaseMetadata()` path that already consumes the same action for `borrow_address_temps`. Concrete string/register maps, destination replacement/freeing, slice ownership, concrete releases, and direct SAB macro-fragment behavior remain emitter-local. Verification: `zig fmt src/codegen.zig`; `zig test src/lowering_rules.zig --test-filter "shared refcell borrow call plan"` (1/1); `zig build --summary all`; `zig build test --summary all` (84/84); local direct SAB no-fallback and SA-text parity for `tests/test_unit_refcell_struct_payload.sla` (7/7 each); local direct SAB no-fallback for `tests/test_unit_borrow_temp_release_order.sla` (25/25) and `tests/test_unit_imported_macro_address_direct.sla` (4/4); `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host strict direct-SAB no-fallback for RefCell payload, borrow-temp, imported macro address, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; host SA-text parity for RefCell payload; host full no-fallback sweep over 102 `tests/test_unit_*.sla` fixture files passed, totaling 254/254 test cases; `git diff --check`. Progress: 100% for this shared borrow-address temp binding-transfer planning sub-slice; broader Y/shared-lowering remains about 98%; direct SAB fallback-removal remains 100%; current no-fallback baseline remains 102/102 fixture files. Out of scope/open: full RefCell borrow-handle lifecycle across all scopes/branches/loops/call args and pointer-backed aggregate alias preservation inside imported macros.
  - [x] Completed active Phase 1 hardening slice: shared borrow-address temp transfer planning. `BorrowAddressTempTransferAction` plus `planBorrowAddressTempTransfer()` in `src/lowering_rules.zig` cover the pure decision that value-state transfer moves borrowed-address release metadata only when the source carries that metadata. `src/codegen.zig` consumes the shared action for SA-text `borrow_source_temps`, and `src/sab_codegen.zig` consumes it for direct SAB `borrow_address_temps`. Concrete string/register maps, destination replacement/freeing, slice ownership, concrete releases, and direct SAB macro-fragment behavior remain emitter-local. Verification: `zig fmt src/lowering_rules.zig src/codegen.zig src/sab_codegen.zig`; `zig test src/lowering_rules.zig --test-filter "shared refcell borrow call plan"` (1/1); `zig build --summary all`; `zig build test --summary all` (84/84); local direct SAB no-fallback and SA-text parity for `tests/test_unit_refcell_struct_payload.sla` (7/7 each); local direct SAB no-fallback for `tests/test_unit_borrow_temp_release_order.sla` (25/25) and `tests/test_unit_imported_macro_address_direct.sla` (4/4); `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host strict direct-SAB no-fallback for RefCell payload, borrow-temp, imported macro address, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; host SA-text parity for RefCell payload; host full no-fallback sweep over 102 `tests/test_unit_*.sla` fixture files passed, totaling 254/254 test cases; `git diff --check`. Progress: 100% for this shared borrow-address temp transfer planning sub-slice; broader Y/shared-lowering remains about 98%; direct SAB fallback-removal remains 100%; current no-fallback baseline remains 102/102 fixture files. Out of scope/open: full RefCell borrow-handle lifecycle across all scopes/branches/loops/call args and pointer-backed aggregate alias preservation inside imported macros.
  - [x] Completed active Phase 1 hardening slice: shared borrow-address temp planning. `BorrowAddressTempPlan` plus `planBorrowAddressTemps()` in `src/lowering_rules.zig` cover the pure decision that borrowed-address release metadata is remembered only when a primary owner temp or extra release temps exist. `src/codegen.zig` consumes the shared plan for SA-text field/index/ordinary deref address temp metadata, and `src/sab_codegen.zig` consumes it for direct SAB `AddressSource` release metadata plus RefCell pointer-payload borrow-slot retention. Concrete SA-text `borrow_source_temps`, direct SAB `borrow_address_temps`, string/register maps, slice allocation, metadata transfer, concrete releases, and direct SAB macro-fragment behavior remain emitter-local. Verification: `zig fmt src/lowering_rules.zig src/codegen.zig src/sab_codegen.zig`; `zig test src/lowering_rules.zig --test-filter "shared refcell borrow call plan"` (1/1); `zig build --summary all`; `zig build test --summary all` (84/84); local direct SAB no-fallback and SA-text parity for `tests/test_unit_refcell_struct_payload.sla` (7/7 each); local direct SAB no-fallback for `tests/test_unit_borrow_temp_release_order.sla` (25/25) and `tests/test_unit_imported_macro_address_direct.sla` (4/4); clean-cache `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host strict direct-SAB no-fallback for RefCell payload, borrow-temp, imported macro address, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; host SA-text parity for RefCell payload; host full no-fallback sweep over 102 `tests/test_unit_*.sla` fixture files passed, totaling 254/254 test cases; `git diff --check`. Progress: 100% for this shared borrow-address temp planning sub-slice; broader Y/shared-lowering remains about 98%; direct SAB fallback-removal remains 100%; current no-fallback baseline remains 102/102 fixture files. Out of scope/open: full RefCell borrow-handle lifecycle across all scopes/branches/loops/call args and pointer-backed aggregate alias preservation inside imported macros.
  - [x] Completed active Phase 1 hardening slice: shared RefCell borrow handle registration plan. `RefCellBorrowHandleRegistrationPlan` plus `planRefCellBorrowHandleRegistration()` in `src/lowering_rules.zig` cover the pure ordinary borrow-handle registration decision that a new `borrow()` / `borrow_mut()` handle records the receiver cell register but does not track that receiver as an owner temp released with the handle. `src/codegen.zig` and `src/sab_codegen.zig` both consume the shared plan while preserving emitter-local handle maps, SA-text `cell_release_temp`, direct SAB `release_regs`, string/register map mutation, concrete releases, and direct SAB macro-fragment behavior. Verification: `zig fmt src/lowering_rules.zig src/codegen.zig src/sab_codegen.zig`; `zig test src/lowering_rules.zig --test-filter "shared refcell borrow call plan"` (1/1); `zig build --summary all`; `zig build test --summary all` (84/84); local direct SAB no-fallback and SA-text parity for `tests/test_unit_refcell_struct_payload.sla` (7/7 each); local direct SAB no-fallback for `tests/test_unit_borrow_temp_release_order.sla` (25/25) and `tests/test_unit_imported_macro_address_direct.sla` (4/4); `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host strict direct-SAB no-fallback for RefCell payload, borrow-temp, imported macro address, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; host SA-text parity for RefCell payload; host full no-fallback sweep over 102 `tests/test_unit_*.sla` fixture files passed, totaling 254/254 test cases; `git diff --check`. Progress: 100% for this shared RefCell borrow handle registration-plan sub-slice; broader Y/shared-lowering remains about 98%; direct SAB fallback-removal remains 100%; current no-fallback baseline remains 102/102 fixture files. Out of scope/open: full RefCell borrow-handle lifecycle across all scopes/branches/loops/call args and pointer-backed aggregate alias preservation inside imported macros.
  - [x] Completed active Phase 1 hardening slice: shared RefCell companion restore plan. `RefCellCompanionRestorePlan` plus `planRefCellCompanionRestore()` in `src/lowering_rules.zig` cover the pure result-slot companion restore decision that a loaded cell handle is tracked as the restored borrow handle owner temp and the companion cell slot is released after restore. `src/codegen.zig` and `src/sab_codegen.zig` both consume the shared plan while preserving emitter-local companion-slot loads, SA-text `cell_release_temp`, direct SAB `release_regs`, string/register map mutation, concrete releases, and direct SAB macro-fragment behavior. Verification: `zig fmt src/lowering_rules.zig src/codegen.zig src/sab_codegen.zig`; `zig test src/lowering_rules.zig --test-filter "shared refcell borrow call plan"` (1/1); `zig build --summary all`; `zig build test --summary all` (84/84); local direct SAB no-fallback and SA-text parity for `tests/test_unit_refcell_struct_payload.sla` (7/7 each); local direct SAB no-fallback for `tests/test_unit_borrow_temp_release_order.sla` (25/25) and `tests/test_unit_imported_macro_address_direct.sla` (4/4); `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host strict direct-SAB no-fallback for RefCell payload, borrow-temp, imported macro address, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; host SA-text parity for RefCell payload; host full no-fallback sweep over 102 `tests/test_unit_*.sla` fixture files passed, totaling 254/254 test cases; `git diff --check`. Progress: 100% for this shared RefCell companion restore-plan sub-slice; broader Y/shared-lowering remains about 98%; direct SAB fallback-removal remains 100%; current no-fallback baseline remains 102/102 fixture files. Out of scope/open: full RefCell borrow-handle lifecycle across all scopes/branches/loops/call args and pointer-backed aggregate alias preservation inside imported macros.
  - [x] Completed active Phase 1 hardening slice: shared RefCell borrow runtime guard plan. `RefCellBorrowRuntimeGuardPlan` plus `planRefCellBorrowRuntimeGuard()` in `src/lowering_rules.zig` cover the pure try-borrow branch decision that a conflict releases the status value and panics with code 107, and a successful borrow releases the status value before borrow-result materialization. `src/codegen.zig` and `src/sab_codegen.zig` both consume the shared plan while preserving emitter-local labels, branch instructions, panic emission, status register/string release calls, and direct SAB macro-fragment behavior. Verification: `zig fmt src/lowering_rules.zig src/codegen.zig src/sab_codegen.zig`; `zig test src/lowering_rules.zig --test-filter "shared refcell borrow call plan"` (1/1); `zig build --summary all`; `zig build test --summary all` (84/84); local direct SAB no-fallback and SA-text parity for `tests/test_unit_refcell_struct_payload.sla` (7/7 each); local direct SAB no-fallback for `tests/test_unit_borrow_temp_release_order.sla` (25/25) and `tests/test_unit_imported_macro_address_direct.sla` (4/4); `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host strict direct-SAB no-fallback for RefCell payload, borrow-temp, imported macro address, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; host SA-text parity for RefCell payload; host full no-fallback sweep over 102 `tests/test_unit_*.sla` fixture files passed, totaling 254/254 test cases; `git diff --check`. Progress: 100% for this shared RefCell borrow runtime guard-plan sub-slice; broader Y/shared-lowering remains about 98%; direct SAB fallback-removal remains 100%; current no-fallback baseline remains 102/102 fixture files. Out of scope/open: full RefCell borrow-handle lifecycle across all scopes/branches/loops/call args and pointer-backed aggregate alias preservation inside imported macros.
  - [x] Completed active Phase 1 hardening slice: shared RefCell borrow-result materialization plan. `RefCellBorrowResultTarget`, `RefCellBorrowResultAction`, `RefCellBorrowResultPlan`, and `planRefCellBorrowResult()` in `src/lowering_rules.zig` cover the pure post-try-borrow decision to use the borrow slot directly, load a pointer payload, or take a pointer payload while retaining the borrow slot as release metadata. `src/codegen.zig` and `src/sab_codegen.zig` both consume the shared plan while preserving emitter-local SA text instructions, SAB `take`, string/register maps, release emission, and direct SAB macro-fragment behavior. Verification: `zig fmt src/lowering_rules.zig src/codegen.zig src/sab_codegen.zig`; `zig test src/lowering_rules.zig --test-filter "shared refcell borrow call plan"` (1/1); `zig build --summary all`; `zig build test --summary all` (84/84); local direct SAB no-fallback and SA-text parity for `tests/test_unit_refcell_struct_payload.sla` (7/7 each); local direct SAB no-fallback for `tests/test_unit_borrow_temp_release_order.sla` (25/25) and `tests/test_unit_imported_macro_address_direct.sla` (4/4); clean-cache `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host strict direct-SAB no-fallback for RefCell payload, borrow-temp, imported macro address, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; host SA-text parity for RefCell payload; host full no-fallback sweep over 102 `tests/test_unit_*.sla` fixture files passed, totaling 254/254 test cases; `git diff --check`. Progress: 100% for this shared RefCell borrow-result materialization-plan sub-slice; broader Y/shared-lowering remains about 98%; direct SAB fallback-removal remains 100%; current no-fallback baseline remains 102/102 fixture files. Out of scope/open: full RefCell borrow-handle lifecycle across all scopes/branches/loops/call args and pointer-backed aggregate alias preservation inside imported macros.
  - [x] Completed active Phase 1 hardening slice: shared RefCell companion-store cleanup plan. `RefCellCompanionStoreCleanupPlan` plus `planRefCellCompanionStoreCleanup()` in `src/lowering_rules.zig` cover the pure cleanup decisions when an active RefCell borrow handle is moved into result-slot companion metadata: consume the handle value, release owner temps when present, release borrow-address temps when present, and clear non-owning metadata when present. `src/codegen.zig` and `src/sab_codegen.zig` both consume the shared plan while preserving emitter-local companion-slot stores, string/register map mutation, concrete releases, and direct SAB macro-fragment behavior. Verification: `zig fmt src/lowering_rules.zig src/codegen.zig src/sab_codegen.zig`; `zig test src/lowering_rules.zig --test-filter "shared refcell borrow call plan"` (1/1); `zig build --summary all`; `zig build test --summary all` (84/84); local direct SAB no-fallback and SA-text parity for `tests/test_unit_refcell_struct_payload.sla` (7/7 each); local direct SAB no-fallback for `tests/test_unit_borrow_temp_release_order.sla` (25/25) and `tests/test_unit_imported_macro_address_direct.sla` (4/4); `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host strict direct-SAB no-fallback for RefCell payload, borrow-temp, imported macro address, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; host SA-text parity for RefCell payload; host full no-fallback sweep over 102 `tests/test_unit_*.sla` fixture files passed, totaling 254/254 test cases; `git diff --check`. Progress: 100% for this shared RefCell companion-store cleanup-plan sub-slice; broader Y/shared-lowering remains about 98%; direct SAB fallback-removal remains 100%; current no-fallback baseline remains 102/102 fixture files. Out of scope/open: full RefCell borrow-handle lifecycle across all scopes/branches/loops/call args and pointer-backed aggregate alias preservation inside imported macros.
  - [x] Completed active Phase 1 hardening slice: shared RefCell handle transfer action. `RefCellHandleTransferAction` plus `planRefCellHandleTransfer()` in `src/lowering_rules.zig` cover the pure decision that value-state transfer should move active RefCell borrow-handle metadata only when the source carries a RefCell handle. `src/codegen.zig` and `src/sab_codegen.zig` both consume the shared action while preserving emitter-local string/register maps, consumed-source marking, borrow-address temp transfer, non-owning metadata, and concrete release mechanics. Verification: `zig fmt src/lowering_rules.zig src/codegen.zig src/sab_codegen.zig`; `zig test src/lowering_rules.zig --test-filter "shared refcell borrow call plan"` (1/1); `zig build --summary all`; `zig build test --summary all` (84/84); local direct SAB no-fallback and SA-text parity for `tests/test_unit_refcell_struct_payload.sla` (7/7 each); local direct SAB no-fallback for `tests/test_unit_borrow_temp_release_order.sla` (25/25) and `tests/test_unit_imported_macro_address_direct.sla` (4/4); clean-cache `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host strict direct-SAB no-fallback for RefCell payload, borrow-temp, imported macro address, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; host SA-text parity for RefCell payload; host full no-fallback sweep over 102 `tests/test_unit_*.sla` fixture files passed, totaling 254/254 test cases; `git diff --check`. Progress: 100% for this shared RefCell handle transfer-action sub-slice; broader Y/shared-lowering remains about 98%; direct SAB fallback-removal remains 100%; current no-fallback baseline remains 102/102 fixture files. Out of scope/open: full RefCell borrow-handle lifecycle across all scopes/branches/loops/call args and pointer-backed aggregate alias preservation inside imported macros.
  - [x] Completed active Phase 1 hardening slice: shared RefCell branch-state merge action. `RefCellBranchStateMergeAction` plus `planRefCellBranchStateMerge()` in `src/lowering_rules.zig` cover the pure merge decision for direct SAB RefCell borrow metadata snapshots: restore pre-branch when both branches terminate, restore else when then terminates, restore then when else terminates, and keep current when both branches fall through. `src/sab_codegen.zig` consumes the shared action in `setMergeRefCellBranchState()` while keeping concrete snapshot maps/restoration emitter-local. Verification: `zig fmt src/lowering_rules.zig src/sab_codegen.zig`; `zig test src/lowering_rules.zig --test-filter "shared refcell borrow call plan"` (1/1); `zig build --summary all`; `zig build test --summary all` (84/84); local direct SAB no-fallback and SA-text parity for `tests/test_unit_refcell_struct_payload.sla` (7/7 each); local direct SAB no-fallback for `tests/test_unit_borrow_temp_release_order.sla` (25/25) and `tests/test_unit_imported_macro_address_direct.sla` (4/4); `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host strict direct-SAB no-fallback for RefCell payload, borrow-temp, imported macro address, pkgjson, imported JSON direct/struct raw-pointer regressions, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; host SA-text parity for RefCell payload and imported macro address; host full no-fallback sweep over 102 `tests/test_unit_*.sla` fixture files passed, totaling 254/254 test cases; `git diff --check`. Progress: 100% for this shared RefCell branch-state merge-action sub-slice; broader Y/shared-lowering remains about 98%; direct SAB fallback-removal remains 100%; current no-fallback baseline remains 102/102 fixture files. Out of scope/open: full RefCell borrow-handle lifecycle across all scopes/branches/loops/call args and pointer-backed aggregate alias preservation inside imported macros.
  - [x] Completed active Phase 1 hardening slice: RefCell borrow-handle release planning and direct SAB early-return branch-state restoration now cover the focused fallthrough leak. `RefCellHandleReleasePlan` plus `planRefCellHandleRelease()` in `src/lowering_rules.zig` cover the shared release decision to decrement the dynamic borrow count, consume the handle value, and release owner temps only when owner temps exist. `src/codegen.zig` and `src/sab_codegen.zig` both consume this shared plan while preserving concrete SA-text/SAB release emission. Direct SAB now deep-clones/restores `refcell_borrow_values` and `borrow_address_temps` around ordinary/value `if` branch lowering so an early-return branch cannot remove RefCell borrow metadata required by the fallthrough branch. `tests/test_unit_refcell_struct_payload.sla` adds `refcell_early_return_value` plus `@test "refcell early return releases borrow handle"`; disassembly confirms borrow-count decrement before both early-return and fallthrough returns. Verification: `zig fmt src/sab_codegen.zig src/lowering_rules.zig src/codegen.zig`; `zig test src/lowering_rules.zig --test-filter "shared refcell borrow call plan"` (1/1); `zig build --summary all`; `zig build test --summary all` (84/84); local direct SAB no-fallback and SA-text parity for `tests/test_unit_refcell_struct_payload.sla` (7/7 each); local direct SAB no-fallback for `tests/test_unit_borrow_temp_release_order.sla` (25/25) and `tests/test_unit_imported_macro_address_direct.sla` (4/4); `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host strict direct-SAB no-fallback for RefCell payload, borrow-temp, imported macro address, pkgjson, imported JSON raw-pointer regressions, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; host SA-text parity for RefCell payload and imported macro address; host full no-fallback sweep over 102 `tests/test_unit_*.sla` fixture files passed, totaling 254/254 test cases; `git diff --check`. Progress: 100% for this focused RefCell release/branch-state early-return sub-slice; broader Y/shared-lowering remains about 98%; direct SAB fallback-removal remains 100%; current no-fallback baseline remains 102/102 fixture files. Out of scope/open: full RefCell borrow-handle lifecycle across all scopes/branches/loops/call args and pointer-backed aggregate alias preservation inside imported macros.
  - [x] Completed active Phase 1 hardening slice: result-slot RefCell borrow-handle load/restore action now lives in `src/lowering_rules.zig`. `ResultSlotRefCellLoadAction` plus `planResultSlotRefCellLoad()` cover the shared decision that a transferring result slot should restore RefCell borrow-handle metadata from the companion slot when handle metadata exists, release an empty preallocated companion slot when no handle was stored, or continue normal value-state transfer otherwise. `src/codegen.zig` and `src/sab_codegen.zig` both consume this shared action while preserving emitter-local companion-slot fetch/remove, concrete load/release instructions, recreated handle metadata, and value-state transfer mechanics. Verification: `zig fmt src/lowering_rules.zig src/codegen.zig src/sab_codegen.zig`; `zig test src/lowering_rules.zig --test-filter "result-slot value transfer"` (1/1); `zig build --summary all`; `zig build test --summary all` (84/84); local direct SAB no-fallback and SA-text parity for `tests/test_unit_refcell_struct_payload.sla` (6/6 each); local direct SAB no-fallback for `tests/test_unit_borrow_temp_release_order.sla` (25/25) and `tests/test_unit_imported_macro_address_direct.sla` (4/4); `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host strict direct-SAB no-fallback for RefCell payload, borrow-temp, imported macro address, pkgjson, imported JSON raw-pointer regressions, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; host SA-text parity for RefCell payload and imported macro address; host full no-fallback sweep over 102 `tests/test_unit_*.sla` fixture files passed, totaling 253/253 test cases; `git diff --check`. Progress: 100% for this shared result-slot RefCell load-action sub-slice; broader Y/shared-lowering remains about 98%; direct SAB fallback-removal remains 100%; current no-fallback baseline remains 102/102 fixture files. Out of scope/open: full RefCell borrow-handle lifecycle across scopes/branches/loops/call args/early returns and pointer-backed aggregate alias preservation inside imported macros.
  - [x] Completed active Phase 1 hardening slice: result-slot RefCell borrow-handle store action now lives in `src/lowering_rules.zig`. `ResultSlotRefCellStoreAction` plus `planResultSlotRefCellStore()` cover the shared decision that a transferring result slot whose source is an active RefCell borrow handle must store the handle's cell pointer/kind through the result-slot companion path; otherwise the emitters continue normal value-state transfer. `src/codegen.zig` and `src/sab_codegen.zig` both consume this shared action while preserving emitter-local companion-slot allocation, concrete store/load instructions, release of owner/address temps, consumed-state bookkeeping, and direct SAB macro-fragment mechanics. Verification: `zig fmt src/lowering_rules.zig src/codegen.zig src/sab_codegen.zig`; `zig test src/lowering_rules.zig --test-filter "result-slot value transfer"` (1/1); `zig build --summary all`; `zig build test --summary all` (84/84); local direct SAB no-fallback and SA-text parity for `tests/test_unit_refcell_struct_payload.sla` (6/6 each); local direct SAB no-fallback for `tests/test_unit_borrow_temp_release_order.sla` (25/25) and `tests/test_unit_imported_macro_address_direct.sla` (4/4); `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host strict direct-SAB no-fallback for RefCell payload, borrow-temp, imported macro address, pkgjson, imported JSON raw-pointer regressions, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; host SA-text parity for RefCell payload and imported macro address; host full no-fallback sweep over 102 `tests/test_unit_*.sla` fixture files passed, totaling 253/253 test cases; `git diff --check`. Progress: 100% for this shared result-slot RefCell store-action sub-slice; broader Y/shared-lowering remains about 98%; direct SAB fallback-removal remains 100%; current no-fallback baseline remains 102/102 fixture files. Out of scope/open: full RefCell borrow-handle lifecycle across scopes/branches/loops/call args/early returns and pointer-backed aggregate alias preservation inside imported macros.
  - [x] Completed active Phase 1 hardening slice: RefCell runtime import scanning now lives in `src/lowering_rules.zig`. `exprNeedsRefCellRuntime()`, `blockNeedsRefCellRuntime()`, and `programNeedsRefCellRuntime()` cover the shared decision that a typed RefCell expression, `RefCell::...` constructor/static call, or call on a `RefCell<T>` receiver requires the `sa_std/core/refcell.sa` runtime surface. `src/codegen.zig` consumes the shared scan for SA-text import emission instead of owning a private recursive RefCell macro scanner, while preserving emitter-local borrow emission, handle tracking, result-slot companion slots, release ordering, and direct SAB macro-fragment emission. Verification: `zig fmt --check src/lowering_rules.zig src/codegen.zig`; `zig test src/lowering_rules.zig --test-filter "shared refcell"` (2/2); `zig build --summary all`; `zig build test --summary all` (84/84); local direct SAB no-fallback and SA-text parity for `tests/test_unit_refcell_struct_payload.sla` (6/6 each); local direct SAB no-fallback for `tests/test_unit_borrow_temp_release_order.sla` (25/25) and `tests/test_unit_imported_macro_address_direct.sla` (4/4); `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host strict direct-SAB no-fallback for RefCell payload, borrow-temp, imported macro address, pkgjson, imported JSON raw-pointer regressions, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; host SA-text parity for RefCell payload and imported macro address; host full no-fallback sweep over 102 `tests/test_unit_*.sla` fixture files passed, totaling 253/253 test cases; `git diff --check`. Progress: 100% for this shared RefCell runtime import-scan sub-slice; broader Y/shared-lowering remains about 98%; direct SAB fallback-removal remains 100%; current no-fallback baseline remains 102/102 fixture files. Out of scope/open: full RefCell borrow-handle lifecycle across scopes/branches/loops/early returns and pointer-backed aggregate alias preservation inside imported macros.
  - [x] Completed active Phase 1 hardening slice: shared imported macro addressable identifier scanning now lives in `src/lowering_rules.zig`. `ImportedMacroCallPlan.addressableIdentifierArgName()` covers the shared decision that a plain identifier passed to a borrowed/addressable imported macro parameter marks that source binding as needing stable addressable storage. `src/sab_codegen.zig` consumes the helper for direct SAB `borrowed_bindings`, replacing the local `callArgNeedsAddressableSlot + identifier` test, and `src/codegen.zig` consumes the same helper for SA-text `addressable_bindings`, while preserving emitter-local address-shape type lookup, field/index/deref materialization, stack allocation/store, release ordering, and concrete SA-text/SAB emission. Verification: `zig fmt --check src/lowering_rules.zig src/codegen.zig src/sab_codegen.zig`; `zig test src/lowering_rules.zig --test-filter "shared imported macro"` (3/3); `zig build --summary all`; `zig build test --summary all` (83/83); local direct SAB no-fallback and SA-text parity for `tests/test_unit_imported_macro_address_direct.sla` (4/4 each); local direct SAB no-fallback for `tests/test_unit_pkgjson_codegen.sla` (1/1), `tests/test_unit_borrow_direct.sla` (2/2), `tests/test_unit_borrow_temp_release_order.sla` (25/25), and `tests/test_unit_refcell_struct_payload.sla` (6/6); `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host strict direct-SAB no-fallback for imported macro address, pkgjson, borrow-temp, RefCell payload, imported JSON raw-pointer regressions, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; host SA-text parity for imported macro address; host full no-fallback sweep over 102 `tests/test_unit_*.sla` fixture files passed, totaling 253/253 test cases; `git diff --check`. Progress: 100% for this shared imported macro addressable-identifier scan sub-slice; broader Y/shared-lowering remains about 98%; direct SAB fallback-removal remains 100%; current no-fallback baseline remains 102/102 fixture files. Out of scope/open: full RefCell borrow-handle lifecycle across scopes/branches/loops/early returns and pointer-backed aggregate alias preservation inside imported macros.
  - [x] Completed active Phase 1 hardening slice: shared imported macro arg lowering action now lives in `src/lowering_rules.zig`. `ImportedMacroArgLoweringAction` plus `ImportedMacroCallPlan.planArgValueBypassAction()` / `planAddressableArgLoweringAction()` cover the shared decision to pass ordinary values, keep raw-pointer borrowed imported-macro args as values, reuse existing addressable symbols, materialize value stack slots, or materialize address-expression stack slots. `src/codegen.zig` and `src/sab_codegen.zig` consume the same action plan while preserving emitter-local address-shape type lookup, macro-context remapping, stack allocation/store, release ordering, and concrete SA-text/SAB emission. Verification: `zig fmt --check src/lowering_rules.zig src/codegen.zig src/sab_codegen.zig`; `zig test src/lowering_rules.zig --test-filter "shared imported macro"` (3/3); `zig build --summary all`; `zig build test --summary all` (83/83); local direct SAB no-fallback and SA-text parity for `tests/test_unit_imported_macro_address_direct.sla` (4/4 each); local direct SAB no-fallback for `tests/test_unit_pkgjson_codegen.sla` (1/1), `tests/test_unit_borrow_direct.sla` (2/2), and `tests/test_unit_borrow_temp_release_order.sla` (25/25); `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host strict direct-SAB no-fallback for imported macro address, pkgjson, borrow direct, borrow-temp, imported JSON raw-pointer regressions, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; host SA-text parity for imported macro address; host full no-fallback sweep over 102 `tests/test_unit_*.sla` fixture files passed, totaling 253/253 test cases; `git diff --check`. Progress: 100% for this shared imported macro arg-lowering action sub-slice; broader Y/shared-lowering remains about 98%; direct SAB fallback-removal remains 100%; current no-fallback baseline remains 102/102 fixture files. Out of scope/open: full RefCell borrow-handle lifecycle across scopes/branches/loops/early returns and pointer-backed aggregate alias preservation inside imported macros.
  - [x] Completed active Phase 1 hardening slice: borrowed identifier recognition now lives in shared lowering rules. `src/lowering_rules.zig` exposes `borrowedIdentifierName()`, covering the pure AST decision that exactly `&identifier` names a borrowed/addressable binding; it deliberately does not recognize `&field`, ordinary identifiers, or `^identifier`. `src/codegen.zig` and `src/sab_codegen.zig` now consume that helper for addressable/borrowed binding scans, and SA-text also uses it in the planned call-arg path that preserves addressable borrowed identifiers, while preserving emitter-local address emission, release handling, and cleanup mechanics. Verification: `zig fmt --check src/lowering_rules.zig src/codegen.zig src/sab_codegen.zig`; `zig test src/lowering_rules.zig --test-filter "shared lowering rules classify call materialization decisions"`; `zig build --summary all`; `zig build test --summary all` (83/83); local direct SAB no-fallback and SA-text parity for `tests/test_unit_borrow_direct.sla` (2/2 each); local direct SAB no-fallback for `tests/test_unit_borrow_temp_release_order.sla` (25/25) and `tests/test_unit_imported_macro_address_direct.sla` (4/4); local and host SA-text parity for imported macro address fixture; `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host strict direct-SAB no-fallback for borrow direct, borrow-temp, imported macro address, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; host full no-fallback sweep over 102 `tests/test_unit_*.sla` fixture files passed, totaling 253/253 test cases. Progress: 100% for this shared borrowed-identifier recognition sub-slice; broader Y/shared-lowering remains about 98%; direct SAB fallback-removal remains 100%; current no-fallback baseline remains 102/102 fixture files.
  - [x] Completed active Phase 1 hardening slice: borrowed/address-taken primitive binding storage now lives in shared lowering rules. `src/lowering_rules.zig` exposes `BorrowedBindingStoragePlan` through `planBorrowedBindingStorage()`, covering the narrow alias/lifetime decision that borrowed or address-taken primitive bindings need stable stack slots while pointer-backed/user-defined bindings remain out of scope. `src/codegen.zig` and `src/sab_codegen.zig` now consume that shared plan instead of duplicating local `ty.* == .primitive` stack-slot checks for borrowed parameters and let/const bindings, while preserving emitter-local stack allocation, store, local mapping, and release mechanics. Verification: `zig fmt --check src/lowering_rules.zig src/codegen.zig src/sab_codegen.zig`; `zig test src/lowering_rules.zig --test-filter "shared borrowed binding storage plan"`; `zig build --summary all`; `zig build test --summary all` (83/83); local strict direct-SAB no-fallback for `tests/test_unit_borrow_direct.sla` (2/2), `tests/test_unit_borrow_temp_release_order.sla` (25/25), `tests/test_unit_refcell_struct_payload.sla` (6/6), and `tests/test_unit_imported_macro_address_direct.sla` (4/4); local SA-text parity for borrow direct, RefCell payload, and imported macro address fixtures; `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host strict direct-SAB no-fallback for the same focused fixtures plus `/home/vscode/projects/sla_ecs/lib/parallel.sla`; host SA-text parity for borrow direct, RefCell payload, and imported macro address fixtures; host full no-fallback sweep over 102 `tests/test_unit_*.sla` fixture files passed, totaling 253/253 test cases. Progress: 100% for this shared borrowed binding storage-plan extraction sub-slice; broader Y/shared-lowering remains about 98%; direct SAB fallback-removal remains 100%; current no-fallback baseline remains 102/102 fixture files.
  - [x] Completed active Phase 1 hardening slice: result-slot transfer planning now lives in shared lowering rules. `src/lowering_rules.zig` exposes `ResultSlotTransferPlan` through `planResultSlotTransfer()`, covering both pointer-passing result-slot value transfer and whether RefCell borrow result slots need companion cell slots. `src/codegen.zig` and `src/sab_codegen.zig` now consume that shared plan instead of duplicating the companion-slot `borrow` type check locally, while preserving emitter-local state transfer, register/string emission, and cleanup ordering. Verification: `sa version` (`0.0.3.3`); `zig fmt --check src/lowering_rules.zig src/codegen.zig src/sab_codegen.zig`; `zig test src/lowering_rules.zig --test-filter "shared lowering rules classify result-slot value transfer"`; `zig build --summary all`; `zig build test --summary all` (82/82); local direct SAB no-fallback and SA-text parity for `tests/test_unit_refcell_struct_payload.sla` (6/6 each); local direct SAB no-fallback for `tests/test_unit_borrow_temp_release_order.sla` (25/25); `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host direct SAB no-fallback and SA-text parity for `tests/test_unit_refcell_struct_payload.sla`; host strict direct-SAB no-fallback for `tests/test_unit_borrow_temp_release_order.sla` and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; host full no-fallback sweep over 102 `tests/test_unit_*.sla` fixture files passed, totaling 253/253 test cases. Progress: 100% for this shared result-slot transfer-plan extraction sub-slice; broader Y/shared-lowering remains about 98%; direct SAB fallback-removal remains 100%; current no-fallback baseline remains 102/102 fixture files.
  - [x] Completed active Phase 1 hardening slice: result-slot transfer metadata and RefCell companion-slot planning now keep pointer-passing branch results as ownership/borrow transfers instead of copy-and-release temps. `src/lowering_rules.zig` exposes `resultSlotStoreTransfersValue`, `src/sab_codegen.zig` transfers release metadata/future state through result slots and allocates RefCell companion cell slots before branch emission, and `src/codegen.zig` mirrors that state transfer for the legacy SA-text path. SA-text also keeps ordinary smart-pointer field borrows (`&holder.boxed`, `&nested.slot.item`) as the smart-pointer value representation expected by existing SA-text smart-pointer functions, while preserving RefCell<Box<T>> borrow-handle representation. `tests/test_unit_refcell_struct_payload.sla` now imports `tests/import_fixtures/imported_refcell_helpers.sa` and covers if-expression RefCell borrow counts plus pointer-backed Box result-slot ownership. Verification: `sa version` (`0.0.3.3`, `/home/vscode/.sa/bin/sa`); `zig fmt src/codegen.zig`; `zig test src/lowering_rules.zig --test-filter "shared lowering rules classify result-slot value transfer"`; `zig build --summary all`; `zig build test --summary all` (82/82); local SA-text parity for `tests/test_unit_refcell_struct_payload.sla`, `tests/test_unit_borrow_temp_release_order.sla` (25/25), `tests/test_unit_smart_pointer_struct_field_cleanup.sla`, and imported macro address fixtures; local strict direct-SAB no-fallback for the same focused fixtures; `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host strict direct-SAB no-fallback for `tests/test_unit_refcell_struct_payload.sla`, `tests/test_unit_borrow_temp_release_order.sla`, `tests/test_unit_imported_macro_address_direct.sla`, `tests/test_unit_smart_pointer_struct_field_cleanup.sla`, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; host SA-text parity for the RefCell, borrow-temp, and imported macro fixtures; local and host full no-fallback sweeps 102/102. Current `/home/vscode/projects/sci` worktree remains dirty with compiler/runtime changes, but this slice did not modify or revert SCI files and revalidated against the installed compiler. Progress: 100% for this result-slot/RefCell metadata transfer and SA-text smart-pointer field-borrow sub-slice; broader Y/shared-lowering remains about 98%; direct SAB fallback-removal remains 100%; current no-fallback baseline remains 102/102.
  - [x] Completed active Phase 1 hardening slice: shared RefCell borrow payload and release planning now lives in `src/lowering_rules.zig`. `RefCellBorrowPlan` now carries `RefCellBorrowValueKind` (`scalar_slot`, `pointer_payload`, `smart_pointer_payload`) plus shared try-borrow/release macro selection through `tryBorrowMacroName()` and `refCellBorrowReleaseMacroName()`. `src/codegen.zig` and `src/sab_codegen.zig` both consume that shared plan while intentionally keeping emitter-local value representation details: SA-text still peels pointer and smart-pointer payloads to payload pointers, while direct SAB keeps smart-pointer payload borrows in the slot and only peels raw pointer payloads. `tests/test_unit_refcell_struct_payload.sla` now imports Box and covers `RefCell<Box<RefCellPayload>>` borrow read/write. Verification: `sa version` (`0.0.3.3`); `zig fmt src/lowering_rules.zig src/codegen.zig src/sab_codegen.zig`; `zig test src/lowering_rules.zig --test-filter "shared refcell borrow call plan"`; `zig test src/lowering_rules.zig --test-filter "shared lowering rules classify address-of shapes"`; local `./zig-out/bin/sla-local-cli sla test tests/test_unit_refcell_struct_payload.sla --test-backend sab --jobs 1`; local `./zig-out/bin/sla-local-cli sla test tests/test_unit_refcell_struct_payload.sla --test-backend sa --jobs 1`; local `./zig-out/bin/sla-local-cli sla test tests/test_unit_borrow_temp_release_order.sla --test-backend sab --jobs 1`; local `./zig-out/bin/sla-local-cli sla test tests/test_unit_imported_macro_address_direct.sla --test-backend sab --jobs 1`; `zig build --summary all`; `zig build test --summary all` (81/81); `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host `SA_PLUGIN_DEV=1 SLA_SAB_NO_FALLBACK=1 sa sla test tests/test_unit_refcell_struct_payload.sla --jobs 1`; host `SA_PLUGIN_DEV=1 sa sla test tests/test_unit_refcell_struct_payload.sla --test-backend sa --jobs 1`; host strict direct-SAB no-fallback for `tests/test_unit_borrow_temp_release_order.sla`, `tests/test_unit_imported_macro_address_direct.sla`, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; local and host full no-fallback sweeps 102/102; `git diff --check`. Out of scope/open: full RefCell borrow-handle lifecycle across scopes/branches/loops/early returns, pointer-backed aggregate alias semantics inside imported macros, and broader shared macro expansion convergence. Progress: 100% for this shared RefCell borrow payload/release-planning sub-slice; broader Y/shared-lowering remains about 98%; direct SAB fallback-removal remains 100%; current no-fallback baseline remains 102/102.
  - [x] Completed active Phase 1 hardening slice: shared smart-pointer/chained-deref address-of classification now distinguishes `AddressOfShape.deref_smart_pointer` for Box/Rc/Arc-like deref sources, including outer borrow/pointer wrappers, instead of collapsing those shapes into emitter-local `.value_temp` handling. `src/lowering_rules.zig` owns the shared classification; `src/sab_codegen.zig` and `src/codegen.zig` consume the explicit shape while keeping concrete emission and lifetime mechanics local; imported-macro addressable-arg planning now treats borrowed smart-pointer deref args as address-expression materialization candidates. `tests/test_unit_imported_macro_address_direct.sla` now imports Box and covers imported macro reads through `*boxed` and `**nested_box`, while the existing user-macro and borrow-temp chained-deref fixtures remain green. Verification: `sa version` (`0.0.3.3`); `zig fmt src/lowering_rules.zig src/codegen.zig src/sab_codegen.zig`; `zig test src/lowering_rules.zig --test-filter "shared lowering rules classify address-of shapes"`; `zig test src/lowering_rules.zig --test-filter "shared imported macro address-expression args materialize stack slots"`; local `./zig-out/bin/sla-local-cli sla test tests/test_unit_imported_macro_address_direct.sla --test-backend sab --jobs 1`; local `./zig-out/bin/sla-local-cli sla test tests/test_unit_imported_macro_address_direct.sla --test-backend sa --jobs 1`; local `./zig-out/bin/sla-local-cli sla test tests/test_unit_borrow_temp_release_order.sla --test-backend sab --jobs 1`; local `./zig-out/bin/sla-local-cli sla test tests/test_unit_user_macro_direct.sla --test-backend sab --jobs 1`; `zig build --summary all`; `zig build test --summary all` (80/80); `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host `SA_PLUGIN_DEV=1 SLA_SAB_NO_FALLBACK=1 sa sla test tests/test_unit_imported_macro_address_direct.sla --jobs 1`; host `SA_PLUGIN_DEV=1 sa sla test tests/test_unit_imported_macro_address_direct.sla --test-backend sa --jobs 1`; host strict direct-SAB no-fallback for `tests/test_unit_user_macro_direct.sla`, `tests/test_unit_borrow_temp_release_order.sla`, `tests/test_unit_borrow_direct.sla`, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; local and host full no-fallback sweeps 102/102; `git diff --check`. Out of scope/open: RefCell/non-smart-pointer `.value_temp` lifecycle planning, pointer-backed aggregate alias semantics inside imported macros, and broader shared macro expansion convergence. Progress: 100% for this shared smart-pointer/chained-deref address-shape sub-slice; broader Y/shared-lowering remains about 98%; direct SAB fallback-removal remains 100%; current no-fallback baseline remains 102/102.
  - [x] Completed active Phase 2A/Phase 1 hardening slice: imported macro address-expression materialization planning. Borrowed imported-macro `field`, `index`, and ordinary `deref` args now materialize through dedicated address-expression helpers in `src/sab_codegen.zig` and `src/codegen.zig`, consuming the shared `ImportedMacroAddressableArgAction.materialize_address_expression_stack_slot` decision from `src/lowering_rules.zig` instead of collapsing back into the generic imported-macro value-temp slot path. Added `tests/import_fixtures/imported_borrow_helpers.sa` plus `tests/test_unit_imported_macro_address_direct.sla`, covering borrowed imported-macro field/index/deref args through direct SAB no-fallback and SA-text parity. Verification: `sa version` (`0.0.3.3`); `zig fmt src/sab_codegen.zig src/codegen.zig`; local `./zig-out/bin/sla-local-cli sla test tests/test_unit_imported_macro_address_direct.sla --test-backend sab --jobs 1`; local `./zig-out/bin/sla-local-cli sla test tests/test_unit_imported_macro_address_direct.sla --test-backend sa --jobs 1`; `zig build test --summary all` (80/80); `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host `SA_PLUGIN_DEV=1 SLA_SAB_NO_FALLBACK=1 sa sla test tests/test_unit_imported_macro_address_direct.sla`; host `SA_PLUGIN_DEV=1 sa sla test tests/test_unit_imported_macro_address_direct.sla --test-backend sa`; host strict direct-SAB no-fallback for `tests/test_unit_pkgjson_codegen.sla`, imported JSON/FS direct fixtures, `tests/test_unit_user_macro_direct.sla`, `tests/test_unit_borrow_direct.sla`, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; host full no-fallback sweep 102/102. Out of scope/open: fuller shared `&**chain` classification, pointer-backed aggregate alias semantics inside imported macros, RefCell lifecycle planning, and broader shared macro expansion convergence. Progress: 100% for this imported macro address-expression materialization sub-slice; broader Y/shared-lowering remains about 98%; direct SAB fallback-removal remains 100%; current no-fallback baseline is 102/102.
  - [x] Completed active Phase 3/Phase 2A hardening slice: direct SAB utf8 const-byte materialization now decodes escaped SLA string literal text before storing `ConstDecl.value.utf8.bytes`, escapes only `literal_text` / `raw_text`, and uses raw byte lengths in the affected direct-SAB const-byte paths. Root cause was plugin-side SAB const payload generation: host object `.rodata` contained literal backslashes for `{"types":"x"}` while runtime length remained 13, so `sa_json_parse` read an invalid truncated escaped prefix and returned null. Verification: `zig fmt src/sab_codegen.zig`; `zig build test --summary all` (80/80); local strict direct-SAB no-fallback for `tests/test_unit_imported_json_direct.sla`, `tests/test_unit_imported_json_struct_direct.sla`, `tests/test_unit_imported_json_string_direct.sla`, and `tests/test_unit_pkgjson_codegen.sla`; `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host strict direct-SAB no-fallback for the same four fixtures plus imported JSON/FS direct fixtures, `tests/test_unit_user_macro_direct.sla`, `tests/test_unit_borrow_direct.sla`, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; host full no-fallback sweep 101/101; `git diff --check`. Out of scope/open: new imported SA macro syntax, `.sla -> .sa -> .sab` fallbacking, broad projection-by-reference mutation semantics, RefCell lifecycle, and broader shared macro expansion convergence. Progress: 100% for this direct SAB utf8 const-byte sub-slice; broader Y/shared-lowering remains about 98%; direct SAB fallback-removal remains 100%; current no-fallback baseline remains 101/101.
  - [ ] Next active Phase 1 hardening slice: broader shared RefCell borrow-handle lifecycle and alias planning across scopes, branches, loops, call args, and early returns, plus the remaining pointer-backed aggregate borrow/alias cases inside imported macros that still rely on emitter-local lifetime decisions. Expected verification: focused shared-rule coverage, `zig build test --summary all`, local and host strict direct-SAB no-fallback for `tests/test_unit_borrow_temp_release_order.sla`, `tests/test_unit_refcell_struct_payload.sla`, `tests/test_unit_imported_macro_address_direct.sla`, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`, followed by docs update and `git diff --check`.
  - [x] Completed active Phase 2A/Phase 1 hardening slice: shared imported macro addressable arg action planning now lives in `src/lowering_rules.zig`. `ImportedMacroCallPlan.planAddressableArgAction` chooses pass-by-value, reuse-existing-addressable-symbol, or materialize-temporary-stack-slot; `src/sab_codegen.zig` keeps macro-context lookup, concrete symbol names, stack allocation/store, and release mechanics local. Verified with `sa version` (`0.0.3.3`), focused lowering-rule test (`shared imported macro call plan`), `zig fmt --check src/lowering_rules.zig src/sab_codegen.zig src/codegen.zig`, `zig build --summary all`, `zig build test --summary all` (75/75), local strict direct-SAB for `tests/test_unit_pkgjson_codegen.sla`, imported JSON/FS direct fixtures, and `tests/test_unit_user_macro_direct.sla` (2/2), local no-fallback sweep 101/101, `sa plugin install --dev .`, `SA_PLUGIN_DEV=1 sa sla help`, host strict direct-SAB for the same imported macro/user macro fixtures, host `/home/vscode/projects/sla_ecs/lib/parallel.sla`, and host no-fallback sweep 101/101. Out of scope/open: new imported SA macro syntax, decoded fragment expansion changes, full imported macro address-expression lowering, RefCell lifecycle, and broader shared macro expansion convergence. Progress: 100% for this shared imported macro addressable arg action sub-slice; broader Y/shared-lowering remains about 98%; direct SAB fallback-removal remains 100% for the tracked unit corpus; current no-fallback baseline is 101/101.
  - [x] Completed active Phase 1 hardening slice: direct SAB user-macro smart-pointer address lowering now supports macro-body `&*boxed` and a focused `&**nested_box` chain through shared smart-pointer action/get plans in `src/lowering_rules.zig`. `src/sab_codegen.zig` shares one deref-address fallback helper between ordinary and macro address paths, while macro deref type/value lowering recognizes smart-pointer deref via `lowering_rules.smartPointerDerefType` and `planSmartPointerGetAction`; macro hygiene, register emission, and release ordering stay local. `tests/test_unit_user_macro_direct.sla` imports Box and covers `borrow_smart_address_macro` returning 42. Verified with `sa version` (`0.0.3.3`), focused lowering-rule test (`shared dyn coercion and receiver plans`), `zig fmt --check src/sab_codegen.zig src/lowering_rules.zig src/codegen.zig`, `zig build --summary all`, `zig build test --summary all` (74/74), local strict direct-SAB for `tests/test_unit_user_macro_direct.sla` (2/2), `tests/test_unit_borrow_temp_release_order.sla` (25/25), `tests/test_unit_smart_pointer_struct_field_cleanup.sla` (3/3), and `tests/test_unit_refcell_struct_payload.sla` (2/2), local no-fallback sweep 101/101, `sa plugin install --dev .`, `SA_PLUGIN_DEV=1 sa sla help`, host strict direct-SAB for the same four focused fixtures, host `/home/vscode/projects/sla_ecs/lib/parallel.sla`, host no-fallback sweep 101/101, and `git diff --check`. Out of scope/open: imported SA macro address lowering, RefCell borrow/borrow_mut lifecycle, std-surface metadata changes, fuller shared projection/`&**chain` classification, and the broader shared macro expansion contract. Progress: 100% for this direct SAB user-macro smart-pointer address sub-slice; broader Y/shared-lowering remains about 98%; direct SAB fallback-removal remains 100% for the tracked unit corpus; current no-fallback baseline is 101/101.
  - [x] Completed active Phase 1 hardening slice: shared smart-pointer address action planning now lives in `src/lowering_rules.zig`. Scope was the existing direct SAB address-of fallback sequence for smart-pointer deref sources: dyn Box identity, `as_ptr` address slot, pointer-backed value take from that slot, nested smart-pointer value-slot handling, and `get` value fallback. Direct SAB consumes the shared action plan while keeping emission and release ordering in `src/sab_codegen.zig`. Out of scope/open: RefCell lifecycle changes, release-order changes, std-surface emission changes, new syntax support, imported macro address lowering, and broader shared macro expansion convergence. Verified with focused shared-rule tests (`shared dyn coercion and receiver plans`, `shared lowering rules classify address-of shapes`), `zig fmt --check src/lowering_rules.zig src/sab_codegen.zig src/codegen.zig`, `zig build --summary all`, `zig build test --summary all` (74/74), local strict direct-SAB for `tests/test_unit_borrow_temp_release_order.sla` (25/25), `tests/test_unit_smart_pointer_struct_field_cleanup.sla` (3/3), and `tests/test_unit_refcell_struct_payload.sla` (2/2), `sa plugin install --dev .`, `SA_PLUGIN_DEV=1 sa sla help`, host strict direct-SAB for the same three fixtures, host `/home/vscode/projects/sla_ecs/lib/parallel.sla`, local and host no-fallback sweeps 101/101, and `git diff --check`. Progress: 100% for this shared smart-pointer address action sub-slice; broader Y/shared-lowering remains about 98%; direct SAB fallback-removal remains 100% for the tracked unit corpus; current no-fallback baseline is 101/101.
  - [x] Completed active Phase 1 hardening slice: shared ordinary index-address target unwrapping now lives in `src/lowering_rules.zig`. Scope was the borrow/pointer peel used when classifying `&values[index]` for ordinary array/slice targets, including `values: &[T; N]`; SA-text and direct SAB now call the same helper instead of duplicating the target-type walk. Vec/std-surface index-address metadata, smart-pointer address planning, `&**chain`, imported macro address lowering, RefCell lifecycle, and broader shared macro expansion convergence remain out of scope/open. Verified with `zig fmt --check src/lowering_rules.zig src/codegen.zig src/sab_codegen.zig`, focused shared-rule test, `zig build --summary all`, `zig build test --summary all` (74/74), local and host SA-text plus strict direct-SAB for `tests/test_unit_borrow_direct.sla`, local and host strict direct-SAB for `tests/test_unit_user_macro_direct.sla`, local and host no-fallback sweeps 101/101, `sa plugin install --dev .`, `SA_PLUGIN_DEV=1 sa sla help`, host `/home/vscode/projects/sla_ecs/lib/parallel.sla`, and `git diff --check`. Progress: 100% for this shared ordinary index-address target unwrapping sub-slice; broader Y/shared-lowering remains about 98%; direct SAB fallback-removal remains 100% for the tracked unit corpus; current no-fallback baseline is 101/101.
  - [x] Completed active Phase 1 hardening slice: direct SAB user-macro address-of lowering now consumes the shared `AddressOfShape`/`AddressOfPlan` classifier for ordinary `&identifier`, `&*borrow_or_pointer`, `&field`, and `&index` forms. `genMacroAddressOf` still owns macro hygiene, argument remapping, and address emission mechanics, but ordinary shape selection comes from `lowering_rules.planAddressOf`. `tests/test_unit_user_macro_direct.sla` covers macro-body `&point.left`, `&values[1]`, and `&*ptr`; `tests/test_unit_borrow_direct.sla` also covers `&values[index]` through a `&[i32; 3]` parameter, so SA-text and direct SAB both unwrap borrow/pointer array targets before ordinary index-address planning. This direct SAB classifier slice did not claim the broader shared macro expansion/substitution contract. Verified with `sa version` (`0.0.3.3`), `zig fmt --check src/codegen.zig src/sab_codegen.zig`, focused shared address-of rule test, `zig build --summary all`, `zig build test --summary all` (74/74), local and host SA-text plus strict direct-SAB for `tests/test_unit_borrow_direct.sla`, local and host strict direct-SAB for `tests/test_unit_user_macro_direct.sla`, local and host no-fallback sweeps 101/101, `sa plugin install --dev .`, `SA_PLUGIN_DEV=1 sa sla help`, host `/home/vscode/projects/sla_ecs/lib/parallel.sla`, and `git diff --check`. Progress: 100% for the tracked direct SAB user-macro ordinary address-of classifier sub-slice; broader Y/shared-lowering remains about 98%; direct SAB fallback-removal remains 100% for the tracked unit corpus.
  - [x] Completed active Phase 1 hardening slice: the first reusable shared address/projection classifier now covers ordinary `&identifier`, `&*borrow_or_pointer`, `&field`, and `&index` forms. `src/lowering_rules.zig` owns `AddressOfShape`/`AddressOfPlan`; SA-text `.borrow_expr` and direct SAB `genAddressOf` consume that shared classification. `tests/test_unit_borrow_direct.sla` now includes a fixed-array `&values[index]` borrow fixture, and SA-text keeps the indexed element address live through `borrow_source_temps` instead of borrowing a loaded value temporary. The `&*` classifier deliberately excludes Box/Rc/Arc/RefCell-like sources, including outer borrow/pointer wrappers, so existing smart-pointer address/lifecycle paths remain in force until the broader shared projection plan is widened. Verified with `sa version` (`0.0.3.3`), `zig fmt --check src/lowering_rules.zig src/codegen.zig src/sab_codegen.zig`, focused shared lowering-rule test, `zig build --summary all`, `zig build test --summary all` (74/74), local focused SA-text and strict direct-SAB for `tests/test_unit_borrow_direct.sla`, local strict SAB regressions for `tests/test_unit_borrow_temp_release_order.sla` and `tests/test_unit_smart_pointer_struct_field_cleanup.sla`, local no-fallback sweep 101/101, `sa plugin install --dev .`, `SA_PLUGIN_DEV=1 sa sla help`, host focused SA-text and strict direct-SAB, host no-fallback sweep 101/101, host `/home/vscode/projects/sla_ecs/lib/parallel.sla`, and `git diff --check`. Progress: 100% for the tracked ordinary address-of classifier sub-slice; broader Y/shared-lowering remains about 98%; direct SAB fallback-removal remains 100% for the tracked unit corpus.
  - [x] Completed active Phase 1 hardening slice: plain nested struct scalar field-chain borrow now passes for `tests/test_unit_borrow_direct.sla` with `&item.inner.value`, covering the non-tuple counterpart to the completed `pair.0.x`/`&pair.0.x` path. SA-text now preserves explicit `&*value` as an address projection instead of materializing a scalar temporary address, explicit borrow receiver materialization no longer gets swallowed by receiver-style auto-borrow, and shared struct-literal field transfer classifies explicit pointer-backed non-copy fields as moves while keeping copy-struct deep-copy paths. This remains short of the full shared address/projection plan. Verified with `zig fmt --check src/codegen.zig src/lowering_rules.zig`, focused shared lowering-rule tests, `zig build --summary all`, `zig build test --summary all` (73/73), focused local SA-text and strict direct SAB for `tests/test_unit_borrow_direct.sla`, `sa plugin install --dev .`, `SA_PLUGIN_DEV=1 sa sla help`, focused host SA-text and strict direct SAB, local and host no-fallback sweeps 101/101, and host `/home/vscode/projects/sla_ecs/lib/parallel.sla`. Progress: 100% for the tracked plain nested struct borrow baseline.
  - [x] Completed active Phase 1/5 hardening slice: tuple ordinal followed by field-chain projection now passes for `tests/test_unit_tuples.sla` with `pair.0.x` and `&pair.0.x`. Scope is lexer numeric-dot disambiguation plus SA-text/direct-SAB parity for tuple/struct field-chain value and borrow projection. The lexer fix owns tokenization of `0.` versus `0 .`; SA-text now mirrors direct SAB for the focused nested aggregate-address path by loading pointer-backed aggregate fields before continuing a field chain. This slice is complete at 100%. Verified with `sa version` (`0.0.3.3`), `zig fmt --check src/lexer.zig src/codegen.zig`, `zig test src/lexer.zig`, `zig build --summary all`, `zig build test --summary all` (73/73), `sa plugin install --dev .`, `SA_PLUGIN_DEV=1 sa sla help`, local and host tuple `sa sla test` runs on `--test-backend sa` and `--test-backend sab`, strict local and host no-fallback sweeps 101/101, host `/home/vscode/projects/sla_ecs/lib/parallel.sla`, tuple/parallel disasm guards, regenerated `tests/test_unit_tuples.test.sa`, `sa test tests/test_unit_tuples.test.sa`, and `git diff --check`. Progress: 100% for the tracked tuple/borrow field-chain slice.
  - [x] Last verified committed baseline before the current worktree slice is `ecd9570` (`Share dyn trait coercion lowering`), with full no-fallback `tests/test_unit_*.sla` sweep at 61/69 passing, Y/shared-lowering approximately 77%, and direct SAB fallback removal approximately 91%.
  - [x] The smart-pointer / borrow / RefCell focused convergence slice is now verified through local and host gates. It includes shared smart-pointer helpers, RefCell borrow planning/metadata transfer, pointer-backed index-address metadata, Rc/Arc clone metadata/macros, borrowed smart-pointer clone receiver materialization through shared `associatedRuleNeedsUnderlyingSmartPointer`, and visible disasm separation of call target and args in SCI.
  - [x] `tests/test_unit_refcell_struct_payload.sla` is no longer the active signal 11 blocker. It passes local and host direct SAB no-fallback 2/2, and the SA-text backend passes 2/2.
  - [x] Focused local/host direct SAB no-fallback gates pass for `tests/test_unit_borrow_temp_release_order.sla` 25/25, `tests/test_unit_smart_pointer_struct_field_cleanup.sla` 3/3, `tests/test_unit_refcell_struct_payload.sla` 2/2, `tests/test_unit_option_methods.sla` 5/5, `tests/test_unit_option_direct.sla` 1/1, `tests/test_unit_result_direct.sla` 1/1, and `/home/vscode/projects/sla_ecs/lib/parallel.sla` 1/1.
  - [x] Local build/test gates pass for this slice: `zig build --summary all` and `zig build test --summary all` (58/58).
  - [x] Official host gate passes: `sa plugin install --dev .` from this repo and `SA_PLUGIN_DEV=1 sa sla help`.
  - [x] Disasm guard for tracked focused artifacts passes: `sa sla sab disasm <sab> | rg 'call .*@[^" ]+\('` returned no matches for latest RefCell/borrow artifacts. SCI now displays `call rN,"@func","args"` while preserving runtime parser compatibility for the underlying call-body representation.
  - [x] Current full dev-mode no-fallback sweep is 103/103 after the Phase 3 array-to-slice call-materialization sub-slice, totaling 256/256 test cases locally and under host `SA_PLUGIN_DEV=1`. The historical fixture additions behind this baseline include `tests/test_unit_array_to_slice_call.sla`, the imported macro address/direct fixture, join2 defer-ready later-ready continuation/resumption fixture, two-sequential-await defer-ready continuation/resumption fixture, copy-struct-capture defer-ready continuation/resumption fixture, multi-capture defer-ready continuation/resumption fixture, two-post-binding defer-ready continuation/resumption fixture, branchy defer-ready continuation/resumption fixture, scalar-linear defer-ready continuation/resumption fixture, post-binding-return-addend defer-ready continuation/resumption fixture, captured-plus-immediate addend defer-ready continuation/resumption fixture, captured-local addend defer-ready continuation/resumption fixture, post-await binding defer-ready continuation/resumption fixture, prebound-local `future::defer_ready(...).await` continuation/resumption fixture, the narrow direct `future::defer_ready(...).await` continuation/resumption fixture, the `future::defer_ready` task-runtime fixture, local pending composite-future `.await` fixture, local future-readiness `.await` fixture, statically ready composite-future `.await` fixture, future-parameter `.await` async state-machine fixture, pending `.await` async state-machine fixture, dynamic `Vec<Task<T>>` executor runtime fixture, `future::select2` runtime fixture, `future::join2` runtime fixture, fixed-array executor runtime fixture, async Poll runtime fixture, async task-state runtime fixture, async pending-future task-poll fixture, async while-let future queue fixture, async block-on fixture, formatted `println` fixture, pointer-backed struct-update fixture, Phase 8 ready-future async subset, ready Future/task runtime surface, the Phase 4/5 Vec index assignment fix, earlier derive/enum/operator/protocol slices, and the extern/export ordering stress probe `tests/test_unit_mixed_collections_order.sla`. There are no remaining tracked unit failures in `tests/test_unit_*.sla`.
  - [x] Current overall estimate after the completed shared borrow-address temp binding-transfer planning sub-slice: Y/shared-lowering approximately 98%; direct SAB fallback-removal 100% for the tracked unit corpus; full dev-mode no-fallback unit-file pass rate 102/102. The generic std-macro call-body/register-naming issue now has a shared SCI flattener subfix for embedded symbol-token remap, committed in `/home/vscode/projects/sci` as `1beccde`; the extern/export ordering audit is closed as no-repro (stress probe + focused exported-helper ordering regression), and the plugin-side fragment cleanup audit is closed (the decode-time hygiene/placeholder adapter is a distinct stage from SCI's flatten-time symbol-ID remap, not a parallel implementation).
  - [x] Dirty/untracked caveat at start of the pointer-backed struct-update slice: `git status --short` is clean. Inspect before staging; do not blindly restore, delete, or commit unrelated/generated files if they appear while working.
  - [x] Phase 2A first classification checkpoint is recorded: `tests/test_unit_pkgjson_codegen.sla` failed before package resolution on the first expression-output imported SA macro call, `let t = SLA_JSON_OBJECT_GET(...)`.
  - [x] Phase 2A implementation is complete through the Y path: imported macro metadata and borrowed argument masks are collected before the emitter split; `lowering_rules.ImportedMacroCallPlan` feeds both SA text and SAB; SAB consumes generic imported macro fragments without fixture-specific `SLA_JSON_*` or `SLA_FS_*` branches.
  - [x] Completed active implementation slice: `tests/test_unit_var_comprehensive.sla` now passes direct SAB no-fallback through shared loop-control/termination rules, direct SAB loop jump cleanup, scoped block locals, explicit release handling, and unique var stack slots.
  - [x] Completed active implementation slice: `tests/test_unit_rc_dyn_trait.sla` now passes direct SAB no-fallback and SA-text parity through shared `DynCoercionPlan`, shared dyn-dispatch receiver planning, and generic std-surface/std-macro consumption for Rc/Box dyn trait objects.
  - [x] Completed active implementation slice: `tests/test_unit_sets.sla` now passes direct SAB no-fallback and SA-text parity through std-surface metadata for HashSet/BTreeSet operations plus the existing generic SAB std-surface consumer.
  - [x] Completed active implementation slice: scalar and pointer-backed struct update now pass direct SAB no-fallback and SA-text parity through shared struct literal field plans. `StructLiteralFieldPlan` carries `field_ty`/`release_loaded`, and `StructLiteralFieldTransfer` classifies explicit/update fields as direct store, deep copy, or move. `tests/test_unit_struct_update_pointer_backed.sla` covers a `Vec<i32>` field preserved under `..base`.
  - [x] Completed active implementation slice: formatted `println` now passes direct SAB no-fallback and SA-text parity through shared `PrintlnArgPlan` classification plus structured SAB `sa_print_bytes`/`sa_fmt_*` emission. `tests/test_unit_println_direct.sla` covers primitive, bool, and literal string formatting; rosetta ready-await demos `75_async_bridge`, `134_join_all_futures`, and `140_yield_now_suspend` no longer fail on `@sla__println`.
  - [x] Completed active implementation slice: async `block_on` over ready futures now passes direct SAB no-fallback and SA-text parity through shared pointer-backed future ABI plus fallible `.sai` extern preservation. `tests/test_unit_async_block_on_direct.sla` covers a `future<i32>` parameter, Box/Pin ready polling, and std time sleep through `sa_time_sleep_ns -> i32!`; rosetta `09_async_await` now passes direct SAB no-fallback and SA-text parity.
  - [x] Completed active implementation slice: async `while let Some(task) = queue.pop()` over `Vec<future<T>>` now passes direct SAB no-fallback through shared while-let pattern planning, shared Option/Result/Vec generic helpers, direct `vec(...)` construction, and direct `Vec::pop()` lowering through `sa_vec_try_pop`. `tests/test_unit_async_while_let_future_queue_direct.sla` and rosetta `136_executor_task_queue` are the focused guards.
  - [x] Completed active implementation slice: `future::pending::<T>()` now passes direct SAB no-fallback and SA-text parity for task polling through shared `FutureRuntimeCallPlan` classification and `FUTURE_PENDING_STATE_NEW`. `tests/test_unit_async_pending_task_runtime.sla` covers a pending `future<i32>` wrapped in a task that remains not ready after polling.
  - [x] Completed active implementation slice: `task::state(task)` now passes direct SAB no-fallback and SA-text parity for task runtime state inspection through shared `TaskRuntimeCallPlan` classification and `TASK_STATE`. `tests/test_unit_async_task_state_runtime.sla` covers ready-task state changing after polling and pending-task state staying not-ready; committed in `942f5fc`.
  - [x] Completed active implementation slice: scalar `poll::ready`, `poll::pending`, `poll::is_ready`, `poll::is_pending`, and `poll::value` now pass direct SAB no-fallback and SA-text parity through shared `PollRuntimeCallPlan` classification and `POLL_*` macros. `tests/test_unit_async_poll_runtime.sla` covers ready-value extraction and pending-state inspection; committed in `2f3e402`.
  - [x] Completed active implementation slice: fixed-array `executor::new`, `executor::poll_one`, and `executor::poll_ready_count` now pass direct SAB no-fallback and SA-text parity for `[Task<T>; N]` task buffers through shared `ExecutorRuntimeCallPlan` classification. `tests/test_unit_async_executor_runtime.sla` covers ready-count, single-index polling, and pending-count behavior. Vec-backed buffers are covered by the later dynamic executor slice; committed in `538194e`.
  - [x] Completed active implementation slice: dynamic `Vec<Task<T>>` executor task buffers now pass direct SAB no-fallback and SA-text parity through shared `ExecutorTaskBufferPlan` classification. `tests/test_unit_async_executor_vec_runtime.sla` covers ready-count, single-index polling, and pending-count behavior over Vec-backed task buffers. This does not claim full async state-machine scheduling; committed in `59eb7f4`.
  - [x] Completed active implementation slice: statically pending `.await` inside `async fn` now propagates the pending future state through shared `AwaitPlan.pending_return_if_async` instead of forcing ready-state extraction. `tests/test_unit_async_pending_await_state_machine.sla` covers `future::pending::<i32>().await` wrapped in `task::new`, with the task remaining not ready after one poll. This does not claim full async state-machine scheduling/resumption; included in the completed slice commit.
  - [x] Completed active implementation slice: ready/pending `future<T>` parameter `.await` inside `async fn -> T` now branches through shared `AwaitPlan.ready_pending_state_return_if_async`; pending/empty states are returned and ready states continue through `FUTURE_READY_STATE_INTO_INNER`. `tests/test_unit_async_param_await_state_machine.sla` covers ready and pending future parameters. This does not claim arbitrary future-state continuation resumption; included in the completed slice commit.
  - [x] Completed active implementation slice: statically ready composite-future `.await` now branches through shared `AwaitPlan.poll_once_if_statically_ready`; both emitters use `FUTURE_POLL` plus `POLL_VALUE` rather than treating `future::join2` state as a ready-future layout. `tests/test_unit_async_join2_await_state_machine.sla` covers inline `future::join2(future::ready(...), future::ready(...)).await`. Local future-readiness dataflow and pending continuation/resumption remain broader hardening; included in the completed slice commit.
  - [x] Completed active implementation slice: local future-readiness `.await` dataflow now feeds shared `AwaitPlan.poll_once_if_statically_ready`; both emitters track `FutureReadiness` for future state temporaries and local bindings. `tests/test_unit_async_join2_local_await_state_machine.sla` covers `let left = future::ready(...); let right = future::ready(...); future::join2(left, right).await`. Pending continuation/resumption remains broader hardening; included in the completed slice commit.
  - [x] Completed active implementation slice: local pending composite-future `.await` propagation now feeds shared `AwaitPlan.pending_return_if_async` from the same readiness dataflow. `tests/test_unit_async_join2_local_pending_await_state_machine.sla` covers local ready+pending `future::join2(left, right).await` inside `async fn`, with the returned task remaining pending after one poll. Full continuation/resumption remains broader hardening; included in the completed slice commit.
  - [x] Completed active implementation slice: prebound-local `future::defer_ready(...).await` continuation/resumption now passes direct SAB no-fallback and SA-text parity through shared `AsyncSingleAwaitContinuationPlan` classification. `tests/test_unit_async_defer_ready_local_await_state_machine.sla` covers `let fut = future::defer_ready(...); let value = fut.await; return value + const;` inside `async fn`, with first poll pending and second poll ready with result `42`. This remains a narrow single-await shape and does not claim arbitrary multi-await/control-flow/captured-local async state-machine resumption.
  - [x] Completed active implementation slice: one scalar pre-await captured addend now survives the narrow `future::defer_ready(...).await` continuation/resumption state across the pending poll. `AsyncSingleAwaitContinuationPlan` carries `captured_addend_name`/`captured_addend_expr`, both emitters store it in the async state at offset 16, and `tests/test_unit_async_defer_ready_capture_state_machine.sla` covers direct-call and prebound-local variants such as `let bump = 2; let value = future::defer_ready(40).await; return value + bump;`. This remains a narrow single-await shape and does not claim arbitrary multi-await/control-flow async state-machine resumption.
  - [x] Completed active implementation slice: one scalar pre-await captured addend plus one immediate addend now survive the narrow `future::defer_ready(...).await` continuation/resumption expression. `AsyncSingleAwaitContinuationPlan` classifies restricted `+` trees with exactly one awaited binding, at most one captured scalar addend, and at most one integer literal immediate; both emitters compute the captured sum plus immediate in the poll helper. `tests/test_unit_async_defer_ready_capture_immediate_state_machine.sla` covers direct-call and prebound-local variants such as `let bump = 1; let value = future::defer_ready(40).await; return value + bump + 1;`. This remains a narrow single-await shape and does not claim arbitrary multi-await/control-flow async state-machine resumption.
  - [x] Completed active Phase 8 slice: post-await binding plus return-time immediate addend now passes direct SAB no-fallback and SA-text parity for the narrow `future::defer_ready(...).await` continuation/resumption path. Target shapes include `let bump = 1; let value = future::defer_ready(40).await; let result = value + bump; return result + 1;` and the prebound-local equivalent. Initial local SA text and direct SAB probes both failed the first-poll pending assertion, proving the shape was not classified into the shared continuation plan. The fix stays in shared `AsyncSingleAwaitContinuationPlan`; SA text and direct SAB consume the same plan. Verified with focused lowering tests, local SA/SAB parity, local sweep 94/94, official `sa plugin install --dev .` plus `SA_PLUGIN_DEV=1 sa sla help`, host focused parity, host sweep 94/94, `/home/vscode/projects/sla_ecs/lib/parallel.sla`, and disasm guard. Owner phase: Phase 8. Progress: 100% for the tracked direct-call and prebound-local shapes.
  - [x] Completed active Phase 8A slice: scalar-linear continuation expression planning now passes direct SAB no-fallback and SA-text parity for narrow `future::defer_ready(...).await` single-await resumption. Target shapes include direct-call and prebound-local `return value - bump;` and `return (value + bump) * 2;`. Owner phase: Phase 8A. Architectural boundary: expression classification lives in shared `AsyncSingleAwaitContinuationPlan`/`AsyncContinuationScalarPlan` metadata in `src/lowering_rules.zig`; SA text and direct SAB consume the same scalar plan. Verified with focused lowering tests, local SA/SAB parity, local sweep 95/95, official host install/help, host focused parity, host sweep 95/95, `/home/vscode/projects/sla_ecs/lib/parallel.sla`, and disasm guard. Progress: 100% for the tracked scalar-linear direct-call and prebound-local shapes.
  - [x] Completed active Phase 8A slice: branchy continuation expression planning now passes direct SAB no-fallback and SA-text parity for narrow `future::defer_ready(...).await` single-await resumption. Target shapes include direct-call and prebound-local `return if value > 0 { value + bump } else { bump };`. Owner phase: Phase 8A. Architectural boundary: branch classification lives in shared `AsyncSingleAwaitContinuationPlan`/`AsyncContinuationBranchPlan` metadata in `src/lowering_rules.zig`; SA text and direct SAB consume the same branch/scalar plan. Verified with focused lowering tests, local SA/SAB parity, local sweep 96/96, official host install/help, host focused parity, host sweep 96/96, `/home/vscode/projects/sla_ecs/lib/parallel.sla`, and disasm guard. Progress: 100% for the tracked branchy direct-call and prebound-local shapes.
  - [x] Completed active Phase 8C slice: multiple scalar pre-await capture planning now passes direct SAB no-fallback and SA-text parity for narrow `future::defer_ready(...).await` single-await resumption. Target shapes include direct-call and prebound-local `let a = 1; let b = 2; let value = future::defer_ready(39).await; return value + a + b;`. Owner phase: Phase 8C. Architectural boundary: capture layout metadata lives in shared `AsyncSingleAwaitContinuationPlan` metadata in `src/lowering_rules.zig`, including offsets 16 and 24 and captured expressions; SA text and direct SAB allocate from `plan.asyncStateSize()`, store/load from `plan.captures[*].offset`, and consume the same scalar plan. Verified with focused lowering tests, local SA/SAB parity, local sweep 98/98, official host install/help, host focused parity, host sweep 98/98, `/home/vscode/projects/sla_ecs/lib/parallel.sla`, and disasm guard. Progress: 100% for the tracked direct-call and prebound-local shapes.
  - [x] Completed active Phase 8D slice: copy-struct pre-await capture planning now passes direct SAB no-fallback and SA-text parity for narrow `future::defer_ready(...).await` single-await resumption. Target shapes include direct-call and prebound-local `let bump = CaptureBump { amount: 3 }; let value = future::defer_ready(39).await; return value + bump.amount;`. Owner phase: Phase 8D. Architectural boundary: copy-struct capture storage and field-addend metadata live in shared `AsyncSingleAwaitContinuationPlan`/`AsyncContinuationScalarPlan`; SA text and direct SAB store/load the owned capture pointer from shared offsets and do not shallow-copy pointer-backed non-copy captures. Verified with focused lowering tests, local SA/SAB parity, local sweep 99/99, official host install/help, host focused parity, host sweep 99/99, `/home/vscode/projects/sla_ecs/lib/parallel.sla`, and disasm guard. Progress: 100% for the tracked direct-call and prebound-local shapes.
  - [x] Completed active Phase 8E implementation slice: two sequential defer-ready awaits for a narrow async state-machine continuation. Target shapes include direct-call and prebound-local functions equivalent to `let a = future::defer_ready(20).await; let b = future::defer_ready(22).await; return a + b;`. Owner phase: Phase 8E. Architectural boundary: shared `AsyncTwoAwaitContinuationPlan` lives in `src/lowering_rules.zig`, with state offsets 0 outer stage, 8 first inner state, 16 second inner state, and 24 saved first scalar; SA text and direct SAB consume that plan. Verified with focused lowering test, `zig fmt --check`, `zig build`, `zig build test` (72/72), local and host SA/SAB parity, local and host sweep 100/100, `/home/vscode/projects/sla_ecs/lib/parallel.sla`, and disasm guards. Progress: 100% for the tracked direct-call and prebound-local shapes.
  - [x] Completed active Phase 8F implementation slice: resume a pending composite `future::join2` after it later becomes ready, instead of only propagating pending states. Target shapes include direct-call and prebound-composite functions equivalent to `let pair = future::join2(future::defer_ready(20), future::ready(22)).await; return future::pair_left(pair) + future::pair_right(pair);`. Owner phase: Phase 8F. Architectural boundary: shared `AsyncJoin2AwaitContinuationPlan` defines state offsets 0 outer stage and 8 composite future state pointer, plus join2 vtable polling and pair-result scalar continuation; SA text and direct SAB consume the same plan. Verified with focused lowering test, `zig fmt --check`, `zig build`, `zig build test` (73/73), local and host SA/SAB parity, local and host sweep 101/101, `/home/vscode/projects/sla_ecs/lib/parallel.sla`, and disasm guards. Progress: 100% for the tracked direct-call and prebound-composite shapes.
  - [x] Completed active Phase 8B slice: two post-await binding continuation planning now passes direct SAB no-fallback and SA-text parity for narrow `future::defer_ready(...).await` single-await resumption. Target shapes include direct-call and prebound-local `let result = value + bump; let final = result + 1; return final;`. Owner phase: Phase 8B. Architectural boundary: post-await tail classification lives in shared `AsyncSingleAwaitContinuationPlan` metadata in `src/lowering_rules.zig`, composing simple `let` bindings into `AsyncContinuationScalarPlan`; SA text and direct SAB consume the same scalar plan. Verified with focused lowering tests, local SA/SAB parity, local sweep 97/97, official host install/help, host focused parity, host sweep 97/97, `/home/vscode/projects/sla_ecs/lib/parallel.sla`, and disasm guard. Progress: 100% for the tracked direct-call and prebound-local shapes.
  - [x] Completed active implementation slice: post-await binding `future::defer_ready(...).await` continuation/resumption now passes direct SAB no-fallback and SA-text parity through shared `AsyncSingleAwaitContinuationPlan` classification. `tests/test_unit_async_defer_ready_post_bind_state_machine.sla` covers direct-call and prebound-local shapes such as `let value = fut.await; let result = value + const; return result;`, with first poll pending and second poll ready with result `42`. This remains a narrow single-await shape and does not claim arbitrary multi-await/control-flow/captured-local async state-machine resumption.
  - [x] Completed active implementation slice: narrow `future::defer_ready(...).await` continuation/resumption now passes direct SAB no-fallback and SA-text parity through shared `AsyncSingleAwaitContinuationPlan` classification. `tests/test_unit_async_defer_ready_await_state_machine.sla` covers `let value = future::defer_ready(...).await; return value + 1;` inside `async fn`, with first poll pending and second poll ready with result `42`. This does not claim arbitrary multi-await/control-flow/captured-local async state-machine resumption.
  - [x] Completed active implementation slice: `future::defer_ready(value)` now passes direct SAB no-fallback and SA-text parity for task-runtime polling. Shared `FutureRuntimeCallPlan` classification and type checking model it as `future<T>` with unknown static readiness; both emitters allocate compiler-owned stage/value state behind `SLA_DEFER_READY_FUTURE_VT`. `tests/test_unit_async_defer_ready_task_runtime.sla` covers first-pending and second-ready task polls plus result extraction. This is a prerequisite runtime surface for pending-then-ready continuation fixtures; full continuation/resumption remains broader hardening beyond this fixture.
  - [x] Completed active implementation slice: `future::join2`, `future::pair_left`, and `future::pair_right` now pass direct SAB no-fallback and SA-text parity for scalar future pairs through shared `FutureRuntimeCallPlan` classification, `FuturePair<T, U>` type checking, and vtable-aware `task::new` wrapping. `tests/test_unit_async_join2_runtime.sla` covers ready+ready pair extraction and ready+pending remaining pending. This does not claim full async state-machine scheduling; committed in `5c40886`.
  - [x] Completed active implementation slice: `future::select2`, `future::either_side`, `future::either_left`, and `future::either_right` now pass direct SAB no-fallback and SA-text parity for scalar future selection through shared `FutureRuntimeCallPlan` classification, `FutureEither<T, U>` type checking, and vtable-aware `task::new` wrapping. `tests/test_unit_async_select2_runtime.sla` covers left-ready/right-pending, left-pending/right-ready, and both-pending behavior. This does not claim full async state-machine scheduling; committed in `e68eda5`.
  - [x] Completed this session: join2 defer-ready later-ready `.await` continuation/resumption, two-sequential-await defer-ready `.await` continuation/resumption, copy-struct-capture defer-ready `.await` continuation/resumption, multi-capture defer-ready `.await` continuation/resumption, two-post-binding defer-ready `.await` continuation/resumption, branchy defer-ready `.await` continuation/resumption, scalar-linear defer-ready `.await` continuation/resumption, post-binding-return-addend defer-ready `.await` continuation/resumption, `enum_match` (shared enum tag/payload layout in `lowering_rules.zig` + SAB `genEnumLiteral`/`genMatch`), `spaceship_cmp` (shared spaceship/Ordering helpers + SAB `genSpaceship`), `for_in_protocol` (SAB `genForOverProtocol` iter_len/iter_at counted loop; made `methodForType` pub), `generic_for_in_protocol`, `derive_semantics`, `vec_index_assign`, nested Vec field/index assignment, ready-future async/await, ready Future/task runtime polling, pointer-backed struct update, formatted `println`, async `block_on` ready-future lowering, async while-let future queue lowering, async pending-future task polling, pending `.await` async propagation, future-parameter `.await` ready/pending propagation, statically ready composite-future `.await` poll-once lowering, local future-readiness `.await` dataflow, local pending composite-future `.await` propagation, direct/prebound-local/post-binding/captured-addend/captured-plus-immediate defer-ready `.await` continuation/resumption, async task-state runtime inspection, async Poll runtime inspection, fixed-array and Vec-backed executor runtime inspection, `future::join2` task-runtime polling, `future::select2` task-runtime polling, and `future::defer_ready` task-runtime polling. Focused fixtures are verified with dev-mode SAB no-fallback and SA-text parity where applicable. Sweep is now 101/101 after adding `tests/test_unit_async_join2_defer_ready_await_state_machine.sla`.
- [ ] **Completed Slice Evidence Ledger: Smart Pointer / Borrow / RefCell**
  - [x] Shared/static void-call planning builds; the earlier LLVM named-void-call verifier symptom is no longer current.
  - [x] Borrow-temp focused filters passed individually: borrowed field call argument release, loop iteration temp release, borrowed rc refcell field release, arc refcell deref-owner release, nested rc/box release, nested box/rc release, and Vec field smart-pointer index borrow owner release.
  - [x] Full local direct SAB no-fallback passed for `tests/test_unit_borrow_temp_release_order.sla` with 25/25 tests.
  - [x] SA-text parity passed for `tests/test_unit_borrow_temp_release_order.sla` with 25/25 tests.
  - [x] Local and host direct SAB no-fallback passed for `tests/test_unit_smart_pointer_struct_field_cleanup.sla` with 3/3 tests.
  - [x] Local and host direct SAB no-fallback passed for `tests/test_unit_refcell_struct_payload.sla` with 2/2 tests.
  - [x] `Rc::clone(&rc1)` / `Arc::clone(&arc1)` now use std-surface associated metadata plus shared receiver materialization; the SAB-only `genRcArcCloneCall` branch was removed.
  - [x] Type checker clone return type now unwraps borrowed receivers (`unwrappedReceiverType(recv_ty)`), so `Rc::clone(&rc)` yields `Rc<T>` rather than `&Rc<T>`.
  - [x] Host `/home/vscode/projects/sla_ecs/lib/parallel.sla` direct SAB no-fallback passes and no tracked disasm contains an illegal visible `@func(arg)` call target.
  - [ ] Remaining Phase 1 hardening is architectural, not a current signal 11 blocker: extract a fuller shared address/projection plan and RefCell lifecycle plan so future borrow surfaces do not grow SAB-local state.
- [ ] **Completed Slice Evidence Ledger: Imported SA Macro Expansion / Borrowed Args**
  - [x] `src/plugin.zig` records imported SA macro source paths, expression-output arity, direct `&%param` use, and nested `EXPAND` borrowed-parameter propagation.
  - [x] `src/lowering_rules.zig` owns `ImportedMacroCallPlan`, expression-output detection, macro-parameter index mapping, addressable-slot requirements, and borrow-expression result release rules shared by call emitters.
  - [x] `src/codegen.zig` now emits imported macro calls through the shared plan instead of local arity/output logic.
  - [x] `src/sab_codegen.zig` consumes the same plan, materializes address-taken imported macro args into stack slots when needed, loads arbitrary imported helper macro fragments through the generic fragment path, and keeps call targets separate from argument text.
  - [x] Local no-fallback SAB passed for `tests/test_unit_pkgjson_codegen.sla`, `tests/test_unit_user_macro_direct.sla`, `tests/test_unit_option_direct.sla`, `tests/test_unit_result_direct.sla`, and the five imported-macro direct fixtures. SA-text parity passed for `pkgjson` and the imported-macro direct fixtures.
  - [x] Official host gates passed: `sa plugin install --dev .`, `SA_PLUGIN_DEV=1 sa sla help`, host no-fallback `tests/test_unit_pkgjson_codegen.sla`, host SA-text `tests/test_unit_pkgjson_codegen.sla`, host no-fallback `tests/test_unit_refcell_struct_payload.sla`, host no-fallback `tests/test_unit_trait_static_dispatch.sla`, and host no-fallback `/home/vscode/projects/sla_ecs/lib/parallel.sla`.
  - [x] Full local no-fallback `tests/test_unit_*.sla` sweep is now 58/69 passing. Feature: imported SA macro expression-output/addressable args 100%; Y/shared-lowering 66% -> 72%; direct SAB fallback-removal 85% -> 88%; committed as `b1c09b6`.
- [ ] **Completed Slice Evidence Ledger: Assignment Move Cleanup**
  - [x] `src/lowering_rules.zig` now owns assignment/stored-value move classification with `rootIdentifier`, `storedValueMovesIdentifier`, and `assignmentMovesIdentifier`.
  - [x] `src/codegen.zig` reuses the shared stored-value move rule for SA-text release decisions.
  - [x] `src/sab_codegen.zig` consumes the shared assignment-move rule after field, index, and identifier assignments, marking moved RHS local registers consumed so loop/body cleanup does not release them again.
  - [x] Local focused gates passed for direct SAB no-fallback and SA-text parity: `tests/test_unit_assign_move_cleanup.sla` and `tests/test_unit_field_assign_move_cleanup.sla`.
  - [x] Official host gates passed: `sa plugin install --dev .`, `SA_PLUGIN_DEV=1 sa sla help`, host no-fallback focused move-cleanup fixtures, host SA-text parity for both move-cleanup fixtures, host no-fallback `tests/test_unit_pkgjson_codegen.sla`, `tests/test_unit_refcell_struct_payload.sla`, `tests/test_unit_trait_static_dispatch.sla`, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`.
  - [x] Full local no-fallback `tests/test_unit_*.sla` sweep is now 59/69 passing. Feature: assignment moved-local cleanup 100%; Y/shared-lowering 72% -> 73%; direct SAB fallback-removal 88% -> 89%; committed in this slice.
- [ ] **Completed Slice Evidence Ledger: Var Loop-Control / Scoped Var Cleanup**
  - [x] `src/lowering_rules.zig` now owns shared loop-control planning and block/statement termination checks, including current-loop-only break/continue detection that ignores nested loops.
  - [x] `src/codegen.zig` delegates loop-control and termination decisions to the shared rules instead of keeping an SA-text-only AST scan.
  - [x] `src/sab_codegen.zig` consumes the shared loop-control plan, emits direct SAB break/continue jumps with typechecker cleanup lists, supports explicit `release`, scopes block locals, uses unique stack slots for shadowed `var` declarations, and keeps ordinary open-scope cleanup from releasing imported macro helper stack slots.
  - [x] Local gates passed: `zig fmt`, `zig build --summary all`, `zig build test --summary all` (59/59), focused direct SAB no-fallback and SA-text parity for `tests/test_unit_var_comprehensive.sla`, direct SAB no-fallback `tests/test_unit_pkgjson_codegen.sla`, assignment cleanup regressions, and full local no-fallback sweep at 60/69.
  - [x] Official host gates passed: `sa plugin install --dev .`, `SA_PLUGIN_DEV=1 sa sla help`, host direct SAB no-fallback and host SA-text parity for `tests/test_unit_var_comprehensive.sla`, host direct SAB no-fallback `tests/test_unit_pkgjson_codegen.sla`, and host direct SAB no-fallback `/home/vscode/projects/sla_ecs/lib/parallel.sla`.
  - [x] Feature: var loop-control/scoped-slot cleanup 100%; Y/shared-lowering 73% -> 75%; direct SAB fallback-removal 89% -> 90%; full local no-fallback unit-file pass rate 60/69; committed in this slice.
- [ ] **Completed Slice Evidence Ledger: Rc Dyn Trait Coercion / Dispatch**
  - [x] `src/lowering_rules.zig` now owns shared dyn coercion classification with `DynCoercionPlan`, plus dyn-dispatch receiver planning for direct dyn receivers versus `Rc<dyn>` receivers that must materialize the inner fat pointer.
  - [x] `src/codegen.zig` consumes the shared dyn coercion and receiver plans for SA-text instead of directly branching on `dyn_box_coercions` / `dyn_rc_coercions` and Rc receiver shape at call sites.
  - [x] `src/sab_codegen.zig` consumes the same shared plans, lowers `Rc::new(T) -> Rc<dyn Trait>` by constructing a `Dyn` fat pointer before the metadata-driven `RC_NEW`, lowers `Box::new(T) -> Box<dyn Trait>` into a direct dyn fat pointer, and avoids re-entering dyn coercion when lowering the underlying Box expression.
  - [x] Dev-mode focused gates passed: `sa plugin install --dev .`, `SA_PLUGIN_DEV=1 sa sla help`, direct SAB no-fallback and SA-text parity for `tests/test_unit_rc_dyn_trait.sla` with 3/3 tests.
  - [x] Local build/test and regression gates passed: `zig fmt`, `zig build --summary all`, `zig build test --summary all` (60/60), dev-mode no-fallback guards for `var_comprehensive`, assignment cleanup, field assignment cleanup, `pkgjson`, RefCell struct payload, trait static dispatch, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`.
  - [x] Full dev-mode no-fallback `tests/test_unit_*.sla` sweep is now 61/69 passing. Disasm guard for latest `rc_dyn_trait` and `parallel.sla` artifacts returned no visible illegal call target matching `call .*@[^" ]+\(`.
  - [x] Feature: Rc dyn trait coercion/dispatch 100%; Y/shared-lowering 75% -> 77%; direct SAB fallback-removal 90% -> 91%; full dev-mode no-fallback unit-file pass rate 61/69; included in the completed slice commit.
- [ ] **Completed Slice Evidence Ledger: Set Collection Std Metadata**
  - [x] `sla_std/std_surface.sla_meta` now describes `HashSet::new`, `BTreeSet::new`, `len(HashSet)`, `len(BTreeSet)`, `HashSet.insert`, `HashSet.contains`, `BTreeSet.insert`, and `BTreeSet.contains`, including `sa_std` helper dependencies for the underlying HashMap/BTreeMap implementation functions.
  - [x] `src/sab_codegen.zig` consumes those rules through the generic std-surface associated/function/method path instead of adding Set-specific branches.
  - [x] Direct SAB string literals used as set keys are stack-allocated `Slice` values and now mark their slice register as non-owning so generic cleanup does not emit an illegal explicit release for stack allocation.
  - [x] Dev-mode focused gates passed: direct SAB no-fallback and SA-text parity for `tests/test_unit_sets.sla` with 2/2 tests.
  - [x] Local build/test and regression gates passed: `zig fmt --check src/sab_codegen.zig`, `zig build --summary all`, `zig build test --summary all` (60/60), dev-mode no-fallback guards for `rc_dyn_trait`, `var_comprehensive`, assignment cleanup, field assignment cleanup, `pkgjson`, RefCell struct payload, trait static dispatch, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`.
  - [x] Full dev-mode no-fallback `tests/test_unit_*.sla` sweep is now 62/69 passing. Disasm guard for latest `test_unit_sets` and `parallel.sla` artifacts returned no visible illegal call target matching `call .*@[^" ]+\(`.
  - [x] Feature: Set collection std metadata 100%; Y/shared-lowering 77% -> 79%; direct SAB fallback-removal 91% -> 92%; full dev-mode no-fallback unit-file pass rate 62/69; included in the completed slice commit.
- [ ] **Current Remaining Failure Ownership Matrix From The 101/101 Sweep**
  - [x] `tests/test_unit_assign_move_cleanup.sla`: Phase 3 assignment moved-local cleanup is complete through the shared assignment-move rule.
  - [x] `tests/test_unit_async_await.sla`: Phase 8 ready-future subset complete; async function pointer ABI, ready-future wrapping, and `.await` ready-state extraction are expressed through shared async plans and pass direct SAB no-fallback plus SA-text parity. Full async state-machine/Future/task executor support remains a broader hardening task beyond this fixture.
  - [x] `tests/test_unit_async_task_runtime.sla`: Phase 8 ready Future/task runtime surface complete for ready-state tasks; `future::ready`, `task::new`, `task::poll`, `task::is_ready`, and `task::result` pass direct SAB no-fallback and SA-text parity. Full state-machine executor support remains a broader hardening task beyond this fixture.
  - [x] `tests/test_unit_async_pending_task_runtime.sla`: Phase 8 pending Future/task polling surface complete for `future::pending::<i32>()`, `task::new`, `task::poll`, and `task::is_ready`; pending tasks remain not ready after polling.
  - [x] `tests/test_unit_async_pending_await_state_machine.sla`: Phase 8 statically pending await propagation complete for `future::pending::<i32>().await` inside `async fn`; shared `AwaitPlan.pending_return_if_async` feeds both emitters, and the returned task remains pending after one poll. Full async scheduling/resumption remains a broader hardening task beyond this fixture.
  - [x] `tests/test_unit_async_param_await_state_machine.sla`: Phase 8 ready/pending future-parameter await propagation complete for `future<T>` parameters in compatible `async fn -> T`; shared `AwaitPlan.ready_pending_state_return_if_async` feeds both emitters. Arbitrary future-state continuation resumption remains broader hardening beyond this fixture.
  - [x] `tests/test_unit_async_join2_await_state_machine.sla`: Phase 8 statically ready composite-future await poll-once complete for inline `future::join2(future::ready(...), future::ready(...)).await`; shared `AwaitPlan.poll_once_if_statically_ready` feeds both emitters. Local readiness dataflow and pending continuation/resumption remain broader hardening beyond this fixture.
  - [x] `tests/test_unit_async_join2_local_await_state_machine.sla`: Phase 8 local future-readiness await poll-once complete for local ready future bindings joined and awaited inside `async fn`; shared `FutureReadiness` and `AwaitPlan.poll_once_if_statically_ready` feed both emitters. Pending continuation/resumption remains broader hardening beyond this fixture.
  - [x] `tests/test_unit_async_join2_local_pending_await_state_machine.sla`: Phase 8 local pending composite-future await propagation complete for local ready+pending future bindings joined and awaited inside `async fn`; shared `FutureReadiness` and `AwaitPlan.pending_return_if_async` feed both emitters. Full continuation/resumption remains broader hardening beyond this fixture.
  - [x] `tests/test_unit_async_defer_ready_local_await_state_machine.sla`: Phase 8 prebound-local defer-ready await continuation/resumption complete for `let fut = future::defer_ready(...); let value = fut.await; return value (+ const);`; shared `AsyncSingleAwaitContinuationPlan` feeds both emitters. Arbitrary multi-await/control-flow/captured-local state machines remain broader hardening beyond this fixture.
  - [x] `tests/test_unit_async_defer_ready_capture_state_machine.sla`: Phase 8 captured-local addend defer-ready await continuation/resumption complete for direct-call and prebound-local single-await shapes such as `let bump = 2; let value = future::defer_ready(...).await; return value + bump;`; shared `AsyncSingleAwaitContinuationPlan` carries the captured addend and both emitters store/load it from the async state. Arbitrary multi-await/control-flow/captured-local state machines remain broader hardening beyond this one-scalar fixture.
  - [x] `tests/test_unit_async_defer_ready_capture_immediate_state_machine.sla`: Phase 8 captured-local plus immediate addend defer-ready await continuation/resumption complete for direct-call and prebound-local single-await shapes such as `let bump = 1; let value = future::defer_ready(...).await; return value + bump + 1;`; shared `AsyncSingleAwaitContinuationPlan` classifies one awaited binding, one captured scalar addend, and one integer literal immediate, and both emitters compute the resumed sum from that shared plan. Arbitrary multi-await/control-flow/captured-local state machines remain broader hardening beyond this one-scalar-plus-immediate fixture.
  - [x] `tests/test_unit_async_defer_ready_post_bind_state_machine.sla`: Phase 8 post-await binding defer-ready await continuation/resumption complete for direct-call and prebound-local single-await shapes such as `let value = fut.await; let result = value + const; return result;`; shared `AsyncSingleAwaitContinuationPlan` feeds both emitters. Arbitrary multi-await/control-flow/captured-local state machines remain broader hardening beyond this fixture.
  - [x] `tests/test_unit_async_defer_ready_await_state_machine.sla`: Phase 8 narrow defer-ready await continuation/resumption complete for `let value = future::defer_ready(...).await; return value (+ const);`; shared `AsyncSingleAwaitContinuationPlan` feeds both emitters. Arbitrary multi-await/control-flow/captured-local state machines remain broader hardening beyond this fixture.
  - [x] `tests/test_unit_async_defer_ready_task_runtime.sla`: Phase 8 defer-ready task-runtime primitive complete for `future::defer_ready(value)`, `task::new`, `task::poll`, `task::is_ready`, and `task::result`; the first poll is pending and the second poll is ready with the stored value. Full async continuation/resumption after arbitrary pending future state kinds later become ready remains broader hardening beyond this fixture.
  - [x] `tests/test_unit_async_task_state_runtime.sla`: Phase 8 task runtime state-inspection surface complete for `task::state(task)` over ready and pending tasks; shared `TaskRuntimeCallPlan` feeds both SA-text and direct SAB.
  - [x] `tests/test_unit_async_poll_runtime.sla`: Phase 8 scalar Poll runtime surface complete for `poll::ready`, `poll::pending`, `poll::is_ready`, `poll::is_pending`, and `poll::value`; shared `PollRuntimeCallPlan` feeds both SA-text and direct SAB.
  - [x] `tests/test_unit_async_executor_runtime.sla`: Phase 8 fixed-array executor runtime surface complete for `executor::new`, `executor::poll_one`, and `executor::poll_ready_count` over contiguous `[Task<T>; N]` buffers; shared `ExecutorRuntimeCallPlan` feeds both emitters, with SA-text unrolling the fixed-count ready-count path.
  - [x] `tests/test_unit_async_executor_vec_runtime.sla`: Phase 8 dynamic executor task-buffer surface complete for `executor::new` over `Vec<Task<T>>` buffers plus `executor::poll_one` and runtime `executor::poll_ready_count`; shared `ExecutorTaskBufferPlan` feeds both emitters with `VEC_AS_PTR`/`VEC_LEN` pointer-length lowering.
  - [x] `tests/test_unit_async_join2_runtime.sla`: Phase 8 narrow `future::join2` task-runtime surface complete for scalar ready/pending futures; shared `FutureRuntimeCallPlan` feeds both emitters, and vtable-aware task wrapping dispatches join states through `FUTURE_JOIN2_STATE_POLL`.
  - [x] `tests/test_unit_async_select2_runtime.sla`: Phase 8 narrow `future::select2` task-runtime surface complete for scalar ready/pending futures; shared `FutureRuntimeCallPlan` feeds both emitters, and vtable-aware task wrapping dispatches select states through `FUTURE_SELECT2_STATE_POLL` plus `FutureEither` accessors.
  - [x] `tests/test_unit_async_block_on_direct.sla`: Phase 8 ready-future `block_on` path complete for future-typed parameters, Box/Pin ready polling, and fallible std-time extern preservation; rosetta `09_async_await` is the host/demo guard.
  - [x] `tests/test_unit_async_while_let_future_queue_direct.sla`: Phase 8 ready-future queue loop complete for `while let Some(task) = queue.pop()` over `Vec<future<i32>>`; rosetta `136_executor_task_queue` is the host/demo guard.
  - [x] `tests/test_unit_derive_semantics.sla`: Phase 5 complete; tracked derive `hash`/`debug` support passes direct SAB no-fallback and SA-text parity. `debug()` uses structured direct SAB formatting calls plus `FORMAT_PUSH_BYTES`, avoiding the nested FORMAT/STRFMT macro hygiene path.
  - [x] `tests/test_unit_enum_match.sla`: Phase 5 complete this session; shared enum tag/payload layout in `lowering_rules.zig` + SAB `genEnumLiteral`/`genMatch`. Verified SAB no-fallback + SA-text parity (clean cache).
  - [x] `tests/test_unit_for_in_protocol.sla`: Phase 6 complete this session; SAB `genForOverProtocol` (iter_len/iter_at counted loop), `methodForType` made pub. Verified SAB no-fallback + SA-text parity (clean cache).
  - [x] `tests/test_unit_generic_for_in_protocol.sla`: Phase 6 tracked fixture complete; passes direct SAB no-fallback and SA-text parity through the protocol loop/std-deps/signature-order path. The broader std-macro fragment naming gap remains a separate `sci` boundary task, not the current fixture blocker.
  - [x] `tests/test_unit_field_compare_and_nested_len.sla`: Phase 4/5 complete; nested Vec field/index assignment passes direct SAB no-fallback and SA-text parity through the std-surface `VEC_SET_TYPED` path with `elem_ty` emitted as a literal type token.
  - [x] `tests/test_unit_rc_dyn_trait.sla`: Phase 3 + Phase 4 dyn Rc/Box trait-object coercion and dispatch is complete through shared dyn coercion/receiver plans plus std surface metadata consumption.
  - [x] `tests/test_unit_sets.sla`: Phase 4 set collection operations are expressed through std surface metadata and `sa_std` macros, not Set-specific Zig code.
  - [x] `tests/test_unit_spaceship_cmp.sla`: Phase 5 complete this session; shared spaceship/Ordering helpers in `lowering_rules.zig` + SAB `genSpaceship` (numeric and same-struct field-wise). Verified SAB no-fallback + SA-text parity (clean cache).
  - [x] `tests/test_unit_struct_update.sla` and `tests/test_unit_struct_update_pointer_backed.sla`: Phase 5 struct update is complete for scalar and pointer-backed tracked fields through shared `planStructLiteralField` plus `StructLiteralFieldTransfer`; pointer-backed `Vec<i32>` update fields move without the previous direct SAB narrowing.
  - [x] `tests/test_unit_vec_index_assign.sla`: Phase 4 complete; scalar and struct Vec slot assignment pass direct SAB no-fallback and SA-text parity through std-surface typed index assignment metadata.
  - [x] `tests/test_unit_var_comprehensive.sla`: Phase 3 + Phase 5 scoped var/loop-control/release subset is complete through shared loop-control rules plus direct SAB scoped-slot implementation.
- [ ] **Phase 1: Borrow, Precedence, And Smart-Pointer Convergence**
  - [x] Focused smart-pointer constructor/get/deref/clone and RefCell struct-payload regressions are verified through local and host direct SAB no-fallback gates.
  - [x] The reported `parallel.sla` illegal visible call-target repro is verified green through host direct SAB no-fallback.
  - [x] Tuple/struct field-chain borrow hardening slice: `pair.0.x` now requires the lexer to stop an integer literal before a non-digit dot, and `&pair.0.x` now has SA-text/direct-SAB parity for the focused tuple slot -> pointer-backed struct -> field address chain. Verified through local/host tuple SA/SAB parity and full direct-SAB no-fallback sweeps at 101/101.
  - [x] Plain nested struct scalar field-chain borrow slice: `tests/test_unit_borrow_direct.sla` now covers `&outer.inner.value`/`&item.inner.value`, proving the focused non-tuple field-chain borrow path before extracting a broader shared projection plan. SA-text and strict direct SAB pass locally and under dev-mode host gates.
  - [ ] Extract a reusable shared address/projection plan for `&field`, `&index`, `&*smart`, `&**chain`, and postfix-under-prefix precedence. The ordinary classifier sub-slice is complete for `&identifier`, ordinary `&*borrow_or_pointer`, `&field`, and `&index`, including shared array/Slice borrow/pointer target unwrapping for ordinary `&values[index]`; the smart-pointer address action decision is shared for the existing SAB path; direct SAB user-macro smart-pointer address lowering now reuses it for focused `&*boxed`/`&**nested_box` forms; imported macro addressable arg action selection is shared for pass-value/reuse-existing/materialize-stack-slot decisions; both emitters now have focused imported-macro address-expression materialization for borrowed `field`/`index`/ordinary `deref` args; borrowed-address temp tracking now has a shared `planBorrowAddressTemps()` decision; and borrowed-address temp transfer plus SA-text let-binding binding-transfer paths now consume shared `planBorrowAddressTempTransfer()`. Fuller chained-deref classification, pointer-backed aggregate alias semantics inside imported macros, and RefCell lifecycle still need the broader shared projection/lifecycle plan.
  - [x] Direct SAB user-macro smart-pointer address sub-slice complete: macro-body `&*boxed` and focused `&**nested_box` reuse shared smart-pointer action/get plans. This narrows the macro-address gap without claiming imported SA macro address lowering or the broader shared macro expansion contract.
  - [x] Shared smart-pointer address action sub-slice complete: the existing direct SAB decision for dyn Box identity, `as_ptr` slot, pointer-backed value take, nested smart-pointer value slot, and `get` fallback moved into `lowering_rules.zig`; emission/release behavior remains in `sab_codegen.zig`.
  - [x] Shared ordinary index-address target unwrapping sub-slice complete: the array/slice borrow/pointer target peel for `&values[index]` moved into `lowering_rules.zig`, and both emitters consume it.
  - [x] Direct SAB user-macro address-of classifier sub-slice complete: `genMacroAddressOf` consumes the shared ordinary `planAddressOf` classifier while preserving macro-specific hygiene and emission behavior. Broader shared macro expansion convergence remains a separate Phase 2 issue.
  - [x] Ordinary address-of classifier sub-slice complete: `AddressOfShape`/`AddressOfPlan` in `src/lowering_rules.zig` is consumed by SA-text and direct SAB for the focused ordinary forms, with smart-pointer-like deref sources explicitly falling back to the existing emitter-owned smart-pointer handling.
  - [ ] Move RefCell borrow/borrow_mut lifecycle into a fuller shared borrow-lifecycle plan, including panic-on-conflict and release across scopes, branches, loops, call args, and early returns. The focused borrow-address temp planning/transfer/binding-transfer, borrow handle registration, companion restore, runtime guard, borrow-result materialization, companion-store cleanup, handle transfer, and early-return branch-state sub-slices are complete through shared plans, and focused fixtures are green; this task stays open for deeper reuse across the full lifecycle surface.
  - [x] Shared result-slot RefCell load-action sub-slice complete: the decision to restore a RefCell borrow handle from companion-slot metadata, release an empty preallocated companion slot, or continue normal transfer moved into `lowering_rules.zig`; concrete loads/releases and handle reconstruction remain emitter-local.
  - [x] Shared result-slot RefCell store-action sub-slice complete: the decision to store an active RefCell borrow handle through the result-slot companion path moved into `lowering_rules.zig`; concrete slot allocation, stores/loads, temporary release, and handle cleanup remain emitter-local.
  - [x] Shared RefCell runtime import-scan sub-slice complete: the decision that RefCell constructor/receiver/typed expressions require the RefCell runtime surface moved into `lowering_rules.zig`; concrete imports, macro emission, borrow-handle state, and cleanup ordering remain emitter-local.
  - [ ] Phase 1 closure target: no new SAB-local borrow/smart-pointer semantic branches; borrow/precedence feature track stays 100% for the focused user blockers while hardening continues as needed.
- [ ] **Phase 2: Macro Expansion Completion Through Shared Rules**
  - [x] Phase 2A imported SA expression-output macro expansion is complete for the tracked fixtures. Next priority is Phase 3 call/result/assignment stabilization unless a new macro-shaped failure appears.
  - [x] Phase 2A classification command run: `SLA_SAB_TRACE_UNSUPPORTED=1 SLA_SAB_NO_FALLBACK=1 timeout 180s ./zig-out/bin/sla-local-cli sla test tests/test_unit_pkgjson_codegen.sla --test-backend sab --jobs 1 --trace-panic`.
  - [x] Phase 2A observed failure: `[sab-direct] stmt let_stmt failed: UnsupportedSabDirectFeature`, then `func package_json_pick_condition block failed`, then direct lowering aborted without fallback.
  - [x] Phase 2A source evidence: `tests/test_unit_pkgjson_codegen.sla` imports `tests/import_fixtures/pkgjson/json_helpers.sa` and `fs_helpers.sa`; those files define imported SA `[MACRO]` helpers such as `SLA_JSON_OBJECT_GET`, `SLA_JSON_STRING_PTR`, `SLA_FS_READ_TO_STRING`, and nested `EXPAND FS_*` wrappers.
  - [x] Phase 2A implementation target: shared imported-macro expansion contract now records macro name, import path/source owner, expression-output out slot, borrowed argument mask, lowered argument ownership, and addressable-slot policy. Both SA text and direct SAB consume that contract.
  - [x] Phase 2A SAB tail target: direct SAB reuses the generic decoded macro-fragment machinery for arbitrary imported helper paths after the shared plan supplies import path and materialized args. Nested `EXPAND` borrowed-mask propagation covers helpers such as `SLA_FS_EXISTS -> FS_EXISTS`.
  - [x] Phase 2A cross-phase dependencies advanced: macro-expanded extern calls and local helper imports now pass the tracked JSON/FS package fixture; broader Phase 3/4/7 work remains for the still-failing non-macro fixtures.
  - [x] Phase 2A completion gate passed: local and host no-fallback `tests/test_unit_pkgjson_codegen.sla`, SA-text backend parity, macro regressions, `sa plugin install --dev .`, `SA_PLUGIN_DEV=1 sa sla help`, and `/home/vscode/projects/sla_ecs/lib/parallel.sla` all pass.
  - [x] Shared imported-macro addressable arg action sub-slice complete: `lowering_rules.ImportedMacroCallPlan.planAddressableArgAction` owns the existing direct SAB decision between pass-value, reusing an existing addressable symbol, and materializing a temporary stack slot; emitted stack/local mechanics remain in SAB codegen.
  - [x] Imported-macro borrowed address-expression materialization sub-slice complete: fresh official SAB artifacts no longer crash on the tracked borrowed `field`/`index`/ordinary `deref` shapes. Both emitters now split generic imported-macro stack-slot materialization from the dedicated address-expression path, and `tests/test_unit_imported_macro_address_direct.sla` passes direct SAB no-fallback plus SA-text parity under local and host dev-mode gates.
  - [x] Focused user-defined macro SA-text inline parity is complete for `tests/test_unit_user_macro_direct.sla`: nested macro calls, macro-local hygiene, caller argument substitution, aggregate/index/static-call expressions, block shadowing, borrowed field/index/deref args, and focused Box smart-pointer address forms now pass SA-text and strict direct-SAB no-fallback locally and through host dev-mode gates.
  - [ ] Ensure future macro expansion decisions live in shared expansion/lowering rules, not SAB-only branches.
  - [ ] Broaden macro convergence beyond the focused user-macro fixture so future nested macro calls, macro-local hygiene, caller substitution, borrow/move prefixes, aggregate literals, tuple destructuring, index access/assignment, block shadowing, and macro-expanded static calls all flow through a shared expansion/substitution contract before emitter-specific output.
  - [x] Keep `tests/test_unit_user_macro_direct.sla` in the regression set: it now covers focused SA-text parity and strict direct-SAB no-fallback for user macro expansion.
  - [ ] Completion rule: no new macro syntax or macro-lowered call shape may pass only through fallback; every direct macro feature needs a direct/no-fallback regression plus SA-text parity when the text backend owns the same surface.
- [ ] **Phase 3: Full Shared Call And Argument Materialization Plan**
  - [ ] Extend `lowering_rules.CallArgMaterializationPlan` into a complete call plan with result destination, argument register ownership, release policy, array-to-slice borrow, dyn fat-pointer borrow, copy-struct value, auto-borrow, move/borrow prefixes, generated identifier classification, and void-result handling.
  - [ ] Make SA-text and SAB ordinary calls, macro-expanded calls, inherent/trait static dispatch, extern calls, method calls, and closure call wrappers consume the same plan.
  - [ ] Target failures: nested import/extern call fixtures and macro-expanded call regressions. `tests/test_unit_rc_dyn_trait.sla`, `tests/test_unit_assign_move_cleanup.sla`, and `tests/test_unit_var_comprehensive.sla` are now completed regressions for shared dyn coercion, assignment, loop-control, and scoped-var cleanup.
  - [ ] Completion rule: if SAB cannot materialize a shared plan kind yet, the task remains open; do not add duplicate emitter-local semantics.
- [ ] **Phase 4: Std Surface Metadata Generalization**
  - [ ] Add or extend metadata rule kinds only when a failing fixture proves the need: constructors, methods, fallible methods, result-valued methods, branch macros, index/index_assign, clone/as_ptr/from_raw/get_mut, set/map helpers, and protocol hooks.
  - [ ] Keep library layout and macro knowledge in `sla_std/std_surface.sla_meta` plus `sa_std` macros, not as hardcoded Zig branches for `Vec`, `Rc`, `Set`, `Option`, `Result`, or future std types.
  - [ ] Target failures: broader targets remain `tests/test_unit_std_import.sla`, `tests/test_unit_sla_import_nested_contract.sla`, Option/Result/Cell regressions, and collection demos. `tests/test_unit_vec_index_assign.sla`, `tests/test_unit_field_compare_and_nested_len.sla`, `tests/test_unit_sets.sla`, `tests/test_unit_rc_dyn_trait.sla`, and typed Vec index reads are completed Phase 3/4 regressions.
  - [ ] Completion rule: every std-shaped feature has metadata, explicit arg slots/deps/panic behavior, receiver materialization, direct SAB verification, and SA-text parity or a documented reason the SA-text path already owns it.
- [ ] **Phase 5: Aggregate, Enum, Derive, And Operator Semantics**
  - [ ] Move remaining aggregate layout/update decisions into shared rules: struct update, enum payload layout, match extraction, derived equality/order/hash/debug metadata, spaceship compare, and copy/drop cleanup policy.
  - [ ] Target failures: future derive/component regressions and untracked aggregate/drop surfaces. `tests/test_unit_field_compare_and_nested_len.sla`, `tests/test_unit_derive_semantics.sla`, `tests/test_unit_enum_match.sla`, `tests/test_unit_spaceship_cmp.sla`, `tests/test_unit_struct_update.sla`, `tests/test_unit_struct_update_pointer_backed.sla`, and `tests/test_unit_var_comprehensive.sla` are now completed Phase 5 regressions.
  - [ ] Completion rule: SA-text and SAB must agree on layout offsets, value sizes, payload extraction, operator dispatch, assignment cleanup, and ownership from shared rules.
  - [x] Pointer-backed struct-update follow-up: safe pointer-backed update is implemented through shared `StructLiteralFieldTransfer` on top of `StructLiteralFieldPlan` (deep-copy for copy-struct fields, move for non-copy pointer-backed fields). `tests/test_unit_struct_update_pointer_backed.sla` covers a `Vec<i32>` untouched field under `..base`; direct SAB no-fallback and SA-text parity pass. Owner phase: Phase 5. Progress: 100% for the tracked follow-up.
- [ ] **Phase 6: Protocol Lowering And Iteration**
  - [ ] Lower basic and generic `for in` protocols through a shared iterator/protocol plan, including range/array/slice/Vec forms, item binding ownership, loop cleanup, and break/continue release state.
  - [ ] Target failures: future array/slice/Vec loop regressions and rosetta iterator demos. `tests/test_unit_for_in_protocol.sla` and `tests/test_unit_generic_for_in_protocol.sla` are completed tracked regressions.
  - [ ] Completion rule: protocol dispatch must be data/planned lowering, not type-name branches in one emitter.
- [ ] **Phase 7: Import, Package, And Std Resolution Parity**
  - [ ] Keep source expansion, import expansion, package metadata, nested contract loading, extern callee resolution, std root resolution, and test filtering in the shared frontend trunk.
  - [ ] Target failures: `tests/test_unit_sla_import_nested_contract.sla`, `tests/test_unit_std_import.sla`, `tests/test_unit_pkgjson_codegen.sla`, workspace/demo package tests, and host-dispatched CLI runs.
  - [ ] Completion rule: both `compileSlaToSaStringWithOptions` and `compileSlaFileToSabWithOptions` receive identical frontend results before their emitter tails.
- [ ] **Phase 8: Async/Await And Remaining Runtime Shapes**
  - [ ] Triage async/await after the shared call/std/control/import phases are stable; reuse std `Future`/task surface metadata and shared state-machine rules rather than SAB-only async lowering.
  - [x] Target fixture `tests/test_unit_async_await.sla` passes for the ready-future subset.
  - [x] Target fixture `tests/test_unit_async_task_runtime.sla` passes for the ready Future/task polling runtime subset.
  - [x] Target fixture `tests/test_unit_async_pending_task_runtime.sla` passes for the pending Future/task polling runtime subset.
  - [x] Target fixture `tests/test_unit_async_pending_await_state_machine.sla` passes for statically pending `.await` propagation from async functions.
  - [x] Target fixture `tests/test_unit_async_param_await_state_machine.sla` passes for ready/pending `future<T>` parameter `.await` propagation from compatible async functions.
  - [x] Target fixture `tests/test_unit_async_join2_await_state_machine.sla` passes for statically ready composite-future `.await` poll-once lowering from async functions.
  - [x] Target fixture `tests/test_unit_async_join2_local_await_state_machine.sla` passes for local future-readiness `.await` poll-once lowering from async functions.
  - [x] Target fixture `tests/test_unit_async_join2_local_pending_await_state_machine.sla` passes for local pending composite-future `.await` propagation from async functions.
  - [x] Target fixture `tests/test_unit_async_defer_ready_local_await_state_machine.sla` passes for a prebound-local single `future::defer_ready(...).await` continuation/resumption shape from async functions.
  - [x] Target fixture `tests/test_unit_async_defer_ready_post_bind_state_machine.sla` passes for a simple post-await binding after direct-call and prebound-local single `future::defer_ready(...).await` continuation/resumption shapes from async functions.
  - [x] Target fixture `tests/test_unit_async_defer_ready_capture_immediate_state_machine.sla` passes for one captured scalar addend plus one immediate addend after direct-call and prebound-local single `future::defer_ready(...).await` continuation/resumption shapes from async functions.
  - [x] Target fixture `tests/test_unit_async_defer_ready_post_bind_return_addend_state_machine.sla` passes for post-await binding plus captured scalar addend in the binding expression and immediate addend in the return expression after direct-call and prebound-local single `future::defer_ready(...).await` continuation/resumption shapes from async functions.
  - [x] Target fixture `tests/test_unit_async_defer_ready_await_state_machine.sla` passes for a narrow single `future::defer_ready(...).await` continuation/resumption shape from async functions.
  - [x] Target fixture `tests/test_unit_async_task_state_runtime.sla` passes for task runtime state inspection over ready and pending tasks.
  - [x] Target fixture `tests/test_unit_async_poll_runtime.sla` passes for scalar Poll ready/pending inspection and value extraction.
  - [x] Target fixture `tests/test_unit_async_executor_runtime.sla` passes for fixed-array `Task<T>` executor construction, single-index polling, and ready-count inspection.
  - [x] Target fixture `tests/test_unit_async_join2_runtime.sla` passes for scalar `future::join2` task polling and `FuturePair` value inspection.
  - [x] Target fixture `tests/test_unit_async_select2_runtime.sla` passes for scalar `future::select2` task polling and `FutureEither` side/value inspection.
  - [x] Target fixture `tests/test_unit_async_block_on_direct.sla` and rosetta `09_async_await` pass for the ready-future `block_on` subset, including `future<T>` parameter ABI and fallible std-time extern preservation.
  - [x] Target fixture `tests/test_unit_async_while_let_future_queue_direct.sla` and rosetta `136_executor_task_queue` pass for the ready-future queue-draining subset, including direct `while let Some(...)`, `vec(...)`, and `Vec::pop()` lowering.
  - [ ] Forward Phase 8A: generalize `AsyncSingleAwaitContinuationPlan` from additive scalar continuations into a broader expression-plan tree over the awaited binding, one or more scalar captured locals, integer immediates, and restricted control-flow expressions. Scalar-linear `value - bump` / `(value + bump) * 2` and the first branchy `if value > 0 { value + bump } else { bump }` sub-slice are complete through shared `AsyncContinuationScalarPlan` / `AsyncContinuationBranchPlan`; broader branch trees, multiple captures, and multi-statement tails remain open.
  - [ ] Forward Phase 8B: extend beyond the completed two-simple-binding tail after a single `future::defer_ready(...).await`. The tracked `let result = value + bump; let final = result + 1; return final;` sub-slice is complete through shared statement-tail scalar composition; longer tails, non-`let` statements, branchy post-await tails, and ownership-bearing post-await locals remain open.
  - [x] Forward Phase 8C: preserve multiple scalar pre-await captures in a narrow single-await state, for example `let a = 1; let b = 2; let value = future::defer_ready(39).await; return value + a + b;`. Completed through shared capture layout metadata with explicit offsets/sizes, consumed by both emitters, and verified through local/host no-fallback sweeps at 98/98.
  - [x] Forward Phase 8D: handle the first non-scalar captured local through a shared async-state ownership/copy/drop policy. Completed sub-slice covers an `@derive(copy)` struct capture and one scalar field read in the continuation; Vec/Box/Rc captures remain unsupported until a later shared policy prevents shallow-copy aliasing.
  - [x] Forward Phase 8E: move from single-await defer-ready continuation to two sequential awaits. Completed sub-slice defines the shared multi-stage state layout for direct-call and prebound-local `future::defer_ready(...).await` pairs. Required proof passed: first poll pending on first future, second poll progresses to second pending, final poll returns ready.
  - [x] Forward Phase 8F: resume a pending composite `future::join2` after it later becomes ready, instead of only propagating pending states. Completed sub-slice covers direct-call and prebound composite-state `future::join2(future::defer_ready(...), future::ready(...)).await` forms. Shared `AsyncJoin2AwaitContinuationPlan` defines state offsets 0 outer stage and 8 composite future state pointer, plus join2 vtable polling and pair-result scalar continuation; SA text and direct SAB consume the same plan. Verified through local/host no-fallback sweeps at 101/101.
  - [ ] Forward Phase 8G: connect executor scheduling with async state-machine continuation for queued tasks beyond ready/defer-ready primitives. Candidate should be a small rosetta-style executor fixture only after Phase 8E/8F have shared state layout coverage.
  - [ ] Forward Phase 8 guardrail: every new async continuation feature must state the async-state memory layout, capture ownership policy, stage transitions, and cleanup behavior in shared lowering/type metadata before emitter implementation. If SA-text and direct SAB cannot consume the same plan, the feature remains a draft and global Y/shared-lowering percentages do not move.
  - [ ] Broader async/Future/task executor/state-machine support remains open for future focused async demos beyond the narrow direct/prebound-local defer-ready single-await continuation shapes, local statically-ready/pending propagation, ready/pending-state awaits, ready/pending/join2/select2 futures, scalar Poll values, fixed-array task buffers, and immediately-ready task polling.
  - [x] Completion rule for the tracked supported subset: async lowering has direct SAB coverage, SA-text parity, and no fallback dependency.
- [ ] **Phase 9: Fallback Removal Gate**
  - [ ] Run the full no-fallback sweep after every completed feature and update this document and `progress.md` with pass/fail counts.
  - [ ] Local sweep command: `passed=0; total=0; rm -f /tmp/sla-sweep-failures.log; for f in tests/test_unit_*.sla; do total=$((total+1)); if SLA_SAB_NO_FALLBACK=1 timeout 120s ./zig-out/bin/sla-local-cli sla test "$f" --test-backend sab --jobs 1 --trace-panic >/tmp/sla-sweep-one.log 2>&1; then passed=$((passed+1)); else printf 'FAIL %s\n' "$f" | tee -a /tmp/sla-sweep-failures.log; tail -30 /tmp/sla-sweep-one.log | tee -a /tmp/sla-sweep-failures.log; fi; done; printf 'PASS %s/%s\n' "$passed" "$total"`.
  - [ ] Host sweep uses the same loop with `SA_PLUGIN_DEV=1 sa sla test` after `sa plugin install --dev .`; do not mix local CLI and host CLI counts in one reported percentage.
  - [x] Drive `tests/test_unit_*.sla` from 68/69 to 69/69 under `SLA_SAB_NO_FALLBACK=1`.
  - [x] Keep the expanded tracked corpus green at 70/70 after adding `tests/test_unit_async_task_runtime.sla`.
  - [x] Keep the expanded tracked corpus green at 73/73 after adding `tests/test_unit_println_direct.sla`.
  - [x] Keep the expanded tracked corpus green at 74/74 after adding `tests/test_unit_async_block_on_direct.sla`.
  - [x] Keep the expanded tracked corpus green at 75/75 after adding `tests/test_unit_async_while_let_future_queue_direct.sla`.
  - [x] Keep the expanded tracked corpus green at 76/76 after adding `tests/test_unit_async_pending_task_runtime.sla`.
  - [x] Keep the expanded tracked corpus green at 77/77 after adding `tests/test_unit_async_task_state_runtime.sla`.
  - [x] Keep the expanded tracked corpus green at 78/78 after adding `tests/test_unit_async_poll_runtime.sla`.
  - [x] Keep the expanded tracked corpus green at 79/79 after adding `tests/test_unit_async_executor_runtime.sla`.
  - [x] Keep the expanded tracked corpus green at 80/80 after adding `tests/test_unit_async_join2_runtime.sla`.
  - [x] Keep the expanded tracked corpus green at 81/81 after adding `tests/test_unit_async_select2_runtime.sla`.
  - [x] Keep the expanded tracked corpus green at 82/82 after adding `tests/test_unit_async_executor_vec_runtime.sla`.
  - [x] Keep the expanded tracked corpus green at 83/83 after adding `tests/test_unit_async_pending_await_state_machine.sla`.
  - [x] Keep the expanded tracked corpus green at 84/84 after adding `tests/test_unit_async_param_await_state_machine.sla`.
  - [x] Keep the expanded tracked corpus green at 85/85 after adding `tests/test_unit_async_join2_await_state_machine.sla`.
  - [x] Keep the expanded tracked corpus green at 86/86 after adding `tests/test_unit_async_join2_local_await_state_machine.sla`.
  - [x] Keep the expanded tracked corpus green at 87/87 after adding `tests/test_unit_async_join2_local_pending_await_state_machine.sla`.
  - [x] Keep the expanded tracked corpus green at 88/88 after adding `tests/test_unit_async_defer_ready_task_runtime.sla`.
  - [x] Keep the expanded tracked corpus green at 89/89 after adding `tests/test_unit_async_defer_ready_await_state_machine.sla`.
  - [x] Keep the expanded tracked corpus green at 90/90 after adding `tests/test_unit_async_defer_ready_local_await_state_machine.sla`.
  - [x] Keep the expanded tracked corpus green at 91/91 after adding `tests/test_unit_async_defer_ready_post_bind_state_machine.sla`.
  - [x] Keep the expanded tracked corpus green at 92/92 after adding `tests/test_unit_async_defer_ready_capture_state_machine.sla`.
  - [x] Keep the expanded tracked corpus green at 93/93 after adding `tests/test_unit_async_defer_ready_capture_immediate_state_machine.sla`.
  - [x] Keep the expanded tracked corpus green at 94/94 after adding `tests/test_unit_async_defer_ready_post_bind_return_addend_state_machine.sla`.
  - [x] Keep the expanded tracked corpus green at 101/101 after adding `tests/test_unit_async_join2_defer_ready_await_state_machine.sla`.
  - [x] Keep the expanded tracked corpus green at 102/102 after the RefCell result-slot transfer and SA-text smart-pointer field-borrow hardening slice.
  - [x] Keep the tracked corpus green at 102/102 fixture files after the shared result-slot transfer-plan extraction; latest host no-fallback sweep passed 253/253 test cases.
  - [x] Keep the tracked corpus green at 102/102 fixture files after the shared borrowed binding storage-plan extraction; latest host no-fallback sweep passed 253/253 test cases.
  - [x] Keep the tracked corpus green at 102/102 fixture files after the shared borrowed-identifier recognition extraction; latest host no-fallback sweep passed 253/253 test cases.
  - [x] Keep the current worktree tracked corpus green at 118/118 fixture files after the docs-priority cleanup/imported-macro/direct-SAB regression fixtures were added. Local strict direct-SAB no-fallback sweep passed 118/118; installed host dev-mode strict direct-SAB no-fallback sweep passed 118/118; installed host `/home/vscode/projects/sla_ecs/lib/parallel.sla` strict direct-SAB passed 1/1 with a clean SAB disasm illegal-call-target guard.
  - [ ] Before declaring 100%: run `zig fmt`, `zig build --summary all`, `zig build test --summary all`, `sa plugin install --dev .`, `SA_PLUGIN_DEV=1 sa sla help`, focused host no-fallback regressions, `/home/vscode/projects/sla_ecs/lib/parallel.sla`, full local no-fallback sweep, full host no-fallback sweep, `git diff --check`, docs sync, and verified commits.
  - [ ] Completion definition: Y/shared-lowering 100%, direct SAB fallback-removal 100% for the tracked unit corpus, all reported macro expansion and borrow/precedence goals complete, no unexpected fallback logs, `progress.md` and `tasks.md` synchronized, and verified commits made for completed slices.
- [ ] **Phase 10: SCI Fragment Naming Boundary**
  - [ ] Implement the generic fix in `/home/vscode/projects/sci` flatten/encode rather than plugin-side fixture patches.
  - [x] Preserve `__sla_macro_arg_N` placeholder tokens as opaque values during embedded token remap, including `^`/`&` sigil contexts.
  - [x] Remap fragment-internal hygiene/register tokens consistently in structured operands, embedded call-body text, raw text, atomic text, and native text for SCI `appendFlattenFragment` cloning, so display/disasm and verifier can share one symbol authority.
  - [x] Preserve imported extern/export declarations and their verifier-sensitive order when flattening filtered std deps. Verified as no-repro: a maximal cross-module helper-overlap stress probe (`tests/test_unit_mixed_collections_order.sla`) passes direct SAB no-fallback and SA-text parity; host disasm shows every `export_decl $name,fn:N` resolving to a valid function with a coherent following body and no illegal visible call targets. A focused Zig regression pins exported-helper full-body cloning in original decoded order.
  - [x] Filtered std-dep injection now includes same-module direct-call dependencies in original decoded-module order and suppresses duplicate selected helper names before cloning signatures/bodies. This is the verified plugin-side half of the current ordering audit and keeps HashSet/BTreeSet helper declarations coherent under no-fallback host tests.
  - [x] Current ordering audit note (CLOSED as no-repro): SCI verifier accepts predecoded `function_sigs`, while `sa_plugin_sla` filtered std-dep injection owns when selected decl/body instructions are appended. The completed filtered-dep closure regression covers helper calls, reachable extern selection, original-order emission, duplicate helper suppression, and unused-helper exclusion. The extern/export ordering audit is now closed: the mixed-collections stress probe reproduces no ordering bug, and a focused Zig regression (`filtered decoded std deps emit exported helper bodies in original order`) pins the exported-helper body/ordering contract. The plugin-side fragment remap/placeholder adapter (`renameFragmentText`/`replaceStdMacroPlaceholders*`) is confirmed to be a distinct decode-time stage (per-fragment `__frag{N}_` hygiene + `__sla_macro_arg_N` caller-arg substitution), NOT a parallel implementation of SCI's flatten-time `cloneRemappedSymbolText` symbol-ID remap; it stays as a thin adapter and no further SCI change is warranted without a concrete failing filtered-dep fixture.
  - [ ] Keep plugin-side `debug()` structured lowering as the accepted long-term path, but do not use plugin-side string rewrites to cover generic `format`/`Vec`/computed-receiver gaps.
  - [ ] Audit `sa_plugin_sla` fragment append code and either route it through SCI-owned remap behavior or document the remaining thin adapter boundary; do not keep parallel semantic rules long term.
  - [ ] Completion rule: focused SCI tests plus `sa_plugin_sla` dev-mode guards pass, full unit sweep stays 88/88 or better, `parallel.sla` passes, and illegal visible `@func(arg)` call targets do not appear in tracked SAB disasm.
- [ ] **100% Execution Order From Current State**
  - [x] Slice 2A, imported-macro/pkg classification and first implementation: `pkgjson_codegen` now passes through shared imported-macro planning and generic imported macro fragments, with no fallback.
  - [ ] Immediate active slice before claiming all-complete: Phase 1 broader shared RefCell lifecycle / pointer-backed aggregate alias planning. Owner boundary: pure classification/lifecycle decisions belong in `src/lowering_rules.zig` or shared frontend/typecheck results; concrete SA-text and direct SAB register/string emission, slot allocation, and cleanup ordering stay emitter-local. Expected gates: focused shared-rule tests, `zig build test --summary all`, local and host strict direct-SAB no-fallback plus SA-text where applicable for `tests/test_unit_borrow_temp_release_order.sla`, `tests/test_unit_refcell_struct_payload.sla`, `tests/test_unit_imported_macro_address_direct.sla`, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`, then docs sync and `git diff --check`.
  - [ ] Slice 2B, broader pure macro convergence: focused user-macro SA-text parity is complete; close future macro expansion gaps through the shared frontend/lowering path, with `tests/test_unit_user_macro_direct.sla` kept as a regression.
  - [ ] Slice 3A, call/result/assignment stabilization: `assign_move_cleanup`, `var_comprehensive`, and `rc_dyn_trait` are closed; continue with call result destinations, remaining move/use state, and full argument materialization through shared plans.
  - [ ] Slice 4A, std metadata batch: sets and Rc/dyn trait metadata are closed; continue with std import helpers and any remaining smart-pointer or collection methods through metadata plus `sa_std` macros if they reappear.
  - [ ] Slice 5A, aggregate/enum/derive/operator batch: close struct update, enum match, derive semantics, spaceship compare, and layout-dependent cleanup together.
  - [ ] Slice 6A, protocol lowering: close non-generic and generic `for in` through one iterator/protocol plan.
  - [ ] Slice 7A, import/pkg parity: close nested contract, std import, package metadata, extern callee, and host/local mismatch issues in the shared frontend trunk.
  - [ ] Slice 8A, async supported subset: only after call/std/control/import stabilize, implement or explicitly scope async/await support through shared Future/task/state-machine rules.
  - [ ] Slice 9A, final fallback removal: local and host sweeps reach 64/64 or every unsupported surface is an explicit unfinished task, then run final Y audit and commit.
- [ ] **Phase Exit Criteria And Estimated Progress Path To 100%**
  - [x] Focused Phase 1 user blockers exit: borrow-temp, RefCell struct payload, smart-pointer cleanup, Option/Result regressions, host CLI gates, `parallel.sla`, and tracked disasm guard pass. Feature: 100%; Y/shared-lowering approximately 66%; direct SAB fallback-removal approximately 85%; sweep 50/64.
  - [x] Phase 2A current status: imported SA expression-output/addressable macro slice is 100%; global Y/shared-lowering reached approximately 72%; direct SAB fallback-removal reached approximately 88%; sweep was 58/69 before the Phase 3 assignment-move slice.
  - [x] Focused Phase 2 user-defined macro SA-text inline parity sub-slice is complete for the tracked user-macro fixture: local/host SA-text and strict direct-SAB no-fallback pass, current tracked corpus remains 102/102 fixture files / 255 test cases, and broader Y/shared-lowering is about 98-99%.
  - [ ] Current all-complete status: not complete. The tracked direct-SAB corpus is green, but final 100% still requires closing or explicitly scoping the remaining shared architecture tasks, re-running final local/host gates, synchronizing docs, and committing verified completed slices.
  - [ ] Phase 2 exit: macro-shaped failures either pass no-fallback through shared expansion/lowering or are reclassified with a concrete owner. Expected movement: macro-expansion convergence 100%; no hidden fallback path for macro-expanded calls; `pkgjson_codegen` either passes or is blocked by a documented Phase 3/4/7 non-macro owner after imported macro support lands.
  - [ ] Phase 3 exit: all static/extern/method/macro-expanded calls consume one shared call plan, including result destination, void-result, array-to-slice, dyn fat pointer, copy-struct value, generated identifier classification, argument release, receiver auto-borrow, assignment-move cleanup, loop-control cleanup, and scoped var slots. Current Phase 3/4 Rc dyn trait sub-slice moved Y/shared-lowering to approximately 77% and direct SAB fallback removal to approximately 91%; remaining Phase 3 movement depends on deeper call/result materialization rather than the closed `rc_dyn_trait` fixture.
  - [ ] Phase 4 exit: remaining std/library-shaped failures are expressed as metadata plus `sa_std` macro/import dependencies. Current Phase 4 set sub-slice moved Y/shared-lowering to approximately 79% and direct SAB fallback removal to approximately 92%; expected movement at full Phase 4 exit remains roughly 80-84% Y/shared and 92-94% fallback removal.
  - [ ] Phase 5 exit: aggregate update, enum/match payload extraction, derive semantics, spaceship/operator dispatch, value-size/layout, and cleanup ownership all use shared layout/operator plans. Expected movement: Y/shared-lowering roughly 88-92%; direct SAB fallback removal roughly 96%.
  - [ ] Phase 6 exit: basic and generic `for in` protocol lowering consumes one iterator/protocol plan after monomorphization. Expected movement: direct SAB fallback removal roughly 98% if both for-in fixtures pass.
  - [ ] Phase 7 exit: package/std/import/nested-contract resolution is byte-identical before the Y split, and extern callee resolution reaches both emitters from the shared frontend trunk.
  - [ ] Phase 8 exit: supported async/await subset lowers through shared Future/task-state/runtime rules with direct SAB no-fallback and SA-text parity. Current tracked supported subset is green at 101/101; broader state-machine executor support remains outside the narrow direct/prebound-local/post-binding/two-post-binding/multi-capture/copy-struct-capture/captured-addend/captured-plus-immediate/post-binding-return-addend/scalar-linear/branchy defer-ready single-await continuation shapes, the Phase 8E two-sequential-await defer-ready shape, the Phase 8F join2 defer-ready later-ready shape, local statically-ready/pending propagation, and the ready/pending/defer-ready/join2/select2 Future/Poll/executor task-runtime subset.
  - [ ] Phase 9 exit: final numbers are recorded in `progress.md` and this file; Y/shared-lowering 100%; direct SAB fallback removal 100% for tracked corpus; full local and host gates pass; all completed slices are committed.
- [ ] **Resume Commands For The Next Slice**
  - [ ] Confirm host plugin surface before host runs: `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`.
  - [ ] Re-run local full sweep when starting a new feature: use the Phase 9 local sweep command and save `/tmp/sla-sweep-failures.log`.
  - [ ] Macro/pkg regression command: `SLA_SAB_TRACE_UNSUPPORTED=1 SLA_SAB_NO_FALLBACK=1 timeout 180s ./zig-out/bin/sla-local-cli sla test tests/test_unit_pkgjson_codegen.sla --test-backend sab --jobs 1 --trace-panic`.
  - [ ] Keep macro regression green: `SLA_SAB_TRACE_UNSUPPORTED=1 SLA_SAB_NO_FALLBACK=1 timeout 180s ./zig-out/bin/sla-local-cli sla test tests/test_unit_user_macro_direct.sla --test-backend sab --jobs 1 --trace-panic` and SA-text parity if behavior changes.
  - [ ] Re-run focused completed-slice guards before committing later work: borrow-temp, RefCell struct payload, smart-pointer cleanup, Option methods/direct, Result direct, and `/home/vscode/projects/sla_ecs/lib/parallel.sla` through host `SA_PLUGIN_DEV=1`.
  - [ ] Verify visible call-target display for tracked SAB artifacts after call-related edits: `sa sla sab disasm <latest.sab> | rg 'call .*@[^" ]+\('` must return no matches.
- [ ] **Final 100% Acceptance Checklist**
  - [ ] Local compiler health: `zig fmt`, `zig build --summary all`, `zig build test --summary all`, and `git diff --check` all pass.
  - [ ] Official plugin health: `sa plugin install --dev .` completes from this repo, and `SA_PLUGIN_DEV=1 sa sla help` shows the SLA CLI surface from the installed dev plugin.
  - [x] Direct SAB health for the current tracked unit corpus: `SLA_SAB_NO_FALLBACK=1` passes the full tracked `tests/test_unit_*.sla` sweep locally and through host `SA_PLUGIN_DEV=1 sa sla test`, with current worktree counts recorded as 118/118 in this file and `progress.md`. This item must be re-run if implementation or fixture files change before final completion.
  - [ ] Reported user blocker health: `/home/vscode/projects/sla_ecs/lib/parallel.sla` passes direct SAB no-fallback through the host CLI and no tracked SAB disasm contains an illegal visible target such as `@func(arg)`.
  - [ ] Macro-expansion health: macro fixtures pass no-fallback without SAB-only macro semantics, and any macro-shaped failure is either fixed or reclassified to a concrete shared call/std/import task.
  - [ ] Borrow/precedence health: focused borrow, smart-pointer, RefCell, address-of-field/index/deref, branch, loop, call-argument, and early-return fixtures pass direct SAB no-fallback and SA-text parity where applicable.
  - [ ] Y-shared health: every remaining high-level semantic decision has a shared owner; no new feature is implemented only as a type/library-name branch in `src/sab_codegen.zig`.
  - [ ] Fallback health: normal SAB path has no unowned fallback use in the tracked unit corpus; any unsupported surface is named by fixture, lowering kind, owner phase, and future contract.
  - [ ] Documentation health: `progress.md` records final feature percent, Y/shared-lowering percent, direct SAB fallback-removal percent, and full sweep counts; `tasks.md` has no stale current-blocker notes.
  - [ ] Commit health: all verified completed slices are committed, and the final commit message names the shared rule/metadata contracts plus the exact verification gates.
- [ ] **Commit And Documentation Rules For 100% Completion**
  - [ ] Never commit a slice that only moves the failing symptom; commit only after the slice's local and host gates pass and `progress.md` records feature percent, Y/shared-lowering percent, direct SAB fallback-removal percent, and full no-fallback sweep count.
  - [ ] Each completed slice must state what shared rule or metadata contract was extracted, which emitter-local branches were removed or delegated, which fixtures prove no-fallback direct SAB, and which SA-text parity checks ran.
  - [ ] If a fixture remains unsupported at final gate, it must have an explicit task with owner phase, failing command, exact unsupported lowering kind, and planned shared-rule/metadata contract; otherwise 100% is not reached.
- [ ] **Progress Report Template To Use After Every Completed Feature**
  - [ ] Report in this exact shape: `Feature: <name> 100%; Y/shared-lowering: <old>% -> <new>%; direct SAB fallback-removal: <old>% -> <new>%; no-fallback sweep: <passed>/<total>; host gate: passed; commit: <hash or pending>`.
  - [ ] If a slice is not 100%, report it as draft evidence only: `Feature: <name> <percent>% drafted; global Y/shared and fallback-removal unchanged; blocker: <exact command/error>`.
  - [ ] Keep `progress.md` and `tasks.md` synchronized in the same edit before a final status update or commit.

### 1. Lexer & Parser Updates
- [x] **Postfix `?` Operator Support**
  - [x] Add `?` token tag to `src/lexer.zig`.
  - [x] Update `lexer.zig` scanner to scan postfix `?` token.
  - [x] Update `src/parser.zig` to parse postfix `?` operator.
- [x] **Generic Struct Literal Support**
  - [x] Parse `Type<T> { field: value }` without adding any new keyword.
  - [x] Keep source cleanup implicit; no `drop` keyword or `drop()` function is introduced.

### 2. Type Checker & Symbol Resolver (Semantic Analyzer)
- [x] **Scope Builder & Symbol Table**
  - [x] Implement nested block scope stack.
  - [x] Track local variable declarations, parameter bindings, and constant values.
- [x] **Type Resolver**
  - [x] Parse and resolve primitive types (`int`, `float`, `bool`, `void`, `ptr`).
  - [x] Bind user-defined structs.
  - [x] Register signatures from FFI contract parser (`.sai`/`.sal`).
- [x] **Borrow Checker & Ownership Validator**
  - [x] Track variable ownership states (`Active` vs `Consumed`).
  - [x] Check move semantics (`^` transfer) and reject use-after-move.
  - [x] Validate borrow rules (`&` reference) and forbid mutable/immutable aliasing conflicts.

### 3. Monomorphization Engine
- [x] **Generic Collection & Instantiation**
  - [x] Traverse the AST to gather all generic struct instantiations and generic function calls.
  - [x] Map generic type parameters (like `T`) to concrete types (like `int`).
  - [x] Generate specialized non-generic structs and functions (e.g. `Option_int_Layout`, `unwrap_or_int`).

### 4. AST to SA Lowerer (Code Generation)
- [x] **Control Flow Flattener**
  - [x] Lower `if/else` conditional expressions into flat, conditional jumps with automated register cleanups before merge targets (preventing `PhiStateConflict`).
  - [x] Lower `switch` pattern matching expressions to flat conditional equality ladders.
- [x] **Hygienic Macro Expander**
  - [x] Perform compile-time Alpha-conversion (renaming local macro variables to globally unique names).
  - [x] Lower `macro` definitions and calls directly to SA's `[MACRO]` / `EXPAND` preprocessor statements.
- [x] **Scope Lifetime Auto-Cleanup Generator**
  - [x] Track active resources at block exits.
  - [x] Automatically inject SA `!reg` cleanup before scope endings so user `.sla` code does not need explicit releases.
- [x] **Loop Allocation Hoister**
  - [x] Detect `stack_alloc` statements inside `for` loops.
  - [x] Hoist allocations outside and before the loop label, reusing memory inside the loop body.
- [x] **Postfix `?` Unwrapper**
  - [x] Lower `expr?` into a conditional error check, injecting automated local variable cleanups and an early `return Err(^err)` on error branch.
- [x] **Struct Literal Lowering**
  - [x] Lower Sla struct literals to flat SA `alloc N` plus `store base+offset` instructions.
  - [x] Keep struct layouts as compile-time Sla metadata instead of emitting forbidden `#struct { ... }` brace syntax into `.sa`.

### 5. Integration & CLI Build Command
- [x] **Code Output Writer**
  - [x] Output compiled `.sa` files; Sla-owned struct layouts stay as compile-time metadata.
  - [x] Connect Lexer -> Parser -> Type Checker -> Monomorphizer -> Codegen -> Output in `src/plugin.zig`.
- [x] **Direct SAB Output Mainline**
  - [x] Add `src/sab_codegen.zig` as a direct SLA AST/type-checker to SAB backend, separate from `.sa` text codegen.
  - [x] Keep `sla -> .sa` and `sla -> .sab` as two independent mainlines; SAB generation does not call `compileSlaToSaString` or the SA text flattener.
  - [x] Confirm the SA compiler dependency from `https://github.com/layola13/sci/` is documented and installed before plugin development/verification.
  - [x] Confirm SA host `.sab` input support is used by `sa sla build-exe` and `sa sla sab workspace` via managed `.sla-cache/sab/...` paths.
  - [x] Add `sa sla sab build` / `sa slab build` support with default managed output under `.sla-cache/sab/`.
  - [x] Support optional visible SAB artifacts through `--out/-o` for `sab build`.
  - [x] Add `sa sla sab workspace` / `sa slab workspace` support with workspace package resolution, managed `.sla-cache/sab/` input for `sa build-exe`, and optional `--sab-out` / `--emit-sab` artifacts.
  - [x] Preserve `sa sla sab disasm` as a debug-only reader for SAB files.
  - [x] Make `sa sla test` default to `--test-backend auto`, writing managed SAB under `.sla-cache/sab/` and invoking `sa test` on SAB by default; the legacy `.test.sa` path is now only used by explicit `--test-backend sa`.
  - [x] Add `--test-backend sab` for explicit SAB artifact verification with no legacy `.sa` backend fallback, and `--test-backend sa` for explicit legacy `.sa` text tests.
  - [x] Add an in-memory SA-compatible SAB encoder fallback inside the SAB mainline so SA features not yet covered by direct AST-to-SAB codegen still produce `.sab` output without writing `.sa` text.
  - [x] Confirm SCI SAB v4 metadata support preserves structured operands, atomic operand text, native register names, package identity, upstream locations, and verified function register ids needed by SA backends without storing per-instruction raw `.sa` text.
  - [x] Add SCI regression coverage for v4 no-raw-text SAB decode, all `InstKind` / `OpKind` / operand tags, structured call parsing, and LLVM lowering of localized const vtable slots.
  - [x] Add direct SAB lowering for language-level function pointer values, including vtable const declarations, borrowed function values, parameter passing, generic function specializations, and `call_indirect`.
  - [ ] Optimize SLA-to-SAB generation time; current SA backend compile from `.sab` is faster than raw `.sa`, but `sa sla sab build` still spends extra time in SA-compatible flatten/verify/encode and cache writes.
  - [x] Cache the resolved `sa_std` root inside each direct SAB codegen instance so repeated std macro fragments do not repeat filesystem root probing.
  - [x] Cache decoded std import modules inside each direct SAB codegen instance so dependency preloading for multiple rules from the same `sa_std` import path does not repeat full SCI flatten/encode/decode work.
  - [x] Cache reusable identifier-only std macro templates inside each direct SAB codegen instance so repeated Option/Result-style macro fragments reuse decoded structured SAB bodies with placeholder substitution.
  - [x] Add focused tests for direct SAB output, managed cache behavior, SAB magic, decoded instructions, and no generated `.sa` side output.
  - [x] Add direct-only regression tests (`allow_fallback = false`) so new direct SAB features cannot silently pass through the SA-compatible fallback path.
  - [x] Add direct SAB lowering for plain struct literals, field access, struct returns, resolved call symbols, and multi-argument function calls.
  - [x] Add a dev/debug no-fallback gate (`SLA_SAB_NO_FALLBACK=1`) so direct SAB gaps fail loudly instead of hiding behind the compatibility encoder.
  - [x] Add direct SAB lowering for ordinary closure bindings and closure calls, including captured outer locals and one/two-parameter inline closure bodies.
  - [x] Add direct SAB lowering for Phase 1 scalar `var` slots, identifier assignment, stack-slot load/store, and basic `while` loops.
  - [x] Fix direct SAB branch-condition cleanup so temporary conditions are released on each branch while local/parameter conditions remain active.
  - [x] Add direct SAB lowering for tuple literals, tuple field access, and tuple destructuring, including direct-only regression coverage.
  - [x] Add direct SAB lowering for fixed array literals, repeat literals, and literal index reads/writes for focused scalar arrays, including direct-only regression coverage.
  - [x] Add direct SAB lowering for dynamic fixed-array index reads/writes and basic numeric range `for` loops, including direct-only regression coverage and no-fallback coverage for `tests/test_unit_arrays.sla`.
  - [x] Add direct SAB lowering for scalar value-producing `if` expressions, typed `if` bindings, nested branch assignments, and f32/f64 arithmetic/comparison op selection.
  - [x] Add parser and direct SAB lowering for boolean `&&` / `||` expressions, including dev-plugin no-fallback verification.
  - [x] Add direct SAB lowering for primitive numeric `as` casts, including structured conversion op operands and dev-plugin no-fallback verification.
  - [x] Add direct SAB lowering for scalar borrow/deref and non-void tail-expression returns, including direct-only regression coverage.
  - [x] Extend direct SAB borrow lowering to addressable field/deref/index sources, covering postfix-under-prefix precedence such as `&item.value` and `&*value` without fallback.
  - [x] Add direct SAB lowering for move-prefixed call arguments, including type-checker single-consumption handling and dev-plugin no-fallback verification.
  - [x] Add direct SAB inline expansion for focused user `macro` calls with hygienic macro-local renaming and caller-scope argument substitution.
  - [x] Extend direct SAB user macro expansion through nested control flow, ordinary/nested macro calls, casts, aggregate literals, tuple destructuring, index access/assignment, and block-scoped hygienic shadowing.
  - [x] Add direct SAB `stack_alloc()` lowering and raw stack-allocation lifetime tracking.
  - [x] Move the first std surface lowering path into generic import metadata / macro-fragment lowering so direct SAB can consume std macros without hardcoding ordinary library logic in Zig.
  - [x] Extend the std surface metadata format beyond the current associated/method/index macro bridge, still without adding compiler branches for `Vec`, `thread`, ECS, or other library names.
  - [x] Add generic fallible std surface macro metadata with explicit ok/output slots and panic-on-false lowering, covering `Vec.remove` without a `Vec.remove` compiler branch.
  - [x] Add generic std surface constructor metadata and result-valued method metadata, covering focused `Option` `Some`/`None` construction plus `is_some`/`is_none` without `Option` compiler branches.
  - [x] Preserve const-bearing std macro fragments in direct SAB lowering, including structured `panic_msg` operands and focused `Option.unwrap()` coverage without `Option` compiler branches.
  - [x] Add focused direct SAB metadata coverage for `Option.unwrap_or`, including no-fallback Some/None branch regression coverage without `Option` compiler branches.
  - [x] Add shared-plan direct SAB coverage for closure-taking `Option.map`, `Option.and_then`, and `Option.unwrap_or_else`, with `Option` payload access routed through std surface metadata (`OPTION_GET`) instead of a SAB-only layout branch.
  - [x] Add focused direct SAB metadata coverage for `Result` `Ok`/`Err` construction plus `is_ok`/`is_err`/`unwrap`/`unwrap_or`, including no-fallback regression coverage without `Result` compiler branches.
  - [x] Share the Y-shaped compile front-end trunk between the SA-text and SAB compile entry points (`compileSlaToSaStringWithOptions` and `compileSlaFileToSabWithOptions`).
    - Progress: extracted `runSlaFrontend` in `src/plugin.zig`, owning the byte-identical pipeline read → source-expand → parse → `@import`-expand → test-filter → monomorphize → load-contracts → type-check → primary-decl filter.
    - Progress: both tails now construct caller-owned `Monomorphizer`/`TypeChecker`, call the shared trunk, and keep them alive across their own codegen tail (SA-text: `rewriteProgramImportsForOutput` + `codegen.generate`; SAB: `pruneUnreachableFilteredTestDecls` + `sab_codegen.generate` with the existing SA-compatible fallback). Per-stage `SLA_PROFILE` timing now also covers the SA-text path.
    - Constraint: the front-end trunk must stay the single source of truth for both tails; new front-end stages go in `runSlaFrontend`, not in a single tail.
    - Verified in the current tree: `zig build --summary all`; `zig build test --summary all` (56/56); SA-text backend runs for `test_unit_generics.sla`, `test_unit_assign_move_cleanup.sla`, `test_unit_overload_add.sla`, and `test_unit_basic.sla`.
    - Progress estimate: 100% for shared front-end trunk extraction; broader Y/shared-lowering progress is approximately 45% after this trunk extraction plus the direct-SAB convergence fixes below.
  - [x] Start the Y-shaped shared lowering-rules path instead of growing independent SA-text and SAB semantic branches.
    - Progress: `src/lowering_rules.zig` now owns shared derive-name matching, struct derive lookup, ordinary static-call target resolution, and call-argument prefix rules used by both `codegen.zig` and `sab_codegen.zig`.
    - Progress: `lowering_rules.StaticCallPlan` now feeds resolved/static call target selection in both emitters, and `prefixedIdentifierCallArg` owns the shared `&name` / `^name` spelling rule for SA text call arguments.
    - Progress: release classification and parameter-aware auto-borrow predicates now live in shared lowering rules, with the SA text emitter delegating to them.
    - Progress: `CallArgMaterializationPlan` now represents array-to-slice borrow, auto-borrow, copy-struct value, ordinary value, and release decisions for resolved static-call arguments, and the SA text resolved-call path consumes it.
    - Progress: resolved static-call dyn borrow arguments now materialize through the shared plan by creating a fat pointer, passing `&fat_reg`, and releasing the fat pointer after the call.
    - Progress: legacy `genCallArgForParam` now delegates copy/value/release decisions to the shared materialization plan.
    - Progress: ordinary function/extern expression-call fallback now consumes the shared materialization helper for array-to-slice borrow, dyn fat-pointer borrow, receiver-style auto-borrow, copy/value, generated-identifier, and release decisions.
    - Progress: remaining SA text legacy method/statement call branches now consume the shared materialization helper, with statement-receiver borrow passthrough captured as a shared predicate.
    - Progress: direct SAB ordinary static calls now consume the shared materialization plan for value arguments, parameter-aware auto-borrow, generated identifier classification, and temporary release selection while leaving unsupported array/dyn/copy SAB materializations explicit.
    - Progress: direct SAB macro-expanded static calls now consume the shared materialization plan using effective macro arguments for planning and macro-context expressions for register materialization.
    - Progress: aggregate ABI layout rules now live in `lowering_rules.zig` and feed both SA-text and SAB emitters, including array-typed struct field pointer-slot layout.
    - Progress: direct SAB dyn-borrow call arguments now consume the shared materialization plan, with shared trait/dyn naming and method-slot rules used by both emitters.
    - Progress: inherent associated calls and dot inherent methods now publish resolved implementation symbols into `resolved_call_symbols`, so both emitters converge through `lowering_rules.planStaticCall` instead of SA-text-only fallback branches.
    - Progress: fixed-array destructuring now consumes shared array element/rest layout rules, and `Slice<T>` index reads flow through std surface metadata just like `Vec<T>` reads.
    - Progress: associated std-surface rules can now consume constructor value arguments, and `Cell<T>` `new`/`get`/`set` are expressed through metadata plus std macros rather than compiler branches.
    - Progress: closure-taking `Option.map`, `Option.and_then`, and `Option.unwrap_or_else` now classify through `lowering_rules.OptionClosureCallPlan`; SA-text uses the shared plan for dispatch, and direct SAB consumes the same plan with std surface `Option` macros including `OPTION_GET` metadata.
    - Progress estimate: the first shared-rules extraction, static-call plan, call-materialization predicate, resolved-call materialization-plan, resolved-call dyn borrow, legacy helper adoption, ordinary function/extern expression-call adoption, remaining SA text legacy method/statement adoption, SAB ordinary/static macro call adoption, aggregate ABI layout, SAB dyn-borrow consumption, static dispatch resolved-symbol convergence, Vec index-assignment metadata, fixed-array destructuring/std-slice-index, Cell std-surface, and Option closure-plan slices are 100%; the broader Y/shared-lowering track is approximately 58%.
    - Constraint: do not implement high-level language/library semantics only in `sab_codegen.zig`; future direct SAB work should extend shared rules/plans or std surface metadata so SA text and SAB emitters converge through the shared contract.
  - [x] Converge direct-SAB control-flow release scoping and struct-operator lowering with the SA-text emitter so both backends agree on a shared corpus.
    - Branch scoping: `genIfStatement`/`genIfValue` now snapshot `self.locals` length + `released_regs` before each branch and restore after, with the merge release state computed as the intersection over non-terminated incoming paths (`restoreReleased`/`setMergeReleased`). Fixes `UnknownRegister` for then-branch `let`s leaking into the else path (e.g. generics `unwrap_or`).
    - Loop scoping: `genWhile` applies the same body-local snapshot/restore so a body `let` (e.g. `let next = ...`) does not leak into a trailing `return`'s `releaseOpenLocals` on the zero-iteration path.
    - Struct operators: `genBinary` delegates struct-typed operands to a new `genStructArithmetic` mirroring SA-text `genStructArithmeticExpr` (same-struct `+`/`-`, scalar `*` either order), field-wise into a fresh alloc instead of a primitive `op` on struct pointers (which segfaulted). Out-of-matrix struct shapes return `UnsupportedSabDirectFeature` so the SA-compatible fallback takes over.
    - Shared call tail: extracted `emitPlannedStaticCall` from `genCall` so the planned static-call emission is reused.
    - Verified in the current tree: `zig build --summary all`; `zig build test --summary all` (56/56); no-fallback SAB for `test_unit_generics.sla`, `test_unit_assign_move_cleanup.sla`, `test_unit_overload_add.sla`, full `test_unit_fn_ptr_value.sla`, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; SA-text backend for the three fixed files plus `test_unit_basic.sla`.
    - Historical snapshot: after the later array-field ABI, dyn-borrow plan-consumption, static dispatch resolved-symbol convergence, Vec index-assignment metadata, fixed-array destructuring/std-slice-index, Cell std-surface, and Option closure-plan slices, the no-fallback `tests/test_unit_*.sla` sweep was 48/64 passing. The then-remaining direct-SAB gaps included async/for-in/enum/set/derive semantics shapes, borrow-temp release ordering, rc/dyn/refcell/smart-pointer paths, import-contract extern callee resolution, std import, struct update, and var comprehensive.
    - Progress estimate: 100% for these three direct-SAB divergence fixes; broader Y/shared-lowering progress is approximately 58%, while direct SAB fallback removal is approximately 81% by feature track after the later convergence slices.
  - [x] Converge array-typed struct field ABI through shared lowering rules instead of a SAB-only layout branch.
    - Progress: `lowering_rules.zig` now owns shared ABI size, aggregate alignment, struct/tuple field layout, and inline fixed-array element layout helpers.
    - Progress: both `codegen.zig` and `sab_codegen.zig` delegate aggregate layout to those helpers. This preserves SA-text behavior while making direct SAB represent `[T; N]` fields as pointer-sized slots, matching the arrays generated by literal lowering.
    - Verified in the current tree: `zig build --summary all`; focused shared ABI Zig unit filter; no-fallback SAB for `test_unit_array_struct_field_cleanup.sla`, `test_unit_struct_field_array_loop.sla`, `test_unit_arrays.sla`, and `test_unit_field_array_cleanup.sla`; SA-text backend for the two fixed fixtures. Full no-fallback `tests/test_unit_*.sla` sweep at this point was 38/64 passing.
    - Progress estimate: 100% for array-typed struct field ABI convergence; broader Y/shared-lowering progress at this point was approximately 48%, and direct SAB fallback removal was approximately 73% by feature track.
  - [x] Consume the shared dyn-borrow call-materialization plan in direct SAB.
    - Progress: pure trait/dyn naming helpers (`mangleTraitMethodName`, `concreteTypeName`, `dynTraitName`, `vtableName`, `dynMethodSlot`) moved into `lowering_rules.zig` and are now delegated by SA-text and SAB emitters.
    - Progress: direct SAB emits trait impl method functions and trait vtable consts, materializes `&Concrete -> &dyn Trait` arguments through `DYN_NEW`, and lowers focused no-extra-arg dyn method calls via shared slot offsets plus structured `call_indirect`.
    - Verified in the current tree: `zig build --summary all`; no-fallback SAB for `test_unit_dyn_borrow_arg.sla`; SA-text backend for the same fixture; no-fallback SAB regression for `test_unit_overload_add.sla`. Full no-fallback `tests/test_unit_*.sla` sweep at this point was 39/64 passing.
    - Progress estimate: 100% for direct SAB dyn-borrow argument materialization through the shared plan; broader Y/shared-lowering progress at this point was approximately 50%, and direct SAB fallback removal was approximately 74% by feature track.
  - [x] Converge inherent and trait static dispatch through the shared resolved-call symbol plan.
    - Progress: type checking for `Type::method(...)` and `value.method(...)` now records resolved implementation symbols, so static dispatch flows through `StaticCallPlan` for both SA-text and SAB.
    - Verified in the current tree: no-fallback SAB and SA-text backend for `test_unit_impl_static_methods.sla` and `test_unit_trait_static_dispatch.sla`; full no-fallback `tests/test_unit_*.sla` sweep at this point was 43/64 passing. This also moved `test_unit_derive_component.sla` and `test_unit_using_static_extension.sla` into the passing set.
    - Progress estimate: 100% for static dispatch resolved-symbol convergence; broader Y/shared-lowering progress at this point was approximately 52%, and direct SAB fallback removal was approximately 76% by feature track.
  - [x] Fix thread-closure SAB call lowering so captured-argument calls keep a pure call target symbol instead of generating illegal `@func(arg)` call text; reproduce with `/home/vscode/projects/sla_ecs/lib/parallel.sla`.
    - Note: the original fallback `parallel.sla` ForbiddenSyntax repro is unblocked in the updated SCI host by accepting fallback-generated single-operand structured `panic_msg` in SAB v4 decode/verify.
    - Boundary: this completed the focused thread-closure/parallel path at the time. The current active slice has a separate general call-plan bug where some void/static calls still disassemble as a target string containing arguments, so the broader pure-call-target audit remains open under Slice 1D / Phase 3.
    - Progress: direct no-fallback lowers the focused escaped thread closure case `thread::spawn(^|| f(value))` where `f` is a captured function-pointer callee, using generated vtable/spawn-wrapper/worker entries and structured `call_indirect` inside the worker.
    - Progress: the full `SLA_SAB_NO_FALLBACK=1 ... /home/vscode/projects/sla_ecs/lib/parallel.sla` path now passes after expanding std-dependency preloading and moving typed `Vec<T>` index reads to std surface metadata.
    - Progress estimate: 100% for the reported illegal call-target blocker.
    - Verified with `SLA_SAB_NO_FALLBACK=1 SLA_PROFILE=1 timeout 180s ./zig-out/bin/sla-local-cli sla test /home/vscode/projects/sla_ecs/lib/parallel.sla --test-backend sab --jobs 1 --trace-panic`.
  - [x] Replace the temporary direct `Vec<T>` index ABI lowering with typed std surface metadata/macro lowering once `sa_std` exposes typed element-load slice/vec macros.
    - Progress: `sla_std/std_surface.sla_meta` uses `VEC_GET_TYPED_{elem_ty}`, direct SAB carries `StdSurfaceArgKind.elem_ty`, `elementLoadType`, and `stdSurfaceMacroName`, and concrete typed Vec/Slice macros provide the element-width-specific loads.
    - Progress estimate: 100%; `genIndex` now uses the generic std surface rule for Vec indexing instead of a direct Vec ABI branch.
  - [x] Move direct SAB `Vec<T>` index assignment onto std surface metadata instead of a compiler-side Vec ABI branch.
    - Progress: `sla_std/std_surface.sla_meta` now has an `index_assign` rule for `Vec`, direct SAB parses/preloads/emits that rule generically, and `sa_std/vec.sa` exposes `VEC_SET_TYPED` with `receiver,index,value,elem_size,elem_ty` so scalar and pointer-backed struct writes share one std-surface contract.
    - Progress: std macro-fragment caching now falls back to real-argument flattening when placeholder type operands are invalid, allowing typed std macros to use concrete primitive type names without per-type compiler branches.
    - Verified in the current tree: no-fallback SAB and SA-text backend for `test_unit_vec_index_assign.sla`; no-fallback SAB regressions for `test_unit_vec_len_direct.sla`, `test_unit_arrays.sla`, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; the later fixed-array destructuring/std-slice-index slice moves the full no-fallback `tests/test_unit_*.sla` sweep to 46/64 passing.
    - Progress estimate: 100% for metadata-driven `Vec<T>` index assignment; broader Y/shared-lowering progress is approximately 55% after the later slice, and direct SAB fallback removal is approximately 79% by feature track.
  - [x] Add direct SAB fixed-array destructuring with blank identifier/rest slice support through shared array/slice ABI and std surface metadata.
    - Progress: `lowering_rules.zig` now exposes `SliceAbi` and fixed-array rest-length calculation, and direct SAB `genLetDestructure` uses those rules plus shared array element layout for `[_, name, ..rest]` instead of returning `UnsupportedSabDirectFeature`.
    - Progress: ordinary `let _ = value` discards the generated value without defining `_`, and rest slices keep the temporary array owner alive until scope cleanup.
    - Progress: `sla_std/std_surface.sla_meta` now maps `Slice<T>` indexing to typed `SLICE_GET_TYPED_{elem_ty}` macro wrappers, so `rest[0]` uses the same metadata/macro path as `Vec<T>` indexing rather than a compiler-side Slice branch.
    - Verified in the current tree: local and host no-fallback SAB for `test_unit_blank_identifier.sla`; SA-text backend for the same fixture; host no-fallback SAB regressions for `test_unit_arrays.sla`, `test_unit_vec_index_assign.sla`, `test_unit_user_macro_direct.sla`, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; `zig build --summary all`; `zig build test --summary all`; full no-fallback `tests/test_unit_*.sla` sweep is 46/64 passing.
    - Progress estimate: 100% for fixed-array destructuring/blank identifier direct SAB support; broader Y/shared-lowering progress is approximately 55%, and direct SAB fallback removal is approximately 79% by feature track.
  - [x] Move focused `Cell<T>` construction/get/set support onto std surface metadata instead of compiler-side Cell branches.
    - Progress: associated std-surface calls can now materialize a `value` argument when the metadata rule asks for it, covering constructor-like APIs such as `Cell::new(value)` generically.
    - Progress: `sla_std/std_surface.sla_meta` describes `Cell new/get/set`, and `sa_std/core/cell.sa` exposes `CELL_NEW_VALUE` as a wrapper over the existing Cell layout macros.
    - Verified in the current tree: official `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host no-fallback SAB for `test_unit_cell_bool.sla`; SA-text backend for the same fixture; host no-fallback SAB regressions for `test_unit_option_direct.sla`, `test_unit_vec_len_direct.sla`, `test_unit_blank_identifier.sla`, and `test_unit_vec_index_assign.sla`; `zig build --summary all`; `zig build test --summary all`; full no-fallback `tests/test_unit_*.sla` sweep is 47/64 passing.
    - Progress estimate: 100% for metadata-driven Cell bool support; broader Y/shared-lowering progress is approximately 56%, and direct SAB fallback removal is approximately 80% by feature track.
  - [x] Move closure-taking `Option` methods onto a shared plan consumed by both emitters.
    - Progress: `lowering_rules.zig` now owns `OptionClosureCallPlan` for `map`, `and_then`, and `unwrap_or_else`; `codegen.zig` delegates Option closure-method classification to that plan, and `sab_codegen.zig` consumes it to emit structured branches plus inline closure bodies.
    - Progress: `sla_std/std_surface.sla_meta` exposes `method Option get ... OPTION_GET out,receiver`, so direct SAB gets Some payloads through std surface macro fragments rather than a compiler-side Option layout branch.
    - Verified in the current tree: official `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host no-fallback SAB and SA-text backend for `test_unit_option_methods.sla`; host no-fallback SAB for `/home/vscode/projects/sla_ecs/lib/parallel.sla`; local no-fallback regressions for `test_unit_option_direct.sla` and `test_unit_result_direct.sla`; `zig build --summary all`; `zig build test --summary all`; full no-fallback `tests/test_unit_*.sla` sweep is 48/64 passing.
    - Progress estimate: 100% for shared-plan Option closure-method support; broader Y/shared-lowering progress is approximately 58%, and direct SAB fallback removal is approximately 81% by feature track.
  - [x] Finish focused smart-pointer construction/deref/borrow support on shared lowering rules plus std surface metadata.
    - Target covered: `Box::new`, `Rc::new`, `Arc::new`, focused `RefCell::new`, `*` / `&*` deref-address paths, pointer-backed index-address metadata, Rc/Arc clone wrappers, and RefCell struct-payload borrow/release paths through shared smart-pointer classification and metadata-driven std macro fragments.
    - Shared contracts: `src/lowering_rules.zig` owns smart-pointer helpers and `associatedRuleNeedsUnderlyingSmartPointer`; `sla_std/std_surface.sla_meta` owns Rc/Arc clone and Vec index-address metadata; `sa_std` owns `RC_CLONE_OUT`, `ARC_CLONE_OUT`, and `VEC_GET_MUT_PTR_U64` wrappers.
    - Important correction: the temporary SAB-only `genRcArcCloneCall` branch was removed. Rc/Arc clone now flows through std-surface associated metadata and shared receiver materialization, while the type checker returns `unwrappedReceiverType(recv_ty)` for clone calls on borrowed receivers.
    - Verified gates: `zig build --summary all`; `zig build test --summary all`; official `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; local and host no-fallback SAB for `tests/test_unit_borrow_temp_release_order.sla` 25/25, `tests/test_unit_refcell_struct_payload.sla` 2/2, `tests/test_unit_smart_pointer_struct_field_cleanup.sla` 3/3, `tests/test_unit_option_methods.sla` 5/5, `tests/test_unit_option_direct.sla` 1/1, `tests/test_unit_result_direct.sla` 1/1, and `/home/vscode/projects/sla_ecs/lib/parallel.sla` 1/1.
    - Disasm gate: tracked focused artifacts no longer display illegal visible call targets matching `@func(arg)`; SCI displays call target and arguments separately for `sa sla sab disasm` while preserving the runtime-compatible call-body representation.
    - Full local no-fallback sweep after this slice: 50/64. The remaining failures are assigned in the top-level 50/64 ownership matrix, not to the completed RefCell/Rc clone blocker.
    - Progress estimate: 100% for focused smart-pointer/borrow/RefCell convergence; broader Y/shared-lowering was approximately 66% and direct SAB fallback removal was approximately 85% after this historical slice. Later slices moved the current baseline to 62/69, approximately 79% Y/shared-lowering, and approximately 92% direct SAB fallback removal.
  - [ ] Extract a shared address-materialization plan for borrow/postfix precedence chains and consume it from both SA-text and SAB emitters.
    - Target: represent addressable sources such as `&item.field`, `&items[index]`, `&*smart`, `&**holder.item`, and nested field/index/deref chains as one shared plan that records base expression, projections, smart-pointer deref operations, required std-surface rules, and release order.
    - Reason: `tests/test_unit_borrow_temp_release_order.sla` is not only smart-pointer construction; it stresses owner temporary cleanup across fields, indexes, loops, branch merges, call arguments, and early returns. This should converge through a shared address plan rather than ad hoc `genAddressOf` behavior in one backend.
    - Implementation direction: add address/projection structs to `lowering_rules.zig`; keep type- and precedence-sensitive classification shared; let SA-text print the existing textual operations from the plan while SAB emits structured loads/stores/std macro fragments from the same plan.
    - Verification gate: no-fallback SAB plus SA-text for focused borrow/preference fixtures: `tests/test_unit_borrow_direct.sla`, `tests/test_unit_borrow_temp_release_order.sla`, array/index borrow tests, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`.
    - Progress estimate at task creation: 0% for shared address-materialization; expected to move broader Y/shared-lowering by about +4 to +6 points when complete.
  - [ ] Move RefCell borrow/borrow_mut handle lifecycle onto a shared borrow-lifecycle plan instead of copying the SA-text cleanup maps into SAB.
    - Target: cover `RefCell<T>::borrow`, `borrow_mut`, payload pointer extraction for pointer-backed payloads such as `RefCell<Box<T>>` and `RefCell<RefCellPayload>`, panic-on-conflict lowering, and deterministic release on normal scope exit, branch merge, loop iteration, void call argument cleanup, and early return.
    - Implementation direction: define shared plan data for borrow mode, handle owner cell register/expression, payload type, release macro, borrowed value register, and branch/loop merge policy; SA-text keeps its current macro spelling through the shared plan, while SAB emits structured std macro fragments and release branches from that same contract.
    - Current local evidence: borrow-temp, smart-pointer cleanup, and RefCell struct-payload fixtures are green through the completed focused regressions. This lifecycle task remains architectural hardening for future guard-release or payload-pointer failures.
    - Boundary: broader `Mutex`/`RwLock` guard lifetimes can reuse this shape later but are not part of the first RefCell completion unless a verified fixture requires them.
    - Verification gate: no-fallback SAB and SA-text for `tests/test_unit_borrow_temp_release_order.sla`, `tests/test_unit_refcell_struct_payload.sla`, focused RefCell demos, and existing Cell/Option/Result regressions; full no-fallback sweep count must be updated.
    - Progress estimate at task creation: 0% for shared RefCell lifecycle; expected to move direct SAB fallback removal by about +2 to +4 points if both borrow fixtures pass.
  - [ ] Finish macro-expansion convergence through the Y-shaped shared lowering path.
    - Target: all user macro expansions that survive parsing/type checking should lower through one shared expansion/substitution contract before emitter-specific output, including nested macro calls, macro-local hygiene, argument substitution, borrow/move prefixes, aggregate/index expressions, and macro-expanded static calls.
    - Current baseline: focused direct SAB user macro expansion, macro-expanded static-call materialization, and focused SA-text inline parity for `tests/test_unit_user_macro_direct.sla` already pass; remaining work is to keep future macro features from creating separate SA-text/SAB semantic branches and to add no-fallback coverage whenever a macro expansion gap appears in the sweep.
    - Implementation direction: put new macro classification or argument/materialization decisions in `lowering_rules.zig` or the shared frontend expansion stage, then have both emitters consume the same expanded AST/plans. Do not add SAB-only macro semantics for library names.
    - Verification gate: no-fallback SAB and SA-text for `tests/test_unit_user_macro_direct.sla`, macro-heavy direct tests, `tests/test_unit_pkgjson_codegen.sla` if it remains macro-related after triage, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`.
    - Progress estimate at task creation: focused macro support is mostly complete, but macro convergence hardening is approximately 70%; mark a sub-slice 100% only after a failing macro-shaped fixture is fixed and installed through `sa plugin install --dev .`.
  - [ ] Generalize std-surface metadata for remaining library-shaped direct SAB gaps instead of adding library branches.
    - Target: extend metadata/rule consumers for smart-pointer clone/as_ptr/from_raw/get_mut, collection/set helpers, std import helpers, and result/option-like branch rules only when a failing no-fallback fixture proves the need.
    - Current known failing candidates from the 62/69 sweep: possible std/import collection surfaces discovered while fixing `tests/test_unit_std_import.sla` or `tests/test_unit_sla_import_nested_contract.sla`. `tests/test_unit_sets.sla`, `tests/test_unit_rc_dyn_trait.sla`, and focused smart-pointer/RefCell tests are completed regressions.
    - Implementation direction: prefer new data-driven rule kinds or shared plans over `if type == Vec/Rc/Set` branches in `sab_codegen.zig`; every new std rule should list macro import path, argument slots, deps, panic behavior, receiver materialization, and result/void contract in `sla_std/std_surface.sla_meta`.
    - Completed metadata example to preserve: Rc/Arc clone now uses metadata plus shared borrowed-receiver materialization. Do not reintroduce a SAB-only clone branch when extending Rc/dyn or Arc helpers.
    - Verification gate: each completed library-shaped slice needs local build/test, official plugin install, `SA_PLUGIN_DEV=1 sa sla help`, focused no-fallback host test, and regression check for `parallel.sla`.
    - Progress estimate at task creation: metadata infrastructure is approximately 65% mature; this track will close remaining direct SAB fallback by several small 100% slices.
  - [x] Struct update (`..base`) through shared aggregate/update plans.
    - Done: both direct-SAB struct-literal emitters and SA-text route every field through `lowering_rules.planStructLiteralField`; `StructLiteralFieldPlan` carries shared ownership inputs (`field_ty`, `release_loaded`) and `StructLiteralFieldTransfer` now owns direct/deep-copy/move classification. Pointer-backed update fields no longer use the previous direct SAB narrowing for tracked safe move/deep-copy cases.
    - Verified: `zig fmt --check src/lowering_rules.zig src/sab_codegen.zig src/codegen.zig`; `zig build --summary all`; `zig build test --summary all` 64/64; `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host SAB no-fallback and SA-text parity for scalar and pointer-backed struct-update fixtures; `parallel.sla` no-fallback regression; full host no-fallback sweep 72/72; disasm guard clean.
  - [ ] Triage and batch the remaining non-library language gaps through shared rules before removing the fallback.
    - Current 63/69 failure list: async/await, derive semantics, enum match, for-in protocols, generic for-in, and spaceship compare.
    - Historical planned batches still apply: enum/match + derive/spaceship shared data-layout rules; for-in protocol + generic iterator lowering; import/extern callee resolution in the shared frontend trunk if it reappears; async/await last unless a smaller shared Future rule emerges.
    - Completion rule: do not call the overall Y-shared work complete until the full `SLA_SAB_NO_FALLBACK=1` sweep is green or every remaining fallback use has a documented shared-rule task with a concrete failing fixture.
    - Verification gate: update pass/fail counts after every feature; commit only verified completed slices, not partial exploration.
    - Current progress estimate after the struct update slice: overall Y/shared-lowering approximately 80%; direct SAB fallback removal approximately 93%; no-fallback unit-file pass rate 63/69.
  - [ ] Add generic exported closure/function-object entry lowering before enabling no-fallback thread-spawn style cases; do not copy the legacy text backend's `thread`-specific lowering into `sab_codegen.zig`.
    - Progress: first direct entry model is in place for zero-arg escaped thread closures, including capture collection, slot materialization, vtable const emission, worker emission, and FFI spawn wrapper emission. General exported closure/function-object lowering beyond this focused consumer remains open.
  - [ ] Remove the remaining in-memory SA-compatible fallback from the normal SAB path by replacing std/macro/closure gaps with generic direct SAB lowering or a generic SAB macro representation, not library-name special cases.
    - Progress estimate: approximately 85% overall direct SAB fallback removal; next priority is macro/pkg classification, then direct SAB consumption of the remaining shared call/materialization plan kinds and result-destination planning so both emitters reuse lowering decisions.
  - [ ] Expand the shared static-call plan into a full call/materialization plan, including parameter-aware auto-borrow, array-to-slice borrow, dyn fat-pointer borrow, temp release policy, and result destination contract shared by SA text and SAB emitters.
    - Progress: shared release policy, auto-borrow predicates, resolved static-call argument materialization, resolved static-call dyn fat-pointer borrow materialization, SA text legacy call array/dyn/materialization adoption, SAB ordinary static-call value/auto-borrow consumption, SAB ordinary static-call array-to-slice borrow materialization for the focused fixture, SAB macro static-call value/auto-borrow consumption, and a drafted `void` result-destination plan are in place; remaining pieces are validation of the result-destination contract plus direct SAB materialization for the remaining dyn/copy/result-destination and broader macro-expanded call shapes.
    - Current status: focused tracked artifacts pass the visible pure-call-target disasm grep, and `tests/test_unit_array_to_slice_call.sla` now passes local/host SA-text plus strict direct-SAB no-fallback. The broader call-plan task remains open for remaining materialization kinds and any future fixture whose SAB cannot express target/args through the shared call plan. Macro-body array-to-slice through macro parameter address semantics remains a Phase 2 macro-convergence gap, not a completed Phase 3 call-plan surface.
    - Verification for the next call-plan sub-slice: focused no-fallback for the failing call/materialization fixture, SA-text parity, `parallel.sla` no-fallback regression, and disasm grep proving tracked SAB artifacts contain no visible call target matching `@func(`.
- [x] **SLA CLI Project Helpers**
  - [x] Add `sa sla init [path]` to scaffold a minimal SLA binary project without overwriting existing files.
  - [x] Add `sa sla skills [--json]` to list plugin capabilities and generate Codex/Claude agent skill files in text mode.
  - [x] Include `sla init` and `sla skills` in plugin skill descriptors and `sa sla help` / per-command help output.
  - [x] Verify host-dispatched `SA_PLUGIN_DEV=1 sa sla skills --json` returns JSON, not text output.
  - [x] Add focused command tests for skills JSON, agent skill generation, and init overwrite protection.
- [x] **SA std imports**
  - [x] Parse top-level `@import "..."` declarations without adding new keywords.
  - [x] Preserve direct `sa_std/...` imports in generated SA.
  - [x] Load imported `.sai` and `.sal` contracts before type checking so std externs resolve in Sla.
  - [x] Resolve `sa_std/...` contracts from `SA_STD_DIR` or `$HOME/projects/sci/sa_std` during Sla type checking.

### 6. Edge Cases & Validation
- [x] **Internal Function Calls**
  - [x] Correctly compile internal Sla calls to native SA `@call` instructions instead of macro expansions.
- [x] **Recursive Loop Allocation Hoisting**
  - [x] Traverses nested loops and conditional branches inside loops recursively to hoist all stack allocations.
- [x] **Hygienic Macro Variable Renaming**
  - [x] Tracks all declared macro-local variables and maps references to their unique mangled names during codegen.
- [x] **Block vs Return Cleanup Resolution**
  - [x] Excludes returned variables from early return cleanup lists, and prevents block-level exit cleanups from overriding return statement cleanup actions.

### 7. Unit Tests & Build Automation
- [x] **Integrated build test Step**
  - [x] Added `zig build test` target in `build.zig` to compile and run all unit tests automatically.
- [x] **Monomorphizer Unit Test**
  - [x] Implemented in `src/monomorphizer.zig` to verify specialization of generic types.
- [x] **Codegen Unit Test**
  - [x] Implemented in `src/codegen.zig` to verify translation of binary expressions and return statements to SA.

### 8. Sla Unit Test Framework (`@test`)
- [x] **Lexer: `@` token + comparison operators**
  - [x] `at` token tag for `@` symbol (no new keyword).
  - [x] `==`, `!=`, `<=`, `>=` two-char token tags added to lexer.
- [x] **AST: `TestDecl` node**
  - [x] `TestDecl` struct in `src/ast.zig`: `name`, `is_ignored`, `should_panic`, `body`.
  - [x] `test_decl: TestDecl` variant added to `Node` union.
- [x] **Parser: `@test` declaration parsing**
  - [x] Parses `@test [ignored] [should_panic] "name"() { ... }` syntax.
  - [x] No new keywords consumed — `ignored` and `should_panic` are identifiers.
  - [x] New comparison ops (`==`, `!=`, `<=`, `>=`) parsed in `parseInfixExpr`.
- [x] **Type Checker: `checkTest`**
  - [x] Type checks test body as void-returning, no-parameter block.
  - [x] `panic()` recognized as built-in void intrinsic.
  - [x] `if` condition accepts integer comparisons (0/1) in addition to booleans.
- [x] **Monomorphizer: `test_decl` traversal**
  - [x] Specializes generic calls inside test bodies.
- [x] **Codegen: `genTestDecl`**
  - [x] Emits `@test [ignored] [should_panic] "name"():` SA header.
  - [x] Unique entry labels per test (`L_TEST_ENTRY_N`).
  - [x] `panic(code)` lowered to SA `panic(code)` syntax.
  - [x] Loop allocation hoisting works inside test bodies.
- [x] **CLI: `sa sla test <file.sla> [options]`**
  - [x] Defaults to `--test-backend auto`, compiling `.sla` to managed SAB under `.sla-cache/sab/` and spawning `sa test <file.sab>`.
  - [x] Supports `--test-backend sab` for explicit SAB output and `--test-backend sa` for the legacy `.sla` -> `.test.sa` temp file path.
  - [x] Strips plugin-only `--test-backend` before forwarding remaining args to `sa test`.
  - [x] Applies `--filter` pruning before monomorphization/type checking on both SAB and SA test paths to keep focused tests small.
  - [x] Skill entry added: `sla test <file> [--test-backend auto|sab|sa] [sa-test-options...]`.
- [x] **Example test files**
  - [x] `tests/test_unit_basic.sla` — add/factorial/counter/ignored tests.
  - [x] `tests/test_unit_panic.sla` — `should_panic` and `ignored+should_panic`.
  - [x] `tests/test_unit_generics.sla` — `Option<T>` generics inside `@test`.

## 2026-07-01 Update: Focused Smart-Pointer / RefCell Slice Verified

- [x] 修复 `Rc::clone(&rc1)` / `Arc::clone(&arc1)` 借用接收者路径，并保持 Y 结构：clone 语义现在通过 `sla_std/std_surface.sla_meta` 的 associated metadata、`sa_std` 的 `RC_CLONE_OUT` / `ARC_CLONE_OUT` 宏、以及 `src/lowering_rules.zig` 的共享 `associatedRuleNeedsUnderlyingSmartPointer` 规则汇合。
- [x] 移除了临时 SAB-only `genRcArcCloneCall` 分支；不要在后续 Rc/Arc 扩展里恢复这种库名分支。
- [x] 修复 type checker 根因：`Rc::clone(&rc1)` / `Arc::clone(&arc1)` 返回 `unwrappedReceiverType(recv_ty)`，避免把 `Rc<T>` 推断成 `&Rc<T>`。
- [x] 验证 focused gates：`zig build --summary all`、`zig build test --summary all`、`sa plugin install --dev .`、`SA_PLUGIN_DEV=1 sa sla help`、本地和 host no-fallback 的 borrow-temp、RefCell struct payload、smart-pointer cleanup、Option/Result 回归以及 `/home/vscode/projects/sla_ecs/lib/parallel.sla`。
- [x] `sa sla sab disasm` 的 tracked artifacts 不再显示 `@func(arg)` 作为 call target；SCI disasm 会把 target 和 args 分开显示，同时保留现有 runtime parser 兼容性。
- [x] Feature completion: smart-pointer / borrow / RefCell focused convergence 100%; Y/shared-lowering approximately 66%; direct SAB fallback-removal approximately 85%; current local full no-fallback sweep 50/64.
- [ ] 历史下一阶段优先级已被顶部 63/69 当前矩阵取代：继续推进 Phase 5 aggregate/enum/derive/operator、Phase 6 protocol、Phase 7 import/pkg、Phase 8 async，最后 Phase 9 69/69 gate。
