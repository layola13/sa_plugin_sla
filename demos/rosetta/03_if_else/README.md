# 003 If Else

This directory matches the branch-selection topic for the catalog slot.

- `main.rs`: Rust reference for the `if`/`else` max selection used by this slot.
- `main.sla`: Sla companion for the `if`/`else` max selection used by this slot.
