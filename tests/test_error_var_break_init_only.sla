fn bad_var_break_init_only(limit: i32) -> i32 {
    var x: i32;
    for i in 0..limit {
        x = i;
        break;
    }
    return x;
}

fn main() -> i32 {
    return bad_var_break_init_only(1);
}
