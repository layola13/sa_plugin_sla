# Current Plan

This is the short recovery point for active `sa_plugin_sla` work. Keep `tasks.md` and `progress.md` synchronized with this file before ending a slice or committing.

## Workspace Rules

- Implementation workspace: `/home/vscode/projects/sa_plugins/sa_plugin_sla`.
- Do not switch implementation or planning into `/home/vscode/projects/sla_ecs`; use `/home/vscode/projects/sla_ecs/lib/parallel.sla` only as a host regression target.
- Official CLI evidence must use dev plugin mode: run `sa plugin install --dev .`, then use `SA_PLUGIN_DEV=1 sa sla ...`.
- `./zig-out/bin/sla-local-cli` is allowed only for secondary debugging evidence.
- Do not rerun the 166-test Zig suite or the full `tests/test_unit_*.sla` corpus after every small edit. Use precise affected tests during iteration; reserve full sweeps for a larger milestone or explicit user request.
- Preserve the Y shape: `SLA source -> AST/typecheck -> shared frontend/lowering rules/std metadata -> {SA text emitter -> .sa -> sa compile, direct SAB emitter -> .sab -> sa compile}`.
- The shared layer is before the SA/SAB fork. Direct SAB must not be normalized through `.sa` text or a `.sla -> .sa -> .sab` path.
- Put semantic decisions in `src/lowering_rules.zig`, shared frontend/typecheck results, `sla_std/std_surface.sla_meta`, or `sa_std`; `src/sab_codegen.zig` should consume those contracts and emit structured SAB.
- Commit only verified completed slices. Do not stage unrelated README, generated `.test.sa` deletions, or unrelated status/docs files.

## Verified State

- Current user-macro Option-try state (2026-07-12): TypeChecker owns per-invocation macro try cleanup lists, excluding only the propagated Option argument; the list follows the active expansion in both emitters. Macro infer typing is resolved from actual argument context, and ordinary try branch lowering remains shared/reused. Some returns 42 after using the live Box; None returns the Option and releases Box/result locals. Build 7/7, full Zig 208/208, installed macro 2/2, filtered/custom try 3/3, owner 15/15, borrow-temp 25/25, imported macro 11/11, official install/help, and host `parallel.sla` pass. Next extend the same invocation metadata to Result/custom Result macro fixtures and audit nested macro try forwarding, without replacing the per-call key with shared AST cleanup.
- Current SA unsafe-inline hygiene state (2026-07-12): active inline macro metadata now lets value-producing unsafe blocks reuse hygienic local aliases/types until their tail and cleanup finish. Exact caller/local name collision is covered and returns 135 in SA/SAB. Build 7/7, full Zig 208/208, installed macro 2/2, Box unsafe 1/1, owner 15/15, borrow-temp 25/25, imported macro 11/11, official install/help, and host `parallel.sla` pass. Unsafe macro lowering is closed for this value-block/local surface. Next work should design shared postfix-try call-site cleanup metadata rather than attempting emitter-only early-return handling.
- Current user-macro unsafe state (2026-07-12): direct SAB has a macro-context unsafe value-block path built on the existing result-slot lifecycle and macro-local scoping. An unsafe-local computes 35 while a caller local remains 100, producing 135 in SA/SAB. Build 7/7, full Zig 208/208, installed macro 2/2, Box unsafe 1/1, owner 15/15, borrow-temp 25/25, imported macro 11/11, official install/help, and host `parallel.sla` pass. Two distinct next gaps are recorded: postfix try needs call-site early-return cleanup metadata, while SA inline unsafe blocks still need recursive hygienic renaming when an inner local has exactly the same name as a caller binding.
- Current user-macro enum-match state (2026-07-12): TypeChecker infers an infer macro scrutinee from the enum-qualified case pattern and records the concrete enum in shared expression metadata. Cached value parameters let SA-text/direct SAB reuse ordinary match lowering, including hygienic branch locals and result-slot merge. A payload match returns 36. Build 7/7, full Zig 208/208, installed macro 2/2, enum 2/2, borrowed enum 1/1, imported macro 11/11, official install/help, and host `parallel.sla` pass. Next distinct macro surface is postfix `?` or `unsafe`; match guards and Option/Result macro matches should be treated as separate extensions if they expose new inference/lifecycle behavior.
- Current macro expression-contained move state (2026-07-12): shared consumption recursion covers binary/field/index/slice/cast/borrow/deref/try/await shapes. A Box moved beneath deref returns 37 in SA/SAB; SA now materializes the raw source register for the load instead of treating `^source` as an address. Installed reuse is rejected as `UseAfterMove`. Build 7/7, full Zig 208/208, macro 2/2, all three macro diagnostics, Box raw 1/1, owner 15/15, imported macro 11/11, official install/help, and host `parallel.sla` pass. The focused explicit-move expression traversal audit is now closed; next choose a distinct macro semantic surface such as direct-SAB macro `match`, `try`, or `unsafe` lowering rather than adding equivalent scanner cases.
- Current aggregate-contained macro move state (2026-07-12): shared consumption scanning covers struct/update, enum, tuple, array, and repeat-array children. A Box moved through a macro into a struct field passes SA/SAB, while an installed diagnostic proves caller reuse is rejected as `UseAfterMove`. Build 7/7, full Zig 208/208, installed macro 2/2, both macro diagnostics, smart-pointer 3/3, owner 15/15, imported macro 11/11, official install/help, and host `parallel.sla` pass. Next close remaining expression recursion such as binary operands, index target/index, cast/borrow/deref/try/await, and struct assignment targets where a move can be nested below another expression node.
- Current macro ordinary-move-call state (2026-07-12): shared effect scanning follows call arguments and shared root lookup unwraps `^`/`&`, so a macro parameter explicitly moved into an ordinary function is both lvalue-bound and consumed at the caller. SA call-argument emission resolves the prefixed macro identifier to its caller alias. The Box move-parameter regression returns 39 in SA/SAB instead of SA `InvalidOperand` on literal `^value`. Build 7/7, full Zig 208/208, installed macro 2/2, conditional diagnostic, fn-ptr 12/12, owner 15/15, imported macro 11/11, official install/help, and host `parallel.sla` pass. Next audit macro explicit moves nested inside aggregate literals or other expression containers; the direct shared scanner currently covers statement/branch/loop and call shapes but not every aggregate expression kind.
- Current conditional macro-consumption state (2026-07-12): shared control-flow rules produce `never/always/conditional` effects and TypeChecker preserves them through nested macro forwarding. Both branches moving remains legal; one branch or a potentially zero-iteration loop moving is rejected as `MacroConditionalConsume`, preventing caller cleanup state from pretending the runtime path is unconditional. Build 7/7, full Zig 208/208, installed positive macro SA/SAB 2/2, nested diagnostic match, owner 15/15, smart-pointer/imported-macro gates, official install/help, and host `parallel.sla` SA/SAB pass. Next macro ownership audit should cover consumption through ordinary move-parameter calls inside macros, since the direct effect classifier currently focuses on explicit `^param`, release, and nested user-macro forwarding.
- Current nested user-macro consumption state (2026-07-12): TypeChecker recursively resolves macro parameter effects through user-macro calls, keyed by macro declaration and parameter index with a visiting set for cycles. Direct move/release classification remains in the shared pure control-flow rule. A wrapper forwarding a Box into an inner moving macro passes SA/SAB and no longer leaves SA caller cleanup stale. Build 7/7, full Zig 207/207, installed macro 2/2, owner 15/15, smart-pointer 3/3, imported macro 11/11, official install/help, and host `parallel.sla` SA/SAB pass. Next audit conditional consumption: the current boolean effect conservatively marks a caller consumed if any reachable macro branch moves it, which should either be rejected as branch-conditional ownership or upgraded to a path-sensitive effect plan rather than silently accepted.
- Current user-macro move state (2026-07-12): shared frontend control-flow rules classify macro parameters consumed by explicit move/release, and TypeChecker applies the effect to caller bindings. SAB marks the substituted caller register consumed; SA storage lowering removes the `^` prefix that is valid for call arguments but invalid as an assignment source, then transfers state from the raw register. A Box moved through a macro returns 42 without double cleanup. Build 7/7, full Zig 207/207, installed macro SA/SAB 2/2, owner 15/15, Box/smart-pointer/shallow-copy gates, imported macro 11/11, official install/help, and host `parallel.sla` SA/SAB pass. Next ownership audit should cover nested user-macro consumption propagation, because the current pure classifier deliberately handles direct macro-body effects but does not recursively resolve another macro's parameter effects.
- Current user-macro enum-literal state (2026-07-12): direct SAB macro expressions now emit user-defined payload and unit enum variants with the shared enum ABI tag/field layout while evaluating field values through macro context. Caller assignment followed by ordinary match proves payload 19+23 and unit replacement behavior. Build 7/7, full Zig 206/206, installed macro SA/SAB 2/2, enum SA/SAB 2/2, imported-macro SAB 11/11, official install/help, and host `parallel.sla` SA/SAB 1/1 pass. Next audit another distinct macro expression surface such as match/try/unsafe; prefer one whose semantics can reuse shared ordinary lowering rather than duplicating control-flow policy.
- Current user-macro loop-control state (2026-07-12): direct SAB macro `while`/numeric `for` expansion now pushes existing loop jump labels, uses shared `planLoopControl()` to avoid unreachable cleanup blocks, separates condition-false from break cleanup, and restores the loop body's released-register snapshot before exit-path emission. A macro range loop with both `continue` and `break` passes installed SA/SAB 2/2. Build 7/7, full Zig 206/206, imported-macro 11/11, borrow-temp 25/25, RefCell 8/8, official install/help, and host `parallel.sla` SA/SAB 1/1 pass. Next audit a distinct unsupported user-macro statement/expression surface; do not add equivalent loop-control cases unless they expose a new ownership/cleanup contract.
- Current nested imported-macro tracked-context closure (2026-07-12): two-level user macro wrappers now prove field/index/deref, read/write-through, and expression-output contexts in both emitters (11/11). Full Zig 206/206 and official macro/borrow/host gates pass. The tracked nested address/type context target is closed; next work should audit a distinct remaining macro surface rather than extend equivalent projection cases.
- Current nested imported-macro context state (2026-07-12): SA argument/type/address planning now resolves nested macro argument types through `macro_arg_types` via `resolvedTypeForExpr()`. Two-level user macro wrappers around imported aggregate field read/write preserve the original holder alias in SA and SAB, both 10/10. Full Zig 206/206 and official macro/host gates pass. Next broaden nested index/deref/output-context coverage or address remaining macro substitution gaps.
- Current macro call-signature classification state (2026-07-12): shared lvalue analysis consults TypeChecker function parameters, so ordinary by-value call uses are cached once while borrow/move uses remain lazy. Nested user/imported macros stay lvalue-conservative. A RefCell side-effect parameter repeated within one ordinary call passes SA/SAB with one evaluation. Full Zig 206/206 and official macro/host gates pass. Next target is nested imported-macro effective address/type context.
- Current user-macro argument evaluation state (2026-07-12): shared lvalue-use classification keeps write-through parameters lazy while direct SAB caches pure value parameters once, matching SA-text. Cached SAB registers are protected during expansion and temporary values are released afterward. A RefCell side-effect argument used twice proves one evaluation and result 2; full Zig 206/206 and all macro/borrow/host gates pass. Next work is deeper nested macro effective-argument type/address context or imported-macro nesting.
- Current result-slot store state (2026-07-12): shared `planResultSlotStoreLifecycle()` selects transfer/release/keep and both emitters' helpers own the action. SA block-tail and Option closure call sites no longer branch on `transfers_value`; SAB no longer performs a separate local source test. Full Zig 205/205, Option SA/SAB 5/5, RefCell SA/SAB 8/8 and official host gates pass. Result-slot load/store convergence is closed for this focused contract; next work is broader macro expansion/substitution planning.
- Current result-slot load state (2026-07-12): shared `planResultSlotLoadLifecycle()` makes non-transfer loads explicit no-ops and transfer loads restore RefCell/value metadata. Both helpers own the decision; SA call sites no longer guard individually. Full Zig 205/205, RefCell SA/SAB 8/8, Option methods 5/5 and official host gates pass. Next converge result-slot store lifecycle.
- Current preserved-value call-plan state (2026-07-12): shared target-aware planning selects `shallow_copy_preserved_value` from later-use plus SAB layout-capability inputs. Ordinary and macro-context SAB consume the action outside the generic value branch; SA-text retains direct value semantics. Full Zig 205/205, thread-partition SA/SAB 11/11, fn-ptr/imported-macro and official host gates pass. Next focus is shared result materialization and macro substitution.
- Current fn-pointer call-plan state (2026-07-12): shared target-aware materialization selects direct SA-text value passing versus direct SAB generated-symbol value-slot or local-slot borrow. The SAB value branch no longer owns those semantic tests. Focused local/installed fn-ptr passes SA/SAB 12/12, full Zig 205/205 and official host gates pass. Next converge preserved non-Copy value argument copy/consume planning, then shared result materialization.
- Current raw-pointer string call-plan state (2026-07-12): shared `CallArgMaterializationKind.raw_pointer_string_literal` now drives SA-text, ordinary direct SAB, and macro-context direct SAB materialization/release. The focused fixture includes a literal-to-`ptr` call and passes SA/SAB; full Zig 205/205 and official host gates pass. Next converge fn-pointer and preserved non-Copy value argument planning, then result materialization.
- Current special loop-consume state (2026-07-12): await, forget, ManuallyDrop extraction, Box raw conversion, and non-Copy try propagation now route through `consumeBinding()`. A Box::into_raw loop diagnostic joins explicit release and implicit move diagnostics; all match under the installed plugin. Full Zig 205/205 plus async, Box raw, owner-release, imported-macro, and host gates pass. User-expression consumed-state writes are now centralized; next work returns to shared call/materialization and macro expansion convergence.
- Current implicit loop-move state (2026-07-12): `consumeBinding()` now owns explicit release/move plus implicit let, assignment, and destructuring consumption. It rejects outer bindings only when the loop body can reach its back-edge; all-paths-terminating loop bodies retain the safe existing move-before-return behavior. Explicit and implicit diagnostic fixtures match, full Zig is 205/205, and owner-release/RefCell/imported-macro/host gates pass. Next extend the same contract to special consuming calls (`await`, forget, raw conversions) before returning to broader call/materialization convergence.
- Current outer-handle loop-consume state (2026-07-12): TypeChecker reports `LoopConditionalConsume` when an explicit `!binding` inside a loop targets a binding declared before that loop. This closes the Active-header/Consumed-backedge `PhiStateConflict` before either emitter. Nested and loop-body locals remain releasable, including the focused RefCell while/for fixture. Full Zig is 205/205; diagnostic, SA/SAB RefCell, owner-release, imported-macro, dev install/help, and host parallel gates pass. Next work is broader implicit move/ownership convergence or shared macro/call materialization.
- Current pointer-backed imported-macro projection state (2026-07-12): shared planning now classifies pointer-ABI aggregate field/index projections as `pass_pointer_backed_projection`, so SA-text and direct SAB pass the loaded aggregate pointer value through borrowed macro parameters instead of copying the pointee into a temporary slot. A macro-helper write regression mutates `holder.pair.left` in place and passes SA/SAB 9/9; full Zig 204/204 and official host gates pass. Remaining macro work is broader expansion/substitution convergence rather than this aggregate projection alias case.
- Current RefCell loop-state state (2026-07-12): shared `planRefCellLoopStateMerge()` restores pre-loop handle/borrow-temp metadata after loop-body generation. SA-text covers for/protocol-for/while/while-let; direct SAB restores the same metadata alongside released registers. The focused fixture now exercises repeated while/for borrows and a post-loop borrow; local/installed SA and strict SAB pass 8/8, full Zig passes 204/204, and all official host gates pass. Next work is conditional mutation of outer handles across loop exits or pointer-backed imported-macro alias planning.
- Current RefCell call-argument lifecycle contract state (2026-07-12): the shared keep/value-release/borrow-handle-release action now exposes `shouldRelease()` and `releasesBorrowHandle()`, and both emitters consume the final shared release decision instead of duplicating action-collapse switches. Shared-rule tests, build/full Zig tests, focused local and installed SA/SAB fixtures, dev install/help, and strict host `parallel.sla` pass. Next work remains fuller lifecycle state across scopes/loops/early returns and pointer-backed imported-macro aliases.
- Project snapshot/background timeout issue closed (2026-07-12): project shortcut rewriting now runs only in the pre-typecheck reachable pass; the post-typecheck syntactic pass no longer mutates typed AST nodes. This removes duplicate work and prevents TypeChecker/SAB `MissingType` desynchronization. Multi-configured passes strict 10s three times (5.64s/7.17s/7.29s) and profiled at 5.97s; background update/warm/wait, collection open/default, config registry, and snapshot config registry strict gates pass. Historical API files are absent. `docs/sab_project_snapshot_background_tests_timeout_issue_cn.md` is closed.

- Current base single-loose inferred state (2026-07-12): snapshot-derived collection facts now retain the primary active/open file, allowing the same exact state model to cover base + loose add/update/close (`root_count 1 -> 0`). Proven inferred defaults set through `set_file_default_for_open_file` reuse the existing lookup literal. State and cached-inferred regressions pass; real full file remains 3/3, while repeated strict 10s stays around 10.02-10.03s, so no stable timing claim is made.

- Current dual-loose inferred-root state (2026-07-12): a narrow root `KnownOpenCollectionState` starts only from known snapshot configured files plus an explicit clean-open collection, propagates two added open files, inferred update/default passthrough, and known secondary close, and folds root-count/contains/open-count/inferred guards before reachability. Exact state-machine regression passes. Real multi-configured remains 3/3 and improves about 10.70s -> 10.46s long-window; strict 10s remains marginally open. Next extend the same model to the base single-loose and session paths.

- Current folded-empty-guard cleanup state (2026-07-12): root project shortcut cleanup removes no-else constant `if false {}` statements after inferred result guards are folded, for both direct and expression-statement AST shapes. Focused inferred/cached regressions and build pass. This is a verified AST cleanup only; repeated real multi-configured strict 10s remains open.

- Current inferred result-chain root folding state (2026-07-12): before imported reachability, focused project shortcuts now constant-fold proven three-project snapshot/project-list/service-list panic guards and dead-prune their now-unused pure query chains. A regression with deliberately invalid heavy bodies proves those functions never enter direct SAB. Real multi-configured remains correct 3/3 and improves from about 11.52s to 10.70s in the stable long-window comparison; strict 10s still fails in repeated runs, so the remaining target is the larger inferred-root open/close/update state chain.

- Current remaining multi-configured audit (2026-07-12): per-root compile profiles identify the third inferred/session test as dominant (about 120 selected snapshot functions versus about 23/30 for the first two; roughly 2.0s import expand, 2.4s typecheck, 5.1s direct codegen under the diagnostic filtered run). Extending lightweight SessionState use to `project_session_from_snapshot` did not change the real graph and was fully reverted. Existing known-field pruning runs after imported reachability, so the next implementation must constant-fold proven inferred/session assertions during root project-shortcut rewriting before reachability, then remove dead heavy constructor chains.

- Current two-configured open-query state (2026-07-12): focused project facts track single-file program containment through configured projects and `project_snapshot_with_secondary_configured`; the open-project query folds to a two-path seven-field literal only when both known files match the collection open file. Focused regression and existing single-project guards pass. Real `test_project_collection_multi_configured_contract.sla` passes 3/3 in a 30s window at 11.52s; strict 10s remains open. Standalone SAB build improved 12.27s -> 10.50s, but output remains 3.1MiB because the file's other projects/default/inferred/session paths retain the main dependency surface.

- Current project open-configured layout state (2026-07-12): the focused single configured-project shortcut now emits all seven current `ProjectOpenConfiguredProjects` fields, including an explicit empty secondary result. Its focused Zig regression, build 7/7, official dev install, and downstream collection-open/default-cache strict SAB gates pass. The broader `test_project_collection_multi_configured_contract.sla` remains open; a diagnostic `sla sab build` finishes in 12.27s with a 3.1MiB output, dominated by 5.9s typecheck and 4.9s direct codegen because the two-project collection path is not yet folded.

- Current shallow/lazy imported-body closure state (2026-07-12): imported modules parse declarations/signatures first, cache exact skipped body spans, and incrementally materialize only reachable top-level/associated functions; lazy child discovery, same-name method narrowing, generic templates, dead-child skipping, and macro fallback are covered. Selective Parser 3/3 plus five exact focused filters pass; real 678KB module selection stays at single-digit parser time across five passes; build 7/7, dev install/help, and strict query pass. This architecture task is closed, while the broader real-world latency/corpus target remains separate.
- Current reachable-contract closure state (2026-07-12): focused test-codegen queues contract imports only from roots and currently contributing modules, retains the preload in the final TypeChecker, and reuses the Module Table for aliases. Dead/type-only/contributing/final-state regressions pass. Current real profiles are query `load contracts 0ms`, aliases 5ms and table `load contracts 0ms`, aliases 0ms; build 7/7, dev install/help, and strict query pass. The separate load-contracts/import-alias target is closed; remaining frontend work is import expansion.
- Current selective-parser gate state (2026-07-12): the historical broad `selectively parses` gate is no longer mixed with a strict direct-SAB host compile. Static matching finds only two Parser tests; the SA and SAB reachable-body materialization regressions have exact independent names. Broad 3/3, exact SA 2/2, exact SAB 2/2, build 7/7, dev install/help, and strict parallel_query 1/1 pass. The old stuck-gate audit is closed.
- Current prescan-expanded-source reuse state (2026-07-12): completed canonical import surfaces retain `source_expand` output, and Module Table `getOrParse()` reuses it when the prescanned child is later materialized as a module. Provisional cycle surfaces are excluded and direct/uncached roots use normal expansion. The shared-module hit regression plus generic/macro-session guards pass; build 7/7, official dev install, focused query SA, and strict SAB pass. Parallel_query reports 12 avoided expansions plus 20 avoided reads and sampled 813ms import expand / 3.82s wall. Next work remains deeper callable traversal/root resolution.
- Current prescan-source reuse state (2026-07-12): completed canonical import-type surfaces retain the original source bytes, and Module Table resolution of ordinary non-glob filesystem `.sla` children reuses them instead of rereading the file immediately after Parser prescan. Cycle guards are incomplete and never exposed as source; glob and standard-library resolution preserve the existing path. The shared-module hit-count regression plus diamond/generic/macro-session guards pass; build 7/7, official dev install, focused query SA, and strict SAB pass. Parallel_query reports 20 avoided source reads (12 surfaces, 25 type hits) and sampled 865ms import expand / 3.67s wall. Next work remains callable traversal/root-resolution reduction.
- Current unchanged-root scan fast-path state (2026-07-12): symbol/const/macro and exported-type root scanners compare the append-only referenced set count with their scanned set before iterating. Equal counts return immediately; explicit invalidation removes scanned entries so new module/macro/caller availability is still rescanned. The exact initial/no-op/invalidate/rescan regression plus incremental, macro-session, and imported-type tests pass; build 7/7, official dev install, focused query SA, and strict SAB pass. Query sampled 757ms import expand / 3.63s wall, though broader host timing remains noisy. Next work should focus on callable body traversal or root module resolution.
- Current exported-type reachability index state (2026-07-12): `SlaCallableIndex` owns direct name-to-declaration entries for structs, enums, traits, and type aliases. Initial/root/imported declarations populate it, lazy module discovery extends it, and full reparses refresh its pointers. Referenced type signatures no longer linearly search all modules for every newly discovered type root. The four-kind index regression plus imported-signature, generic-argument, incremental, and macro-session tests pass; build 7/7, official dev install, focused SA hosts, and strict parallel_query SAB pass. Query had a good 820ms/3.58s sample while table was noisy at 1625ms/6.83s, so only the structural search-complexity reduction is claimed. Next profiling should target callable/body traversal or root module resolution.
- Current shared import-type surface cache state (2026-07-12): recursive parser prescans cache canonical `.sla` paths to their exact transitive type/enum surfaces, restore those surfaces on hits, and use provisional empty entries to stop cycles. `SlaModuleTable` carries the cache across independently created declaration-only module parsers, eliminating repeated dependency reads without globally exposing unrelated type names. Parser diamond/repeated-import and Module Table shared-dependency regressions pass, as do cached-reparse/span/generic/macro-session guards; build 7/7, official dev install, focused SA hosts, and strict parallel_query SAB pass. Profile output now reports cache entries/hits; parallel_query shows 12/25. Timing is noisy (query 769-899ms import expand and table 1073ms), so the verified claim is structural duplicate-scan elimination rather than a stable end-to-end latency win. Next work should inspect reachability-extension and root-resolution costs.
- Current final-TypeChecker contract reuse state (2026-07-12): focused test-codegen passes its caller-owned TypeChecker into import expansion, so the reachable contracts/macros already loaded for syntactic reachability remain available through monomorphization and final typecheck. The later `load contracts` stage is skipped only for this preloaded test path; non-test builds and compatibility callers keep their original behavior. A regression proves a contributing module's imported macro and extern survive deletion of their source files before final typecheck, while dead-module and type-only contract pruning guards remain green. Exact reuse/dead-module/type-only/referenced-macro/macro-session/resolved-import tests pass; build 7/7, official dev install/help, SA query/table hosts, and strict parallel_query SAB pass. Query profile is now import expand 802ms, load contracts 0ms, wall 3.77s (previous 998ms/305ms/4.47s); table is 1040ms/0ms/4.15s (previous 1096ms/247ms/5.08s). Next work should reduce root module resolution and repeated reachability extension inside import expansion.
- Current function-body span materialization state (2026-07-12): declaration-only Parser passes retain exact `{ ... }` source spans for skipped top-level and associated functions. `SlaModule` indexes those spans by exported callable symbol, and cumulative reachability selections parse only newly selected spans into the existing declarations, so the live callable/reachability session keeps stable pointers. Macro-body growth intentionally falls back to the prior full-module selected reparse. New Parser/Module Table regressions prove exact spans, incremental top-level/associated materialization, invalid-unselected-body isolation, and independence from the original module file after initial parse. Exact span/cache/incremental/macro-session/generic/registry/macro-fallback/namespace tests pass; build 7/7, official dev install/help, focused SA query/table hosts, and strict parallel_query SAB pass. Five 678KB `world_table_erased.sla` selection passes now spend about 4-5ms total parsing only new bodies; observed import expand is 998ms for parallel_query and 1096ms for parallel_table_erased, versus the previous 1849-2347ms range. Next work should profile and reduce root resolution, reachability extension, and macro-contract processing; selected-body reparsing is no longer the dominant cost.
- Current cached import-type prescan state (2026-07-11): the first decl-only module parse now stores the complete local/transitive imported type and enum name surface. Selected-body reparses seed those cached names and disable recursive `.sla` import type prescans, preserving imported struct-literal disambiguation without rereading the import graph. Reparse profiling now separates per-module source size, newly/total selected bodies, parser/export/commit time. Added a regression that deletes an imported child after the first parse and still successfully materializes a body containing the imported struct literal, proving the reparse consumes cached names. Exact cache/incremental/generic/macro-session/namespace Zig filters, build 7/7, dev install, focused SA profile, and parallel_query strict SAB pass. The pathological 3.9KB `table_erased_access.sla` reparse fell from about 582ms across three passes to 2ms because it no longer recursively prescans 678KB `world_table_erased.sla`; query secondary-module reparse fell 112ms -> 40ms, and table import expand measured 2242ms versus 2347ms. Query total wall timing was noisy at 2232ms, so no total-query improvement is claimed. The remaining proven hotspot is `world_table_erased.sla` itself, fully scanned five times for cumulative selected-body growth; next work should cache function body spans and materialize newly selected bodies in place.
- Current in-session macro-contract reachability state (2026-07-11): potential imported-macro calls are tracked as a distinct provenance kind from unresolved direct/associated functions, preserving caller identity even when the pre-contract call already resolves to an ordinary same-named function. Contributing contracts mutate the existing imported-macro map; the live `ReachabilitySession` marks only newly available macro records, requeues their exact caller/root, drains direct callee reachability, and continues lazy discovery/materialization without rebuilding callable/worklist/fact state. `UnresolvedCallableSet` now hash-buckets record indexes by call name, avoiding the O(n^2) cost exposed when every potential macro call was recorded. Added a focused same-name-function regression proving `macro_helper` is absent before contract refresh, becomes reachable afterward, and a second refresh is a no-op. Exact macro-session/lazy/generic/dead-child/macro/method/fact/namespace Zig filters, build 7/7, dev install, focused SA, and parallel_query strict SAB pass. Final clean profiles: parallel_query import expand 1849ms (prior 2258ms; macro contracts 560ms -> 228ms), parallel_table_erased 2347ms (older stable 4207ms; macro contracts 592ms -> 221ms); both macro stages report 0 reparses instead of 4/7. The remaining dominant cost is selected-body parsing/reparsing in the initial reachable-materialize phase, not reachability rebuild or contract refresh.
- Current lazy-discovery reachability-session state (2026-07-11): reachability now records unresolved direct and associated callable provenance separately from the mixed referenced-type/const/macro set, including caller and receiver type. A stable macro-contract phase keeps one callable/worklist/fact session while new modules are discovered; only callers whose unresolved callable gains a real declaration/candidate are requeued, while root calls are narrowly rescanned. Associated exports can drive lazy discovery using receiver-aware symbol matching. A focused regression proves a generic callable and typed associated method materialize after module addition while a same-named type-only reference does not select the colliding function. Exact session/generic/dead-child/macro/method/fact/namespace Zig filters, build 7/7, dev install, focused SA, and parallel_query strict SAB pass. Final clean-host parallel_query profile was 2258ms import expand versus the prior 2334ms, with first-phase module reparses remaining 26 rather than the rejected experiment's 41. `parallel_table_erased` wall timing remains noisy (3624-4837ms versus prior 4207ms), though one full reachability initialization per macro-contract phase is now structural. Next work must keep the same session across contributing macro-contract loads and requeue exact unresolved macro callers, eliminating the 4/7 macro-stage reparses visible in the two host profiles.
- Current incremental import-materialization state (2026-07-11): a stable module/macro-contract slice now builds `SlaCallableIndex`, worklist scan markers, exported-type roots, and cross-caller reachability facts once, then extends that state after each selected-body reparse. Reparse refreshes only declaration pointers, queues newly materialized function/alias bodies, and reopens newly materialized macro roots; module discovery or macro-contract growth still starts a fresh state boundary. Lazy discovery and selective append now allow an already-proven child to pass through a non-contributing wrapper without emitting that wrapper or invalid dead children. Added a three-body incremental-chain regression asserting 3 reparses/3 extensions/4 passes. Exact incremental, macro, method, lazy/dead-child, generic-template, fact-merge, and namespace Zig filters pass; build 7/7, dev install, clean-host focused SA, and parallel_query strict SAB pass. Warm clean `parallel_query import expand` improved 3483ms -> 2334ms (`reachable materialize` 2866ms -> 1514ms), and `parallel_table_erased` improved 6006ms -> 4207ms (5113ms -> 3257ms). The latency target remains open: 4-5 independent materialization initializations, selected-body reparsing, and macro-contract reload/reanalysis now dominate.
- Current import-expand profiling/convergence state (2026-07-11): profile output now separates root resolution, reachable materialization, macro contracts, selective append, and per-materialization pass/reparse/select/rebuild cost. Dynamic child discovery processes newly appended modules in the same call when current reachability already proves contribution. Exact lazy/dead-child/generic/namespace/fact tests, build 7/7, dev install, clean-host focused SA, and parallel_query strict SAB pass. Warm clean profiles remain slow (`parallel_query import expand 3483ms`, `parallel_table_erased 6006ms`); 4-5 materialization calls and repeated global reachability rebuilds dominate. Next performance work must cache or incrementally extend callable/reachability analysis across body-materialization passes rather than adding more selection heuristics.
- Current lazy generic-template discovery state (2026-07-11): unresolved `generic_func_ref` names are now shared referenced-symbol roots, so a transitive child module can be discovered and its selected generic body materialized before monomorphization. This restores clean-HEAD `sla_ecs` `ecs_box_drop<T>` paths that previously failed `TemplateNotFound` after import expansion. Exact Zig, build 7/7, dev install, two focused clean-host SA tests, and parallel_query strict SAB pass. Clean profile evidence is still poor (`parallel_query import expand 4828ms`, `parallel_table_erased 5946ms`), so correctness is restored but the primary latency objective remains open.
- Current empty-body void exit state (2026-07-11): audit confirms raw/borrow parameters are caller-managed in both backends. SA-text empty callees intentionally contain no parameter release, and call sites end argument temporaries; direct SAB consumes the same capability contract. A new fixture passes SA/SAB 1/1 and an exact Zig structural test locks the generated-SA shape. No compiler semantic change or full rerun was needed. This closes the tracked function-exit lifecycle audit.
- Current ordinary async pending-exit state (2026-07-11): TypeChecker records exact per-await cleanup metadata at the suspension point, excluding the returned future and its borrow-alias source chain while retaining materialized non-escaping locals/handles. SA-text consumes that metadata only on generated pending-return branches with branch-scoped state restoration; direct SAB retains its existing exact local cleanup. A new RefCell pending-exit fixture passes SA/SAB 1/1, and existing pending/parameter-await fixtures pass SA/SAB 3/3. Focused gates include the exact TypeChecker filter, build 7/7, and dev install. No full rerun; authoritative corpus remains 130/130.
- Current explicit/generated function-exit state (2026-07-11): `control_flow_rules.planFunctionExitCleanup()` is the cycle-free shared direct-result contract. TypeChecker records ordinary borrow-alias provenance so returned alias chains transfer their source borrow lifetime, while computed returns schedule RefCell dynamic-borrow cleanup. SA-text consumes the exact list and removes the rejected broad active-handle sweep; direct SAB keeps ordinary borrow aliases on their source register for exact result exclusion. Specialized single-await, two-await, and join2 SA-text entry emitters release stored state inputs and remaining parameters, matching direct SAB. The owner fixture retains fourteen prior tests, adds explicit computed/direct returns, and passes SA/SAB 15/15; a new borrow-alias fixture passes SA/SAB 1/1. Focused gates: four exact Zig filters, build 7/7, dev install/help, four representative async continuation fixtures SA/SAB, and Rosetta 026 plus alias-return SA/SAB. No full rerun; authoritative corpus remains 130/130.
- Current implicit-tail function-exit state (2026-07-11): shared lowering distinguishes direct tail-result transfer from ordinary local cleanup. SA-text emits the TypeChecker tail cleanup list before returning and excludes the transferred binding without using the rejected broad active-RefCell-handle sweep. Direct SAB evaluates the tail under caller-escaping result context and keeps exact result-register exclusion in `releaseOpenLocals`. SA-text also rebinds RefCell owner metadata for non-identifier initializer results, closing the stale temporary owner exposed by an existing if-result test. The owner fixture retains all thirteen previous tests, adds one tail cleanup/result-transfer case, and passes SA/SAB 14/14. Focused gates: exact lowering Zig filter, build 7/7, dev install/help, affected SA fixture, and strict no-fallback SAB fixture. No full rerun; authoritative corpus remains 130/130.
- Current postfix-try / let-else lifecycle state (2026-07-11): parser lookahead distinguishes postfix `result?` from ternary `flag ? a : b`; TypeChecker consumes non-Copy try containers and includes borrow locals in propagation cleanup. SA-text restores branch-local consumed/borrow/RefCell state after try and terminating let-else failure emission. Direct SAB structurally lowers Option/Result/custom Result try plus shared-plan let-else, including metadata-aware early-return cleanup that respects stack-managed locals. The owner fixture retains eleven prior tests and adds try plus let-else cases, passing SA/SAB 13/13. A new filtered/custom Result fixture passes SA/SAB 3/3 and exact filtered strict SAB 1/1. Focused gates: parser/TypeChecker/filtered-scope Zig filters, build 7/7, dev install/help, rosetta 105 SA/SAB 1/1, and `test_spec.sla` check/direct SAB build/disasm with try labels. No full rerun; authoritative corpus remains 130/130.
- Current loop-jump ownership cleanup state (2026-07-11): TypeChecker includes borrow-like locals in break/continue cleanup, so RefCell handles end on jump paths just as they do on ordinary block fallthrough. SA-text numeric for and plain while emit natural cleanup/backedges only when shared `blockTerminates()` reports a live body fallthrough. The owner fixture retains ten previous tests and adds one covering repeated while continue, direct while break, numeric-for continue/break, and nested return-owner-move versus break fallthrough, passing SA/SAB 11/11. Focused gates: two new Zig filters, shared loop-control Zig, build 7/7, dev install/help, loop-body cleanup SA/SAB 1/1, var comprehensive SA/SAB 16/16, plain/generic protocol for-in SA/SAB 1/1 each, and both loop initialization negatives remain UseBeforeInit. No full rerun; authoritative corpus remains 130/130. The temporary filtered-call scope hypothesis was disproven: call-body temps are present in decoded function reg scopes and serialized exact filters pass.
- Current match/switch ownership intersection state (2026-07-11): shared `control_flow_rules` owns the active/consumed/uninitialized value state, live-arm count plan, and state intersection. TypeChecker merges only live switch/user-enum/Option/Result arms and checks all Option/Result arms before returning a type. Direct SAB match emission snapshots/intersects release, RefCell handle, and borrow-temp state; case-condition temps now have path-consistent lifetimes and void match results are defined before cleanup. The owner-release fixture retains nine previous tests and adds one match case covering one-live plus all-live owner moves, passing SA/SAB 10/10. Focused gates: two Zig filters, build 7/7, dev install/help, enum match strict SAB 2/2, borrowed enum strict SAB 1/1, and the existing negative fixture remains UseAfterMove. No full rerun; authoritative corpus remains 130/130.
- Current chained-if-let ownership merge state (2026-07-11): TypeChecker now consumes `control_flow_rules.planBranchStateMerge()` for `if let` chains, matching ordinary `if` and direct SAB. Terminating then/else owner moves restore the only live branch state; two live branches retain Phi cleanup. The owner-release fixture keeps all eight prior tests and adds one chained case covering both terminating directions, passing SA/SAB 9/9. Focused gates: new and ordinary-if Zig filters, build 7/7, dev install/help, rosetta 104 SA/SAB 1/1, and the existing negative one-branch fixture remains UseAfterMove. No full rerun; authoritative corpus remains 130/130.
- Current ordinary-if ownership merge state (2026-07-11): `control_flow_rules.planBranchStateMerge()` is the pure shared termination contract used by TypeChecker and the lowering/emitter RefCell snapshot path. Terminating branch moves restore the live fallthrough state; two live branches still use Phi resolution; both terminating branches restore pre-state for continued code generation only. The owner-release fixture retains six previous tests and adds terminating-branch and if-result owner moves, passing SA/SAB 8/8. Focused final gates: three Zig filters 2/2 each, build 7/7, dev install/help, negative one-branch release remains UseAfterMove, RefCell payload SA/SAB 7/7, shallow-copy SA/SAB 3/3, borrow-temp SA/SAB 25/25, async branch SA/SAB 1/1, and host parallel SA/SAB 1/1. No full rerun; authoritative corpus remains 130/130.
- Current RefCell logical-owner release state (2026-07-11): release-only bindings survive shared pruning, non-Copy let aliases are moves in TypeChecker, and `planRefCellHandleOwnerTransfer()` rebinds all active handles from a moved source owner to its destination before release-by-cell scanning. The owner-release fixture retains its first four cases and adds multiple active handles plus moved-owner release, passing SA/SAB 6/6. Focused final gates also cover two Zig filters 2/2 each, build 7/7, dev install/help, imported-macro alias SA/SAB 8/8, shallow-copy SA/SAB 3/3, aggregate-reassign SA/SAB 1/1, RefCell payload SA/SAB 7/7, borrow-temp SA/SAB 25/25, and host parallel SA/SAB 1/1. No full rerun; authoritative corpus remains 130/130.
- Current RefCell call-argument lifecycle state (2026-07-11): `planRefCellCallArgLifecycle()` explicitly distinguishes keep, ordinary temporary release, and dynamic borrow-handle release after a call. SA-text and direct SAB planned-call cleanup consume the shared action. The owner-release fixture retains both prior tests and adds direct `if`-companion-to-call plus looped temporary shared/mutable borrow arguments; borrow counts return to zero after every call. Focused final gates: lowering 2/2, build 7/7, dev install/help, owner-release SA/SAB 4/4 each, RefCell payload SA/SAB 7/7 each, borrow-temp SA/SAB 25/25 each, imported-macro address SA/SAB 8/8 each, and host parallel SA/SAB 1/1. No full rerun; authoritative corpus remains 130/130.
- Current imported-macro aggregate-alias state (2026-07-11): moved pointer-backed aggregate bindings transfer cleanup ownership in direct SAB using shared `storedValueMovesIdentifier()`, and dereferenced borrowed aggregate aliases use the shared typed imported-macro action to pass the aggregate address directly rather than creating a ptr-to-ptr slot. Existing tests were retained and two regressions were added. Focused final gates: lowering 2/2, build 7/7, dev install/help, imported-macro address SA/SAB 8/8 each, borrow-temp SAB 25/25, RefCell SAB 7/7, shallow-copy SAB 3/3, smart-pointer cleanup SAB 3/3, and host parallel SA/SAB 1/1. No full suite/corpus rerun; authoritative corpus remains 130/130.
- Current shared RefCell value-state transfer state (2026-07-11): ordinary bindings and result-slot/branch transfers now consume one `lowering_rules.planRefCellValueStateTransfer()` bundle for borrow-handle plus borrow-address-temp movement. Concrete map/register mutation, direct-SAB same-register aliases, consumed markers, and release instructions remain emitter-local. Focused final gates: lowering regression 2/2, build 7/7, official dev install/help, borrow-temp SA/SAB 25/25 each, RefCell payload SA/SAB 7/7 each, imported-macro address SA/SAB 6/6 each, and host `sla_ecs/lib/parallel.sla` SA/SAB 1/1 each. No full suite/corpus rerun; authoritative strict no-fallback corpus remains 130/130.
- Current escaped loop-thread capture and corpus state (2026-07-11): direct SAB now loads the value held in a stack-backed scalar local before copying it into an escaped closure environment. This removes pointer-address-dependent behavior in the loop function-pointer thread case. Added a structural Zig regression and verified `test_unit_fn_ptr_value.sla` SA/SAB 11/11. A first milestone sweep after closing the old queue was 129/130 and exposed this interaction; the final identical dev-mode strict no-fallback sweep is 130/130. The 166-test Zig suite was intentionally not rerun per the no-repeated-full-test rule.
- Current user-macro emit reachability state (2026-07-11): called local macro bodies are now traversed during exact post-typecheck reachability, including nested local macros, so helper functions referenced only from inline-expanded macro code remain in final SA/SAB output. A scanned-macro marker prevents repeated/recursive traversal. This closes the missing `direct_macro_add`/`direct_macro_read_i64` callees. Focused final gates: user-macro reachability Zig regression, `zig build`, official dev install, user-macro SA 2/2 and strict SAB 2/2. No full sweep.
- Current cross-caller syntactic fact merge state (2026-07-11): conflicting known field values and local types are now removed when a reachable function is observed from multiple call sites. This fixes the shallow-copy fixture's false branch in both output tails; the emitter was not the root cause. Focused final gates: fact-merge Zig regression, `zig build`, official dev install, shallow-copy SA 3/3 and strict SAB 3/3. No full sweep.
- Current filtered std-constant scope state (2026-07-11): fixed `HASHSET_SENTINEL` in both set fixtures. Filtered decoded imports now deduplicate and hoist embedded constants, and cloned function signatures retain the verified source `reg_ids`, including stable const ids that are intentionally not renamed as locals. This closes both the constant-ordering duplication and the missing function-scope membership exposed in `sa_set_insert`. Focused final gates: filtered-const Zig regression, `zig build`, official dev install, sets SA/SAB 2/2 each, mixed collections SA/SAB 2/2 each. No full sweep.
- Current for-in protocol pruning state (2026-07-11): fixed both hidden-call reachability gaps for `iter_len`/`iter_at`. Syntactic pre-typecheck reachability now retains typed associated protocol candidates (or conservatively falls back when a generic inferred local lacks a syntactic type), and post-typecheck emit reachability retains the exact methods selected by checked iterable type. This closes the earlier Iterable mismatch and the exposed follow-up SAB missing-callee error for both plain and generic fixtures. Focused final gates: pruning Zig regression, `zig build`, official dev install, SA 2/2, strict SAB 2/2. No full sweep.
- Current local function-pointer test-pruning state (2026-07-11): fixed `test_unit_fn_ptr_value.sla`'s focused `Undefined call: f` failures in shared reachability. Project-shortcut cleanup considers identifier/field initializers pure and removes unused test-local lets; call-usage analysis scanned arguments but not the plain callee name, so function-pointer bindings were deleted before TypeChecker. `reachabilityNodeUsesIdentifier()` now counts a non-associated `f(...)` callee as use of `f`. Added a focused parse/filter/prune/typecheck Zig regression. Final precise gates: focused Zig pass, `zig build`, official dev install/help, affected SA 3/3, affected strict SAB 3/3, neighboring Vec function-pointer strict SAB 1/1. No full suite/corpus rerun.
- Current stack-allocation cleanup state (2026-07-11): fixed `StackEscape` in array-to-slice, async while-let Vec-pop, and derive formatting paths. Temporary/out `stack_alloc` registers no longer emit `move_`/`release` through generic cleanup; their lifetime is function-stack managed while compiler release/readiness metadata is updated. The typed-borrow capability experiment was reverted after a focused borrow-temp guard found `CapabilityMismatch`. Focused final dev gates: build 7/7, install/help, strict SAB array 1/1, async queue 1/1, derive 2/2, borrow-temp 25/25, plus SA parity for array/async/derive. No full suite was rerun per user instruction.
- Current sibling-scope plain `let` register-identity state (2026-07-11): fixed the next compiler layer in `docs/sab_compile_emit_text_timeout_issue_cn.md`. Direct SAB previously interned every binding by source name for the whole function, so `scan_ident`'s early-return `let id_len` and post-loop `let id_len` shared one register; one path consumed/released it and the sibling lexical binding then hit `UseAfterMove`/`UnknownRegister`. `src/sab_codegen.zig` now prepares a repeated-`let` name set separately for every generated function/test and gives each occurrence a fresh register, while `locals` continues resolving the innermost source name. Added `tests/test_unit_sibling_scope_plain_let_direct.sla`. A shared conditional stack-slot approach and a cross-function global-name approach were both rejected by focused/full evidence before the final per-function design. Final gates: `zig fmt --check`; `zig build -j1 --summary all` 7/7; batched Zig suite 166/166; `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; installed fixture strict SAB + SA-text 2/2 each; installed downstream `test_program_emit_text_contract.sla` strict SAB 1/1. Focused fix 100%; broader Y/shared-lowering/fallback-removal percentages unchanged.
- Current `src/plugin.zig` functional split state (2026-07-10): user requested code-index first, then split by the index map. MCP code-index rebuild now works for the concrete repo root and was run before editing (`modules 1491`, `functions 3674`, `edges 8528`) and again after the first split (`modules 1493`, `functions 3675`, `edges 8529`, Zig modules 23). The refreshed map still shows `src/plugin.zig` as the largest module, with natural skeleton groups around handler ABI, skills/capability output, import/module table, reachability/project shortcuts, compile pipeline, CLI, and SAB commands. First low-risk extraction moved the C ABI handler surface to `src/plugin_handler.zig` and the SLA skills/capability output to `src/plugin_skills.zig`; `src/plugin.zig` keeps explicit aliases/call sites. Static verification passed: `zig fmt --check src/plugin.zig src/plugin_handler.zig src/plugin_skills.zig src/parser.zig` and `git diff --check`. No Zig tests, broad filters, or full tests were run.
- Current selective-body hang audit state (2026-07-10): the broad `zig build test -j1 -Dtest-filter="selectively parses"` gate was not rerun and should stay paused until the next serial verification pass. Static inspection showed the old broad filter included three plugin tests and likely got stuck in the strict direct-SAB half of the reachable-body regression after the SA-text assertions call into `compileSlaFileToSabWithOptions()`. The plugin tests have been retargeted to exact names outside the `selectively parses` broad filter, including a separate `sla sab test codegen materializes reachable bodies in contributing module` gate. `rg` now confirms `selectively parses` only matches the two parser tests. The materialization loop also now compares selected imported body sets by contents rather than count and releases temporary selected-body maps per module iteration. Evidence so far is limited to process cleanup, `zig fmt --check src/plugin.zig src/parser.zig`, `git diff --check`, and static `rg`; do not count this as a completed slice until the exact plugin gates are run serially.
- Current lazy transitive SLA module discovery state (2026-07-10): focused test-codegen import expansion uses demand-driven transitive `.sla` parsing only for small root programs. Direct root `.sla` imports are parsed for signatures, child `.sla` imports are discovered once a parent contributes reachable surface, and large roots keep eager transitive collection to avoid an extra whole-root reachability pass. `appendModuleDeclsSelective()` also avoids parsing children of non-contributing modules on the lazy path. Added regressions prove both sides: a parser-invalid dead child import under a non-contributing parent is skipped, while a reachable parent `child::value()` call still discovers and materializes the child module. Latest serial evidence: new transitive regressions 2/2, `selectively parses` 5/5, `sla module table` 10/10, `zig fmt --check src/parser.zig src/plugin.zig`, `zig build -j1 --summary all` 7/7, `zig build test -j1 --summary all` 165/165, official dev install, and real `sla_ecs` profile checks. Primary profile evidence is noisy but bounded: `parallel_query.sla` import expand 265-314ms, `parallel_table_erased.sla` 360-420ms, `test_ecs_mut_parallel.sla` 571ms, and `system_param_table_erased.sla` 766-899ms. Treat this as a correctness-preserving lazy-discovery step, not closure of the real ECS latency target.
- Current same-named imported method selection state (2026-07-10): selective reachable-body parsing now avoids selecting every imported impl method that shares a bare name such as `value`, `new`, or `get`. Parser method-body selection uses mangled method symbols, and syntactic reachability records local concrete receiver types from annotations, struct literals, and function parameters so `item.value()` can mark `UsedBox_value` without selecting `DeadBox_value`. Unknown receiver types still fall back to the prior conservative associated-candidate marking. Added regression `sla test codegen selectively parses same named imported methods by symbol`, where the dead same-named method body is parser-invalid and remains unparsed. Latest serial evidence: focused same-name method regression, `selectively parses reachable` 2/2, `sla module table` 10/10, `zig fmt --check src/parser.zig src/plugin.zig`, real `sla_ecs` profiles for `system_param_table_erased.sla` (`import expand 667ms`, 1/1) and `tests/test_ecs_mut_parallel.sla` (`import expand 707ms`, 16/16), `zig build -j1 --summary all` 7/7, `zig build test -j1 --summary all` 163/163, official `sa plugin install --dev .`, `SA_PLUGIN_DEV=1 sa sla help`, and `git diff --check`.
- Current reachable referenced-surface contract queue state (2026-07-10): the newest Module Table performance slice narrows non-SLA contract queueing for contributing imported modules. `src/plugin.zig` now checks resolved non-SLA sources for referenced `[MACRO]`, `@extern`, and `#def` layout symbols before queueing them; dead `.sai/.sal` imports are skipped even if another function body in the same `.sla` module is reachable, while `.sa` sources with `@import` / `@expand_tuple` remain conservative transitive scan roots. The contributing-module contract regression now includes a parser-invalid dead `.sai` beside the used macro import. Latest serial evidence: contract-loading 2/2, contributing-module contract import regression, referenced macro import from type-only modules, `sla module table` 10/10, resolved non-SLA reuse regression, nested contract SAB smoke, `zig fmt --check src/plugin.zig src/parser.zig`, `zig build -j1 --summary all` 7/7, `zig build test -j1 --summary all` 159/159, official `sa plugin install --dev .`, `SA_PLUGIN_DEV=1 sa sla help`, and `git diff --check`. Rebuilt local CLI real `sla_ecs` focused SA profiles passed but remain above target/noisy: `parallel_query.sla` `parse 183ms`, `import expand 2820ms`, `load contracts 228ms`, `import aliases 12ms`, `pre-typecheck reachable decl filter 43ms`; `parallel_table_erased.sla` `parse 182ms`, `import expand 4140ms`, `load contracts 257ms`, `import aliases 0ms`, `pre-typecheck reachable decl filter 43ms`. Treat this as correctness/queue cropping, not real latency closure.
- Current Module Table selective reachable-body parsing state (2026-07-10): the active slice extends lazy imported-body materialization to focused test-codegen without returning to full imported body parsing. `src/parser.zig` supports `Parser.Options.function_body_names` and `macro_body_names`; `SlaModuleTable` stores expanded source plus parsed function/macro body-name sets, and reparses modules with only the selected reachable function/method/operator/macro body names. `materializeReachableImportedModuleBodies()` now runs whenever registry-loaded bodies are requested, including `prune_for_test_codegen`, and iterates until helpers or referenced macros discovered inside newly parsed bodies are selected. `moduleQualifiedCallableForCaller()` only qualifies same-module helper calls when the caller is already a module-qualified alias (`dep__entry`), preserving bare imported-function flattening while supporting same-named helper bodies from sibling imports. Added regressions: parser selective function/macro bodies, selective imported function/method bodies, selective imported macro bodies, same-name helper reachability, and imported-const pruning. Latest serial evidence: parser/selective/same-name focused tests, `sla module table` 10/10, imported pruning, registry-loaded imported bodies 2/2, contract-loading 2/2, contributing-module contract import, referenced macro import from type-only modules, `zig fmt --check src/parser.zig src/plugin.zig`, `zig build -j1 --summary all` 7/7, `zig build test -j1 --summary all` 162/162, official `sa plugin install --dev .`, `SA_PLUGIN_DEV=1 sa sla help`, and installed real `sla_ecs` focused SA profiles: `parallel_query` parse 21ms/import expand 289ms/load contracts 14ms/import aliases 0ms/elapsed 2.70s, `parallel_table_erased` parse 32ms/import expand 406ms/load contracts 11ms/import aliases 0ms/elapsed 3.03s. Remaining Module Table performance work is broader corpus validation and cleanup of the remaining `import expand` tail.
- Current `sla_tsgo` project snapshot/collection/config/API timeout status (2026-07-09): `docs/sab_project_snapshot_background_tests_timeout_issue_cn.md` is still open, but background/config-registry/snapshot-config-registry representative paths now pass strict 10s, API open/update correctness blockers are fixed, and both `test_project_collection_open_contract.sla` and `test_project_collection_default_cache_contract.sla` now pass installed strict SAB 10s in serial (`open` about 4.2s, `default_cache` about 4.3s). `src/plugin.zig` now has fact-aware focused reachability for no-import/parser-import-count facts, referenced imported macro/extern surface loading, shallow struct field int/bool facts from literals plus no-arg constructors, zero import-scan call simplification, cached-default open-project folding, and cached-default inferred-project lookup folding for `/dev/null/inferred`. Added/extended focused regressions cover shallow-copy call args, referenced macro imports from type-only modules, import-scan pruning through imported wrappers, known struct field branch pruning, lightweight project snapshot, cached default open-project folding, and cached default inferred-project lookup folding. Latest serial gates passed: new inferred/default-open/lightweight regressions, import-scan 3/3, known-field regression, `zig build -j1 --summary all`, `zig build test -j1 --summary all` 155/155, official dev install/help, and downstream `test_project_collection_default_cache_contract.sla` strict 10s. Cache evidence: `collection_open` is about 1.7KB / 68 disasm lines, and `collection_default_cache` is about 21KB / 769 disasm lines; both no longer contain parser/scanner/session/snapshot/default fallback symbols relevant to their old timeout paths. Completion for the whole project snapshot/collection/config/API issue is now about 91%; next target is broader collection/API strict 10s, not resurrecting the reverted `STR_PTR(identifier)` / `STR_LEN(identifier)` direct Slice field-read fast path.
- Current downstream `system_param_table_erased` removed-components closure (2026-07-09): `docs/sab_system_param_removed_components_memoryleak_issue_cn.md` is now closed for the current compiler/downstream state. During full downstream retest, the old `removed_components` MemoryLeak was gone but default/SAB exposed two direct-SAB owner-state gaps: same-expression by-value call arg consumption before later struct fields / outer call sibling args, and branch merge Active/Consumed mismatch when one branch moves an outer local. `src/sab_codegen.zig` now tracks current-expression later-use nodes for struct literal later fields plus planned-call sibling args, and `genIfStatement()` uses then/else exit pads to balance outer-local cleanup before merge. Added `tests/test_unit_struct_literal_call_arg_later_field_direct.sla`. Latest serial gates passed: new strict direct-SAB fixture 3/3 locally and installed, new SA-text fixture 3/3 locally, reused-Vec strict SAB 1/1, thread-closure merge strict SAB 1/1, `zig fmt --check src/sab_codegen.zig`, `zig build --summary all`, `zig build test --summary all` 147/147, official dev install/help, downstream `sa sla check lib/system_param_table_erased.sla`, downstream generated-SA whole-file 43/43, and downstream default/SAB whole-file 43/43. No parallel tests were run.
- Current test-codegen unreferenced SLA macro body pruning sub-slice (2026-07-09): focused test-codegen reachability no longer treats every local/imported `.sla` macro declaration body as an initial root. `collectSyntacticReachableExpr()` records local SLA macro calls as referenced symbol roots, `scanReferencedSymbolRoots()` scans macro bodies only after their macro name is referenced, and import expansion plus pre-typecheck pruning filter out unreferenced macro declarations. Added regression `sla test codegen prunes unreferenced sla macro bodies`, where root and imported unused macro bodies call helpers that would fail type checking if scanned, while the focused test only calls a reachable imported function. Latest serial gates passed: new regression, `macro` 10/10, `sla module table` 10/10, imported pruning 2/2, `contract loading` 2/2, `zig fmt --check src/plugin.zig`, `zig build --summary all`, `zig build test --summary all` 147/147, official dev install/help, installed focused `tests/test_unit_user_macro_direct.sla` generated-SA macro regression, and installed real ECS focused generated-SA profiles. Current profile evidence is noisy: `parallel_table_erased.sla` reported `parse: 248ms`, `import expand: 952ms`, `load contracts: 244ms`, `import aliases: 0ms`, `pre-typecheck reachable decl filter: 8ms`, `elapsed=4.87 maxrss=113920`; `parallel_query.sla` reported `parse: 220ms`, `import expand: 712ms`, `load contracts: 239ms`, `import aliases: 3ms`, `pre-typecheck reachable decl filter: 7ms`, `elapsed=4.05 maxrss=104576`. Treat this as dead macro body semantic pruning, not latency-target closure.
- Current type-only imported module contract queue sub-slice (2026-07-09): contract/import loading for imported `.sla` modules now distinguishes type-only contribution from body/const/macro contribution. `moduleNeedsContractImportsForReachability()` loads a module's non-SLA `.sai/.sal/.sa` imports only when a function/method body is reachable or an exported const/macro is referenced; modules that contribute only exported type declarations still flatten those types but no longer force dead extern/macro contracts into pre-typecheck imported-macro preload or final contract loading. Added regression `sla test codegen skips contract loading for type only imported modules`, where an imported struct is used by the focused test while the module's dead `.sai` import is parser-invalid and skipped. Latest serial gates passed: new regression, `contract loading` 2/2, contributing-module contract import regression, imported macro direct-callee regression, resolved non-SLA reuse regression, `sla module table` 10/10, imported pruning 2/2, `zig fmt --check src/plugin.zig`, `zig build --summary all`, `zig build test --summary all` 146/146, official dev install/help, and installed real ECS focused generated-SA profiles. Current profile evidence is noisy: `parallel_table_erased.sla` reported `parse: 240ms`, `import expand: 890ms`, `load contracts: 199ms`, `import aliases: 0ms`; `parallel_query.sla` reported `parse: 297ms`, `import expand: 754ms`, `load contracts: 270ms`, `import aliases: 4ms`. Treat this as another reachable contract-loading queue cleanup, not latency-target closure.
- Current test-codegen reachable contract preload sub-slice (2026-07-09): imported-macro metadata preloading before test-codegen reachability now uses a reachable-module fixpoint instead of loading non-SLA imports from every imported `.sla` module. Import expansion first preloads root non-SLA contract/macro imports, builds reachability, then loads `.sai/.sal/.sa` imports only from currently contributing imported modules; if newly loaded imported macros add direct callees, reachability is rebuilt and the process repeats. Added regressions proving a non-contributing imported module with parser-invalid `.sai` is skipped, while a contributing imported module's macro import is still loaded so its direct callee remains reachable. Latest serial gates passed: both new regressions, existing imported macro direct-callee regression, resolved non-SLA reuse regression, `sla module table` 10/10, imported pruning 2/2, `zig fmt --check src/plugin.zig`, `zig build --summary all`, `zig build test --summary all` 145/145, official dev install/help, and installed real ECS focused generated-SA profiles. Current profile evidence is noisy: `parallel_table_erased.sla` reported `parse: 281ms`, `import expand: 1178ms`, `load contracts: 272ms`, `import aliases: 0ms`; `parallel_query.sla` reported `parse: 256ms`, `import expand: 688ms`, `load contracts: 223ms`, `import aliases: 4ms`. Treat this as a reachable-only contract-loading queue cleanup, not as latency-target closure.
- Current non-test build Module Table shallow-then-materialize sub-slice (2026-07-09): `SlaModuleTable` modules now remember source text and whether function bodies were parsed, and can reparse a module in place with full function bodies when a reachable function/method body is required. For `load_reachable_imported_bodies_from_registry` with `prune_for_test_codegen = false`, import expansion starts from shallow imported signatures, full-parses only modules that have reachable function/method bodies, then recomputes reachability so helper calls discovered in materialized imported bodies are preserved before flattening. Test-codegen deliberately stays on the previous full-parse strategy because real focused ECS tests double-parsed too many modules under the two-stage approach. Added regression `sla build codegen skips parsing non contributing imported function bodies`, where an unused imported `.sla` module contains parser-invalid function body syntax and normal build codegen still succeeds. Latest serial gates passed: new regression 1/1, `registry` regressions 4/4, `sla module table` 10/10, imported pruning regressions 2/2, `zig fmt --check src/plugin.zig`, `zig build --summary all` 7/7, `zig build test --summary all` 143/143, official dev install/help, installed real ECS `parallel_table_erased.sla` generated-SA profile with `parse: 254ms`, `import expand: 875ms`, `load contracts: 238ms`, `import aliases: 0ms`, and installed real ECS `parallel_query.sla` generated-SA profile with `parse: 270ms`, `import expand: 754ms`, `load contracts: 246ms`, `import aliases: 4ms`. Remaining work is true lazy parsing/materialization inside contributing modules and full reachable-only contract loading.
- Current plain `.sa` contract-loading queue filter sub-slice (2026-07-09): plain `.sa` imports that cannot contribute TypeChecker contracts or imported macros are no longer added to the contract-loading queue, while still being emitted as normal output imports. `appendResolvedNonSlaImportDecl()` queues non-SLA imports only for `.sai`, `.sal`, `.sa` sources containing `[MACRO]`, `.sa` sources containing `@import`, or `.sa` sources containing `@expand_tuple`; test-codegen imported-macro preload uses the same predicate. Contract loading also short-circuits per-source import scanning when expanded source has no `@import`, and macro scanning when expanded source has no `[MACRO]`. Added regression `sla contract loader fast paths macro free sources`, and extended `sla load contracts reuses resolved non-sla imports` with a plain `.sa` helper that is emitted but not contract-loaded. Latest serial gates passed: new regression 1/1, resolved non-SLA reuse regression 1/1, `sla load` regressions 2/2, `zig fmt --check src/plugin.zig`, `zig build --summary all` 7/7, `zig build test --summary all` 142/142, official dev install/help, installed real ECS `parallel_table_erased.sla` generated-SA profile with `parse: 198ms`, `import expand: 798ms`, `load contracts: 204ms`, `import aliases: 0ms`, installed real ECS `parallel_query.sla` generated-SA profile with `parse: 305ms`, `import expand: 808ms`, `load contracts: 283ms`, `import aliases: 5ms`, and installed real ECS `world_table_erased.sla` check with `elapsed=1.36 maxrss=104832`. This is a queue/filtering cleanup, not full reachable-only contract loading; generated-SA frontend latency remains open.
- Current SLA check imported-function body parse-skip sub-slice (2026-07-09): `SlaModuleTable` now supports `initWithParserOptions()` and keeps its default build/test behavior unchanged, while `sa sla check` initializes the import Module Table with `parse_function_bodies = false` and `parse_test_bodies = false`. Imported macro bodies are still parsed for macro surfaces. Added regression `sla check skips parsing imported function bodies`, where an imported function has parser-invalid body syntax but check succeeds from its signature. Latest serial gates passed: focused new regression 1/1, existing `sla check uses imported` regressions 4/4, `sla module table` 10/10, `zig fmt --check src/plugin.zig src/parser.zig`, `zig build --summary all` 7/7, `zig build test --summary all` 141/141, official dev install/help, installed real ECS `world_table_erased.sla` check with `elapsed=1.65 maxrss=105088`, and installed real ECS `system_param_table_erased.sla` check with `elapsed=3.77 maxrss=125056`. This slice closes a check-path shallow parsing gap only; build/test generated-SA still need lazy materialization of uncalled imported function bodies during Module Table parsing/expansion plus full reachable-only contract loading.
- Current SLA import type pre-scan body-skip sub-slice (2026-07-09): `src/parser.zig` now has independent parser options for function, macro, and test bodies. The recursive `.sla` import type pre-scan path uses `parse_function_bodies = false`, `parse_macro_bodies = false`, and `parse_test_bodies = false`, so root parsing can collect imported type/module names without duplicating imported body parsing. Normal root parsing and Module Table imported-module parsing remain body-preserving except for the prior imported-test skip. Added regression `sla import type prescan skips imported function bodies`, which imports a file containing a needed struct plus a parser-invalid function body and proves root parsing still recognizes the imported struct literal. Latest serial gates passed: focused parser regression 1/1, full `zig test src/parser.zig` 10/10, `sla module table` 10/10, `zig fmt --check src/parser.zig src/plugin.zig`, `zig build --summary all` 7/7, `zig build test --summary all` 140/140, official dev install/help, installed real ECS `parallel_table_erased.sla` focused generated-SA profile with `parse: 234ms`, `import expand: 868ms`, `load contracts: 204ms`, `import aliases: 0ms`, installed real ECS `parallel_query.sla` focused generated-SA profile with `parse: 259ms`, `import expand: 683ms`, `load contracts: 271ms`, `import aliases: 5ms`, and installed real ECS `world_table_erased.sla` check. Remaining work is lazy materialization of uncalled imported function bodies during Module Table parsing/expansion and full reachable-only contract loading.
- Current imported-module test-body lazy parsing sub-slice (2026-07-09): `src/parser.zig` now has parser options plus a token-balanced `skipBlock()` path for `@test` bodies, and `SlaModuleTable.getOrParse()` parses imported `.sla` modules with `parse_test_bodies = false`. Root files still parse test bodies normally. Added regression `sla module table skips imported test body parsing`, which places invalid parser syntax inside an imported module `@test` body while proving import expansion still materializes the imported function. Latest serial gates passed: new regression 1/1, `sla module table` 10/10, `zig fmt --check src/parser.zig src/plugin.zig`, `zig build --summary all` 7/7, `zig build test --summary all` 139/139, official dev install/help, installed real ECS `parallel_table_erased.sla` focused generated-SA profile with `parse: 408ms`, `import expand: 963ms`, `load contracts: 212ms`, `import aliases: 0ms`, installed real ECS `parallel_query.sla` focused generated-SA profile with `parse: 323ms`, `import expand: 707ms`, `load contracts: 266ms`, `import aliases: 5ms`, and installed real ECS `world_table_erased.sla` check. Remaining work is lazy parsing of uncalled imported function bodies and full reachable-only contract loading.
- Current resolved non-SLA import contract/macro reuse (2026-07-09): `src/plugin.zig` now carries already-resolved non-SLA imports (`.sa/.sai/.sal`) from `expandSlaImportsWithModuleTable()` into the contract-loading phase, so normal frontend and `sa sla check` load those contracts/macros from `ResolvedImport.source` instead of resolving and rereading emitted non-SLA imports. Test-codegen imported-macro preloading also reuses the root/module resolved imports already available during import expansion. Added regression `sla load contracts reuses resolved non-sla imports`, which deletes a `.sa` macro import and `.sai` contract import after expansion and still loads both from the resolved sources. Latest serial gates passed: new regression 1/1, expanded-source macro loading 1/1, imported macro direct-callee 1/1, imported alias reuse 1/1, `sla module table` 9/9, `zig build --summary all` 7/7, `zig build test --summary all` 138/138, official dev install/help, installed real ECS `parallel_table_erased.sla` focused generated-SA profile with `import expand: 962ms`, `load contracts: 198ms`, `import aliases: 0ms`, installed real ECS `parallel_query.sla` focused generated-SA profile with `import expand: 809ms`, `load contracts: 270ms`, `import aliases: 5ms`, and installed real ECS `world_table_erased.sla` check. Remaining work is true shallow scan/lazy body parsing and full reachable-only contract loading.
- Current source expansion/load-contract duplicate-work reduction (2026-07-09): `src/source_expand.zig` now fast-paths sources without `@expand_tuple` by returning an owned copy instead of byte-walking through the template expander, and `src/plugin.zig` parses imported macros from the already-expanded source inside `loadImportContractsRecursive()` instead of expanding imported contract/macro source twice. Added regressions `expand without tuple directive returns owned copy` and `sla load imported macros parses already expanded source`. Latest serial gates passed: focused source-expand test 1/1, focused plugin expanded-macro test 1/1, imported macro direct-callee 1/1, `zig fmt --check src/source_expand.zig src/plugin.zig`, `zig build --summary all` 7/7, `zig build test --summary all` 137/137, official dev install/help, installed real ECS `parallel_table_erased.sla` focused generated-SA profile with `import expand: 1303ms`, `load contracts: 320ms`, `import aliases: 0ms`, installed real ECS `parallel_query.sla` focused generated-SA profile with `import expand: 893ms`, `load contracts: 245ms`, `import aliases: 3ms`, and installed real ECS `world_table_erased.sla` check. Remaining real ECS frontend performance work is true shallow scan/lazy body parsing and reachable-only contract loading.
- Current Module Table import-alias reuse performance slice (2026-07-09): `src/plugin.zig` now reuses the `SlaModuleTable` and root resolved imports built during `expandSlaImportsWithModuleTable()` when registering imported function aliases, instead of reparsing root `.sla` imports in a fresh module table. The normal frontend and `sa sla check` path both call `registerImportedFunctionAliasesFromResolvedImports()`. New regression `sla imported aliases reuse parsed module table` deletes the imported source file after expansion and proves alias registration uses the parsed table. Latest serial gates passed: new regression 1/1, imported alias metadata 1/1, namespace alias collision 1/1, namespace call 1/1, `sla module table` 9/9, `zig build --summary all` 7/7, `zig build test --summary all` 135/135, official dev install/help, installed real ECS `parallel_table_erased.sla` focused generated-SA profile with `import aliases: 0ms`, installed real ECS `parallel_query.sla` focused generated-SA profile with `import aliases: 4ms`, installed real ECS `world_table_erased.sla` check, and `git diff --check`. Remaining real ECS frontend performance work is `import expand`, `parse`, and `load contracts`; true shallow scan/lazy body parsing is still open.
- Current strict-SAB ECS table-erased cleanup (2026-07-09): focused `/home/vscode/projects/sla_ecs/lib/system_param_table_erased.sla --filter "table erased entity query commands param defers spawned entity"` strict direct-SAB no-fallback and default paths now pass, and the previously open `lib/parallel_table_erased.sla` default/SAB `8701` path is current pass. Root causes fixed in `src/sab_codegen.zig`: cached decoded std macro templates now own decoded symbol text before the encode buffer is freed, SCI-hygiened std macro placeholders still resolve to caller args, and planned by-value identifier consumes are suppressed when the identifier is used later in the current block. Added `tests/test_unit_reused_vec_value_call_direct.sla` and Zig std macro template cache/placeholder regressions. Latest serial gates passed: `zig fmt --check src/lowering_rules.zig src/sab_codegen.zig src/plugin.zig src/monomorphizer.zig`; focused std macro template Zig tests 2/2; focused decoded std deps 4/4; `zig build --summary all` 7/7; `zig build test --summary all` 134/134; official dev install/help; installed strict-SAB reused-Vec fixture 1/1; installed strict-SAB thread-closure merge fixture 1/1; installed `system_param_table_erased.sla` focused strict-SAB 1/1 and default 1/1; installed `parallel_iterator.sla` strict-SAB 10/10; installed `parallel.sla` strict-SAB 1/1; installed `parallel_table_erased.sla` strict-SAB/default 1/1 each. Remaining real ECS work is frontend performance: shallow scan/lazy parsing and reachable-only contract/alias construction.
- Current `sla_ecs` real-world Module Table/performance correction (2026-07-09): synthetic Module Table/macro benchmarks are not sufficient evidence for the target latency. Earlier real downstream profile from `/home/vscode/projects/sla_ecs` showed focused generated-SA tests still took seconds, not hundreds of milliseconds: `examples/parallel_query.sla --filter "parallel query demo reads shared table erased world snapshot on threads" --test-backend sa` reported `parse 294ms`, `import expand 979ms`, `load contracts 329ms`, `import aliases 279ms`; `lib/parallel_table_erased.sla --filter "table erased readonly parallel runner executes no conflict systems on threads" --test-backend sa` reported `parse 271ms`, `import expand 1270ms`, `load contracts 334ms`, `import aliases 285ms`. Import-alias reuse reduced the alias stage to near-zero, source-expansion cleanup removes avoidable duplicate expansion work, resolved non-SLA import reuse avoids rereading emitted `.sa/.sai/.sal` imports during contract loading, imported test-body skip avoids parsing imported module test bodies, root `.sla` import type pre-scan now skips imported function/macro/test bodies, check-mode skips imported function bodies, and plain `.sa` imports without contract/macro/import surface are no longer contract-loaded. Current installed profiles are `parallel_table_erased.sla`: `parse 198ms`, `import expand 798ms`, `load contracts 204ms`, `import aliases 0ms`; `parallel_query.sla`: `parse 305ms`, `import expand 808ms`, `load contracts 283ms`, `import aliases 5ms`. The current Module Table milestone is still not performance-complete for real ECS macro/import workloads. The next frontend performance slice must target remaining `import expand`, Module Table full-body parsing, and contract scanning through lazy materialization of uncalled imported function bodies plus full reachable-only contract construction. Overall performance-to-target estimate: about 60-64%; Module Table architecture remains 100%, but real-world latency target remains open.
- Current `sla_ecs` real-world regression issue record: `docs/sla_ecs_module_table_real_world_regression_issue_cn.md` is the recovery document for this performance correction. The earlier `world_table_erased.sla` check abort is fixed/current non-repro: `world_table_erased.sla`, `system_param_table_erased.sla`, `tests/test_ecs_result_facades.sla`, and `lib/parallel_iterator.sla` now pass whole-file `sa sla check` with the dev plugin. The former default/SAB `8701` paths such as `lib/parallel_table_erased.sla` and the focused `system_param_table_erased.sla` strict-SAB filter now pass and should remain regression gates. Remaining real ECS risk is separate from Module Table architecture: focused generated-SA frontend latency is still seconds, not hundreds of milliseconds.
- Latest strict-SAB planned-call owner merge cleanup (2026-07-09): whole-file `/home/vscode/projects/sla_ecs/lib/parallel_iterator.sla` strict direct-SAB no-fallback is fixed/current pass. Root cause was a direct-SAB planned static call that passed a local `Vec` owner by value on one `if` branch without emitting a matching `move_`, while the sibling `thread::spawn(^|| ...)` closure branch consumed the same `chunk`, causing `PhiStateConflict` at the merge. `src/lowering_rules.zig` now plans by-value call-arg consumption for identifier args only when the source is an appropriate owner transfer: standard owner locals such as `Vec<T>` consume after the call; function parameters only consume on old result-forwarding/escaping paths; reusable scalar/config structs such as `EcsParallelTaskPool` are not spuriously consumed. `src/sab_codegen.zig` feeds `source_is_param` and std-owner classification into that shared rule. Added `tests/test_unit_thread_closure_direct_call_merge_direct.sla`. Serial verification passed: `zig fmt --check src/lowering_rules.zig src/sab_codegen.zig src/plugin.zig src/monomorphizer.zig`; focused shared lowering-rule Zig tests 5/5; `zig build --summary all` 7/7; `zig build test --summary all` 130/130; local and installed strict-SAB for the new fixture 1/1; local and installed `/home/vscode/projects/sla_ecs/lib/parallel_iterator.sla` strict-SAB 10/10; local and installed `/home/vscode/projects/sla_ecs/lib/parallel.sla` strict-SAB 1/1; official dev install/help. The old `parallel.sla` panic 15110 and whole-file `parallel_iterator.sla` TypeChecker/strict-SAB items are current non-repro.
- Latest committed baseline before this slice: `b6224f0` (`Index SLA callable associated-method candidates`). Earlier recent slices include the SLA frontend callable-index foundation, dev-install ReleaseFast latency fix, Module Table stopgap, callable-index associated-candidate indexing, and the test-codegen vtable-aware trait impl output pruning. P1 compiler-side stability metadata and P1 compiler-owned benchmark harness formalization remain committed earlier baselines; SCI embedded symbol-token/call-body remap is still committed in `/home/vscode/projects/sci` as `1beccde`. (`Add compiler pipeline benchmark harness`). Earlier recent slices include docs-priority scalar parameter cleanup plus temporary auto-borrow receiver cleanup, imported-macro whole-aggregate borrowed address follow-up, docs-priority Result/EntityItem cleanup plus primitive reassigned scalar slotting, direct SAB array-to-slice planned call-arg materialization, SA-text `if let` chain std macro/import pre-scan, shared RefCell runtime scan for chained `if let` values, docs-priority direct SAB chained `if let` for rosetta `104_if_let_chains`, focused user-defined macro SA-text inline parity, and shared imported macro address-slot arg planning. SCI embedded symbol-token/call-body remap is committed in `/home/vscode/projects/sci` as `1beccde` (`Remap fragment embedded symbol text`).
- Latest completed slices (verified with dev-mode SAB no-fallback + SA-text parity where applicable): docs-priority aggregate move-call UseAfterMove fix, P1 compiler-side stability metadata support, P1 compiler-owned benchmark harness formalization, docs-priority scalar parameter cleanup plus temporary auto-borrow receiver cleanup, imported-macro whole-aggregate borrowed address follow-up, docs-priority Result/EntityItem cleanup plus primitive reassigned scalar slotting, direct SAB array-to-slice planned call-arg materialization, SA-text `if let` chain std macro/import pre-scan, shared RefCell runtime scan for chained `if let` values, docs-priority direct SAB chained `if let` for rosetta `104_if_let_chains`, focused user-defined macro SA-text inline parity, shared imported macro address-slot arg planning, shared borrow-address temp release/transfer/planning slices, shared RefCell lifecycle-planning sub-slices, std surface/aggregate/async runtime slices, and the ready/pending async continuation subset tracked below.
- Current authoritative direct SAB no-fallback sweep (2026-07-11 dev-mode milestone audit): 130/130 `tests/test_unit_*.sla` files passed with `SA_PLUGIN_DEV=1`, `SLA_SAB_NO_FALLBACK=1`, `--test-backend sab`, and `--jobs 1`. The first milestone pass was 129/130 and exposed the loop-thread stack-slot capture bug in `test_unit_fn_ptr_value.sla`; after the focused fix and 11/11 SA/SAB retest, the identical sweep passed 130/130. This supersedes the earlier 120/130 audit. Do not generalize this tracked corpus result to global compiler completion or unrelated downstream projects.
- Current global estimates: Y/shared-lowering about 98-99%; direct SAB fallback-removal is 100% for the tracked unit corpus, with current worktree corpus pass rate now 119/119 locally and through the installed host CLI. This is not global 100% completion.
- Current correction note: do not rely on earlier broad green wording for the docs-priority cleanup/imported-macro fixtures without the narrower 10s evidence. This pass fixed and reverified three current blockers: SA-text `JoinHandle.join()` no longer releases an already consumed receiver; strict SAB `STR_PTR/STR_LEN` no longer moves non-owning stack-derived Slice sources out of borrowed stack-storage `let` bindings; strict SAB aggregate identifier assignment marks the RHS consumed after `assign` instead of emitting an extra `move_`. Focused local 10s gates now pass for `test_unit_join_handle_vec_direct.sla`, `test_unit_str_ptr_len_identifier_direct.sla`, `test_unit_sa_aggregate_reassign_move_cleanup.sla`, plus loop cleanup, reassignment after release, fn-ptr value/thread bool, binary Copy param, string equality, and RefCell owner release. No cache deletion was performed; large source edits still required normal Zig recompile steps of about 46-67s.
- Current SLA frontend Module Table milestone: complete for the current architecture slice. `src/plugin.zig` owns a per-pass `SlaModuleTable` keyed by resolved path; each `SlaModule` has AST, export indexes, and direct `resolved_imports`; exported function/type/const/macro signatures are indexed separately from function bodies; check-mode validates imported functions/methods/trait methods from signatures without deep-checking imported bodies; build/test codegen paths load only reachable imported bodies from the registry; and namespace import aliases emit/materialize as module-qualified symbols in SA-text and direct-SAB. Focused final gates passed: Module Table 9/9, namespace alias 1/1, same-export namespace collision 1/1, registry-loaded build bodies 1/1, imported dyn-dispatch registry body 1/1, check-mode imported-signature tests 4/4, TypeChecker imported-signature unit 1/1, shared static-call alias metadata 1/1, imported-macro direct-callee test-codegen 1/1, and namespace-alias reachable collector 1/1. Full gate passed: `timeout 180s zig build --summary all` (7/7), current `timeout 300s zig build test --summary all` (127/127), official `timeout 180s env SA_PLUGIN_DEV=1 sa plugin install --dev .`, `timeout 60s env SA_PLUGIN_DEV=1 sa sla help`, and installed imported-macro address fixture SA/SAB 6/6 each with `--jobs 1`. Downstream `sla_tsgo` checker-pool/project-background probes now either pass SA backend after frontend stages or reach later backend/cleanup failures, so remaining failures belong to docs/SAB cleanup tracks rather than Module Table import/typecheck.
- Current SLA test-codegen pruning checkpoint: completed as part of the Module Table milestone. Test-input generation uses syntactic pre-typecheck reachability plus exact post-typecheck reachability, and registry-loaded body mode restores only reachable imported bodies before SA-text/direct-SAB codegen. Trait impl signatures remain available for vtable/type completeness while unused broken bodies are omitted or decl-only.
- Post-Module-Table open work for the next slice: Module Table itself is complete; do not reopen it for fixed/current non-repro failures. Closed in the current worktree: `world_table_erased.sla` duplicate-key check abort, `system_param_table_erased.sla` whole-file `TemplateNotFound`, `test_ecs_result_facades.sla` Iterable/typecheck failure, whole-file `parallel_iterator.sla` check failure, whole-file `parallel_iterator.sla` strict-SAB `PhiStateConflict`, local fnptr/thread partition failures, `/home/vscode/projects/sla_ecs/lib/parallel.sla` strict-SAB 15110, focused/default SAB `8701` for `lib/parallel_table_erased.sla`, the `system_param_table_erased.sla --filter "table erased entity query commands param defers spawned entity"` strict-SAB path, the duplicated `import aliases` frontend stage, avoidable duplicate source expansion in imported macro/contract loading, rereading already-resolved emitted non-SLA imports during contract loading, parsing imported module `@test` bodies, parser type pre-scan parsing imported function/macro/test bodies, `sa sla check` Module Table parsing imported function bodies, and contract-loading queue scans for plain `.sa` files without contract/macro/import surface. Remaining verified gap is real ECS generated-SA frontend performance: profile-driven lazy materialization of uncalled imported function bodies during build/test import expansion plus full reachable-only contract work.
- Latest Phase 1 RefCell lifecycle sub-slice: shared RefCell owner-release planning now covers active borrow handles whose `cell_reg` matches the owner being released. `src/lowering_rules.zig` owns `planRefCellHandleCellRelease`; SA-text consumes it for the existing owner cleanup scan, direct SAB releases matching active RefCell borrow handles before releasing the owner register, and direct SAB branch-local cleanup now uses a RefCell/borrow-temp aware helper with a branch-local `seen` set so fail-path cleanup emits dynamic-borrow releases without mutating global release/RefCell maps. SA-text chained `if let` fail-path cleanup now snapshots and restores `consumed_bindings`, `borrow_source_temps`, and `refcell_borrow_handles` after writing fail-path cleanup instructions, so fail-path state does not pollute success-path generation. `tests/test_unit_refcell_owner_release_direct.sla` now covers explicit owner release with an active borrow handle and chained `if let` failure cleanup with RefCell owner plus active borrow handle. Verification: `zig fmt --check src/lowering_rules.zig src/codegen.zig src/sab_codegen.zig`; focused lowering-rule test; `zig build --summary all`; `zig build test --summary all` (94/94); local SA-text and strict direct-SAB focused fixture 2/2; rosetta 104 SA/SAB 1/1; existing RefCell payload SA/SAB fixture 7/7; borrow-temp strict SAB 25/25; official `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; installed SA-text and strict direct-SAB focused fixture 2/2; installed rosetta 104 SA/SAB 1/1; full local and installed strict direct-SAB sweeps 119/119. This is a Phase 1 lifecycle hardening slice, not a docs-priority issue closure and not global completion.
- Latest docs-priority compiler issue status: `docs/sa_scope_taskset_move_cleanup_useaftermove_issue_cn.md` cleanup subproblem is fixed/verified. `src/codegen.zig` now treats non-Copy, non-borrow-like identifier RHS assignment as a move for compiler-side state without emitting an extra `!rhs`: after `bag = next_bag` / `a = next_a`, value-state metadata transfers to the target and the RHS is marked consumed so loop-body cleanup skips it. Added `tests/test_unit_sa_aggregate_reassign_move_cleanup.sla` for tuple-destructured aggregate reassignment. Verification: local and installed SA/SAB focused fixture 1/1; installed SA guards for reassignment/loop cleanup; downstream `/home/vscode/projects/sla_ecs/lib/entity_dynamic.sla` generated-SA 7/7; downstream `test_ecs_mut_parallel.sla --filter "task pool scoped task set" --test-backend sa` 16/16; full downstream generated-SA `test_ecs_mut_parallel.sla` 129/129; full downstream default/SAB `test_ecs_mut_parallel.sla` 129/129. The earlier `ecs_parallel_scope_run_result_recursive` 21 args vs 22 params `InvalidArgsCount` is historical/transient and no longer reproduces.
- Latest docs-priority compiler issue status: `docs/execute_extra_parse_tsconfig_useaftermove_issue_cn.md` and the original blocker in `docs/sab_large_execute_unknown_register_issue_cn.md` are fixed/verified for the current compiler repros and downstream `sla_tsgo` host surface. SA-text now records imported macro value materialization releases, clears consumed state after identifier reassignment, and uses lexical cleanup releases for break/continue/non-fallthrough and natural loop backedges. Direct SAB now materializes by-value primitive Copy params into entry stack slots before consuming SAB ops. Added focused regressions: `tests/test_unit_tsconfig_buffer_cleanup.sla`, `tests/test_unit_reassign_after_release.sla`, `tests/test_unit_loop_body_local_cleanup.sla`, and `tests/test_unit_sab_binary_copy_param_direct.sla`. Verification: `zig fmt --check src/codegen.zig src/sab_codegen.zig`; `zig build --summary all`; `zig build test --summary all` (94/94); focused SA/SAB guards for the new fixtures; official `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; downstream `members/execute/src/cli.sla` check; downstream `test_cli_contract.sla` SA/default 8/8; downstream `test_execute_extra_contract.sla` SA/default 23/23; downstream `test_execute_contract.sla` default/SAB no longer verifier-traps and currently stops only on historical runtime/business assertions (33/49 passed). `docs/ls_autoinsert_aggregate_scanner_register_redefinition_issue_cn.md` is now also fixed and downstream-reverified; `sla_tsgo` restored the historical LS autoinsert aggregate assertion and it passes under SA backend.
- Current feature report: P1 compiler-side stability metadata support is complete. `src/stability_metadata.zig` owns schema text, example manifest, manifest validation, and text/JSON reports. `sa sla stability schema` emits the schema, and `sa sla stability verify <manifest.json> [--json]` validates downstream manifests for schema version, label format, duplicates, artifact paths, declared-label references, and evidence status. `sap.json` help and `skills --json` expose the new command. `docs/stability_metadata_cn.md` documents the boundary: this repo supplies contract/tooling only; downstream projects own concrete labels, business fixtures, semantics, and thresholds. Verification stayed green: `zig test src/stability_metadata.zig`, `zig build test -Dtest-filter="sla stability" --summary all` (3/3), `zig build test -Dtest-filter="valid stability metadata" --summary all` (1/1), `zig build --summary all`, `zig build test --summary all` (90/90), local and installed CLI schema/verify smoke, `sa plugin install --dev .`, and `SA_PLUGIN_DEV=1 sa sla help`. Broader full-completion work remains open.
- Latest docs-priority follow-up status: `docs/document_symbol_protocol_summary_memoryleak_issue_cn.md`, `docs/linked_editing_direct_tsx_memoryleak_issue_cn.md`, and the currently available scanner-helper surface in `docs/module_specifier_subpath_match_memoryleak_issue_cn.md` are fixed/verified by the imported macro value-arg receiver-temp cleanup. Downstream temporary probes passed and were removed: document symbol direct summary SA-text 2/2 and strict SAB 2/2; linked editing TSX direct SA-text 1/1 and strict SAB 1/1; module specifier scanner helpers SA-text 1/1 and strict SAB 1/1. Stable downstream contracts passed SA/default: document symbols 9/9, linked editing 5/5, modulespecifiers 19/19. No new compiler code was required beyond `src/codegen.zig` / `src/sab_codegen.zig`; plugin unit coverage is `tests/test_unit_str_ptr_len_identifier_direct.sla`.
- Latest docs-priority issue status: `docs/selection_ranges_for_positions_memoryleak_issue_cn.md` is fixed for the current compiler repro and downstream `sla_tsgo` host surface. `tests/test_unit_str_ptr_len_identifier_direct.sla` now covers `STR_PTR(text)` / `STR_LEN(text)` on an owned string/Slice binding flowing into an ordinary call. The leak came from imported macro value-arg cleanup using ordinary call-argument rules, so receiver load temps generated for `text` stayed active after `STR_PTR` / `STR_LEN` macro expansion. `src/codegen.zig` and `src/sab_codegen.zig` now release imported macro value args when argument expression lowering produced a temporary result. Verification: `zig build --summary all`; `zig build test --summary all` (93/93); local rebuilt CLI SA-text and strict direct-SAB no-fallback for the new fixture (1/1 each); official `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; installed CLI SA-text and strict direct-SAB no-fallback for the new fixture (1/1 each); downstream temporary direct probe SA-text and strict SAB (2/2 each); downstream `selectionrange.sla` check; downstream stable `tests/test_selectionrange_contract.sla` SA-text and default backend (4/4 each). The downstream temporary probe was removed after verification.
- Previous docs-priority issue status: `docs/join_handle_vec_memory_leak_issue_cn.md` is fixed for the current local compiler repro. `tests/test_unit_join_handle_vec_direct.sla` now covers `Vec<JoinHandle<i32>>` push, index read, `join().unwrap()`, and loop aggregation. The generated-SA leak came from ordinary binary-expression cleanup using call-argument rules: `len(handles) as i64` materialized a cast temp that survived the comparison. `src/lowering_rules.zig` now owns shared pointer-carrier cast classification and cast-result materialization planning; `src/codegen.zig` uses that plan to release binary operand result registers only when the cast actually created a temporary. Verification: `zig build --summary all`; `zig build test --summary all` (93/93); focused codegen Zig test; local rebuilt CLI SA-text and strict direct-SAB no-fallback for the new fixture (1/1 each); official `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; installed CLI SA-text and strict direct-SAB no-fallback for the new fixture (1/1 each). Larger downstream arbitrary-N executor coverage remains downstream-owned regression evidence after `sla_ecs` restores the business test.
- Previous docs-priority issue status: `docs/sab_aggregate_mut_parallel_use_after_move_issue_cn.md` is fixed for the current local compiler repro and downstream host surface. Direct SAB planned-call move-prefixed args now transfer values into fresh temps before emitting `^tmp`, including `^field` / `^index` non-identifier move expressions, and mark moved source bindings consumed so tail cleanup does not release them again. Added `tests/test_unit_borrowed_enum_field_arg_direct.sla`; updated the move-call Zig regression to require fresh-temp move args. Verification: `zig build --summary all`; `zig build test --summary all` (92/92); focused shared-rule Zig test; local SA-text and strict direct-SAB no-fallback focused fixture; local strict direct-SAB `tests/test_unit_array_direct.sla`; local full strict direct-SAB sweep 110/110 files and 264/264 cases; official `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; installed focused fixture SA/SAB; installed downstream `test_ecs_mut_parallel.sla` strict direct-SAB 91/91; installed downstream `parallel.sla` strict direct-SAB 1/1; `git diff --check`. `sla_ecs` remains downstream regression evidence only.
- Previous feature report: direct SAB now consumes shared `CallArgMaterializationKind.array_to_slice_borrow` for focused ordinary planned static calls. `tests/test_unit_array_to_slice_call.sla` covers `&[T; N] -> &[T]` call arguments from local arrays and temporary array literals. `src/sab_codegen.zig` emits stack `Slice` materialization with `SLICE_NEW`, marks the stack slice non-owning, and releases temporary backing arrays after the call; `src/codegen.zig` also releases temporary backing arrays for SA-text `&[literal]` call args. Verification stayed green: `zig build --summary all`, `zig build test --summary all` (85/85), local and host SA-text plus strict direct-SAB no-fallback for the focused fixture (1/1 each), local/host full no-fallback sweeps 103/103 and 256/256 cases, official dev install/help, host borrow-temp (25/25), host `/home/vscode/projects/sla_ecs/lib/parallel.sla` (1/1), and focused SAB disasm illegal-call-target grep with no matches. Broader shared call/result/materialization planning remains open; macro-body `&values` array-to-slice through macro parameter address semantics remains a Phase 2 macro-convergence gap, and rosetta `116_va_list_variadic` remains a separate demo issue.
- Previous feature report: shared RefCell runtime scanning now includes all chained `if let` condition values. `src/lowering_rules.zig` scans every `IfLetCond.value`, so RefCell constructors/receivers in second and later `&& let` conditions participate in the shared runtime import decision instead of being invisible unless they also appeared in branch bodies. Verification stayed green: `zig test src/lowering_rules.zig --test-filter "shared refcell runtime scanner detects constructor and receiver calls"`, `zig build --summary all`, `zig build test --summary all` (84/84), local/host strict direct-SAB no-fallback for RefCell payload (7/7) and borrow-temp (25/25), official dev install/help, and host strict direct-SAB `/home/vscode/projects/sla_ecs/lib/parallel.sla` (1/1). Full RefCell lifecycle remains open.
- Inserted docs issue audit is complete and does not change global feature percentages. `tasks.md` now records the audit before the RefCell continuation target. `docs/sab_call_target_issue_cn.md` is marked verified-fixed with host `parallel.sla` strict direct-SAB plus disasm guard evidence. `docs/sla_compiler_issues_and_lua_refactor_diagnostics_cn.md` now marks SCI macro-body context escape, SLA output-relative imports, and SCI `PackageNotResolved` double-free as fixed/verified; the `sa_lua` empty-test issue remains open outside this compiler repo. `docs/trace.md` now marks focused user-macro SA-text parity, rosetta 104 direct-SAB chained `if let`, and tracked struct-update/call-target issues as fixed while keeping broader macro convergence, full RefCell lifecycle, pointer-backed imported-macro alias semantics, shared call planning, closures, full async, SCI boundary, and global 100% completion open.
- Current SCI boundary sub-slice: embedded symbol-token/call-body remap is implemented in `/home/vscode/projects/sci/src/flattener.zig` and verified. Extern/export ordering is now closed as no-repro (stress probe + exported-helper ordering regression), and the plugin-side fragment cleanup audit concluded the plugin decode-time adapter is not a duplicate of SCI's flatten-time remap. Broader SCI boundary convergence is about 80%; full generic fragment naming remains a longer-term SCI task.
- Remaining tracked unit failures: the 7-file known queue listed in the last-sweep/focused-closure entry above.

## Recently Completed Slices

- `docs_result_entityitem_cleanup_scalar_reassign`: docs-priority `Result<EntityItem<T>>` cleanup issue is fixed for the current host repro surface. Direct SAB now covers borrowed-composite result cleanup, generic Box raw-pointer roundtrips, void function-pointer indirect calls, unsafe block expressions, pointer-to-pointer bitcasts into fresh temps, and primitive reassigned let/param bindings through stack slots. Added focused Result/Box/fn-ptr/scalar fixtures and verified local sweep 107/107 files, installed host ECS SA/SAB 172/172 each, installed `parallel.sla`, borrow-temp, RefCell payload, and SAB call-target disasm guard. This closes the focused docs issue without claiming global completion.

- `direct_sab_array_to_slice_call_arg`: direct SAB now materializes borrowed fixed-array call arguments as stack `Slice` values through the shared `CallArgMaterializationKind.array_to_slice_borrow` decision. The focused fixture passes SA-text and strict direct-SAB no-fallback locally and under host dev-mode gates, including temporary array literal backing-array cleanup. This closes the focused ordinary planned-call gap without claiming the broader shared call-plan result contract or macro parameter address semantics.

- `sa_text_if_let_chain_std_macro_scan`: SA-text std macro/import pre-scanning now traverses every `IfLetCond.value` in a chained `if let`, not just the first parser condition and branch bodies. The common `if_expr` scanner helpers now cover the current std macro/import families, while thread-spawn helper discovery, loop-counter/hoisted-allocation pre-passes, and identifier-consumption checks inspect the same chain values. This is a focused SA-text pre-scan hardening slice and does not claim broader shared macro expansion/call planning or full RefCell lifecycle completion.

- `shared_refcell_if_let_chain_runtime_scan`: shared RefCell runtime import scanning now traverses every `IfLetCond.value` in a chained `if let`, not just the parser's first condition value. This keeps RefCell runtime-surface decisions in `lowering_rules.zig` and avoids SA-text/SAB emitter-specific import heuristics. The slice is intentionally narrow; full RefCell borrow-handle lifecycle planning remains open.

- `direct_sab_if_let_chain_pattern`: direct SAB now lowers chained `if let` statements and value-producing `if let` expressions for rosetta `104_if_let_chains` without fallback. Shared `planLetPattern()` / `LetPatternPlan` classifies Option/Result/user-enum tag checks before the emitter split, mirroring the existing `while let` contract. SAB consumes that plan and handles concrete branch labels, result-slot merge, branch-local cleanup, and RefCell branch-state snapshot/restore locally. This closes the focused docs-priority rosetta 104 issue without claiming broader pattern-surface completion.

- `focused_user_macro_sa_text_inline_parity`: SA-text now inline-expands parsed user-defined macros at call sites instead of emitting legacy `[MACRO]` / `EXPAND` text blocks. The type checker records full macro bodies with infer-typed parameters, and `src/codegen.zig` maps macro params to caller registers, tracks call-site and macro-local let types, preserves macro-local hygiene/block shadowing, allocates macro-local for-loop counter slots, supports nested macro calls, aggregate/index/static-call expressions, borrowed field/index/deref args, and focused Box smart-pointer `&*boxed` / `&**nested_box` address forms. Direct SAB remains on its existing structured macro path. This closes the focused SA-text parity gap for `tests/test_unit_user_macro_direct.sla` without claiming the broader shared macro expansion/substitution contract.

- `shared_imported_macro_address_slot_arg_plan`: imported macro scanning now records direct `%param+offset` address-slot uses separately from legacy `&%param` borrowed uses, including nested `EXPAND` propagation. Shared `ImportedMacroCallPlan` carries `address_slot_arg_mask` and classifies direct address-slot args before the emitter split: field/index/deref address expressions pass the true address, value temps pass values, and pointer-like or inferred identifier values pass directly to avoid pointer-to-pointer macro expansion. SA-text and direct SAB consume the same actions while keeping concrete register/operand allocation, release tracking, and macro-fragment emission local. `tests/test_unit_imported_macro_address_direct.sla` now covers an imported macro that writes through `point.left`, and `tests/test_unit_async_block_on_direct.sla` guards the std `POLL_IS_READY`/`POLL_VALUE` inferred-Poll identifier path.

- `shared_borrow_address_temp_release_plan`: borrow-address temporary release planning now lives in `lowering_rules.planBorrowAddressTempRelease()`. SA-text and direct SAB both consume the shared plan to decide when a borrowed address value carries release metadata and its source temporaries must be recursively released. Concrete SA-text `borrow_source_temps`, direct SAB `borrow_address_temps`, register/string release emission, slice freeing, and macro-fragment behavior stay emitter-local. This is a focused shared-lowering extraction and does not claim full RefCell borrow-handle lifecycle reuse across all scopes/branches/loops/call args or pointer-backed aggregate alias preservation inside imported macros.

- `shared_borrow_address_temp_binding_transfer_plan`: SA-text let-binding `borrow_source_temps` metadata transfer now consumes `lowering_rules.planBorrowAddressTempTransfer()` when moving metadata from the generated value temp into the binding name. Direct SAB's value-state transfer path already consumes the same plan in `transferReleaseMetadata()` for `borrow_address_temps`. Concrete SA-text `borrow_source_temps`, direct SAB `borrow_address_temps`, destination replacement/freeing, register/string allocation, metadata transfer, and release emission stay emitter-local. This is a focused shared-lowering extraction and does not claim full RefCell borrow-handle lifecycle reuse across all scopes/branches/loops/call args or pointer-backed aggregate alias preservation inside imported macros.

- `shared_borrow_address_temp_transfer_plan`: borrow-address temporary metadata transfer now lives in `lowering_rules.planBorrowAddressTempTransfer()`. SA-text and direct SAB both consume the shared action when moving value state from a source binding/register to a destination, so both branches agree that borrow-address release metadata moves only when the source carries it. Concrete SA-text `borrow_source_temps`, direct SAB `borrow_address_temps`, destination replacement/freeing, register/string allocation, metadata transfer, and release emission stay emitter-local. This is a focused shared-lowering extraction and does not claim full RefCell borrow-handle lifecycle reuse across all scopes/branches/loops/call args or pointer-backed aggregate alias preservation inside imported macros.

- `shared_borrow_address_temp_plan`: borrow-address temporary tracking now lives in `lowering_rules.planBorrowAddressTemps()`. SA-text and direct SAB both consume the shared plan to decide whether a borrowed address should carry release metadata for a primary owner temp and/or extra release temps. Concrete SA-text `borrow_source_temps`, direct SAB `borrow_address_temps`, register/string allocation, metadata transfer, and release emission stay emitter-local. This is a focused shared-lowering extraction and does not claim full RefCell borrow-handle lifecycle reuse across all scopes/branches/loops/call args or pointer-backed aggregate alias preservation inside imported macros.

- `shared_refcell_borrow_handle_registration_plan`: ordinary RefCell borrow handle registration now lives in `lowering_rules.planRefCellBorrowHandleRegistration()`. SA-text and direct SAB both consume the shared plan when registering a newly created `borrow()` / `borrow_mut()` handle, so both branches agree that the receiver cell register is recorded as the borrowed cell but is not tracked as an owner temp released with the handle. Concrete SA-text `RefCellBorrowHandle`, direct SAB `RefCellBorrowValue`, map/register mutation, and release emission stay emitter-local. This is a focused shared-lowering extraction and does not claim full RefCell borrow-handle lifecycle reuse across all scopes/branches/loops/call args or pointer-backed aggregate alias preservation inside imported macros.

- `shared_refcell_companion_restore_plan`: RefCell result-slot companion restore behavior now lives in `lowering_rules.planRefCellCompanionRestore()`. SA-text and direct SAB both consume the shared plan for tracking the loaded cell handle as an owner temp on the restored borrow handle and releasing the companion cell slot after restore. Concrete loads, SA-text `cell_release_temp`, direct SAB `release_regs`, map/register mutation, and release emission stay emitter-local. This is a focused shared-lowering extraction and does not claim full RefCell borrow-handle lifecycle reuse across all scopes/branches/loops/call args or pointer-backed aggregate alias preservation inside imported macros.

- `shared_refcell_borrow_runtime_guard_plan`: RefCell borrow runtime guard behavior now lives in `lowering_rules.planRefCellBorrowRuntimeGuard()`. SA-text and direct SAB both consume the shared plan for releasing the try-borrow status value on conflict, using panic code 107 for conflict, and releasing the status value on success before result materialization. Concrete labels, branch instructions, panic emission, status register/string release calls, and borrow-result materialization remain emitter-local. This is a focused shared-lowering extraction and does not claim full RefCell borrow-handle lifecycle reuse across all scopes/branches/loops/call args or pointer-backed aggregate alias preservation inside imported macros.

- `shared_refcell_borrow_result_materialization_plan`: RefCell borrow-result materialization now lives in `lowering_rules.planRefCellBorrowResult()`. SA-text consumes the shared plan to use scalar borrow slots directly and load pointer/smart-pointer payloads before releasing the temporary borrow slot; direct SAB consumes the same shared plan to use scalar/smart-pointer slots directly and take raw pointer payloads while retaining the borrow slot as release metadata. Concrete SA text instructions, SAB `take`, release calls, and metadata map mutation stay emitter-local. This is a focused shared-lowering extraction and does not claim full RefCell borrow-handle lifecycle reuse across all scopes/branches/loops/call args or pointer-backed aggregate alias preservation inside imported macros.

- `shared_refcell_companion_store_cleanup_plan`: RefCell result-slot companion-store cleanup now lives in `lowering_rules.planRefCellCompanionStoreCleanup()`. SA-text and direct SAB both consume the shared plan for consumed-handle marking, owner-temp release, borrow-address-temp release, and non-owning metadata clearing while keeping concrete companion-slot stores, map/register mutation, and release emission local. This is a focused shared-lowering extraction and does not claim full RefCell borrow-handle lifecycle reuse across all scopes/branches/loops/call args or pointer-backed aggregate alias preservation inside imported macros.

- `shared_refcell_handle_transfer_action`: RefCell borrow-handle transfer selection now lives in `lowering_rules.planRefCellHandleTransfer()`. SA-text and direct SAB both consume the shared action when moving value state from a source binding/register to a destination; concrete handle maps, register maps, consumed-state marking, and release metadata remain emitter-local. This is a focused shared-lowering extraction and does not claim full RefCell borrow-handle lifecycle reuse across all scopes/branches/loops/call args or pointer-backed aggregate alias preservation inside imported macros.

- `shared_refcell_branch_state_merge_action`: RefCell `if` branch-state merge selection now lives in `lowering_rules.planRefCellBranchStateMerge()`. Direct SAB consumes the shared action to choose whether to restore the pre-branch, then-branch, else-branch, or current RefCell borrow metadata snapshot; concrete snapshot storage/restoration remains emitter-local. This is a focused shared-lowering extraction over the already verified early-return branch-state behavior and does not claim full RefCell borrow-handle lifecycle reuse across all scopes/branches/loops/call args or pointer-backed aggregate alias preservation inside imported macros.

- `refcell_release_branch_state_early_return`: RefCell borrow-handle release planning now lives in `lowering_rules.planRefCellHandleRelease()`. SA-text and direct SAB both consume the shared plan for dynamic RefCell borrow release, consumed-handle marking, and owner-temp release decisions while keeping concrete SA-text/SAB emission local. Direct SAB now snapshots/restores RefCell borrow values plus borrow-address temps around ordinary/value `if` branch lowering, so a terminated branch that releases a borrow handle does not delete the metadata needed by a fallthrough branch. `tests/test_unit_refcell_struct_payload.sla` covers an early-return/fallthrough RefCell borrow and the disassembly shows borrow-count decrement on both returns. This is a focused early-return branch-state sub-slice; full RefCell borrow-handle lifecycle reuse across all scopes/branches/loops/call args and pointer-backed aggregate alias preservation inside imported macros remain open.

- `shared_result_slot_refcell_load_action`: result-slot RefCell borrow-handle load/restore planning now lives in `lowering_rules.planResultSlotRefCellLoad()`. SA-text and direct SAB both consume the shared action when loading a value from a result slot, so both branches agree before emission on whether to restore borrow-handle metadata from the companion slot, release an unused preallocated companion slot, or continue ordinary value-state transfer. This keeps concrete companion-slot fetch/remove, load/release instructions, recreated handle metadata, and transfer mechanics local to each emitter. It does not claim full RefCell borrow-handle lifecycle reuse across scopes/branches/loops/call args/early returns or pointer-backed aggregate alias preservation inside imported macros.
- `shared_result_slot_refcell_store_action`: result-slot RefCell borrow-handle store planning now lives in `lowering_rules.planResultSlotRefCellStore()`. SA-text and direct SAB both consume the shared action when storing a source value into a result slot, so both branches agree before emission on whether an active RefCell borrow handle should be moved into companion-slot metadata or whether normal value-state transfer is enough. This keeps concrete companion-slot allocation, store/load instructions, release ordering, consumed-state bookkeeping, and direct SAB macro-fragment mechanics local to each emitter. It does not claim full RefCell borrow-handle lifecycle reuse across scopes/branches/loops/call args/early returns or pointer-backed aggregate alias preservation inside imported macros.
- `shared_refcell_runtime_import_scan`: RefCell runtime import scanning now lives in `lowering_rules.programNeedsRefCellRuntime()` and its expression/block helpers. SA-text consumes the shared scan to decide whether to import `sa_std/core/refcell.sa`, while direct SAB keeps its structured macro-fragment emission and both emitters keep concrete borrow handle tracking, result-slot companion slots, release ordering, and cleanup mechanics local. This is a conservative shared-dependency classification slice and does not claim full RefCell borrow-handle lifecycle reuse across scopes/branches/loops/call args/early returns or pointer-backed aggregate alias preservation inside imported macros.
- `shared_imported_macro_addressable_identifier_scan`: imported macro borrowed/addressable identifier-arg scanning now lives in `ImportedMacroCallPlan.addressableIdentifierArgName()`. Direct SAB consumes the helper for `borrowed_bindings`, and SA-text consumes the same helper for `addressable_bindings`, so both branches agree before the split on which plain identifier argument should have stable addressable storage. This keeps field/index/deref address materialization and concrete stack/register emission local to each emitter and does not widen pointer-backed aggregate alias semantics, address-expression lowering, or full RefCell borrow-handle lifecycle reuse across scopes/branches/loops/early returns.
- `shared_imported_macro_arg_lowering_action`: imported macro argument lowering now uses shared `ImportedMacroArgLoweringAction` planning for the value-bypass and addressable-argument phases. Both SA-text and direct SAB first ask the shared plan whether a non-addressable or raw-pointer borrowed macro arg should remain a value, then only compute address shape for addressable args and consume the shared final action for reuse/materialize/address-expression materialize. This keeps direct SAB direct and leaves concrete symbol lookup, macro-context remapping, stack allocation/store, and release mechanics local to each emitter. This does not widen pointer-backed aggregate alias semantics inside imported macros or claim full RefCell borrow-handle lifecycle reuse across scopes/branches/loops/early returns.
- `shared_borrowed_identifier_recognition`: pure AST recognition of `&identifier` now lives in `lowering_rules.borrowedIdentifierName()`. SA-text and direct SAB both consume this helper for addressable/borrowed binding scans, and SA-text also uses it in the planned call-arg path that preserves addressable borrowed identifiers. The helper deliberately recognizes only `&identifier`; `&field`, ordinary identifiers, and `^identifier` remain outside this classifier. This is a conservative shared-recognition extraction and does not widen pointer-backed aggregate alias semantics, imported macro pointer-backed alias preservation, or full RefCell borrow-handle lifecycle reuse across scopes/branches/loops/early returns.
- `shared_borrowed_binding_storage_plan`: borrowed/address-taken primitive binding storage is now represented by `lowering_rules.BorrowedBindingStoragePlan`, returned by `planBorrowedBindingStorage()`. SA-text and direct SAB both consume this shared plan instead of duplicating the local `ty.* == .primitive` stack-slot decision for borrowed parameters and let/const bindings. This is a conservative alias/lifetime classification extraction; it does not widen pointer-backed aggregate alias semantics, imported macro pointer-backed alias preservation, or full RefCell borrow-handle lifecycle reuse across scopes/branches/loops/early returns.
- `shared_result_slot_transfer_plan`: result-slot transfer classification is now represented by `lowering_rules.ResultSlotTransferPlan`, returned by `planResultSlotTransfer()`. The plan covers both pointer-passing ownership/borrow transfer and whether a RefCell borrow result slot needs a companion cell slot; SA-text and direct SAB now consume this shared plan instead of duplicating the companion-slot type check. This is an architectural extraction over the previous result-slot metadata fix and does not claim full RefCell lifecycle reuse across scopes/loops/early returns or pointer-backed aggregate alias preservation inside imported macros.
- `result_slot_refcell_metadata_transfer`: result-slot stores for pointer-passing values now transfer ownership/borrow metadata instead of copying a temp and releasing it before the merged value can carry the right state. `src/lowering_rules.zig` owns the shared transfer classifier through `resultSlotStoreTransfersValue()`. Direct SAB transfers release metadata, future state, non-owning borrow state, and RefCell borrow metadata through result slots, and preallocates RefCell companion cell slots before branch emission. SA-text mirrors the transfer metadata and uses a narrow smart-pointer field-borrow compatibility path for ordinary field borrows such as `&holder.boxed` / `&nested.slot.item`. `tests/test_unit_refcell_struct_payload.sla` plus `tests/import_fixtures/imported_refcell_helpers.sa` cover if-expression RefCell borrow counts and pointer-backed Box if-expression ownership. This completes the focused result-slot/RefCell metadata transfer sub-slice without claiming full RefCell lifecycle reuse across scopes/loops/early returns or pointer-backed aggregate alias preservation inside imported macros.
- `shared_refcell_borrow_payload_release_planning`: RefCell borrow planning now shares borrow kind, try-borrow macro selection, release macro selection, and borrowed payload category through `RefCellBorrowPlan` plus `RefCellBorrowValueKind` in `src/lowering_rules.zig`. `src/codegen.zig` and `src/sab_codegen.zig` both consume that shared plan while intentionally keeping emitter-local value representation details: SA-text still peels pointer and smart-pointer payload borrows to payload pointers, while direct SAB keeps smart-pointer payload borrows in the slot and only peels raw pointer payloads. `tests/test_unit_refcell_struct_payload.sla` now imports Box and covers `RefCell<Box<RefCellPayload>>` borrow read/write. This completes a conservative shared-classification slice for RefCell borrow payload/release planning without claiming full borrow-handle lifecycle reuse across scopes/branches/loops/early returns, pointer-backed aggregate alias preservation inside imported macros, or broader shared macro expansion convergence.
- `shared_smart_pointer_chained_deref_address_shape`: shared address-of planning now classifies Box/Rc/Arc-like deref sources as `AddressOfShape.deref_smart_pointer` instead of collapsing them into `.value_temp`. `src/lowering_rules.zig` owns that classification, imported-macro addressable-arg planning consumes it for borrowed smart-pointer deref args, `src/sab_codegen.zig` and `src/codegen.zig` consume the explicit shape while preserving local emission/lifetime mechanics, and `tests/test_unit_imported_macro_address_direct.sla` now covers imported macro reads through `*boxed` and `**nested_box`. This completes the conservative shared-classification slice for the remaining smart-pointer-led `&**chain` cases without claiming RefCell lifecycle planning, pointer-backed aggregate alias preservation inside imported macros, or broader shared macro expansion convergence.
- `imported_macro_address_expression_materialization`: imported macro borrowed `field`, `index`, and ordinary `deref` args now have a dedicated address-expression materialization path in both emitters instead of collapsing back into the generic imported-macro value-temp stack-slot helper. `src/lowering_rules.zig` still owns the shared action classification; `src/sab_codegen.zig` and `src/codegen.zig` now keep the concrete address lookup, slot allocation/store, and release mechanics local. `tests/import_fixtures/imported_borrow_helpers.sa` plus `tests/test_unit_imported_macro_address_direct.sla` cover borrowed imported-macro field/index/deref args under dev-mode direct SAB no-fallback and SA-text parity. This remains a focused imported-macro addressability slice and does not claim broader pointer-backed aggregate alias semantics, fuller `&**chain` classification, RefCell lifecycle planning, or broader shared macro expansion convergence.
- `imported_macro_addressable_arg_action`: direct SAB imported macro calls now consume `lowering_rules.ImportedMacroCallPlan.planAddressableArgAction` for the existing addressable-argument decision. The shared plan chooses between pass-by-value, reusing an existing addressable symbol, and materializing a temporary stack slot; `src/sab_codegen.zig` still owns macro-context symbol lookup, stack allocation/store, and release mechanics. This is a focused decision extraction and does not add new imported SA macro syntax, decoded fragment behavior, full imported macro address-expression lowering, RefCell lifecycle changes, or broader shared macro expansion convergence.
- `borrow_direct_macro_smart_pointer_address`: direct SAB user-macro address lowering now supports macro-body `&*boxed` and a focused `&**nested_box` chain through the shared smart-pointer action/get plans. `src/sab_codegen.zig` reuses one deref-address fallback helper for ordinary and macro address paths, and macro deref type/value lowering now recognizes smart-pointer deref through `lowering_rules.smartPointerDerefType` / `planSmartPointerGetAction`. `tests/test_unit_user_macro_direct.sla` imports Box and covers the new smart-pointer macro-address case. This narrows the Phase 1 macro-address gap without changing imported SA macro address lowering, RefCell lifecycle, std-surface metadata, or the broader shared macro expansion contract.
- `borrow_direct_shared_smart_pointer_address_action`: direct SAB smart-pointer address-of fallback now consumes a shared action plan from `src/lowering_rules.zig`. The shared plan covers the existing `dyn Box` identity, `as_ptr` address slot, pointer-backed value take from the `as_ptr` slot, nested smart-pointer value-slot handling, and `get` value fallback decisions; `src/sab_codegen.zig` still owns emission and release ordering. This completes a focused Phase 1 decision-plan extraction without changing RefCell lifecycle, std-surface method emission, or syntax support. `&**chain`, imported macro address lowering, fuller RefCell lifecycle planning, and broader shared macro expansion convergence remain separate open work.
- `borrow_direct_shared_index_address_target_unwrap`: ordinary `&values[index]` target classification now unwraps borrow/pointer array and Slice targets in shared lowering rules. `src/lowering_rules.zig` owns the borrow/pointer peel and ordinary array/slice addressability helper; SA-text passes the index target type from type checking, and direct SAB consumes the same helper for direct and macro address paths while preserving Vec/std-surface fallback behavior. This completes the focused Phase 1 ordinary index-address target unwrapping sub-slice; smart-pointer address plans, `&**chain`, imported macro address lowering, RefCell lifecycle, and broader shared macro expansion convergence remain separate open work.
- `borrow_direct_nested_struct_field_chain`: SA-text and strict direct SAB now pass the plain nested struct scalar field-chain borrow baseline in `tests/test_unit_borrow_direct.sla`, covering `&item.inner.value` where `inner` is a pointer-backed nested struct slot and `value` is scalar. The fix also corrects ordinary `&*borrow_or_pointer` in SA-text so it remains an address projection instead of loading a scalar and taking that temporary address. Shared lowering rules now keep explicit borrow call arguments out of receiver-style auto-borrow and classify explicit pointer-backed struct literal fields through `StructLiteralFieldTransfer`. This is a focused Phase 1 hardening slice; the broader reusable address/projection plan for field/index/deref chains remains open.
- `async_defer_ready_copy_struct_capture_state_machine`: direct SAB and SA-text now support a Phase 8D sub-slice for one `@derive(copy)` struct pre-await capture in narrow defer-ready continuations: `let bump = CaptureBump { amount: 3 }; let value = future::defer_ready(39).await; return value + bump.amount;`, both direct-call and prebound-local. Shared `AsyncSingleAwaitContinuationPlan` in `src/lowering_rules.zig` records copy-struct capture storage separately from scalar slots, and `AsyncContinuationScalarPlan` records the captured field name. Both emitters store/load the owned capture pointer from the shared offset and load the planned field scalar before computing the result. `tests/test_unit_async_defer_ready_copy_struct_capture_state_machine.sla` covers first-pending, not-ready state, second-ready, ready state, and result `42`. Vec/Box/Rc captures and multi-await state machines remain open in Phase 8E-8G.
- `async_defer_ready_multi_capture_state_machine`: direct SAB and SA-text now support a Phase 8C sub-slice for two scalar pre-await captures in narrow defer-ready continuations: `let a = 1; let b = 2; let value = future::defer_ready(39).await; return value + a + b;`, both direct-call and prebound-local. Shared `AsyncSingleAwaitContinuationPlan` in `src/lowering_rules.zig` carries `captures[*]` layout metadata with offsets 16 and 24 plus `AsyncContinuationScalarPlan.captured2_coeff`; both emitters allocate from `plan.asyncStateSize()` and store/load from the shared offsets. `tests/test_unit_async_defer_ready_multi_capture_state_machine.sla` covers first-pending, not-ready state, second-ready, ready state, and result `42`. This remains a narrow single-await scalar-capture slice; non-scalar captures and multi-await state machines remain open in Phase 8D-8G.
- `async_defer_ready_two_post_bind_state_machine`: direct SAB and SA-text now support a Phase 8B sub-slice for two simple post-await bindings after a narrow defer-ready continuation: `let result = value + bump; let final = result + 1; return final;`, both direct-call and prebound-local. Shared `AsyncSingleAwaitContinuationPlan` in `src/lowering_rules.zig` composes the post-await tail into the existing `AsyncContinuationScalarPlan`; both emitters consume that scalar plan in the generated poll helper. `tests/test_unit_async_defer_ready_two_post_bind_state_machine.sla` covers first-pending, not-ready state, second-ready, ready state, and result `42`. This remains a narrow single-await tail-composition slice; longer statement tails, multiple captures, non-scalar captures, and multi-await state machines remain open in Phase 8B-8G.
- `async_defer_ready_branch_expr_state_machine`: direct SAB and SA-text now support a branchy Phase 8A sub-slice for narrow defer-ready continuations: `return if value > 0 { value + bump } else { bump };`, both direct-call and prebound-local. Shared `AsyncContinuationBranchPlan` in `src/lowering_rules.zig` represents the restricted condition and scalar-linear then/else result plans; both emitters consume that plan in the generated poll helper. `tests/test_unit_async_defer_ready_branch_expr_state_machine.sla` covers first-pending, not-ready state, second-ready, ready state, and result `42`. This remains a narrow single-await branchy slice; arbitrary branch trees, multiple captures, non-scalar captures, multi-statement tails, and multi-await state machines remain open in Phase 8A-8G.
- `async_defer_ready_scalar_expr_state_machine`: direct SAB and SA-text now support a scalar-linear Phase 8A sub-slice for narrow defer-ready continuations: `return value - bump;` and `return (value + bump) * 2;`, both direct-call and prebound-local. Shared `AsyncContinuationScalarPlan` in `src/lowering_rules.zig` represents the result as `awaited_coeff * awaited + captured_coeff * captured + immediate`; both emitters consume that plan in the generated poll helper. `tests/test_unit_async_defer_ready_scalar_expr_state_machine.sla` covers first-pending, not-ready state, second-ready, ready state, and result `42`. This remains a narrow single-await scalar-linear slice; broader branch expression trees, multiple captures, non-scalar captures, and multi-await state machines remain open in Phase 8A-8G.
- `async_defer_ready_post_bind_return_addend_state_machine`: direct SAB and SA-text now support the narrow composition where a post-await binding computes the awaited value plus one scalar pre-await captured addend, and the final return adds one integer immediate: `let bump = 1; let value = future::defer_ready(...).await; let result = value + bump; return result + 1;` plus the prebound-local equivalent. Shared `AsyncSingleAwaitContinuationPlan` classification in `src/lowering_rules.zig` folds the return-time immediate into the same plan consumed by both emitters. `tests/test_unit_async_defer_ready_post_bind_return_addend_state_machine.sla` covers first-pending, not-ready state, second-ready, ready state, and result `42`. This remains a narrow single-await resumption slice; arbitrary multi-await/control-flow/multi-capture/non-scalar async state machines remain open and are tracked in Phase 8A-8G.
- `async_defer_ready_capture_immediate_state_machine`: direct SAB and SA-text now preserve one scalar pre-await captured addend plus one immediate addend across the narrow defer-ready continuation shape: `let bump = 1; let value = future::defer_ready(...).await; return value + bump + 1;` and the prebound-local equivalent. Shared `AsyncSingleAwaitContinuationPlan` classification in `src/lowering_rules.zig` now accepts a restricted `+` expression tree containing exactly one awaited binding, at most one captured scalar addend, and at most one integer literal immediate. Both emitters consume the same plan and compute the captured sum plus immediate in the generated poll helper before returning ready on the second poll. `tests/test_unit_async_defer_ready_capture_immediate_state_machine.sla` covers direct-call and prebound-local variants with first-pending, not-ready state, second-ready, ready state, and result `42`. This is still a narrow single-await resumption slice; arbitrary multi-await/control-flow/captured-local async state machines remain open.
- `async_defer_ready_capture_state_machine`: direct SAB and SA-text now preserve one scalar pre-await local addend across the narrow defer-ready continuation shape: `let bump = 2; let value = future::defer_ready(...).await; return value + bump;` and the prebound-local equivalent. Shared `AsyncSingleAwaitContinuationPlan` classification in `src/lowering_rules.zig` carries the captured addend name/expression and remains explicitly limited to `future::defer_ready`. Both emitters consume the plan, allocate a 24-byte async state for this shape, store the captured scalar at offset 16, and reload it in the poll helper before returning ready on the second poll. `tests/test_unit_async_defer_ready_capture_state_machine.sla` covers direct-call and prebound-local variants with first-pending, not-ready state, second-ready, ready state, and result `42`. This is still a narrow single-await resumption slice; arbitrary multi-await/control-flow/captured-local async state machines remain open.
- `async_defer_ready_post_bind_state_machine`: direct SAB and SA-text now support one simple post-await local binding in the narrow defer-ready continuation shape: `let value = future::defer_ready(...).await; let result = value + const; return result;` and the prebound-local equivalent. Shared `AsyncSingleAwaitContinuationPlan` classification in `src/lowering_rules.zig` records the post binding and remains explicitly limited to `future::defer_ready`. Both emitters consume the plan and materialize the post-await binding in the generated poll helper before returning ready on the second poll. `tests/test_unit_async_defer_ready_post_bind_state_machine.sla` covers direct-call and prebound-local variants with first-pending, not-ready state, second-ready, ready state, and result `42`. This is still a narrow single-await resumption slice; arbitrary multi-await/control-flow/captured-local async state machines remain open.
- `async_defer_ready_local_await_state_machine`: direct SAB and SA-text now support the prebound-local variant of the narrow defer-ready continuation shape: `let fut = future::defer_ready(...); let value = fut.await; return value (+ const);`. Shared `AsyncSingleAwaitContinuationPlan` classification in `src/lowering_rules.zig` now recognizes both the direct-call await shape and this local future binding shape, while remaining explicitly limited to `future::defer_ready`. Both emitters continue to consume the same plan and store the inner defer-ready state in the generated async state. `tests/test_unit_async_defer_ready_local_await_state_machine.sla` covers first-pending, not-ready state, second-ready, ready state, and result `42`. This is still a narrow single-await resumption slice; arbitrary multi-await/control-flow/captured-local async state machines remain open.
- `async_defer_ready_await_state_machine`: direct SAB and SA-text now support a narrow pending-then-ready continuation shape inside async functions: `let value = future::defer_ready(...).await; return value (+ const);`. Shared `AsyncSingleAwaitContinuationPlan` classification lives in `src/lowering_rules.zig` and is explicitly limited to `future::defer_ready`; both emitters consume that plan to allocate an async state with stage at offset 0 and the inner defer-ready state at offset 8. The generated async poll helper returns pending on the first task poll, resumes on the second poll, stores the completed stage, and returns the continued value. `tests/test_unit_async_defer_ready_await_state_machine.sla` covers first-pending, not-ready state, second-ready, ready state, and result `42`. This is a narrow resumption slice only; arbitrary multi-await/control-flow/captured-local async state machines remain open.
- `async_defer_ready_task_runtime`: direct SAB and SA-text now expose `future::defer_ready(value)` as a compiler-owned stateful future primitive for tests and later continuation hardening. Shared `FutureRuntimeCallPlan` classification lives in `src/lowering_rules.zig`; the type checker returns `future<T>` from the single value argument and keeps readiness `.unknown`. Both emitters allocate a state with stage at offset 0 and value at offset 8, attach `SLA_DEFER_READY_FUTURE_VT`, and poll as pending on the first task poll, ready with the stored value on the second task poll, then pending on later polls. `tests/test_unit_async_defer_ready_task_runtime.sla` covers `task::new`, first-pending poll, second-ready poll, `task::is_ready`, and `task::result`. This is a prerequisite runtime surface for later pending-then-ready continuation fixtures; full async continuation/resumption after arbitrary pending future state kinds become ready remains open.
- `async_executor_vec_runtime`: direct SAB and SA-text now expose `executor::new(tasks_vec)` for dynamic `Vec<Task<T>>` buffers while preserving the fixed-array executor path. Shared `ExecutorTaskBufferPlan` classification lives in `src/lowering_rules.zig` and distinguishes fixed arrays from Vec-backed contiguous buffers. The type checker accepts both `[Task<T>; N]` and `Vec<Task<T>>`; SA-text derives Vec pointer/length with `VEC_AS_PTR`/`VEC_LEN` and uses runtime `EXECUTOR_POLL_READY_COUNT` when no fixed count is known; direct SAB emits the same Vec pointer/length fragments before `EXECUTOR_NEW`. `tests/test_unit_async_executor_vec_runtime.sla` covers ready-count, single-index polling, and pending-count behavior over Vec-backed task buffers. This does not claim full async state-machine scheduling.
- `async_join2_local_pending_await_state_machine`: direct SAB and SA-text now use local future-readiness facts when deciding whether an awaited composite future should return pending from an async function. `src/lowering_rules.zig` feeds `AwaitPlan.pending_return_if_async` from `exprFutureReadiness(expr, readiness_by_name)`, so `let left = future::ready(...); let right = future::pending::<T>(); future::join2(left, right).await` returns the pending composite state instead of forcing ready-state extraction. `tests/test_unit_async_join2_local_pending_await_state_machine.sla` covers the task remaining pending after one poll. This remains propagation-only; full continuation/resumption after later readiness remains open.
- `async_join2_local_await_state_machine`: direct SAB and SA-text now propagate local future-readiness facts into the shared await plan. Both emitters maintain readiness maps for future state temporaries and local bindings, while `src/lowering_rules.zig` owns the `FutureReadiness` lattice and `join2Readiness`/`select2Readiness` combination rules. `tests/test_unit_async_join2_local_await_state_machine.sla` covers `let left = future::ready(...); let right = future::ready(...); future::join2(left, right).await` inside an async function. This remains a poll-once statically-ready dataflow slice; pending continuation/resumption remains open.
- `async_join2_await_state_machine`: direct SAB and SA-text now poll statically ready composite futures once when awaited, instead of treating a `future::join2(...)` state as a ready-future layout. Shared `AwaitPlan.poll_once_if_statically_ready`, `exprIsStaticallyReadyFuture`, and `exprNeedsPollOnceForReadyAwait` live in `src/lowering_rules.zig`; both emitters consume that plan with `FUTURE_POLL` plus `POLL_VALUE`. `tests/test_unit_async_join2_await_state_machine.sla` covers `future::join2(future::ready(...), future::ready(...)).await` inside an async function. This is still a statically-ready hardening slice; local future-readiness dataflow and pending continuation/resumption remain open.
- `async_param_await_state_machine`: direct SAB and SA-text now branch on the ready/pending state slot for compatible `future<T>` awaits inside `async fn -> T`. If the awaited ready/pending state is empty or pending, the async function returns that future state; otherwise it extracts the ready inner value and continues. Shared `AwaitPlan.ready_pending_state_return_if_async` and `futureInnerType`/`typesEquivalent` classification live in `src/lowering_rules.zig`; both emitters consume the same plan. `tests/test_unit_async_param_await_state_machine.sla` covers ready and pending future parameters. This remains a ready/pending-state hardening slice and does not complete full continuation resumption for arbitrary future state kinds.
- `async_pending_await_state_machine`: direct SAB and SA-text now propagate a statically pending `.await` inside an `async fn` by returning the pending future state instead of forcing `FUTURE_READY_STATE_INTO_INNER`. Shared `AwaitPlan` classification lives in `src/lowering_rules.zig`; both emitters consume it and stop lowering the remaining statement tail after the generated async pending return. `tests/test_unit_async_pending_await_state_machine.sla` wraps `wait_on_pending()` in `task::new`, polls once, and asserts the task remains not ready. This is a narrow statically-pending await hardening slice and does not complete full async scheduling/resumption.
- `async_select2_runtime`: direct SAB and SA-text now expose the narrow `future::select2(left, right)` runtime path over scalar ready/pending futures, plus `future::either_side(either)`, `future::either_left(either)`, and `future::either_right(either)` accessors for the `FutureEither<T, U>` pointer returned by `task::result(task)`. Shared `FutureRuntimeCallPlan` classification lives in `src/lowering_rules.zig`; the type checker validates `future<T>` inputs, models the result as `future<FutureEither<T, U>>`, and keeps either accessors scalar. Both emitters record future-state vtable metadata so `task::new(selected_future)` uses the generated `SLA_SELECT2_FUTURE_VT` poll helper. `tests/test_unit_async_select2_runtime.sla` covers left-ready/right-pending, left-pending/right-ready, and both-pending behavior. This does not claim full async state-machine scheduling.
- `async_join2_runtime`: direct SAB and SA-text now expose the narrow `future::join2(left, right)` runtime path over scalar ready/pending futures, plus `future::pair_left(pair)` and `future::pair_right(pair)` accessors for the `FuturePair<T, U>` pointer returned by `task::result(task)`. Shared `FutureRuntimeCallPlan` classification lives in `src/lowering_rules.zig`; the type checker validates `future<T>` inputs, models the result as `future<FuturePair<T, U>>`, and keeps pair accessors scalar. Both emitters record future-state vtable metadata so `task::new(joined_future)` uses the generated `SLA_JOIN2_FUTURE_VT` poll helper instead of the ready-future helper. `tests/test_unit_async_join2_runtime.sla` covers ready+ready pair extraction and ready+pending remaining pending. This does not claim full async state-machine scheduling.
- `async_executor_runtime`: direct SAB and SA-text now expose the narrow fixed-array executor runtime surface `executor::new(tasks_array)`, `executor::poll_one(executor, index)`, and `executor::poll_ready_count(executor)` for contiguous `[Task<T>; N]` buffers. Shared `ExecutorRuntimeCallPlan` classification lives in `src/lowering_rules.zig`; the type checker validates fixed arrays of `Task<T>` and returns `Executor<T>`, `bool`, or `u64` as appropriate. Direct SAB emits `sa_std/core/task.sa` `EXECUTOR_*` macro fragments. SA-text emits the stable `EXECUTOR_NEW`/`EXECUTOR_POLL_ONE` macros and uses a fixed-count unrolled `TASK_POLL` sequence for `poll_ready_count`, avoiding the current text-macro shape that fails for this lowering. `tests/test_unit_async_executor_runtime.sla` covers ready-count, single-index polling, and pending-task count behavior. Vec-backed buffers are covered by `async_executor_vec_runtime`; neither executor slice claims full async state-machine scheduling.
- `async_poll_runtime`: direct SAB and SA-text now expose scalar `poll::ready(value)`, `poll::pending::<T>()`, `poll::is_ready(poll)`, `poll::is_pending(poll)`, and `poll::value(poll)` over numeric `Poll<T>` values. Shared `PollRuntimeCallPlan` classification lives in `src/lowering_rules.zig`; type checking validates `Poll<T>` and scalar numeric payloads; SA-text emits `POLL_*` macros, and SAB consumes the shared poll runtime plan to emit `sa_std/core/future.sa` macro fragments. `tests/test_unit_async_poll_runtime.sla` covers ready-value extraction and pending-state inspection.
- `async_task_state_runtime`: direct SAB and SA-text now expose `task::state(task)` over `Task<T>` values. Shared `TaskRuntimeCallPlan` classification lives in `src/lowering_rules.zig`; the type checker validates the argument is `Task<T>` and returns `u64`; SA-text emits `EXPAND TASK_STATE`, and SAB consumes the shared task runtime plan to emit the `TASK_STATE` macro fragment. `tests/test_unit_async_task_state_runtime.sla` covers ready-task state changing after `task::poll` and pending-task state staying not-ready.
- `async_pending_task_runtime`: direct SAB and SA-text now expose `future::pending::<T>()` as a pending future state that can be polled through the existing task runtime. Shared `FutureRuntimeCallPlan` classification lives in `src/lowering_rules.zig`; type checking accepts the flattened turbofish spelling, and both emitters consume the shared future runtime call plan to emit `FUTURE_PENDING_STATE_NEW`.
- `async_while_let_future_queue_direct`: direct SAB now lowers `while let Some(task) = queue.pop()` over `Vec<future<T>>` without fallback. Shared `WhileLetPatternPlan`/pattern classification and generic `Option`/`Result`/`Vec` helpers live in `src/lowering_rules.zig`; SAB consumes them for enum-pattern `while let`, direct `vec(...)` construction, and direct `Vec::pop()` via `sa_vec_try_pop`, with std deps preloaded for the special direct vector paths.
- `async_block_on_direct`: direct SAB now passes `future<T>` values through the shared pointer-backed ABI, and imported `.sai` contracts preserve fallible return markers (`i32!`) in direct SAB extern signatures. This lets rosetta `09_async_await` lower and run a small `block_on` over ready futures while calling the std time sleep macro without fallback.
- `println_direct`: direct SAB now treats `println` as compiler builtin print formatting instead of an ordinary static call, consuming shared `lowering_rules.planPrintlnArg` classification and emitting structured `sa_print_bytes`/`sa_fmt_*` calls. SA-text literal string `println` now uses a borrowed const label, matching the print extern contract.
- `struct_update`: direct SAB and SA-text struct literals route explicit/update fields through shared `lowering_rules.planStructLiteralField` plus the shared field transfer policy; scalar update, copy-struct deep-copy, and pointer-backed field move/deep-copy paths are complete for the tracked fixtures.
- `enum_match`: shared enum tag/payload layout lives in `src/lowering_rules.zig`; SAB `genEnumLiteral`/`genMatch` consume it.
- `spaceship_cmp`: shared numeric/Ordering helpers feed SAB `genSpaceship`.
- `for_in_protocol`: SAB `genForOverProtocol` lowers iter_len/iter_at counted loops; `TypeChecker.methodForType` is public for this path.
- `generic_for_in_protocol`: tracked generic protocol fixture now passes direct SAB no-fallback and SA-text parity.
- `derive_semantics`: tracked derive fixture now passes; `debug()` emits structured direct SAB formatting calls plus `FORMAT_PUSH_BYTES` rather than nested text macro expansion.
- `vec_index_assign` and nested Vec field/index assignment: std-surface macro fragment emission now treats `elem_ty` as a literal type token while keeping register args as placeholders, so `VEC_SET_TYPED` can flatten/encode without an invalid placeholder type.
- `async_await`: tracked ready-future async subset now passes through shared async return/await plans; async functions return pointer ABI ready-state values and `.await` consumes `FUTURE_READY_STATE_INTO_INNER`.
- `async_task_runtime`: ready futures can now flow through `future::ready`, `task::new`, `task::poll`, `task::is_ready`, and `task::result` in both SA-text and direct SAB. This covers ready-state task polling, not full async state-machine/executor lowering.
- `sci_embedded_token_remap`: SCI fragment cloning now remaps symbol tokens embedded in `.text`, `raw_text`, atomic text, and native text through the same source-symbol/remap/target-symbol table used by structured operands; `__sla_macro_arg_N` placeholders remain opaque, and string/comment text is left untouched.

## Verified Gates For Recent Slices

- `zig fmt --check src/type_checker.zig src/codegen.zig src/sab_codegen.zig` passed.
- `zig build --summary all` passed.
- `zig build test --summary all` passed 63/63.
- `sa plugin install --dev .` passed (dev plugin reinstalled from `sa_plugin_sla` dir).
- `SA_PLUGIN_DEV=1 sa sla help` passed.
- Focused dev-mode SAB no-fallback and SA-text parity passed for `tests/test_unit_struct_update.sla`, `tests/test_unit_enum_match.sla`, `tests/test_unit_spaceship_cmp.sla`, `tests/test_unit_for_in_protocol.sla`, `tests/test_unit_generic_for_in_protocol.sla`, and `tests/test_unit_derive_semantics.sla`.
- Dev-mode no-fallback regression guards passed for `tests/test_unit_sets.sla` and `/home/vscode/projects/sla_ecs/lib/parallel.sla`.
- Focused dev-mode SAB no-fallback and SA-text parity passed for `tests/test_unit_vec_index_assign.sla` and `tests/test_unit_field_compare_and_nested_len.sla`.
- Focused dev-mode SAB no-fallback and SA-text parity passed for `tests/test_unit_async_await.sla`.
- Focused dev-mode SAB no-fallback and SA-text parity passed for `tests/test_unit_async_task_runtime.sla`.
- Full dev-mode no-fallback sweep passed 73/73 after adding `tests/test_unit_println_direct.sla`.
- Formatted `println` slice passed: `zig fmt --check src/lowering_rules.zig src/sab_codegen.zig src/codegen.zig`; `zig build --summary all`; `zig build test --summary all` (64/64); local and host SAB no-fallback plus SA-text parity for `tests/test_unit_println_direct.sla`; host SAB no-fallback for rosetta `75_async_bridge`, `134_join_all_futures`, and `140_yield_now_suspend`; host `/home/vscode/projects/sla_ecs/lib/parallel.sla`; local and host full no-fallback sweeps 73/73; disasm guard clean for the print fixture, the three rosetta demos, and parallel.
- Async `block_on` ready-future slice passed: `zig fmt --check src/contract_parser.zig src/lowering_rules.zig src/sab_codegen.zig`; `zig build --summary all`; `zig build test --summary all` (65/65); focused parser/lowering Zig tests; local and host SAB no-fallback plus SA-text parity for `tests/test_unit_async_block_on_direct.sla` and rosetta `09_async_await`; host SAB no-fallback for async rosetta guards `75`, `133`, `134`, `135`, `139`, and `140`; host `/home/vscode/projects/sla_ecs/lib/parallel.sla`; local and host full no-fallback sweeps 74/74; disasm guard clean for the new fixture, rosetta `09`, and parallel, with `sa_time_sleep_ns` preserved as `i32!`; `git diff --check`.
- Async `while let` future queue slice passed: `zig fmt --check src/lowering_rules.zig src/sab_codegen.zig`; `zig build --summary all`; `zig build test --summary all` (67/67); local focused SA backend and SAB no-fallback for `tests/test_unit_async_while_let_future_queue_direct.sla`; local focused SA backend and SAB no-fallback for rosetta `136_executor_task_queue`; local SAB guard batch for async/Option/Vec regressions; `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host SAB no-fallback for `tests/test_unit_async_while_let_future_queue_direct.sla`, rosetta `136_executor_task_queue`, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; local and host full no-fallback sweeps 75/75; disasm guard clean for the queue fixture, rosetta `136`, and parallel.
- Async pending-future task poll slice passed: `zig fmt --check src/lowering_rules.zig src/type_checker.zig src/codegen.zig src/sab_codegen.zig`; `zig build --summary all`; `zig build test --summary all` (68/68); local SA backend and SAB no-fallback for `tests/test_unit_async_pending_task_runtime.sla`; local async guard batch for ready task polling, async await, block_on, while-let queue, rosetta `09`, and rosetta `136`; local full no-fallback sweep 76/76; `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host SAB no-fallback and SA-text parity for the pending fixture; host SAB no-fallback for `tests/test_unit_async_task_runtime.sla` and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; host full no-fallback sweep 76/76; disasm guard clean for the pending fixture and parallel.
- Async task-state runtime slice passed: `zig fmt --check src/lowering_rules.zig src/type_checker.zig src/codegen.zig src/sab_codegen.zig`; `zig build --summary all`; `zig build test --summary all` (68/68); local SA backend and SAB no-fallback for `tests/test_unit_async_task_state_runtime.sla`; local async guard batch for task-state, pending task polling, ready task polling, async await, block_on, while-let queue, rosetta `09`, and rosetta `136`; local full no-fallback sweep 77/77; `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host SAB no-fallback and SA-text parity for the task-state fixture; host SAB no-fallback for `/home/vscode/projects/sla_ecs/lib/parallel.sla`; host full no-fallback sweep 77/77; disasm guard clean for the task-state fixture and parallel.
- Async Poll runtime slice passed: `zig fmt --check src/lowering_rules.zig src/type_checker.zig src/codegen.zig src/sab_codegen.zig`; `zig build --summary all`; `zig build test --summary all` (68/68); local SA backend and SAB no-fallback for `tests/test_unit_async_poll_runtime.sla`; local async guard batch for task runtime, pending task polling, task-state, async await, block_on, while-let queue, rosetta `09`, and rosetta `136`; local full no-fallback sweep 78/78; `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host SAB no-fallback and SA-text parity for the Poll fixture; host SAB no-fallback for task runtime, pending task polling, task-state, rosetta `136`, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; host full no-fallback sweep 78/78; disasm guard clean for the Poll fixture and parallel.
- Async fixed-array executor runtime slice passed: `zig fmt --check src/lowering_rules.zig src/type_checker.zig src/codegen.zig src/sab_codegen.zig`; `zig build --summary all`; `zig build test --summary all` (68/68); local SA backend and SAB no-fallback for `tests/test_unit_async_executor_runtime.sla`; local async SA/SAB guard batches for task runtime, pending task polling, task-state, scalar Poll runtime, async await, block_on, and while-let queue; local full no-fallback sweep 79/79; `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host SAB no-fallback and SA-text parity for the executor fixture; host no-fallback `/home/vscode/projects/sla_ecs/lib/parallel.sla`; host full no-fallback sweep 79/79; disasm guard clean for the executor fixture and parallel.
- Async `future::join2` runtime slice passed: `zig fmt --check src/lowering_rules.zig src/type_checker.zig src/codegen.zig src/sab_codegen.zig`; `zig build --summary all`; `zig build test --summary all` (68/68); local SA backend and direct-SAB no-fallback for `tests/test_unit_async_join2_runtime.sla`; local async SAB guards for task runtime, pending task polling, scalar Poll runtime, and fixed-array executor runtime; local full no-fallback sweep 80/80; `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host direct-SAB no-fallback and SA-text parity for the join2 fixture; host no-fallback task/pending/executor guards and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; host full no-fallback sweep 80/80; disasm guard clean for the join2 fixture and parallel; `git diff --check`.
- Async `future::select2` runtime slice passed: `zig fmt --check src/lowering_rules.zig src/type_checker.zig src/codegen.zig src/sab_codegen.zig`; `zig build --summary all`; `zig build test --summary all` (68/68); local SA backend and direct-SAB no-fallback for `tests/test_unit_async_select2_runtime.sla`; local async SAB guards for join2, task runtime, pending task polling, scalar Poll runtime, fixed-array executor runtime, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; local full no-fallback sweep 81/81; `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host direct-SAB no-fallback and SA-text parity for the select2 fixture; host no-fallback join2/task/pending/executor guards and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; host full no-fallback sweep 81/81; disasm guard clean for the select2 fixture and parallel; `git diff --check`.
- Dynamic executor Vec task-buffer slice passed: `zig fmt --check src/lowering_rules.zig src/type_checker.zig src/codegen.zig src/sab_codegen.zig`; `zig test src/lowering_rules.zig --test-filter "shared executor task buffer classification"`; `zig build --summary all`; `zig build test --summary all` (69/69); local SA backend and direct-SAB no-fallback for `tests/test_unit_async_executor_vec_runtime.sla`; local full no-fallback sweep 82/82; `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host direct-SAB no-fallback and SA-text parity for the Vec executor fixture; host SA-text parity for the fixed-array executor fixture; host no-fallback guard batch for Vec executor, fixed-array executor, select2, join2, task runtime, pending task polling, scalar Poll runtime, and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; host full no-fallback sweep 82/82; disasm guard clean for the Vec executor fixture and parallel; `git diff --check`.
- Pending `.await` async state-machine sub-slice passed: `zig fmt src/lowering_rules.zig src/codegen.zig src/sab_codegen.zig`; `zig test src/lowering_rules.zig --test-filter "shared future runtime call classification"`; `zig build --summary all`; `zig build test --summary all` (69/69); local SA backend and direct-SAB no-fallback for `tests/test_unit_async_pending_await_state_machine.sla`; local async guard batch for await/task/executor/join/select paths; local full no-fallback sweep 83/83; `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host direct-SAB no-fallback and SA-text parity for the new fixture; host async guard batch including `/home/vscode/projects/sla_ecs/lib/parallel.sla`; host full no-fallback sweep 83/83; disasm guard clean for the new fixture and parallel; `git diff --check`.
- Future-parameter `.await` async state-machine sub-slice passed: `zig fmt src/lowering_rules.zig src/codegen.zig src/sab_codegen.zig`; `zig test src/lowering_rules.zig --test-filter "shared future runtime call classification"`; `zig build --summary all`; `zig build test --summary all` (69/69); local SA backend and direct-SAB no-fallback for `tests/test_unit_async_param_await_state_machine.sla`; local async guard batch including rosetta async demos and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; local full no-fallback sweep 84/84; `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host direct-SAB no-fallback and SA-text parity for the new fixture; host async guard batch including `/home/vscode/projects/sla_ecs/lib/parallel.sla`; host full no-fallback sweep 84/84; disasm guard clean for the new fixture and parallel; `git diff --check`.
- Statically ready composite-future `.await` sub-slice passed: `zig fmt src/lowering_rules.zig src/codegen.zig src/sab_codegen.zig`; `zig test src/lowering_rules.zig --test-filter "shared future runtime call classification"`; `zig build --summary all`; `zig build test --summary all` (69/69); local SA backend and direct-SAB no-fallback for `tests/test_unit_async_join2_await_state_machine.sla`; local async guard batch including rosetta async demos and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; local full no-fallback sweep 85/85; `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host direct-SAB no-fallback and SA-text parity for the new fixture; host async guard batch including `/home/vscode/projects/sla_ecs/lib/parallel.sla`; host full no-fallback sweep 85/85; disasm guard clean for the new fixture and parallel; `git diff --check`.
- Local future-readiness `.await` dataflow sub-slice passed: `zig fmt src/lowering_rules.zig src/codegen.zig src/sab_codegen.zig`; `zig test src/lowering_rules.zig --test-filter "shared future runtime call classification"`; `zig build --summary all`; `zig build test --summary all` (69/69); local SA backend and direct-SAB no-fallback for `tests/test_unit_async_join2_local_await_state_machine.sla`; generated SA shows `FUTURE_POLL` plus `POLL_VALUE` for local `join2(...).await`; local async guard batch including rosetta async demos and `/home/vscode/projects/sla_ecs/lib/parallel.sla`; local full no-fallback sweep 86/86; `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host direct-SAB no-fallback and SA-text parity for the new fixture; host async guard batch including `/home/vscode/projects/sla_ecs/lib/parallel.sla`; host full no-fallback sweep 86/86; disasm guard clean for the new fixture and parallel; `git diff --check`.
- Local pending composite-future `.await` propagation sub-slice passed: `sa version` (`0.0.3.3`); `zig fmt src/lowering_rules.zig`; `zig test src/lowering_rules.zig --test-filter "shared future runtime call classification"`; `zig build --summary all`; `zig build test --summary all` (69/69); local direct-SAB no-fallback and SA-text parity for `tests/test_unit_async_join2_local_pending_await_state_machine.sla`; local full no-fallback sweep 87/87; `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host direct-SAB no-fallback and SA-text parity for the new fixture; host full no-fallback sweep 87/87; host `/home/vscode/projects/sla_ecs/lib/parallel.sla`; disasm guard clean for the new fixture and parallel.
- Captured-plus-immediate defer-ready async await resumption slice passed: `sa version` (`0.0.3.3`); `zig fmt --check src/lowering_rules.zig src/codegen.zig src/sab_codegen.zig`; `zig test src/lowering_rules.zig --test-filter "async single await continuation"` (2/2); `zig test src/lowering_rules.zig --test-filter "parsed captured addend"`; `zig build --summary all`; `zig build test --summary all` (71/71); local direct-SAB no-fallback and SA-text parity for `tests/test_unit_async_defer_ready_capture_immediate_state_machine.sla`; prior local defer-ready async guards; local full no-fallback sweep 93/93; `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host direct-SAB no-fallback and SA-text parity for the new fixture; host full no-fallback sweep 93/93; host `/home/vscode/projects/sla_ecs/lib/parallel.sla`; disasm guard clean for the new fixture and parallel; `git diff --check`.
- Captured-addend defer-ready async await resumption slice passed: `sa version` (`0.0.3.3`); `zig fmt --check src/lowering_rules.zig src/codegen.zig src/sab_codegen.zig`; `zig test src/lowering_rules.zig --test-filter "async single await continuation"`; `zig test src/lowering_rules.zig --test-filter "parsed captured addend"`; `zig build --summary all`; `zig build test --summary all` (71/71); local direct-SAB no-fallback and SA-text parity for `tests/test_unit_async_defer_ready_capture_state_machine.sla`; prior local defer-ready async guards; local full no-fallback sweep 92/92; `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host direct-SAB no-fallback and SA-text parity for the new fixture; host full no-fallback sweep 92/92; host `/home/vscode/projects/sla_ecs/lib/parallel.sla`; disasm guard clean for the new fixture and parallel; `git diff --check`.
- Defer-ready post-await binding async await resumption slice passed: `sa version` (`0.0.3.3`); `zig fmt src/lowering_rules.zig src/codegen.zig src/sab_codegen.zig`; `zig test src/lowering_rules.zig --test-filter "async single await continuation"`; `zig build --summary all`; `zig build test --summary all` (70/70); local direct-SAB no-fallback and SA-text parity for `tests/test_unit_async_defer_ready_post_bind_state_machine.sla`; prior local defer-ready async guards; local full no-fallback sweep 91/91; `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host direct-SAB no-fallback and SA-text parity for the new fixture; host defer-ready async guards; host full no-fallback sweep 91/91; host `/home/vscode/projects/sla_ecs/lib/parallel.sla`; disasm guard clean for the new fixture and parallel.
- Prebound-local defer-ready async await resumption slice passed: `sa version` (`0.0.3.3`); `zig fmt src/lowering_rules.zig`; `zig test src/lowering_rules.zig --test-filter "async single await continuation"`; `zig build --summary all`; `zig build test --summary all` (70/70); local direct-SAB no-fallback and SA-text parity for `tests/test_unit_async_defer_ready_local_await_state_machine.sla`; local full no-fallback sweep 90/90; `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host direct-SAB no-fallback and SA-text parity for the new fixture; host full no-fallback sweep 90/90; host `/home/vscode/projects/sla_ecs/lib/parallel.sla`; disasm guard clean for the new fixture and parallel.
- Defer-ready async await resumption slice passed: `sa version` (`0.0.3.3`); `zig fmt src/lowering_rules.zig src/codegen.zig src/sab_codegen.zig`; `zig test src/lowering_rules.zig --test-filter "async single await continuation"`; `zig build --summary all`; `zig build test --summary all` (70/70); local direct-SAB no-fallback and SA-text parity for `tests/test_unit_async_defer_ready_await_state_machine.sla`; local full no-fallback sweep 89/89; `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host direct-SAB no-fallback and SA-text parity for the new fixture; host full no-fallback sweep 89/89; host `/home/vscode/projects/sla_ecs/lib/parallel.sla`; disasm guard clean for the new fixture and parallel.
- Defer-ready future task-runtime slice passed: `sa version` (`0.0.3.3`); `zig fmt src/lowering_rules.zig src/type_checker.zig src/codegen.zig src/sab_codegen.zig`; `zig test src/lowering_rules.zig --test-filter "shared future runtime call classification"`; `zig build --summary all`; `zig build test --summary all` (69/69); local direct-SAB no-fallback and SA-text parity for `tests/test_unit_async_defer_ready_task_runtime.sla`; local full no-fallback sweep 88/88; `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; host direct-SAB no-fallback and SA-text parity for the new fixture; host full no-fallback sweep 88/88; host `/home/vscode/projects/sla_ecs/lib/parallel.sla`; disasm guard clean for the new fixture and parallel.
- Filtered std-dep closure sub-slice passed: `appendDecodedModuleFiltered` now includes same-module direct-call dependencies in original decoded-module order and skips duplicate selected helper names. This keeps verifier-visible helper signatures/bodies coherent for std helpers such as HashSet/BTreeSet dependencies.
- SCI focused tests passed: `zig test src/flattener.zig --test-filter "frontend cache clone remaps embedded call text tokens with operands"`, `zig test src/flattener.zig --test-filter "frontend cache clone remaps instruction symbols and owned metadata"`, `zig test src/flattener.zig --test-filter "frontend cache append fragment remaps and merges end to end"`, and `zig test src/sab.zig --test-filter "disasmModule separates call target from call args"`.
- SCI `zig fmt --check src/flattener.zig` passed. SCI full `zig build test --summary all` was attempted but hit an environment/plugin-state failure in `plugin_host_smoke.test.runtime blocks privileged installed plugins outside dev mode` because a dev plugin is installed; no flattener failure was observed.
- After the SCI remap change, `sa_plugin_sla` `zig build --summary all` passed, `zig build test --summary all` passed 62/62, `sa plugin install --dev .` passed, `SA_PLUGIN_DEV=1 sa sla help` passed, focused macro/fragment guards passed, `/home/vscode/projects/sla_ecs/lib/parallel.sla` passed, full dev-mode no-fallback sweep stayed 69/69, and the parallel SAB disasm illegal-call-target guard returned no matches.

## Remaining Tracked Unit Failures

- None in `tests/test_unit_*.sla` under dev-mode direct SAB no-fallback.
- Broader hardening remains: generic SCI fragment naming and full async/Future state-machine support beyond local statically-ready/pending propagation and ready/pending-state task-runtime subsets.

### SCI Boundary Note

The generic std-macro fragment naming problem is still a real SCI-boundary task: `flatten/encode` should eventually provide one authoritative mechanism for placeholder args, embedded hygiene-token replacement, call-body text remap, and extern/export declaration preservation. Do not keep adding plugin-side fixture hacks for that class. Plugin-side work is acceptable only where the SAB emitter is naturally more structured anyway; `debug()` now follows that rule by formatting primitive debug fields through direct structured `sa_fmt_*_into` calls plus the shared `FORMAT_PUSH_BYTES` path.

## Next Active Slice

- Continue docs issue audit while moving the SLA frontend import architecture beyond the first Module Table slice. The user-requested strict glob `docs/*issue.md` currently has no matches in this repo; the actual issue-ticket set is `docs/*issue*.md`, including `*_issue_cn.md`, and must be considered for each slice. Current open/partial issue docs include the `sla_tsgo` project snapshot/API timeout, compiler checker-pool timeout, `parallel_runner.sla` / `task_pool_builder.sla` whole-file dangerous smoke, and the external `sa_lua` runtime entry issue. The prior `removed_components` downstream whole-file retest is now closed in `docs/sab_system_param_removed_components_memoryleak_issue_cn.md`.
- Next implementation target: reduce the still-high `import expand` cost in real `sla_ecs` focused profiles by continuing shallow scan/lazy materialization work below the current parser/reparse level. Keep referenced-surface contract queueing, Module Table cache, namespace aliasing, and selective reachable-body parsing as foundations, but do not claim the real latency target until `parallel_query.sla` and `parallel_table_erased.sla` move on real profile numbers.
- Completed docs-priority compiler issue: `docs/sab_anyof9_table_erased_memoryleak_issue_cn.md` `first_type_id` MemoryLeak is fixed for the compiler-owned SAB cleanup surface. Direct SAB now emits visible `.move_` consume instructions for entry stack-slotted by-value Copy scalar params and for param cleanup consume paths, instead of only marking those registers consumed in emitter state. Verification: `zig fmt --check src/sab_codegen.zig`; `zig build --summary all`; `zig build test --summary all` (94/94); official `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; local and installed strict SAB focused scalar/COPY-param fixtures; downstream strict SAB `system_param_table_erased.sla --filter "filtered pair mut system params"` pass; downstream relationship/observer AnyOf wrapper strict SAB filters pass; full local strict SAB unit sweep 118/118; full installed host strict SAB unit sweep 118/118; `git diff --check`. Downstream `world_table_erased.sla --filter "anyof nested"` no longer verifier-traps but fails with `panic 15305` under both SA backend and strict SAB, so that remaining assertion is downstream semantic/business evidence rather than a current SAB cleanup blocker.
- Completed Phase 1 RefCell lifecycle sub-slice: owner release now uses shared `planRefCellHandleCellRelease` to release active RefCell borrow handles attached to the owner before the owner is released. Direct SAB branch-local cleanup now emits RefCell dynamic-borrow release and borrow-temp cleanup through a branch-local `seen` set without mutating global release state, and SA-text chained `if let` fail-path cleanup restores consumed/borrow/RefCell state after emitting fail cleanup. Added/extended `tests/test_unit_refcell_owner_release_direct.sla` to 2/2; verified local/installed SA-text and strict direct-SAB focused fixture, rosetta 104 SA/SAB, existing RefCell payload SA/SAB 7/7, borrow-temp strict SAB 25/25, `zig build`, `zig build test` 94/94, official dev install/help, and full local/installed strict direct-SAB sweeps 119/119.
- The tracked function-exit lifecycle audit is closed: ordinary branch/loop/try/let-else exits, implicit/explicit returns, specialized continuation entries, async pending returns, and empty void exits now have focused parity evidence. Resume the primary open implementation target above: profile-driven reduction of real `sla_ecs` `import expand` cost through deeper shallow scan/lazy materialization work, without adding flattened-name compatibility hacks.
- Keep `sla_ecs` usage limited to downstream host regression evidence. Compiler support belongs in this repo's shared frontend/lowering/std metadata and direct SAB emitter.
- Expected verification for the next slice: exact affected Zig filter, focused local fixture, strict direct SAB no-fallback, SA-text parity where applicable, `zig build --summary all`, official `sa plugin install --dev .`, `SA_PLUGIN_DEV=1 sa sla help`, and relevant downstream host checks. Do not rerun the full Zig or unit corpus until another real milestone.
- Current baseline: broader Y/shared-lowering about 99%; direct SAB tracked-corpus fallback removal remains 100%; latest authoritative dev-mode strict direct-SAB unit sweep is 130/130 fixture files. The 166-test Zig milestone baseline remains valid and was intentionally not rerun for this focused slice.

## Latest Completed Slice

- SLA frontend Module Table lazy transitive module discovery (2026-07-10): small focused test-codegen roots now avoid eager recursive parsing of child `.sla` imports under non-contributing modules, while reachable child modules are discovered through a contributing-parent fixpoint and large roots retain eager recursion. Verification: new transitive regressions 2/2, selective parsing 5/5, `sla module table` 10/10, format check, `zig build -j1`, `zig build test -j1` 165/165, official dev install, and real `sla_ecs` profile probes. Feature completion: this lazy-discovery sub-slice 100%; real ECS latency target remains open because profile results are noisy and `system_param_table_erased.sla` is still above target.

- SLA frontend Module Table same-named imported method body selection (2026-07-10): imported impl/overload method bodies are now selected by mangled method symbol instead of bare method name, and syntactic reachability uses local receiver type facts to mark only matching associated-call candidates when possible. Verification: focused same-name method regression, selective reachable-body regressions, `sla module table` 10/10, `zig fmt --check src/parser.zig src/plugin.zig`, real `sla_ecs` profiles for `system_param_table_erased.sla` and `tests/test_ecs_mut_parallel.sla`, `zig build -j1 --summary all`, `zig build test -j1 --summary all` 163/163, official dev install/help, and `git diff --check`. Feature completion: this symbol-narrowing sub-slice 100%; real ECS latency target remains open for broader corpus validation and import-expansion tail work.

- SLA frontend Module Table referenced-surface contract queue (2026-07-10): contributing imported modules now queue non-SLA contracts by referenced surface rather than by whole-module reachability. Dead `.sai/.sal` imports are skipped when they do not declare a referenced extern/layout symbol, used macro/extern/layout surfaces are retained, and `.sa` transitive wrappers remain conservative. Verification: contract-loading 2/2, contributing-module contract import regression, referenced type-only macro import, `sla module table` 10/10, resolved non-SLA reuse, nested contract SAB smoke, `zig fmt --check src/plugin.zig src/parser.zig`, `zig build -j1 --summary all`, `zig build test -j1 --summary all` 159/159, official dev install/help, real `sla_ecs` focused SA profiles passed, and `git diff --check`. Feature completion: this queue-cropping sub-slice 100%; real ECS latency target remains open because `load contracts` stayed around 228-257ms and `import expand` stayed high/noisy.

- SLA frontend Module Table selective reachable-body parsing for test-codegen (2026-07-10): imported `.sla` modules can now be shallow parsed, then reparsed in place with only the selected reachable function/method/operator/macro body names discovered by the Module Table worklist, including helper and macro bodies found after the first reachable body is parsed. Same-module helper calls are namespace-qualified only for already-qualified imported callers, preserving bare flatten compatibility while supporting sibling modules with same-named helpers. Verification: parser/selective/same-name focused tests, `sla module table` 10/10, imported pruning, registry-loaded imported bodies 2/2, contract-loading 2/2, `zig fmt --check src/parser.zig src/plugin.zig`, `zig build -j1 --summary all` 7/7, `zig build test -j1 --summary all` 162/162, official dev install/help, and real `sla_ecs` focused SA profiles (`parallel_query` import expand 289ms/load contracts 14ms; `parallel_table_erased` import expand 406ms/load contracts 11ms). Feature completion: this selective parsing sub-slice 100%; real ECS latency target remains open for broader corpus validation and remaining import-expansion tail work.

- SLA frontend Module Table namespace-alias callable index ownership (2026-07-08): the callable index now has module-qualified alias keys for imported top-level functions, which reduces one of the remaining namespace-isolation risks in the Module Table layer.
- Implementation: `SlaCallableIndex.addDeclsFromModule()` records `<module-namespace>__<function>` aliases such as `dep__imported_a` and `sibling__imported_a` alongside the existing bare-name compatibility entry. The export-index regression now adds both sibling modules to one callable index and proves each alias carries the correct module source and distinct function body.
- Verification, run serially with explicit timeouts and no full-suite gate this slice: `timeout 120s zig fmt --check src/plugin.zig`; `timeout 180s zig build test -Dtest-filter="sla module exports index records per-module" --summary all` (1/1); `timeout 180s zig build test -Dtest-filter="sla module table" --summary all` (9/9); `timeout 120s git diff --check`.
- Feature completion: namespace-alias callable index ownership 100%; overall Module Table target approximately 76%. Remaining Module Table work includes full namespace-isolated TypeChecker/codegen symbol emission, deeper direct-SAB dependency loading, downstream timeout validation, and broader verification before claiming the lazy imported-body architecture complete.

- SLA frontend Module Table imported dyn-dispatch registry-body coverage (2026-07-08): normal SA-text build and direct-SAB build now have focused coverage proving registry-loaded imported bodies preserve vtable-needed trait impl bodies for imported dyn trait dispatch.
- Implementation: added regression `sla build codegen keeps imported dyn trait impl bodies from registry`, covering `Box<dyn Identified>` built from an imported concrete type and dispatched through an imported trait impl. The test checks generated SA contains the reachable `ImportedThing__Identified_get_id` body and omits the unused broken `missing_dyn_body`, then decodes direct SAB and checks the reachable trait method signature is present while the unused method signature is absent.
- Verification, run serially with explicit timeouts and no full-suite gate this slice: `timeout 120s zig fmt --check src/plugin.zig`; `timeout 180s zig build test -Dtest-filter="sla build codegen keeps imported dyn trait impl bodies from registry" --summary all` (1/1); `timeout 180s zig build test -Dtest-filter="sla module table" --summary all` (9/9); `timeout 120s git diff --check`.
- Feature completion: imported dyn-dispatch registry-body coverage 100%; overall Module Table target approximately 74%. Remaining Module Table work includes deeper direct-SAB dependency loading, namespace isolation cleanup, downstream timeout validation, and broader verification before claiming the lazy imported-body architecture complete.

- SLA frontend Module Table normal build/codegen registry-loaded body wiring (2026-07-08): normal SA-text build and direct-SAB build now use registry-loaded reachable imported bodies by default. This moves the body-registry strategy beyond test-codegen into ordinary compile/codegen paths while leaving `sa sla check` on its signature-only lazy-body path.
- Implementation: added `defaultSlaCompileOptions()` with `load_reachable_imported_bodies_from_registry = true`; `compileSlaToSaString` and `compileSlaFileToSab` now use it. `runSlaFrontend` already forwards that option to `expandSlaImports` as `imported_bodies_decl_only + load_reachable_imported_bodies_from_registry`.
- Verification, run serially with explicit timeouts and no full-suite gate this slice: `timeout 120s zig fmt --check src/plugin.zig`; `timeout 180s zig build test -Dtest-filter="sla build codegen uses registry loaded imported bodies" --summary all` (1/1); `timeout 180s zig build test -Dtest-filter="sla build rewrites sla imports relative to final output path" --summary all` (1/1); `timeout 180s zig build test -Dtest-filter="sla sab build emits direct SAB without SA source output" --summary all` (1/1); `timeout 180s zig build test -Dtest-filter="sla module table" --summary all` (9/9); `timeout 120s git diff --check`.
- Feature completion: normal build/codegen registry-loaded body wiring 100%; overall Module Table target approximately 72%. Remaining Module Table work includes deeper direct-SAB dependency loading, namespace isolation cleanup, dynamic dyn-dispatch signature/body coverage, and broader verification before claiming the lazy imported-body architecture complete.

- SLA frontend Module Table test-codegen registry-loaded body wiring (2026-07-08): SA-text and direct-SAB test-input compilation now enable registry-loaded reachable imported bodies by default. This wires the previous explicit expansion mode into the real test-codegen front-end path.
- Implementation: `SlaCompileOptions` gained `load_reachable_imported_bodies_from_registry`; `runSlaFrontend` forwards it to `expandSlaImports` as `imported_bodies_decl_only + load_reachable_imported_bodies_from_registry`; `compileSlaSaTestInput` and `compileSlaSabTestInput` set it to true alongside `prune_for_test_codegen`.
- Verification, run serially with explicit timeouts and no full-suite gate this slice: `timeout 120s zig fmt --check src/plugin.zig`; `timeout 180s zig build test -Dtest-filter="sla test codegen uses registry loaded imported bodies" --summary all` (1/1); `timeout 180s zig build test -Dtest-filter="sla test import expansion prunes unreachable imported methods" --summary all` (1/1); `timeout 180s zig build test -Dtest-filter="sla sab test codegen omits unreachable functions after type checking" --summary all` (1/1); `timeout 180s zig build test -Dtest-filter="sla module table" --summary all` (9/9); `timeout 120s git diff --check`.
- Feature completion: test-codegen registry-loaded body wiring 100%; overall Module Table target approximately 69%. Remaining Module Table work includes wiring normal build/codegen paths, direct-SAB dependency loading, and dynamic dyn-dispatch signature/body coverage.

- SLA frontend Module Table registry-loaded reachable bodies expansion mode (2026-07-08): import expansion now has an explicit mode that keeps imported declarations in signature/stub form while restoring only reachable imported bodies through the Module Table registry. This is the first concrete connection between the body registry and the expansion strategy.
- Implementation: `SlaImportExpansionOptions` gained `load_reachable_imported_bodies_from_registry`. When it is paired with `imported_bodies_decl_only`, reachable top-level functions, inherent methods, and trait methods keep real bodies, while unused trait impl methods remain `decl_only` stubs and unused broken functions remain omitted.
- Verification, run serially with explicit timeouts and no full-suite gate this slice: `timeout 120s zig fmt --check src/plugin.zig`; `timeout 180s zig build test -Dtest-filter="sla module table loads reachable imported bodies from registry while stubbing others" --summary all` (1/1); `timeout 180s zig build test -Dtest-filter="sla module table" --summary all` (9/9); `timeout 120s git diff --check`.
- Feature completion: registry-loaded reachable imported bodies expansion mode 100%; overall Module Table target approximately 66%. Remaining Module Table work includes wiring this mode into the default build/test/codegen front-end paths, direct-SAB dependency loading, and dynamic dyn-dispatch signature/body coverage.

- SLA frontend Module Table body-registry lookup foundation (2026-07-08): `SlaModuleTable` now has explicit function-body lookup APIs for later lazy body loading. The table can return top-level bodies by `(module_path, name)`, associated method bodies by `(module_path, mangled_symbol)`, namespace-qualified bodies from an importing module, and import-mangled top-level bodies such as `dep__imported_value`.
- Implementation: added `functionBody(...)`, `associatedFunctionBody(...)`, `functionBodyForImportNamespace(...)`, and `functionBodyForImportedMangledName(...)` to `src/plugin.zig`. These APIs return `*ast.FuncDecl` from the owning module's AST, separate from exported signature metadata.
- Verification, run serially with explicit timeouts and no full-suite gate this slice: `timeout 120s zig fmt --check src/plugin.zig`; `timeout 180s zig build test -Dtest-filter="sla module table resolves imported function bodies by module and namespace" --summary all` (1/1); `timeout 180s zig build test -Dtest-filter="sla module table" --summary all` (8/8); `timeout 120s git diff --check`.
- Feature completion: Module Table body-registry lookup foundation 100%; overall Module Table target approximately 63%. Remaining Module Table work includes wiring this registry into lazy build/test/codegen traversal, direct-SAB dependency loading, and dynamic dyn-dispatch signature/body coverage.

- SLA frontend Module Table decl-only method backend contract (2026-07-08): SA-text and direct-SAB codegen now skip `decl_only` impl/overload methods instead of attempting to emit empty function bodies. This makes signature-only imported method stubs a valid backend input, which is required before extending Module Table lazy-body loading beyond `check`.
- Implementation: `src/codegen.zig` and `src/sab_codegen.zig` both continue emitting concrete impl/overload methods, but now `continue` when a method's `FuncDecl` is `decl_only`. Top-level `decl_only` behavior is unchanged.
- Verification, run serially with explicit timeouts and no full-suite gate this slice: `timeout 120s zig fmt --check src/plugin.zig src/codegen.zig src/sab_codegen.zig`; `timeout 180s zig build test -Dtest-filter="sla sab codegen skips decl only imported trait impl methods" --summary all` (1/1); `timeout 180s zig build test -Dtest-filter="sla sab test codegen omits unreachable trait impls after type checking" --summary all` (1/1); `timeout 180s zig build test -Dtest-filter="sla module table prunes unreachable trait impl methods in contributing module" --summary all` (1/1); `timeout 180s zig build test -Dtest-filter="sla module table" --summary all` (7/7); `timeout 120s git diff --check`.
- Feature completion: decl-only method backend contract 100%; overall Module Table target approximately 60%. Remaining Module Table work includes body registry loading, extending lazy body traversal beyond `check`, direct-SAB dependency loading, and dynamic dyn-dispatch signature coverage.

- SLA frontend Module Table check-mode imported-trait signature lookup (2026-07-08): check-mode lazy imported-body support now covers imported trait impl method signatures for static dot calls and trait associated calls. Root code can call imported trait methods without TypeChecker entering the imported trait method body.
- Implementation: `registerImportedFunctionAliases()` now registers trait impl method signatures through the existing trait-method mangling path. `resolveTraitMethodSymbolForType(...)` accepts symbols backed by either full `FuncDecl` bodies or `imported_function_signatures`, and the trait dot-call / associated-call branches use a shared symbol-or-signature argument checker.
- Verification, run serially with explicit timeouts and no full-suite gate this slice: `timeout 120s zig fmt --check src/plugin.zig src/type_checker.zig src/codegen.zig`; `timeout 180s zig build test -Dtest-filter="sla check uses imported trait method signatures without checking imported trait bodies" --summary all` (1/1); `timeout 180s zig build test -Dtest-filter="sla check uses imported trait associated signatures without checking imported trait bodies" --summary all` (1/1); `timeout 180s zig build test -Dtest-filter="sla check uses imported method signatures without checking imported method bodies" --summary all` (1/1); `timeout 180s zig build test -Dtest-filter="sla module table" --summary all` (7/7); `timeout 120s zig test src/type_checker.zig --test-filter "type checker resolves imported function signature without function body"`; `timeout 120s git diff --check`.
- Feature completion: check-mode imported-trait signature lookup 100%; overall Module Table target approximately 58%. Remaining Module Table work includes extending lazy body traversal beyond `check`, direct-SAB dependency loading, and dynamic dyn-dispatch signature coverage.

- SLA frontend Module Table check-mode imported-method signature lookup (2026-07-08): check-mode lazy imported-body support now covers imported inherent/overload-style method calls. Root code can call methods on imported types without TypeChecker entering the imported method body.
- Implementation: `registerImportedFunctionAliases()` now registers signature metadata for imported non-trait impl methods under TypeChecker's existing mangled method symbols. The ordinary method-call path falls back to `imported_function_signatures` when no full method body is present in `tc.funcs`, preserving resolved-call metadata and async return wrapping. Trait dispatch signatures are covered by the later check-mode imported-trait signature slice.
- Verification, run serially with explicit timeouts and no full-suite gate this slice: `timeout 120s zig fmt --check src/plugin.zig src/type_checker.zig src/codegen.zig`; `timeout 180s zig build test -Dtest-filter="sla check uses imported method signatures without checking imported method bodies" --summary all` (1/1); `timeout 180s zig build test -Dtest-filter="sla check uses imported signatures without checking imported function bodies" --summary all` (1/1); `timeout 180s zig build test -Dtest-filter="sla module table" --summary all` (7/7); `timeout 120s zig test src/type_checker.zig --test-filter "type checker resolves imported function signature without function body"`.
- Feature completion: check-mode imported-method signature lookup 100%; overall Module Table target approximately 55%. Remaining Module Table work includes extending lazy body traversal beyond `check` and direct-SAB dependency loading.

- SLA frontend Module Table check-mode lazy imported-body gate (2026-07-08): `sa sla check` now runs import expansion with imported `.sla` function/method bodies converted to `decl_only` stubs while preserving signatures and non-function declaration surfaces. Root code can call imported functions through Module Table registered signatures, but check-mode no longer deep-checks imported function bodies.
- Implementation: `SlaImportExpansionOptions` gained `imported_bodies_decl_only`; `appendModuleDeclsSelective()` and filtered impl/overload append paths can clone reachable imported functions/methods as declaration-only stubs. The CLI `check` command enables this option. Build/test/codegen paths intentionally keep previous body behavior because they still need reachable bodies for output.
- Verification, run serially with explicit timeouts and no full-suite gate this slice: `timeout 120s zig fmt --check src/plugin.zig src/type_checker.zig src/codegen.zig`; `timeout 180s zig build test -Dtest-filter="sla check uses imported signatures without checking imported function bodies" --summary all` (1/1); `timeout 180s zig build test -Dtest-filter="sla module table" --summary all` (7/7); `timeout 120s zig test src/type_checker.zig --test-filter "type checker resolves imported function signature without function body"`; `timeout 180s zig build test -Dtest-filter="sla module namespace call resolves through imported function alias" --summary all` (1/1).
- Feature completion: check-mode lazy imported-body gate 100%; overall Module Table target approximately 52%. This is a concrete lazy-typecheck entry point for `check`, not full lazy imported-body typecheck across build/test/codegen and not direct-SAB dependency loading.

- SLA frontend Module Table imported-signature TypeChecker lookup foundation (2026-07-08): `TypeChecker` now owns a lightweight imported `FunctionSignature` table and can validate ordinary calls against registered exported signatures when no full `FuncDecl` body exists in `tc.funcs`. The existing body-backed path is unchanged and still wins when present; the signature fallback checks arguments, returns the signature return type, handles async return wrapping, and records resolved alias metadata for namespaced calls.
- Implementation: added `TypeChecker.FunctionSignature`, `imported_function_signatures`, and `registerImportedFunctionSignature(...)`; factored ordinary call argument checking into a signature-based helper consumed by both full functions and imported signatures. `registerImportedFunctionAliases()` now registers Module Table exported function signatures for both bare names and namespace aliases, and `SlaModuleExports.FunctionSignature` records `is_async` so async exports are representable as metadata.
- Verification, run serially with explicit timeouts and no full-suite gate this slice: `timeout 120s zig fmt --check src/type_checker.zig src/plugin.zig src/codegen.zig`; `timeout 120s zig test src/type_checker.zig --test-filter "type checker resolves imported function signature without function body"`; `timeout 180s zig build test -Dtest-filter="sla module namespace call resolves through imported function alias" --summary all` (1/1); `timeout 180s zig build test -Dtest-filter="sla module table" --summary all` (7/7); `timeout 120s zig test src/type_checker.zig --test-filter "type checker resolves registered function alias"`.
- Feature completion: imported-signature TypeChecker lookup foundation 100%; overall Module Table target approximately 48%. This proves TypeChecker can consume exported signatures without function bodies, but it is not yet full lazy imported-body typecheck or direct-SAB dependency loading; current codegen and reachable compile paths can still require flattened bodies.

- SLA frontend Module Table namespace-call compatibility bridge (2026-07-08): `.sla` import basename namespaces now work for calls through the current flattened compatibility frontend. `@import "dep.sla"` registers namespace `dep`, `dep::imported_a()` parses to `dep__imported_a`, frontend alias registration maps that synthetic namespace symbol back to the imported export `imported_a`, and TypeChecker/codegen use the resolved target so generated SA calls the existing flattened function symbol (`@sla__imported_a`) rather than `@sla__dep__imported_a`.
- Implementation: `TypeChecker.resolveFunctionAlias()` is now public for backend consumers; `src/codegen.zig` resolves aliases in ordinary void call statement lowering before looking up function params and emitting the callee; `src/plugin.zig` adds regression `sla module namespace call resolves through imported function alias` with generated-SA assertions. The earlier parser namespace and TypeChecker alias unit tests remain the focused lower-level guards.
- Verification, run serially with explicit timeouts and no full-suite gate this slice: `timeout 120s zig fmt --check src/plugin.zig src/type_checker.zig src/codegen.zig`; `timeout 180s zig build test -Dtest-filter="sla module namespace call resolves through imported function alias" --summary all` (1/1); `timeout 120s zig test src/type_checker.zig --test-filter "type checker resolves registered function alias"`; `timeout 120s zig test src/parser.zig --test-filter "parse sla import basename as module namespace"`; `timeout 180s zig build test -Dtest-filter="sla module table" --summary all` (7/7).
- Feature completion: namespace-call compatibility bridge 100%; overall Module Table target approximately 45%. This is still a bridge over flattened declarations, not full namespace isolation, exported-signature-only TypeChecker traversal, lazy imported-body typecheck, or lazy direct-SAB dependency loading. Next architecture work should push imported function/type lookup into TypeChecker through exported signatures instead of adding more flattened-name compatibility cases.

- SLA frontend Module Table module-qualified export lookup follow-up (2026-07-08): `SlaModuleTable` now exposes module-qualified lookup helpers for exported function/type/const/macro signatures using `(module_path, name)`. This lets the frontend distinguish same-name exports from sibling modules at the metadata layer before the future namespace resolver is wired into TypeChecker.
- Added/extended regression: `sla module exports index records per-module function sources` now creates both `dep.sla` and `sibling.sla` with same-named exported function/type/const/macro declarations, imports both, and asserts the table returns signatures for the requested module path rather than a flattened global name.
- Latest verification, batched per the current documentation cadence instead of updating docs after every test, run serially with explicit timeouts: `timeout 120s zig fmt --check src/plugin.zig`; `timeout 180s zig build test -Dtest-filter="sla module exports index records per-module" --summary all` (1/1); `timeout 180s zig build test -Dtest-filter="sla module table" --summary all` (7/7); `timeout 180s zig build test -Dtest-filter="sla test import expansion prunes unreachable imported" --summary all` (2/2); `timeout 180s zig build test -Dtest-filter="sla sab test codegen omits unreachable" --summary all` (2/2); `timeout 180s zig build --summary all`; `timeout 240s zig build test --summary all` (108/108); `env SA_PLUGIN_DEV=1 timeout 180s sa plugin install --dev .`; `env SA_PLUGIN_DEV=1 timeout 60s sa sla help`; installed strict SAB guards for `test_unit_sla_import.sla` (2/2) and `test_unit_trait_static_dispatch.sla` (4/4); `timeout 120s git diff --check`.
- Feature completion: this is a ModuleGraph namespace/data-contract hardening step. It is not completed namespace isolation, exported-signature-only TypeChecker traversal, lazy imported-body typecheck, or lazy direct-SAB dependency loading.

- SLA frontend Module Table exported const/macro signature surface follow-up (2026-07-08): `SlaModuleExports` now stores metadata-only `ConstSignature` and `MacroSignature` tables beside `const_decls` and `macro_decls`. Const signatures carry exported const name, optional declared type, and owning module path; macro signatures carry exported macro name, parameter names, and owning module path. This completes the current function/type/const/macro exported metadata surface while preserving compatibility flattening.
- Added/extended regression: `sla module exports index records per-module function sources` now also asserts `constSignature("IMPORTED_VALUE")` and `macroSignature("imported_macro")` return the expected metadata.
- Latest verification, run serially with explicit timeouts so far: `timeout 120s zig fmt --check src/plugin.zig`; `timeout 180s zig build test -Dtest-filter="sla module exports index records per-module" --summary all` (1/1). `timeout 180s zig build test -Dtest-filter="sla module table" --summary all` (7/7), import-pruning filter (2/2), and SAB unreachable-codegen filter (2/2) also pass. `timeout 180s zig build --summary all`; `timeout 240s zig build test --summary all` (108/108); `env SA_PLUGIN_DEV=1 timeout 180s sa plugin install --dev .`; `env SA_PLUGIN_DEV=1 timeout 60s sa sla help`; and installed strict SAB guards for `test_unit_sla_import.sla` (2/2) and `test_unit_trait_static_dispatch.sla` (4/4) also pass. `timeout 120s git diff --check` is clean.
- Feature completion: this is an exported const/macro signature data-contract hardening step, not full exported-signature-only TypeChecker traversal. Namespace isolation, lazy imported-body typecheck, module-qualified name resolution, and lazy direct-SAB dependency loading remain open.

- SLA frontend Module Table exported-type signature surface follow-up (2026-07-08): `SlaModuleExports` now stores a metadata-only `TypeSignature` table beside `type_decls`, carrying exported type name, type kind, generic parameter names, and owning module path. This creates a concrete imported-type metadata lookup surface for future exported-signature-only TypeChecker traversal while preserving current compatibility flattening.
- Added/extended regression: `sla module exports index records per-module function sources` now also asserts `typeSignature("ImportedTag")` returns the expected `struct_decl` signature and module path.
- Latest verification, run serially with explicit timeouts so far: `timeout 120s zig fmt --check src/plugin.zig`; `timeout 180s zig build test -Dtest-filter="sla module exports index records per-module" --summary all` (1/1). `timeout 180s zig build test -Dtest-filter="sla module table" --summary all` (7/7), import-pruning filter (2/2), and SAB unreachable-codegen filter (2/2) also pass. `timeout 180s zig build --summary all`; `timeout 240s zig build test --summary all` (108/108); `env SA_PLUGIN_DEV=1 timeout 180s sa plugin install --dev .`; `env SA_PLUGIN_DEV=1 timeout 60s sa sla help`; and installed strict SAB guards for `test_unit_sla_import.sla` (2/2) and `test_unit_trait_static_dispatch.sla` (4/4) also pass. `timeout 120s git diff --check` is clean.
- Feature completion: this is an exported-type signature data-contract hardening step, not full exported-signature-only TypeChecker traversal. Namespace isolation, lazy imported-body typecheck, module-qualified name resolution, and lazy direct-SAB dependency loading remain open.

- SLA frontend Module Table exported-signature surface follow-up (2026-07-08): `SlaModuleExports` now stores a metadata-only `FunctionSignature` table beside `function_decls`, carrying the exported function name, params, return type, extern/ABI/no-mangle flags, and owning module path. This creates a concrete signature lookup surface for future exported-signature-only TypeChecker traversal, while preserving the current compatibility flatten behavior.
- Added/extended regression: `sla module exports index records per-module function sources` now asserts `functionSignature("imported_a")` returns the expected zero-arg `i32` signature and module path.
- Latest verification, run serially with explicit timeouts so far: `timeout 120s zig fmt --check src/plugin.zig`; `timeout 180s zig build test -Dtest-filter="sla module exports index records per-module" --summary all` (1/1). `timeout 180s zig build test -Dtest-filter="sla module table" --summary all` (7/7), import-pruning filter (2/2), and SAB unreachable-codegen filter (2/2) also pass. `timeout 180s zig build --summary all`; `timeout 240s zig build test --summary all` (108/108); `env SA_PLUGIN_DEV=1 timeout 180s sa plugin install --dev .`; `env SA_PLUGIN_DEV=1 timeout 60s sa sla help`; and installed strict SAB guards for `test_unit_sla_import.sla` (2/2) and `test_unit_trait_static_dispatch.sla` (4/4) also pass. `timeout 120s git diff --check` is clean.
- Feature completion: this is an exported-signature data-contract hardening step, not full exported-signature-only TypeChecker traversal. Namespace isolation, lazy imported-body typecheck, module-qualified name resolution, and lazy direct-SAB dependency loading remain open.

- SLA frontend Module Table explicit dependency-graph follow-up (2026-07-08): `SlaModule` now stores `resolved_imports` when the `SlaModuleTable` parses each module. This makes each module carry the requested Module Registry shape of AST + exports + direct dependency edges. `appendModuleDeclsSelective()` and `collectSlaModulesRecursive()` now traverse the stored graph instead of rescanning import declarations and resolving paths again.
- Added/extended regression: `sla module exports index records per-module function sources` now also asserts that a parsed module records its direct `.sla` child import in `resolved_imports`.
- Latest verification, run serially with explicit timeouts so far: `timeout 120s zig fmt --check src/plugin.zig`; `timeout 180s zig build test -Dtest-filter="sla module exports index records per-module" --summary all` (1/1); `timeout 180s zig build test -Dtest-filter="sla module table" --summary all` (7/7); `timeout 180s zig build test -Dtest-filter="sla test import expansion prunes unreachable imported" --summary all` (2/2); `timeout 180s zig build test -Dtest-filter="sla sab test codegen omits unreachable" --summary all` (2/2). `timeout 180s zig build --summary all`; `timeout 240s zig build test --summary all` (108/108); `env SA_PLUGIN_DEV=1 timeout 180s sa plugin install --dev .`; `env SA_PLUGIN_DEV=1 timeout 60s sa sla help`; and installed strict SAB guards for `test_unit_sla_import.sla` (2/2) and `test_unit_trait_static_dispatch.sla` (4/4) also pass. `timeout 120s git diff --check` is clean.
- Feature completion: this is a ModuleGraph data-contract hardening step, not full lazy imported-body typecheck. Namespace isolation, exported-signature-only TypeChecker traversal, module-qualified name resolution, and lazy direct-SAB dependency loading remain open.

- SLA frontend Module Table selective-flatten slice (2026-07-08): `expandSlaImports()` now uses the parsed `SlaModuleTable` graph and `SlaCallableIndex` to compute reachable imported callables before flattening. On both test-codegen and normal compile/check paths, non-contributing imported `.sla` modules have their function/method bodies skipped instead of being blindly appended for TypeChecker. Contributing modules still provide required type/const/macro/trait surfaces plus reachable top-level functions and inherent/overload methods. Trait impl methods are filtered too; unreachable trait methods become decl-only stubs so TypeChecker sees complete trait impls without checking unused broken bodies. Referenced imported const initializers and macro bodies are scanned as symbol roots, so helper functions used only by imported consts/macros stay reachable. Referenced exported type signatures are scanned as well, so parent-module types can pull in child-module field/variant/trait/alias type dependencies. Generic type arguments on calls and generic function references are now scanned too, so `foo<ImportedType>(...)` and `foo<ImportedType>` pull in the imported type's module without flattening unrelated imported function bodies. A transitive guard ensures a non-contributing parent module does not block flattening of a reachable child module.
- Added regressions: `sla module table skips non contributing imported module bodies`, `sla module table selective flatten in non test compile path`, `sla module table reaches contributing transitive module through non contributing parent`, `sla module table prunes unreachable trait impl methods in contributing module`, `sla module table follows imported const initializer reachability`, `sla module table follows imported type signature dependencies`, and `sla module table follows generic argument type dependencies`.
- Latest verification, run serially with explicit timeouts: `timeout 120s zig fmt --check src/plugin.zig`; `timeout 180s zig build test -Dtest-filter="sla module table follows generic argument type dependencies" --summary all` (1/1); `timeout 180s zig build test -Dtest-filter="sla module table" --summary all` (7/7); `timeout 180s zig build test -Dtest-filter="sla test import expansion prunes unreachable imported" --summary all` (2/2); `timeout 180s zig build test -Dtest-filter="sla sab test codegen omits unreachable" --summary all` (2/2); `timeout 180s zig build --summary all`; `timeout 240s zig build test --summary all` (108/108); `env SA_PLUGIN_DEV=1 timeout 180s sa plugin install --dev .`; `env SA_PLUGIN_DEV=1 timeout 60s sa sla help`; installed strict SAB guards for `test_unit_sla_import.sla` (2/2) and `test_unit_trait_static_dispatch.sla` (4/4); `timeout 120s git diff --check`.
- Feature completion: this is a completed selective-flatten architecture slice, not full lazy imported-body typecheck. Full namespace isolation, exported-signature-only TypeChecker traversal, module-qualified name resolution, and lazy direct-SAB dependency loading remain open. Next work should broaden verification (`zig build test`, dev install/help, focused host import/SAB guards) and then push TypeChecker toward exported signatures instead of compatibility flattening.

- SLA frontend Module Table export-index foundation (2026-07-08): `src/plugin.zig` now has `SlaModuleExports` (indexes each resolved `.sla` module's exported type/function/const/macro/impl surface by name at parse time) and `SlaCallableIndex.module_sources` (records the owning module path for every registered callable symbol, including top-level functions and inherent/trait/overload methods). `buildReachableImportedTestSymbols` routes imported modules through `SlaCallableIndex.addDeclsFromModule(decls, module)`, so the test-codegen reachability computation now attributes each reachable callable to its source module instead of re-scanning every flattened body. Added Zig regression `sla module exports index records per-module function sources` proving `exportsType/exportsFunction` plus `moduleSource(...)` attribute the correct owning module for top-level and inherent-method callables. This is the first slice of the real ModuleGraph exported-signature index and the on-ramp to module-qualified lazy reachability. It is not yet namespace isolation or lazy imported-body typecheck: imported function/method bodies are still flattened, and the TypeChecker/monomorphizer still see the flattened surface.
- Verification: `zig fmt --check src/plugin.zig`; `zig build --summary all`; focused `zig build test -Dtest-filter="sla module exports index records per-module"` (1/1); focused `zig build test -Dtest-filter="sla test import expansion prunes unreachable imported"` (2/2); focused `zig build test -Dtest-filter="sla sab test codegen omits unreachable"` (2/2); full `zig build test --summary all` (101/101, up from 100/100); `SA_PLUGIN_DEV=1 sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; installed host strict `SLA_SAB_NO_FALLBACK=1` dev-mode guards for `test_unit_sla_import.sla` (2/2), `test_unit_sla_import_nested_contract.sla` (0 skipped), `test_unit_sla_import_wildcard.sla` (1/1), `test_unit_sla_import_wildcard_bare.sla` (1/1), `test_unit_trait_static_dispatch.sla` (4/4), `test_unit_rc_dyn_trait.sla` (3/3), and `test_unit_parallel_runner_loop_fnptr_transfer_direct.sla` (1/1). Known pre-existing/unrelated: `test_unit_sla_import_sa_std_output_path.sla` strict SAB `panic 1501` reproduces identically on the committed baseline, so it is not a regression of this slice.
- Feature completion: 100% for the per-module export-symbol index + module-source attribution foundation; module-qualified reachability, namespace isolation, lazy imported-body typecheck, and lazy direct-SAB dependency loading remain open.

- SLA frontend Module Table stopgap (earlier slice): `expandSlaImports()` now parses each resolved `.sla` module once per expansion pass and emits each imported module's declarations once, while preserving flattened-program compatibility for the existing TypeChecker and monomorphizer.
- Verification: `zig fmt --check src/plugin.zig`; `zig build --summary all`; focused import smoke for direct/nested/wildcard/bare/sa-std-output-relative imports; `zig build test -Dtest-filter="output-relative imports" --summary all`; 10s strict SAB focused parallel-runner fixtures. `sla_tsgo` project snapshot and compiler checker-pool profiles confirm the remaining bottleneck is still frontend flatten/typecheck work, not SA backend execution.
- Feature completion: 100% for anti-duplicate parsed-module cache; 0% for true ModuleGraph namespace isolation and lazy imported-body typecheck.
- SLA test-codegen reachable-body pruning: `compileSlaSabTestInput()` and `compileSlaSaTestInput()` now set `prune_for_test_codegen`. The frontend prunes unreachable top-level functions and unreachable inherent impl/overload methods before typecheck, then prunes the exact typechecked unreachable output set before direct-SAB codegen. Trait impls remain whole for vtable safety. Added `sla test codegen prunes unreachable functions before type checking` and `sla sab test codegen omits unreachable functions after type checking`.
- Verification: `zig fmt --check src/plugin.zig`; focused Zig tests for unreachable-function pruning, post-typecheck output pruning, unmatched-test pruning, and empty-filter skip; `zig build --summary all`; 10s local strict SAB tests for `test_unit_trait_static_dispatch.sla`, `test_unit_rc_dyn_trait.sla`, `test_unit_fn_ptr_value.sla`, and `test_unit_parallel_runner_loop_fnptr_transfer_direct.sla`; 10s SA import smoke. Downstream `sla_tsgo` 10s profiles still timeout, but typecheck/output pruning now complete quickly on the test path.
- Feature completion: 100% for test-only pre-typecheck and post-typecheck function/method pruning; full ModuleGraph/lazy import/codegen remains open.
- SLA test-codegen import-expansion reachable-body pruning: the test-codegen path now collects the imported `.sla` module graph in `SlaModuleTable`, computes selected-test syntactic reachability before flattening, and skips unreachable imported top-level functions plus unreachable inherent impl/overload methods while retaining declarations and trait impls. Added `sla test import expansion prunes unreachable imported functions`.
- Verification: `zig fmt --check src/plugin.zig`; focused Zig tests for import-expansion pruning, pre-typecheck pruning, post-typecheck pruning, and unmatched-test pruning; `zig build --summary all` (47.93s, MaxRSS about 1GB after `plugin.zig` edit); full `zig build test --summary all` (98/98, 53.80s, MaxRSS about 1.1GB); 10s local strict SAB import smoke and `test_unit_parallel_runner_loop_fnptr_transfer_direct.sla`. Downstream `sla_tsgo` still times out at 10s, but checker-pool improved to `import expand` 696ms and `type check` 1658ms before timeout; project background update improved to `import expand` 4834ms and `type check` 330ms before timeout.
- Feature completion: 100% for test-only import-expansion function/method pruning; full ModuleGraph namespace isolation, lazy imported-body typecheck, and lazy direct-SAB dependency loading remain open.
- SLA frontend callable-index foundation: early test-codegen reachability now uses `SlaCallableIndex` to register callable names and function/method bodies once, then follows reachable worklists by direct hash lookup. This removes the previous repeated root/imported decl scan per reachable name and gives the next ModuleGraph/lazy typecheck slice a concrete callable-body lookup structure. Full ModuleGraph exported-signature indexing, namespace isolation, lazy imported-body typecheck, and lazy direct-SAB dependency loading remain open.
- Verification: `zig fmt --check src/plugin.zig`; focused Zig tests for import-expansion pruning and pre-typecheck pruning; `zig build --summary all` (40.94s, MaxRSS about 1GB); full `zig build test --summary all` (99/99, 53.60s, MaxRSS about 1.1GB); `SA_PLUGIN_DEV=1 sa sla help`; downstream `sla_tsgo` checker-pool and project-background 10s profile refreshes. Both downstream tests still timed out, so their docs remain open. The initial `sa plugin install --dev .` failure was later traced to forced ReleaseFast compilation and recovered by the dev install gate slice below.
- SLA frontend callable-index associated-candidate foundation: `SlaCallableIndex` now records short method-name candidate lists for mangled inherent/trait/overload methods, so syntactic reachability for method and associated calls no longer performs a suffix scan across every callable symbol. Added `sla test import expansion prunes unreachable imported methods`, proving imported reachable methods are retained while unused broken methods are pruned before typecheck. Verification: focused imported-method/imported-function/pre-typecheck pruning tests, `zig build --summary all`, full `zig build test --summary all` (100/100), official `SA_PLUGIN_DEV=1 sa plugin install --dev .`, `SA_PLUGIN_DEV=1 sa sla help`, and local/installed strict SAB guards for impl static methods, Rc dyn trait, and SLA imports. Downstream 10s profiles still timeout: checker-pool reached `import expand` 558ms / `type check` 1340ms / `reachable decl filter` 5ms; project background reached `import expand` 4235ms / `type check` 347ms / `reachable decl filter` 0ms. This is a completed callable-index performance slice, not closure for lazy imported-body typecheck or direct-SAB dependency loading.

## Latest Completed Slice (Module Table foundation thread)
- SLA frontend callable-index associated-candidate slice committed as `b6224f0`. `SlaCallableIndex` now records short method-name candidate lists for mangled inherent/trait/overload methods while registering callable bodies, so syntactic reachability for method/associated calls is a candidate-table lookup instead of a full-symbol suffix scan per call. Added regression `sla test import expansion prunes unreachable imported methods` proving imported inherent methods keep the called method while pruning an unused broken method before typecheck.
- Verification: `zig fmt --check src/plugin.zig`; `zig build --summary all`; full `zig build test --summary all` (100/100); focused tests via `-Dtest-filter` for imported methods/functions and pre-typecheck pruning; official `SA_PLUGIN_DEV=1 sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; local and installed strict `SLA_SAB_NO_FALLBACK=1` guards for `tests/test_unit_sla_import.sla`, `tests/test_unit_trait_static_dispatch.sla`, `tests/test_unit_rc_dyn_trait.sla`, and `tests/test_unit_parallel_runner_loop_fnptr_transfer_direct.sla`. Downstream `sla_tsgo` checker-pool/project-background 10s profiles still timeout, so docs remain open.
- Dev install gate recovery: `build.zig` now routes installer-requested `-Doptimize=ReleaseFast` to Debug only under `SA_PLUGIN_DEV=1/true`, because the installer's fixed ReleaseFast build exceeded a direct 300s timeout with no output. This restores the official dev-install feedback loop without changing non-dev ReleaseFast behavior. Verification: `SA_PLUGIN_DEV=1 sa plugin install --dev .` passed in 0.31s; `zig build --summary all` and full `zig build test --summary all` (99/99) passed from cache; `SA_PLUGIN_DEV=1 sa sla help` passed; installed downstream checker-pool/project-background 10s profiles still timeout and remain open.
- Resolved stale docs issue: `docs/sa_backend_mut_parallel_whole_file_for_stmt_typemismatch_issue_cn.md` now records the historical generated-SA whole-file `test_ecs_mut_parallel.sla` `for_stmt` TypeMismatch as current-non-repro. The later aggregate reassignment cleanup slice covered the whole-file path: downstream generated-SA `test_ecs_mut_parallel.sla` passes 129/129 and default/SAB also passes 129/129. Future failures in this file should be reduced as fresh current-stage issues instead of reviving the stale `for_stmt` blocker.
- SLA test-codegen vtable-aware trait impl output pruning: the post-typecheck reachable-output filter now drops trait impl declarations only when no method is reachable and no reachable dyn borrow/Box dyn/Rc dyn coercion requires the impl vtable. Vtable-needed impls remain whole. Added `sla sab test codegen omits unreachable trait impls after type checking`.
- Verification: focused Zig trait-impl pruning test; `tests/test_unit_rc_dyn_trait.sla` strict SAB 10s guard; `zig build --summary all` (67.42s, MaxRSS about 1GB); full `zig build test --summary all` (99/99, 64.42s, MaxRSS about 1.1GB). Downstream checker-pool and project-background still time out at 10s, so the large timeout docs remain open.
- Feature completion: 100% for vtable-aware unused trait impl output pruning; direct-SAB dependency loading and true lazy imported-body typecheck remain open.
- Completed docs-priority direct-SAB register-scan issue: `docs/sab_task_pool_custom_batch_width_unknown_dst_issue_cn.md` is fixed. `recordCallBodyRegs()` now records direct call targets, but ordinary call-body identifiers are retained only when they are already current-scope registers, preventing std macro fragment/global names like `dst` from becoming undeclared current-function registers.
- Verification: `zig fmt --check src/sab_codegen.zig`; `zig build test -Dtest-filter="direct sab instruction reg scan records call body refs" --summary all`; `zig build --summary all` (44.34s, MaxRSS about 1GB); full `zig build test --summary all` (99/99, 43.26s, MaxRSS about 1.1GB); `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; installed downstream strict SAB 10s focused custom batch width (1/1, about 1.79s) and external executor identity (1/1, about 1.93s); local `zig-out` strict SAB custom batch width rerun (1/1, about 2.18s). No compile caches were deleted.
- Feature completion: 100% for this focused UnknownRegister `dst` register-scan slice; broader ModuleGraph/lazy import/codegen work remains open.

- Phase 1 RefCell lifecycle hardening: shared owner-release planning now decides when an active RefCell borrow handle must be released because its `cell_reg` is the owner being released. `src/lowering_rules.zig` adds `planRefCellHandleCellRelease`; `src/codegen.zig` consumes it for SA-text's owner cleanup scan; `src/sab_codegen.zig` applies the same decision before owner release and also uses a branch-local RefCell/borrow-temp aware cleanup helper for branch-local releases. SA-text chained `if let` fail-path cleanup now snapshots/restores consumed, borrow-source, and RefCell-handle state after writing fail cleanup so success-path lowering stays live. `tests/test_unit_refcell_owner_release_direct.sla` covers explicit owner release and chained `if let` failure cleanup with an active RefCell borrow handle.
- Verification: `zig fmt --check src/lowering_rules.zig src/codegen.zig src/sab_codegen.zig`; `zig test src/lowering_rules.zig --test-filter "shared refcell borrow call plan tracks payload kind and release macro"`; `zig build --summary all`; `zig build test --summary all` (94/94); local and installed SA-text plus strict direct-SAB no-fallback for the focused fixture (2/2); local and installed rosetta 104 SA/SAB (1/1); existing RefCell payload SA/SAB 7/7; borrow-temp strict SAB 25/25; `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; full local and installed strict direct-SAB unit sweeps 119/119.
- Feature completion: 100% for this focused RefCell owner-release / branch-cleanup / chained-if-let fail-state sub-slice; broader RefCell lifecycle, pointer-backed imported-macro alias semantics, shared call/materialization convergence, and global compiler completion remain open.

## Previous Completed Slice

- `docs/sa_scope_taskset_move_cleanup_useaftermove_issue_cn.md` cleanup subproblem is fixed for the current compiler repro and downstream host surface. Full downstream generated-SA and default/SAB `test_ecs_mut_parallel.sla` both pass 129/129 instead of hitting the old `next_a`/`next_world` UseAfterMove cleanup boundary.
- Implementation: added `tests/test_unit_sa_aggregate_reassign_move_cleanup.sla`; `src/codegen.zig` now handles non-Copy identifier RHS assignment by transferring compiler-side value-state metadata and marking the RHS consumed without emitting an extra release. This preserves target liveness after `bag = next_bag` while suppressing RHS loop-body cleanup.
- Verification: `zig fmt --check src/codegen.zig`; `zig build --summary all`; local rebuilt CLI SA focused fixture and cleanup guards; official `sa plugin install --dev .`; installed SA/SAB focused fixture; installed old cleanup guards; downstream `/home/vscode/projects/sla_ecs/lib/entity_dynamic.sla` generated-SA 7/7; downstream `test_ecs_mut_parallel.sla --filter "task pool scoped task set" --test-backend sa` 16/16; full downstream generated-SA `test_ecs_mut_parallel.sla` 129/129; full downstream default/SAB `test_ecs_mut_parallel.sla` 129/129. The earlier `InvalidArgsCount` no longer reproduces.
- Feature completion: 100% for this focused docs issue slice; broader Y/shared-lowering and full compiler completion remain open.

## Previous Completed Slice

- `docs/execute_extra_parse_tsconfig_useaftermove_issue_cn.md` is fixed, and the original UnknownRegister blocker in `docs/sab_large_execute_unknown_register_issue_cn.md` is resolved for current downstream execute/cli host evidence. `docs/ls_autoinsert_aggregate_scanner_register_redefinition_issue_cn.md` is fixed and downstream-reverified; the historical LS aggregate assertion has been restored and passes under SA backend.
- Implementation: `src/codegen.zig` records actual ordinary/resource/container releases from imported macro value materialization, removes reassigned identifiers from `consumed_bindings`, uses lexical cleanup release for branch/loop cleanup, and releases loop-body top-level locals before natural while backedges. `src/sab_codegen.zig` materializes by-value primitive Copy params into entry stack slots and consumes the original param register after initial store.
- Verification: `zig fmt --check src/codegen.zig src/sab_codegen.zig`; `zig build --summary all`; `zig build test --summary all` (94/94); focused local SA/SAB guards for tsconfig buffer cleanup, reassignment after release, loop-body local cleanup, and SAB binary Copy param reuse; `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; downstream `sla_tsgo` `members/execute/src/cli.sla` check; downstream `test_cli_contract.sla` SA/default 8/8; downstream `test_execute_extra_contract.sla` SA/default 23/23; downstream `test_execute_contract.sla` default/SAB no verifier trap, with only historical runtime/business assertions remaining (33/49 passed).
- Feature completion: 100% for this focused docs issue slice; broader Y/shared-lowering and full compiler completion remain open.

## Earlier Completed Slice

- `docs/sab_empty_filter_memoryleak_issue_cn.md` is fixed. Empty SAB test filters now return the same `0 passed; 0 failed; 0 skipped` result as SA-text without compiling or encoding a SAB test artifact.
- Implementation: `src/plugin.zig` adds `testFilterSelectsNoTests()` and `writeEmptyTestResult()`, then short-circuits `sa sla test` before `compileSlaSabTestInput()` / `compileSlaSaTestInput()` when an explicit non-empty filter selects no expanded tests. The new unit test `sla test empty filter skips sab compilation` proves the broken test body is ignored only in the zero-selected-tests case and no managed SAB path is created.
- Verification: `zig build test -Dtest-filter="empty filter skips sab" --summary all`; `zig build --summary all`; `zig build test --summary all` (92/92); `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; downstream strict SAB no-fallback empty filters for `definitely no such ecs test` and stale `ecs_world_try_query_single returns one`; downstream strict SAB no-fallback real filter `ecs_world_try_get returns ok for present component` (1/1).
- Feature completion: 100% for this focused docs issue slice; broader Y/shared-lowering and full compiler completion remain open.

## Earlier Completed Slice

- `docs/sab_thread_fnptr_ready_batch_unknown_register_issue_cn.md` is fixed. Direct SAB now handles paired thread-spawn closures that capture function-pointer parameters and cloned Arc raw-pointer world handles.
- Implementation: `tests/test_unit_fn_ptr_thread_pair_direct.sla` adds the local repro; `src/sab_codegen.zig` records call-body referenced registers in function metadata and emits Rc/Arc receiver `clone()` through direct smart-pointer clone macros; `src/plugin.zig` adds a paired escaped thread function-pointer direct-SAB regression.
- Verification: focused Zig tests for escaped and paired escaped thread function-pointer lowering; `zig build --summary all`; local SA-text and strict direct-SAB no-fallback for the new fixture; local strict direct-SAB no-fallback for function-pointer value and void fixtures; `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`; downstream focused `test_ecs_mut_parallel.sla` ready-pair, ready-triple, and width-dispatch SAB/default tests.
- Feature completion: 100% for this focused docs issue slice; broader Y/shared-lowering and full compiler completion remain open.

## Earlier Completed Slice

- P1 compiler-side stability metadata support is complete. `src/stability_metadata.zig` defines the schema, example manifest, validator, and text/JSON reports; `sa sla stability schema` and `sa sla stability verify <manifest.json> [--json]` expose it through the plugin CLI.
- `docs/stability_metadata_cn.md` documents the compiler/downstream boundary: this repo validates manifest structure and label references, but downstream projects own concrete labels, business fixtures, semantics, and thresholds.
- Verification: `zig test src/stability_metadata.zig`; `zig build test -Dtest-filter="sla stability" --summary all` (3/3); `zig build test -Dtest-filter="valid stability metadata" --summary all` (1/1); `zig build --summary all`; `zig build test --summary all` (90/90); local and installed CLI schema/verify smoke; `sa plugin install --dev .`; `SA_PLUGIN_DEV=1 sa sla help`.
- Feature completion: 100% for this compiler-owned stability metadata slice; broader Y/shared-lowering and full compiler completion remain open.

## Earlier Completed Slice

- P1 compiler-owned benchmark harness formalization is complete and committed in `d6f5211`. `tools/bench_sla_pipeline.sh` records JSONL timings for `.sla -> .sab` cold/warm, `.sla -> native`, SAB fallback-allowed/direct tests, and generic external hooks.
- Earlier focused docs-priority and shared-lowering slices remain verified; global 100% completion is not claimed.

## Dirty Worktree Caveat

After this verified slice is committed, the plugin worktree should be clean. Continue to inspect status before staging future work; do not restore, delete, stage, or commit unrelated/generated changes if they appear while working.
