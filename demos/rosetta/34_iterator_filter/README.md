# 034 Iterator Filter

This directory matches the iterator-filter topic for the catalog slot.

- `main.rs`: Rust reference for the filtered iterator sum semantics used by this slot.
- `main.sla`: Sla companion for the filtered iterator sum semantics used by this slot.
