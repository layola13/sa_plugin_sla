fn integration_service_total() -> i32 {
    let users = 2;
    let orders = 4;
    return users + orders;
}

fn main() -> i32 {
    let users = 2;
    let orders = 4;
    println("{}", users + orders);
    return 0;
}

@test "rosetta 070 70_integration_service"() {
    let got = integration_service_total();
    if got != 6 {
        panic(70);
    };
}
