# 062 Channel Pingpong

This directory matches the channel ping-pong round-trip topic for the catalog slot.

- `main.rs`: Rust reference for the two-step send/recv round-trip semantics used by this slot.
- `main.sla`: Sla companion for the two-step send/recv round-trip semantics used by this slot.
