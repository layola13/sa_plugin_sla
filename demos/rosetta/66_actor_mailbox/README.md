# 066 Actor Mailbox

This directory matches the actor-mailbox message topic for the catalog slot.

- `main.rs`: Rust reference for the structured mailbox message scoring semantics used by this slot.
- `main.sla`: Sla companion for the structured mailbox message scoring semantics used by this slot.
