# 035 Iterator Fold

This directory matches the iterator-fold topic for the catalog slot.

- `main.rs`: Rust reference for the folded string-length accumulation semantics used by this slot.
- `main.sla`: Sla companion for the folded string-length accumulation semantics used by this slot.
