fn while_let_sum() -> i32 {
    let values = vec(1, 2, 3);
    let total = 0;
    while let Some(value) = values.pop() {
        total = total + value;
    }
    return total;
}

@test "while let option smoke"() {
    let got = while_let_sum();
    if got != 6 {
        panic(7008);
    };
}
