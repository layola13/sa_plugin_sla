# 049 Pipeline Map

This directory matches the iterator-pipeline topic for the catalog slot.

- `main.rs`: Rust reference for iterator mapping pipeline semantics used by this slot.
- `main.sla`: Sla companion for iterator mapping pipeline semantics used by this slot.
