# 020 Boxed Value

This directory matches the boxed-value topic for the catalog slot.

- `main.rs`: Rust reference for the `Box::new` value semantics used by this slot.
- `main.sla`: Sla companion for the `Box::new` value semantics used by this slot.
