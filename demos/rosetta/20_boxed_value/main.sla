@import "sa_std/io/print.sai"
@import "sa_std/fmt.sai"

fn main() -> i32 {
    let value = Box::new(9);
    println("{}", value);
    return 0;
};

@test "rosetta 020 boxed value"() {
    let value = Box::new(9);
    println("{}", value);
};
