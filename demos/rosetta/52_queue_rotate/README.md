# 052 Queue Rotate

This directory matches the queue-rotation topic for the catalog slot.

- `main.rs`: Rust reference for the `VecDeque::rotate_left` semantics used by this slot.
- `main.sla`: Sla companion for the `VecDeque::rotate_left` semantics used by this slot.
