# 083 Blob Chunk

This directory matches the blob-chunk layout topic for the catalog slot.

- `main.rs`: Rust reference for the chunk-count plus tail-layout semantics used by this slot.
- `main.sla`: Sla companion for the chunk-count plus tail-layout semantics used by this slot.
