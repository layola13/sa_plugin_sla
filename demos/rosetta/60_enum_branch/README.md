# 060 Enum Branch

This directory matches the enum-branch topic for the catalog slot.

- `main.rs`: Rust reference for the enum branch selection semantics used by this slot.
- `main.sla`: Sla companion for the enum branch selection semantics used by this slot.
