# 076 Lockfree Counter

This directory matches the lock-free counter topic for the catalog slot.

- `main.rs`: Rust reference for the atomic counter update semantics used by this slot.
- `main.sla`: Sla companion for the atomic counter update semantics used by this slot.
