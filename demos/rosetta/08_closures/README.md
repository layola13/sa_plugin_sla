# 008 Closures

This directory matches the closure-capture topic for the catalog slot.

- `main.rs`: Rust reference for the captured-offset semantics used by this slot.
- `main.sla`: Sla companion for the captured-offset semantics used by this slot.
