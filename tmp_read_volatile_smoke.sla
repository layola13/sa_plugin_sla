@import "sa_std/ptr.sa"

fn read_volatile_value() -> i32 {
    let value: i32 = 5;
    let seen = unsafe { ptr::read_volatile(&value) };
    return seen;
}

@test "read volatile smoke"() {
    let got = read_volatile_value();
    if got != 5 {
        panic(120);
    };
}
