# 045 Config Merge

This directory matches the config-merge topic for the catalog slot.

- `main.rs`: Rust reference for the field-wise config merge semantics used by this slot.
- `main.sla`: Sla companion for the field-wise config merge semantics used by this slot.
