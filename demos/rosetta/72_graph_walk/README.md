# 072 Graph Walk

This directory now records the current graph-walk gap honestly.

- `main.rs`: Rust reference for the weighted edge-walk accumulation semantics used by this slot.
- `main.sla`: current Sla companion shape for the same accumulation intent, but the checked path still leaves `graph` live at function exit locally.
