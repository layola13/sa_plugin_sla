fn bad_var_release_in_one_branch(cond: bool) -> i32 {
    var x: i32;
    x = 1;
    if cond {
        !x;
    } else {
    };
    return x;
}

fn main() -> i32 {
    return bad_var_release_in_one_branch(true);
}
