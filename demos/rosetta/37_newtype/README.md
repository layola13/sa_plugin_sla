# 037 Newtype

This directory matches the newtype topic for the catalog slot.

- `main.rs`: Rust reference for the wrapped-identifier semantics used by this slot.
- `main.sla`: Sla companion for the wrapped-identifier semantics used by this slot.
