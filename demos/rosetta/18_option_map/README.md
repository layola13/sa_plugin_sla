# 018 Option Map

This directory matches the `Option` mapping topic for the catalog slot.

- `main.rs`: Rust reference for the `Some(...).map(...).unwrap_or(...)` semantics used by this slot.
- `main.sla`: Sla companion for the `Some(...).map(...).unwrap_or(...)` semantics used by this slot.
