# 006 Enum And Match

This directory matches the enum-match topic for the catalog slot.

- `main.rs`: Rust reference for the message dispatch semantics used by this slot.
- `main.sla`: Sla companion for the message dispatch semantics used by this slot.
