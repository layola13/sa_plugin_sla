# 036 Tuple Struct

This directory matches the tuple-struct topic for the catalog slot.

- `main.rs`: Rust reference for the tuple-struct field access semantics used by this slot.
- `main.sla`: Sla companion for the tuple-struct field access semantics used by this slot.
