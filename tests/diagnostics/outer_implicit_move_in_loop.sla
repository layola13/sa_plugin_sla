@import "sa_std/core/box.sa"

fn reject_outer_implicit_move_in_loop() -> void {
    let outer: Box<i64> = Box::new(7i64);
    for n in 0..1 {
        if n == 0 {
            let moved = outer;
            !moved;
        };
    }
}
