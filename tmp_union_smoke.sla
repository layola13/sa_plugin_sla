union Payload {
    i: i32,
    b: u8,
}

fn union_smoke() -> i32 {
    let payload = Payload { i: 36 };
    let value = unsafe { payload.i };
    return value;
}

@test "union ffi payload"() {
    let got = union_smoke();
    if got != 36 {
        panic(1);
    };
};
