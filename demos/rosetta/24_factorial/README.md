# 024 Factorial

This directory matches the recursive factorial topic for the catalog slot.

- `main.rs`: Rust reference for the factorial recursion semantics used by this slot.
- `main.sla`: Sla companion for the factorial recursion semantics used by this slot.
