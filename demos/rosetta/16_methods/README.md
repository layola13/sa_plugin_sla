# 016 Methods

This directory matches the instance-method topic for the catalog slot.

- `main.rs`: Rust reference for the `Vec2::square_len` semantics used by this slot.
- `main.sla`: Sla companion for the `Vec2::square_len` semantics used by this slot.
